# Part 7. 실무 적용 가이드 — 하네스를 실제로 쓰는 법

---

# 15장. 하네스별 실전 적용

> 1~14장은 **무엇인지**를 다뤘다. 이 장은 **실제로 어떻게 쓰는지**만 다룬다.
> 7축(Context · Permission · Verification · Tool · Subagent/Worktree · Debug · State)을 하나씩,
> **언제 쓰는가 → 어떻게 만드는가 → 어떻게 확인하는가** 순서로 본다.
>
> ⚠️ **한 번에 다 하지 않는다.** 15.1~15.2만 하고 나머지는 필요할 때 돌아온다.
> 예시는 **스택 중립**으로 썼다. 명령은 `<테스트 명령>` 같은 자리표시자로 두고, **스택별 대응표**를 함께 실었다.
> 경로(`src/features/…`)와 파일명은 예시이므로 **각자 프로젝트 구조로 바꿔 읽는다.**

**이 장의 지도**

| 절 | 하네스 | 만드는 것 |
|---|---|---|
| **15.2** | **진단** | **내 프로젝트의 답을 뽑아내는 절차 — 여기서 시작한다** |
| 15.3 | **Context** | `CLAUDE.md` · 요청 템플릿 |
| 15.4 | **Permission** | `.claude/settings.json` |
| 15.5 | **Verification** | `git diff` 읽는 습관 |
| 15.6~15.8 | **Tool** | Skill · MCP · Hook |
| 15.9~15.10 | **작업 분리** | Subagent · Worktree |
| 15.11 | **Debug** | 5단계 프롬프트 |
| 15.12 | **State** | 세션 운영 · 파일 승격 |
| 15.13 | 전체 | 버그 하나를 처음부터 끝까지 |

---

## 15.1 첫날 셋업 (30분)

| 순서 | 할 일 | 목적 | 소요 |
|---|---|---|---|
| 1 | **위험 파일 찾기** (15.2 ②) | deny 목록 도출 | 5분 |
| 2 | **`CLAUDE.md` 초안 생성 후 손보기** (15.2 ③) | 반복 설명 제거 | 15분 |
| 3 | `settings.json`에 `deny` 3~4개 (15.2 ② → 15.4) | 사고 방지 | 5분 |
| 4 | **작동 점검 5문항** (15.2 ⑤) | 설정이 진짜 먹는지 확인 | 5분 |
| 5 | 요청 템플릿으로 작업 1회 (15.3 ②) | 습관화 | — |
| 6 | `git diff --stat` 확인 (15.5) | 검증 습관화 | 5분 |

⚠️ **Skill · MCP · Hook · Subagent · Worktree는 오늘 만들지 않는다.** 같은 불편을 3번 겪은 뒤에 만든다. (9.7 안티패턴 · 11.4 "역할이 겹치면 만들지 않는다")

> 처음부터 다 만들면 **유지보수할 설정만 늘고 효과는 없다.** 15.14의 표를 보고 "지금 이 불편을 겪고 있나?"로 판단한다.

---

## 15.2 내 프로젝트에 맞게 채우는 법 (진단 30분)

> 뒤 절들의 예시는 **채운 결과물**이다. 그대로 베끼면 안 된다.
> 이 절은 **우리 프로젝트의 답을 직접 뽑아내는 절차**다. 여기부터 시작한다.

### ① 최종 형태 — 다 만들면 이렇게 된다

```text
내프로젝트/
├── CLAUDE.md                      ← 15.3  프로젝트 규칙 (git 커밋)
├── .mcp.json                      ← 15.7  외부 시스템 연결 (필요할 때만)
└── .claude/
    ├── settings.json              ← 15.4  팀 공유 권한 (git 커밋)
    ├── settings.local.json        ← 15.4  개인 설정 (.gitignore)
    ├── skills/
    │   └── debug-report/
    │       └── SKILL.md           ← 15.6  반복 절차 (필요할 때만)
    ├── agents/
    │   ├── explorer.md            ← 15.9  탐색 전용 (필요할 때만)
    │   └── verifier.md            ← 15.9  검증 전용 (필요할 때만)
    └── hooks/
        └── protect.sh             ← 15.8  강제 차단 (필요할 때만)
```

⚠️ **첫날 만드는 건 굵은 두 개뿐이다** — `CLAUDE.md`, `.claude/settings.json`.
나머지는 "필요할 때만"이라고 적힌 그대로, 15.15의 표에 해당하는 불편을 겪은 뒤에 만든다.

`.gitignore`에 이 줄을 추가한다.

```gitignore
.claude/settings.local.json
```

### 먼저 — 우리 프로젝트의 명령 3개를 확정한다

뒤 절들의 템플릿에는 `<테스트 명령>` 같은 **빈칸**이 나온다. 매번 "우리는 뭐였지?" 하지 않도록 **지금 한 번 확인해서 적어둔다.**

**어디서 찾나** — 이 순서로 본다.

```bash
cat README.md | head -40                       # 대개 여기 적혀 있다
cat package.json Makefile 2>/dev/null | head   # 웹·서버
cat .github/workflows/*.yml 2>/dev/null        # CI가 돌리는 명령 = 정답
```

> **CI 설정이 가장 정확하다.** README는 낡았을 수 있지만 CI는 실제로 돌아가고 있다.

**스택별 예시**

| | 웹 · Node | 서버 · Python | Android | iOS |
|---|---|---|---|---|
| **빌드** | `npm run build` | (별도 빌드 없음) | `./gradlew assembleDebug` | `xcodebuild -scheme App` |
| **테스트** | `npm test` | `pytest` | `./gradlew test` | `xcodebuild test -scheme AppTests` |
| **린트** | `npm run lint` | `ruff check .` | `./gradlew ktlintCheck` | `swiftlint` |

**확정한 값이 들어가는 곳은 세 군데다** (웹 프로젝트를 예로)

| 어디 | 어떻게 들어가나 |
|---|---|
| `CLAUDE.md`의 `## 개요` (15.3) | `- 테스트: npm test` |
| `settings.json`의 `allow` (15.4) | `"Bash(npm test *)"` — 승인 없이 돌게 |
| 요청 템플릿의 `검증 방법` (15.3 ②) | `검증 방법: npm test 통과` |

⚠️ **사람이 이 세 줄을 모르면 AI도 모른다.** Claude에게 `CLAUDE.md` 초안을 시켜도 마찬가지다 — 맞는지 확인해줄 사람이 없으면 **추측한 명령이 그대로 문서에 남고**, 이후 모든 세션이 그 틀린 명령을 믿는다.

### ② deny 목록 뽑기 — 우리 레포의 위험 파일 찾기

**질문**: *"AI가 이 파일을 잘못 건드리면 되돌리는 데 얼마나 걸리나?"*
**하루 이상**이면 deny 후보다.

레포에서 직접 찾는다.

```bash
# 1) 시크릿·자격증명 후보
find . -type f \( -name "*.env*" -o -name "*.p12" -o -name "*.pem" -o -name "*.key" \
  -o -name "*.keystore" -o -name "*.jks" -o -name "*.mobileprovision" \
  -o -name "*.xcconfig" -o -name "google-services.json" -o -name "GoogleService-Info.plist" \) \
  -not -path "./.git/*" -not -path "*/node_modules/*"

# 2) 배포·CI 설정
ls -d .github/workflows fastlane .circleci Jenkinsfile Dockerfile 2>/dev/null

# 3) 서명·권한·빌드 설정 (모바일)
find . -name "*.entitlements" -o -name "Info.plist" -o -name "AndroidManifest.xml" \
  -o -name "build.gradle*" -o -name "*.pbxproj" | grep -v Pods
```

나온 목록을 세 칸에 나눠 담는다.

| 칸 | 기준 | 규칙 형태 |
|---|---|---|
| **읽기도 금지** | 유출되면 끝나는 것 — 키·인증서·토큰 | `Read(./.env)` |
| **수정만 금지** | 읽는 건 도움이 되지만 고치면 위험한 것 — 매니페스트·배포 설정 | `Edit(<경로>/**)` |
| **명령 금지** | 되돌릴 수 없는 동작 | `Bash(git push *)` |

**스택별로 이 칸에 들어가는 것**

| | 읽기도 금지 | 수정만 금지 |
|---|---|---|
| **공통** | `.env`, `.env.*`, `*.pem`, `*.key` | `.github/workflows/`, `Dockerfile` |
| **웹 · Node** | `.npmrc` (토큰 포함 시) | `next.config.*`, `vercel.json` |
| **서버** | 인증서, `secrets/` | `k8s/`, `terraform/`, 마이그레이션 디렉터리 |
| **Android** | `*.keystore`, `*.jks`, `google-services.json` | `AndroidManifest.xml`, `build.gradle*` |
| **iOS** | `*.p12`, `*.mobileprovision`, `GoogleService-Info.plist` | `Info.plist`, `*.entitlements`, `fastlane/` |

⚠️ **처음부터 다 넣지 않는다.** 위 3칸에서 **가장 무서운 것 3~4개**만 골라 시작한다. 나머지는 "어? 이걸 하려고 했다고?" 하는 순간에 한 줄씩 추가한다.

### ③ CLAUDE.md 채우기 — 빈 화면부터 시작하지 않는다

**Claude에게 초안을 만들게 하고 내가 고친다.** 이게 가장 빠르다.

```text
이 저장소를 훑어보고 CLAUDE.md 초안을 만들어줘.

- 빌드 / 테스트 / 린트 명령은 실제 파일(package.json, Makefile, *.xcodeproj,
  build.gradle, README 등)에서 찾아서 넣어. 못 찾은 건 "확인 필요"라고 표시해.
- 코드에서 반복적으로 보이는 컨벤션을 5개만 뽑아줘. 추측한 건 표시해.
- 디렉터리 구조를 3줄로 요약해줘.
- 전체 40줄 이내. 아직 "절대 규칙"은 비워둬.
```

초안을 받은 뒤 **내가 직접** 채워야 하는 건 "절대 규칙"뿐이다. 아래 질문으로 뽑는다.

| 질문 | 나오면 규칙이 된다 |
|---|---|
| 신입이 들어오면 **반드시 알려주는 것**은? | → 컨벤션 |
| PR 리뷰에서 **세 번 이상 지적한 것**은? | → 컨벤션 |
| 건드리면 **다른 팀에 영향 가는** 코드는? | → 수정 금지 범위 |
| "이건 물어보고 해라"라고 말하게 되는 작업은? | → 계획 먼저 보고 |
| 우리 팀이 **"완료"라고 부르는 조건**은? | → 완료 기준 |

> 다섯 번째가 가장 중요하다. *"테스트 통과를 확인하지 않고 완료라고 말하지 않는다"* 한 줄이 검증 하네스의 출발점이다.

### ④ allow 목록 뽑기 — 미리 정하지 않는다

**하루 써보고 정한다.** 승인 프롬프트가 **세 번 이상 뜬 명령**만 `allow`에 올린다.

* 읽기·검색·린트·테스트 계열 → 대부분 `allow`로 올려도 안전
* 커밋·설치·빌드 → `ask`에 두는 편이 낫다 (되돌릴 수는 있지만 흔적이 남는다)

> ⚠️ 귀찮다고 `"Bash"`를 통째로 allow하지 않는다. 굳이 해야 한다면 **위험한 것만 Hook으로 막는 구성**(15.8)과 반드시 함께 쓴다.

### ⑤ 작동 점검 — 설정이 진짜 먹는지 5분 확인

**만들었다고 작동하는 게 아니다.** 새 세션을 열고 순서대로 확인한다.

| # | 확인할 것 | 입력 | 기대 |
|---|---|---|---|
| 1 | 설정이 로드됐나 | `/status` | `Setting sources`에 우리 파일이 보임 |
| 2 | CLAUDE.md가 읽혔나 | *"프로젝트 규칙을 요약해봐"* | 내가 쓴 내용이 나옴 |
| 3 | **deny가 진짜 막나** | *".env 파일 읽어줘"* | **차단됨** |
| 4 | **deny가 진짜 막나** | *"git push 해줘"* | **차단됨** |
| 5 | allow가 통과하나 | *"테스트 돌려줘"* | 승인 프롬프트 없이 실행 |

⚠️ **3·4번이 통과되면 설정이 안 먹고 있는 것이다.** 파일 위치(`.claude/settings.json`)와 JSON 문법을 먼저 확인한다. 흔한 실수:

* `Write(...)`나 `Glob(...)`으로 경로 규칙을 씀 → **조용히 무시된다.** `Read`/`Edit`만 검사된다
* JSON 마지막 원소 뒤에 쉼표 → 파일 전체가 무시된다
* 프로젝트 루트가 아닌 곳에서 세션을 시작함

### ⑥ 프로젝트 유형별 조정

| 유형 | 달라지는 점 |
|---|---|
| **모노레포 / 멀티모듈** | 루트 `CLAUDE.md`는 공통 규칙만. 모듈별 규칙은 각 디렉터리에 따로 두고, 요청 시 **수정 가능 범위를 모듈 단위로** 지정한다 |
| **레거시 코드가 큰 프로젝트** | `CLAUDE.md`에 *"`legacy/`는 읽기만"*을 먼저 넣는다. 탐색용 Subagent(15.9)의 효용이 가장 큰 유형이다 |
| **서버 / 인프라 코드** | 마이그레이션·배포 명령을 **전부 `deny`**로 시작한다. `ask`도 위험하다 — 습관적으로 승인하게 된다 |
| **팀 규칙이 아직 없는 팀** | `CLAUDE.md`를 **규칙 문서가 아니라 합의 초안**으로 쓴다. 리뷰에서 정해진 것만 한 줄씩 추가한다 |

### ⑦ 잘못 만들었을 때 되돌리기

* `.claude/settings.json`은 **git으로 관리**한다 → `git checkout .claude/settings.json`
* 규칙이 과해서 일이 안 될 때: 지우지 말고 **`deny` → `ask`로 한 단계 낮춘다**
* 개인적으로만 풀고 싶으면 `settings.local.json`에서 조정한다 (팀 파일은 그대로)
* Hook이 오작동하면: `.claude/settings.json`의 `hooks` 블록만 잠시 지운다 (스크립트 파일은 그대로 둬도 무해하다)

---

## 15.3 Context 하네스 — 무엇을 보게 할 것인가

### ① `CLAUDE.md` — 매번 설명하지 않아도 되는 것

프로젝트 루트에 둔다. 세션 시작 시 자동으로 읽힌다.

```markdown
# 프로젝트 규칙

## 개요
- 스택: <언어 / 프레임워크 / 아키텍처>
- 빌드: <빌드 명령>
- 테스트: <테스트 명령>
- 린트: <린트 명령>

## 절대 규칙
- 테스트 통과를 확인하지 않은 상태로 "완료"라고 말하지 않는다
- <배포 설정 경로>, <매니페스트 파일>은 읽기만 하고 수정하지 않는다
- <공유 레이어 경로>의 공개 인터페이스는 변경하지 않는다
- 스키마 변경은 먼저 계획을 보고한 뒤 실행한다

## 컨벤션
- <에러 처리 규칙 — 예: 예외를 삼키지 않는다, 결과 타입으로 반환한다>
- <널/옵셔널 처리 규칙>
- <새 기능을 추가할 위치 — 예: src/features/<이름>/ 아래 3파일>
```

**채우는 예 — 세 번째 "절대 규칙"을 스택별로 옮기면**

| 스택 | `<공유 레이어 경로>` |
|---|---|
| 웹 · Node | `src/lib/api/` |
| 서버 | `app/repository/`, 마이그레이션 디렉터리 |
| Android | `core/network/` |
| iOS | `Sources/Network/` |

**만들 때 지킬 것**

* **짧게.** 200줄 / 25KB를 넘으면 뒷부분은 로드되지 않는다 (5.2)
* **특정 작업에만 필요한 절차는 넣지 않는다** → 그건 Skill(15.6)로
* **정말 막아야 하는 건 여기 쓰지 않는다** → 그건 Permission(15.4)으로. `CLAUDE.md`는 **부탁이지 강제가 아니다** (5.4)

**확인법**: 새 세션에서 *"프로젝트 규칙을 요약해봐"*라고 물어본다. 빠진 게 있으면 로드 범위를 넘긴 것이다.

### ② 요청 템플릿 — 작업 하나를 넘길 때

파일 2개 이상 얽히거나 범위가 애매하면 이 형식으로 준다. (4.2)

```text
목표: 로그인 실패 시 사용자에게 에러 메시지를 보여준다
현재 상태(재현): 잘못된 비밀번호 입력 → 아무 반응 없음. 콘솔에 401만 찍힘
수정 가능 범위: src/features/login/
수정 금지 범위: src/network/, <배포 설정>, <매니페스트>
유지 조건: 로그인 화면 레이아웃과 기존 성공 플로우는 변경 금지
검증 방법: <테스트 명령> 통과 + 잘못된 비밀번호로 수동 확인
완료 보고 형식: 변경 파일 목록 + 테스트 결과 + 확인한 예외 케이스
```

**나쁜 요청과 좋은 요청의 실제 차이**

| | 나쁜 요청 | 좋은 요청 |
|---|---|---|
| 입력 | "로그인 에러 처리 좀 해줘" | 위 템플릿 |
| 결과 | 에러 처리는 됐는데 **네트워크 레이어까지 리팩터링**, 성공 플로우 동작 변경 | Login 폴더 안에서만 수정, 테스트 통과, 변경 파일 3개 보고 |
| 리뷰 시간 | diff 전체를 다시 읽어야 함 | 3파일만 확인 |

⚠️ **오버스펙 주의** — 오타 수정, 문구 한 줄 변경까지 이 양식을 채우지 않는다 (4.3). 검증 범위가 이미 명확하면 그냥 짧게 요청하는 게 빠르다.

### ③ 세션 위생 — 언제 끊는가

| 신호 | 조치 |
|---|---|
| 작업이 바뀐다 (로그인 → 결제) | **`/clear`** — 이전 맥락을 끌고 가지 않는다 |
| 같은 작업이 길어진다 | **`/compact`** + 남길 항목 지시 |
| 엉뚱한 파일을 보기 시작한다 | 설명 추가 ✕ → **파일 범위를 다시 좁혀 재요청** |
| 같은 실수를 반복한다 | 이미 오염됐다 → **`/clear` 후 좁은 범위로 다시** |

`/compact` 할 때는 **무엇을 남길지 직접 지시한다.**

```text
/compact 변경한 파일 목록, 실행한 명령어와 결과, 확인된 원인, 남은 할 일만 남겨줘
```

---

## 15.4 Permission 하네스 — 무엇을 하게 할 것인가

`.claude/settings.json` 한 파일로 6장의 **Safe / Ask / Deny** 3단계가 구현된다.

```json
{
  "permissions": {
    "allow": [
      "Bash(git log *)",
      "Bash(git diff *)",
      "Bash(git status)",
      "Bash(<린트 명령> *)"
    ],
    "ask": [
      "Bash(git commit *)",
      "Bash(<빌드 명령> *)"
    ],
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Read(**/*.pem)",
      "Edit(.github/workflows/**)",
      "Edit(<배포 설정 경로>/**)",
      "Bash(git push *)",
      "Bash(rm -rf *)"
    ]
  }
}
```

**`deny`의 마지막 두 줄을 스택별로 옮기면**

| 스택 | 추가할 `deny` |
|---|---|
| 웹 · Node | `Read(./.npmrc)` · `Edit(vercel.json)` · `Bash(npm publish *)` |
| 서버 | `Edit(k8s/**)` · `Edit(terraform/**)` · `Bash(* migrate *)` |
| Android | `Read(**/*.keystore)` · `Edit(**/AndroidManifest.xml)` · `Edit(**/build.gradle*)` |
| iOS | `Read(**/*.p12)` · `Edit(**/Info.plist)` · `Edit(fastlane/**)` |

**규칙 문법 — 이것만 알면 된다**

| 쓰고 싶은 것 | 규칙 |
|---|---|
| 정확히 이 명령만 | `Bash(npm run build)` — `npm run build --watch`는 매칭 안 됨 |
| 접두사 매칭 | `Bash(git log *)` = `Bash(git log:*)` |
| 파일 읽기 차단 | `Read(./.env)` · `Read(**/*.pem)` |
| 파일 수정 차단 | `Edit(.github/workflows/**)` |
| 도메인 제한 | `WebFetch(domain:github.com)` |
| 도구를 아예 안 보이게 | deny에 괄호 없이 `"Bash"` |

⚠️ **경로 규칙은 `Read`/`Edit`만 검사된다.** `Write(...)`나 `Glob(...)`으로 쓰면 조용히 무시된다.
⚠️ **평가 순서는 deny → ask → allow.** 먼저 매칭된 것이 이긴다. 좁은 allow가 넓은 deny를 이기지 못한다.

**실전 운영법 — 처음부터 완벽하게 쓰지 않는다**

1. `deny`에 **되돌릴 수 없는 것 3~4개**만 넣고 시작 (`git push`, 시크릿 읽기, 배포 스크립트, `rm -rf`)
2. 작업하다 승인 프롬프트가 **자꾸 뜨는 명령**을 `allow`로 올린다 (테스트·린트·읽기 계열)
3. **"어? 이걸 하려고 했다고?"** 싶은 순간이 오면 그때 `deny`에 추가한다

**확인법**: 세션 안에서 `/status` → `Setting sources` 줄에 어떤 파일이 로드됐는지 나온다.

**팀 공유 vs 개인**: 팀 규칙은 `.claude/settings.json`(커밋), 개인 실험은 `.claude/settings.local.json`(gitignore). (5.4)

---

## 15.5 Verification 하네스 — diff를 읽는 법

> **이 절이 실무에서 가장 자주 쓰는 하네스다.** AI가 "완료했습니다"라고 말한 다음에 하는 일 전부가 여기에 있다.

### ① 순서: `--stat` 먼저, 그다음 본문

```bash
git diff --stat
```

```text
 src/features/login/LoginForm       |  12 ++++--
 src/features/login/LoginViewModel  |  34 ++++++++++---
 src/features/login/LoginError      |  18 ++++++++
 src/network/ApiClient              |  47 ++++++++++++-------   ← ⚠️
 4 files changed, 89 insertions(+), 22 deletions(-)
```

**여기서 이미 멈춰야 한다.** 수정 금지 범위로 지정한 `src/network/`가 바뀌었다. 본문을 읽기 전에 `--stat`만으로 잡을 수 있다.

> **`--stat`을 먼저 보는 이유**: 파일 목록과 변경량만 보면 되므로 **10초**면 된다. 요청 범위와 다르면 본문을 읽을 필요조차 없다.

### ② 본문에서 확인할 6가지

```bash
git diff
git diff src/network/ApiClient   # 의심 가는 파일만
```

| 확인 | 무엇을 찾는가 |
|---|---|
| **요청 밖 파일** | 지정하지 않은 디렉터리가 바뀌었는가 |
| **불필요한 리팩터링** | 시킨 것 외에 "김에 정리한" 코드가 있는가 |
| **공개 API 변경** | 함수 시그니처·프로토콜·`public` 선언이 바뀌었는가 |
| **응답 모델 변경** | 직렬화 모델(DTO)의 필드가 추가·삭제·이름 변경됐는가 |
| **시크릿** | 키·토큰이 코드에 들어왔는가, 로그로 찍히는가 |
| **삭제된 코드** | `-` 줄 중에 지우면 안 되는 게 있는가 |

⚠️ **삭제(`-`) 줄을 특히 본다.** 추가된 코드는 눈에 띄지만, 조용히 사라진 예외 처리나 가드는 놓치기 쉽다.

### ③ AI에게 diff를 설명시키지 않는다

> ❌ *"이 diff 괜찮은지 봐줘"*

작성한 쪽이 검토하면 **자기 결정을 다시 확인하는 과정**이 될 뿐이다 (7.4). 검토가 필요하면:

* **사람이 직접 읽거나**
* **별도 세션 / Subagent**에게 원본 요구사항과 diff만 주고 검토시킨다 (15.9)

### ④ 반례를 요청한다

테스트가 통과했다고 끝이 아니다. 마지막에 이걸 묻는다.

```text
지금 수정한 코드가 깨질 수 있는 입력을 3개 제시해줘.
각각 왜 깨지는지와, 현재 코드가 그 경우를 처리하는지 함께 알려줘.
추측하지 말고 실제 코드를 근거로 답해.
```

### ⑤ 승인 전 5문답 (7.6)

- [ ] 뭘 고쳤는가?
- [ ] `git diff --stat`이 요청 범위와 일치하는가?
- [ ] 어떤 테스트를 통과했는가? (직접 돌려봤는가)
- [ ] 어떤 반례를 확인했는가?
- [ ] 문제가 생기면 어떻게 되돌리는가?

하나라도 막히면 **"완료"가 아니라 "검증되지 않은 변경"**이다.

### 확인법 — 이 하네스가 작동하고 있나

| 신호 | 판정 |
|---|---|
| 최근 머지한 PR 3건에서 **`--stat`을 본 기억이 없다** | ✕ 습관이 안 잡혔다 |
| 요청 범위 밖 파일이 머지된 적이 있다 | ✕ 4단계를 건너뛰고 있다 |
| "테스트 통과했대"까지만 보고 승인한다 | ✕ 반례 단계가 빠졌다 |
| 되돌리는 방법을 답할 수 없는 변경이 있다 | ✕ 5문답이 형식이 됐다 |

> 셋 이상 ✕면 **속도를 얻고 검증을 잃은 상태**다. 15.4의 `deny`를 늘려 애초에 범위를 좁히는 편이 빠르다.

---

## 15.6 Tool 하네스 ① Skill — 반복 절차를 파일로

### 언제 만드나

**같은 지시를 3번 이상 반복하고 있을 때.** 그전엔 만들지 않는다.

### 어떻게 만드나

`.claude/skills/debug-report/SKILL.md` — **디렉터리 이름이 곧 `/debug-report` 명령**이 된다.

```markdown
---
description: 버그를 증상·증거·검증경로로 구조화한다. 에러·크래시·테스트 실패를 들고 올 때 사용.
allowed-tools: Read Grep Glob
---

## 현재 변경사항

!`git diff HEAD --stat`

## 지시

먼저 고치지 말고 아래 순서로만 답한다.

1. **증상**: 무엇이 언제 실패하는가 (한 문장)
2. **증거**: 제공된 로그에서 확인 가능한 사실만
3. **원인 후보**: 가능성 순 최대 3개, 각각 근거와 함께
4. **확인 방법**: 각 후보를 확인할 최소 명령어
5. **반례**: 1순위 가설이 틀릴 수 있는 조건

코드 수정은 사용자가 후보를 선택한 뒤에 한다.
```

| 필드 | 역할 |
|---|---|
| **`description`** | Claude가 **언제 이 Skill을 꺼낼지** 판단하는 근거. **가장 중요하다** |
| `allowed-tools` | 이 Skill이 도는 턴 동안 승인 없이 쓸 도구 |
| `disable-model-invocation: true` | Claude가 스스로 꺼내지 못하게 하고 `/이름`으로만 실행 (배포 같은 위험 절차에 사용) |

> `` !`git diff HEAD --stat` `` 는 **동적 컨텍스트 주입**이다. Claude가 읽기 전에 명령이 실행되어 결과가 그 자리에 박힌다. Skill 하나로 3장의 Context Pack이 자동으로 채워진다.

### 어떻게 쓰나

* **직접 호출**: `/debug-report` 라고 입력
* **자동 호출**: *"로그인에서 크래시가 나는데 스택트레이스 여기 있어"* → `description`을 보고 Claude가 알아서 꺼낸다

### 확인법

만든 뒤 새 세션에서 **description에 해당하는 상황**을 던져본다. 안 꺼내지면 `description`이 모호한 것이다 — "언제 쓰는지"를 더 구체적으로 쓴다.

**위치**: 프로젝트 `.claude/skills/` · 개인 `~/.claude/skills/`

---

## 15.7 Tool 하네스 ② MCP — 외부 시스템 연결

### 언제 붙이나

**Claude가 "그 정보는 제가 볼 수 없습니다"라고 말할 때.** 절차가 부족한 게 아니라 **접근 권한이 없어서 멈출 때**만 붙인다. (10.3)

> 반대로, 외부 접근이 필요 없는 반복 절차에 MCP를 붙이면 **네트워크가 필요 없는 작업에 네트워크 레이어를 추가**하는 셈이다 (9.7).

### 어떻게 붙이나

프로젝트 루트 `.mcp.json` — 팀과 공유된다.

```json
{
  "mcpServers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": { "Authorization": "Bearer ${GITHUB_PAT}" }
    }
  }
}
```

* 원격 서버 → `type` + `url` (+ `headers`)
* 로컬 프로세스 → `command` + `args` (+ `env`)
* **`${VAR}` 확장이 되므로 토큰을 파일에 직접 쓰지 않는다**

CLI로도 된다.

```bash
claude mcp add --scope project --transport http github https://api.githubcopilot.com/mcp/
claude mcp list
```

### 붙였으면 반드시 범위를 좁힌다

MCP를 붙이는 순간 **Trust Boundary가 넓어진다** (9.2). 도구 이름은 `mcp__<서버>__<도구>` 형식이다.

```json
{
  "permissions": {
    "allow": ["mcp__github__get_*"],
    "ask":   ["mcp__github__create_pull_request"],
    "deny":  ["mcp__github__delete_*"]
  }
}
```

⚠️ allow 규칙에서는 **서버 이름 자리에 와일드카드를 못 쓴다** (`mcp__*`는 무시된다). deny에는 `mcp__*`로 전체 차단이 가능하다.

### 어떻게 쓰나

```text
이슈 #412를 읽고, 재현 단계와 기대 동작을 정리해줘.
코드는 아직 수정하지 마.
```

Claude가 `mcp__github__get_issue`를 호출해 이슈를 직접 읽는다. **복사해서 붙여넣을 필요가 없어진다.**

### 확인법

세션에서 `/mcp` → 서버 연결 상태와 사용 가능한 도구 목록이 나온다.

---

## 15.8 Tool 하네스 ③ Hook — 시스템이 강제하는 규칙

### 언제 만드나

**`CLAUDE.md`에 적었는데도 자꾸 어길 때.** 문서는 부탁이고 Hook은 강제다 (9.3).

### 예시 A — 보호 파일 수정을 실제로 막기

`.claude/settings.json`

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/protect.sh" }
        ]
      }
    ]
  }
}
```

`.claude/hooks/protect.sh` (`chmod +x` 필수) — 훅은 **stdin으로 JSON을 받는다**.

```bash
#!/bin/bash
FILE=$(jq -r '.tool_input.file_path // empty')

case "$FILE" in
  *.env|*.pem|*/.github/workflows/*|*/<배포 설정 경로>/*)
    jq -n --arg f "$FILE" '{hookSpecificOutput: {
       hookEventName: "PreToolUse",
       permissionDecision: "deny",
       permissionDecisionReason: ("보호된 파일: " + $f)}}' ;;
  *) exit 0 ;;   # 판단 없음 → 평소 권한 흐름대로
esac
```

| 차단 방법 | 동작 |
|---|---|
| stdout에 `permissionDecision: "deny"` | 차단 + 이유를 Claude에게 전달 |
| **`exit 2`** (stderr가 이유가 됨) | 차단. **allow 규칙보다도 먼저 걸린다** |

> `exit 2` 덕분에 **`Bash`를 전부 allow해두고 위험한 것만 훅으로 막는** 구성이 가능하다.

### 예시 B — 저장 후 자동 포매팅

```json
{
  "hooks": {
    "PostToolUse": [
      { "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "<포매터 명령> $CLAUDE_FILE_PATHS" }] }
    ]
  }
}
```

⚠️ **`PostToolUse`는 이미 실행된 뒤다.** 되돌리는 용도로는 쓸 수 없다. 막으려면 반드시 `PreToolUse`.

### 자주 쓰는 이벤트

| 이벤트 | 시점 | 용도 |
|---|---|---|
| `PreToolUse` | 실행 **전** | 검사 · 차단 |
| `PostToolUse` | 실행 **후** | 포매터 · 린터 |
| `Stop` | 턴 종료 전 | "테스트 안 돌렸으면 끝내지 마" |
| `SessionStart` | 세션 시작 | 필요한 정보 주입 |
| `PreCompact` | 압축 직전 | 중요 정보 보존 |

### 확인법

보호 파일을 일부러 수정 요청해본다. 차단 메시지가 뜨면 정상이다. **테스트하지 않은 Hook은 없는 것과 같다.**

---

### 덧붙임 — Plugin은 언제

지금까지 만든 것(`settings.json` · `skills/` · `hooks/` · `agents/`)을 **팀 전체에 같은 형태로 배포**해야 할 때 Plugin으로 묶는다 (9.5).

> 다만 **레포에 `.claude/`를 커밋하는 것만으로 팀 공유는 이미 된다.** Plugin이 필요한 건 **여러 레포에 같은 설정을 배포**할 때다. 레포가 하나면 만들지 않는다.

---

## 15.9 Subagent 하네스 — 컨텍스트를 분리해서 맡기기

### 언제 쓰나

| 쓴다 | 쓰지 않는다 |
|---|---|
| 여러 파일을 **넓게 조사**해야 할 때 | 도구 몇 번이면 끝날 일 |
| **작성자와 검토자를 분리**해야 할 때 | 자기 작업을 자기가 재확인 |
| 조사 과정이 길어서 **메인 세션이 지저분해질 때** | 역할이 겹치는 에이전트를 여러 개 |

> 핵심 이유는 "빠르게"가 아니라 **컨텍스트 격리**다. Subagent는 자기 작업 공간에서 헤매고 **결론만 요약해서 돌려준다** (11.2).

### 어떻게 만드나

`.claude/agents/explorer.md`

```markdown
---
name: explorer
description: 코드베이스에서 관련 파일을 찾아 구조만 요약한다. 수정 범위를 정하기 전에 사용.
tools: Read, Grep, Glob
model: sonnet
---

너는 탐색 전용이다. 코드를 절대 수정하지 않는다.

1. 질문과 관련된 파일을 찾는다
2. 각 파일이 어떤 책임을 갖는지 한 줄로 정리한다
3. 수정 후보 파일과 건드리면 안 될 파일을 구분해서 제시한다

출력은 15줄 이내로 요약한다. 읽은 파일 전문을 붙여넣지 않는다.
```

`.claude/agents/verifier.md`

```markdown
---
name: verifier
description: 변경된 코드를 독립적으로 검증한다. 구현이 끝난 뒤 리뷰가 필요할 때 사용.
tools: Read, Grep, Glob, Bash
permissionMode: plan
model: opus
maxTurns: 20
---

너는 이 코드를 작성하지 않았다. 작성 의도를 추측하지 말고 실제 출력만 평가한다.

1. `git diff`로 실제 변경 범위를 확인한다
2. 요청 범위를 벗어난 변경을 지적한다
3. 이 코드가 깨질 수 있는 입력(반례)을 3개 제시한다
4. 테스트를 실행하고 결과를 그대로 보고한다

"괜찮아 보인다"는 결론은 금지한다. 기준을 충족하지 못한 지점을 근거와 함께 보고한다.
```

| 프론트매터 | 왜 넣는가 |
|---|---|
| **`description`** | **이게 곧 역할 경계다.** Claude가 이걸 보고 위임 여부를 판단한다 (11.4) |
| `tools` | 탐색용에 편집 도구를 주지 않는다 — **모든 Subagent에게 모든 도구를 주지 않는다** (11.3) |
| `permissionMode: plan` | 검증용은 읽기·계획만. 수정 못 하게 |
| `maxTurns` | 폭주 방지 |
| `isolation: worktree` | 파일 격리까지 필요하면 (15.10) |

### 어떻게 호출하나

**① 이름을 지목한다** (확실한 방법)

```text
explorer 에이전트로 로그인 실패 처리와 관련된 파일을 먼저 찾아줘.
```

**② 자동 위임** — `description`에 맞는 상황이면 Claude가 알아서 부른다.

```text
로그인 화면 쪽 구조를 잘 모르겠어. 어디를 봐야 하는지부터 알려줘.
```

### 결과를 어떻게 받나

메인 세션에는 **탐색 과정 전체가 아니라 요약만** 돌아온다.

```text
관련 파일 4개
- src/features/login/LoginForm       : 화면. 에러 표시 UI 없음
- src/features/login/LoginViewModel  : 상태 관리. 에러 catch 블록이 비어 있음  ← 원인 후보
- src/network/ApiClient              : 401을 그대로 던짐. 수정 금지 범위
- src/features/login/LoginError      : 없음 (신규 생성 필요)

수정 후보: LoginViewModel, LoginForm, LoginError(신규)
건드리면 안 됨: ApiClient
```

> 이 요약이 그대로 **다음 작업의 Context Pack 재료**가 된다 (15.3).

### 3역할 실전 흐름

```text
① explorer  (읽기 전용)      → 관련 파일과 범위 파악
② 메인 세션 (편집 권한)       → 요약을 근거로 실제 수정
③ verifier  (plan 모드)      → 새 컨텍스트에서 diff·테스트 검증
```

②와 ③을 **같은 세션에서 하지 않는 것**이 핵심이다 (7.4).

### 확인법

`/agents` 로 등록된 에이전트 목록을 본다. 자동 위임이 안 되면 `description`이 모호한 것이다.

---

## 15.10 Worktree 하네스 — 파일 시스템 분리

### 언제 쓰나

**동시에 두 가지 이상 작업할 때.** 세션만 나누면 대화는 분리돼도 **파일 시스템은 공유**되어 diff가 뒤섞인다 (12.1).

> 혼자 한 가지 작업만 한다면 **필요 없다.**

### 명령 흐름

```bash
# 1. worktree를 만들면서 세션 시작 (독립 브랜치)
claude --worktree

# 2. 기준점 재동기화
git status
git remote set-head origin -a

# 3. worktree 경로를 .gitignore에 추가

# 4. 작업 끝나면 개별 제거
git worktree remove <경로>
```

### 작업을 어떻게 쪼개나

**worktree 하나에 책임 하나.**

```text
worktree A : login-error   → 로그인 실패 에러 표시만
worktree B : login-validate → 입력값 검증만
```

⚠️ 같은 브랜치를 두 세션이 동시에 편집하면 **병렬이 아니라 혼란**이다.

### 진짜 관건은 머지

* 각 worktree는 **자기 diff와 자기 테스트를 스스로 책임**진다
* 메인에서 **순차적으로** 머지한다 (동시에 우르르 ✕)
* 충돌 시 **바로 "고쳐줘"라고 시키지 않는다** — 한쪽을 기계적으로 덮어쓸 위험이 있다

**충돌 해결 프롬프트 (이 순서를 지킨다)**

```text
1단계: 지금 충돌이 왜 났는지 설명해줘.
       양쪽이 같은 로직을 어떻게 다르게 고쳤는지 알려주고, 아직 수정하지는 마.

2단계: 두 변경을 어떻게 합칠지 계획을 먼저 제시해줘.

3단계: 그 계획대로 수정하고 관련 테스트를 실행해줘.
```

⚠️ **함정**: `CLAUDE.local.md`는 worktree별로 분리되지만 **오토메모리는 같은 레포의 모든 worktree가 하나의 디렉터리를 공유**한다 (5.3). 한쪽에서 학습한 게 다른 쪽에 영향을 준다.

### 확인법

```bash
git worktree list          # 어떤 worktree가 어느 브랜치에 있나
git -C <worktree경로> status   # 각 worktree의 변경이 섞이지 않았나
```

| 확인 | 정상 |
|---|---|
| `git worktree list` | 작업 수만큼 있고, **브랜치가 서로 다르다** |
| 각 worktree의 `git diff --stat` | **자기 관심사 파일만** 나온다 |
| 메인 worktree의 `git status` | **깨끗하다** (baseline이 오염되지 않았다) |

⚠️ 두 worktree의 `--stat`에 **같은 파일이 겹쳐 나오면** 분리에 실패한 것이다. 머지 전에 한쪽을 정리한다.

---

## 15.11 Debug 하네스 — 풀 수 있는 형태로 바꾸기

### 언제 쓰나

**로그를 줬는데 엉뚱한 원인을 짚을 때.** 정보가 부족한 게 아니라 **구조화되지 않은 것**이다 (8장).

### 5단계 프롬프트

```text
[문제] 로그인 시 잘못된 비밀번호를 넣으면 아무 반응이 없다.
       에러 메시지도, 로딩 종료도 안 된다.

[환경] 스테이징 환경에서만 재현. 로컬(목 서버)에서는 정상.
       클라이언트 버전·OS와 무관하게 동일하게 발생.

[증거]
- 로그: `401 unauthorized` 1줄 후 아무것도 없음
- src/features/login/LoginViewModel 의 에러 catch 블록이 비어 있음
- 관련 테스트 없음

[제약]
- src/network/ 는 수정 금지
- 기존 성공 플로우 동작 변경 금지
- 먼저 수정하지 말고 원인 후보부터 좁힐 것

[요청]
1. 원인 후보를 가능성 순으로 최대 3개
2. 각 후보를 확인할 최소한의 명령어 또는 확인할 파일
3. 회귀 테스트가 필요한지 여부
```

**핵심은 `[제약]`의 마지막 줄이다.** 이게 없으면 첫 번째 그럴듯한 원인을 잡고 바로 코드를 고치기 시작한다.

### 환경 차이는 값이 아니라 표로

"로컬은 되는데 서버에서는 안 됨"은 대부분 코드 버그가 아니라 **설정 문제**다. **값을 보여주지 말고** 네 가지만 준다 (8.3).

| 키 | 로컬 | 스테이징 | 운영 |
|---|---|---|---|
| `API_BASE_URL` | 존재 (`.env.local`) | 존재 | 존재 |
| `AUTH_CLIENT_ID` | 존재 | ❌ 전달 안 됨 | 존재 |

→ 실제 값을 몰라도 **문제 위치가 표에서 드러난다.** 시크릿을 컨텍스트에 올리지 않는다는 점에서 15.3과도 이어진다.

### 원인을 확신할 때 반드시 되묻기

```text
이 가설이 틀릴 수 있는 조건은 뭐야?
이 로그와 모순되는 다른 원인은 없어?
```

---

## 15.12 State 하네스 — 기록을 남기기

### 언제 쓰나

| 신호 | 무엇을 만드나 |
|---|---|
| 지난주에 정한 규칙을 이번 주에 또 설명하고 있다 | `CLAUDE.md`로 승격 (①) |
| 작업이 **이틀 이상** 이어진다 | `state.md` (②) |
| 새 세션마다 "어디까지 했더라"로 시작한다 | `state.md` (②) |
| 같은 실패를 두 번째 반복했다 | `state.md`의 "반복하면 안 되는 실패" (②) |

> 하루 안에 끝나는 작업이면 **아무것도 만들지 않는다.** `/clear`로 충분하다.

### ① 대화에 있는 규칙은 사라진다

작업 중 나온 결론(금지 사항·테스트 명령·결정)은 `/compact`에서 사라질 수 있다. **파일로 승격시킨다** (4.5).

```text
대화 속 규칙  = 인메모리 캐시 (휘발)
CLAUDE.md    = 로컬 DB (지속)
```

**실전 습관**: 작업이 끝날 때 *"이번에 정한 규칙 중 앞으로도 계속 지켜야 할 게 있으면 `CLAUDE.md`에 추가할 문장으로 정리해줘"* 라고 요청하고, 내가 확인 후 커밋한다.

### ② 긴 작업은 파일에 진행 상황을 남긴다

며칠 걸리는 작업(마이그레이션·대규모 리팩터링)이라면 `state.md`를 둔다 (27.3).

```markdown
# 진행 상황

## 완료
- 로그인 화면 에러 처리 (PR #412)

## 진행 중
- 회원가입 화면에 동일 패턴 적용

## 다음
- 나머지 5개 화면

## 결정 사항
- 에러 타입은 LoginError로 통일, 화면별 enum 만들지 않음

## 반복하면 안 되는 실패
- 네트워크 레이어에서 에러 문구를 만들려고 시도 → 그 레이어의 책임이 아님
```

새 세션은 **이 파일부터 읽고** 멈춘 곳에서 이어간다.

### ③ 세션을 되돌릴 수 있다

기록은 지워지지 않고 계속 이어붙는다(append-only). 이전 세션을 다시 열거나 특정 시점으로 되돌아갈 수 있다 (4.6). 단 **이전에 허용했던 위험한 권한까지 복원되지는 않는다.**

### 확인법

**새 세션을 열고 아무 설명 없이 이렇게 묻는다.**

```text
state.md를 읽고, 지금 이어서 할 작업이 뭔지 알려줘.
```

| 결과 | 판정 |
|---|---|
| 다음 작업을 정확히 짚는다 | ○ State 하네스가 작동한다 |
| "어떤 작업인지 알려주세요"라고 되묻는다 | ✕ `state.md`가 비었거나 너무 추상적이다 |
| 이미 끝낸 작업을 다시 하려 한다 | ✕ "완료" 항목이 갱신되지 않았다 |

> 이 테스트를 통과하지 못하면 **다음 재시작 때 반드시 같은 작업을 반복한다.**

---

## 15.13 전체 흐름 — 버그 하나를 처음부터 끝까지

> 앞의 하네스들이 실제 작업에서 **어떤 순서로** 맞물리는지 본다.
> 과제: *"로그인 실패 시 에러 메시지가 안 뜬다"*

### 0단계 · 준비 (이미 되어 있음)

`CLAUDE.md`(15.3) + `settings.json`(15.4)이 있다. 매번 하는 일이 아니다.

### 1단계 · 탐색 — Context + Subagent

```text
explorer 에이전트로 로그인 실패 처리와 관련된 파일을 찾아줘.
수정 후보와 건드리면 안 되는 파일을 구분해서 알려줘.
```

→ 요약 4줄을 받는다. **메인 세션은 아직 깨끗하다.**

### 2단계 · 원인 좁히기 — Debug

15.10의 5단계 프롬프트를 던진다. `[제약]`에 **"먼저 수정하지 말 것"**을 넣는다.

→ 원인 후보 3개와 확인 방법을 받는다. **아직 코드는 한 줄도 안 바뀌었다.**

### 3단계 · 범위를 정해 수정 — Context Pack

후보 1번이 맞다고 확인한 뒤, 15.2의 요청 템플릿으로 넘긴다.

```text
목표: 로그인 실패 시 사용자에게 에러 메시지를 보여준다
수정 가능 범위: src/features/login/
수정 금지 범위: src/network/
검증 방법: <테스트 명령>
완료 보고 형식: 변경 파일 목록 + 테스트 결과
```

> 영향 범위가 불확실하면 여기서 **계획만 먼저** 요청한다 (Plan-First, 13.1).

### 4단계 · diff 검증 — Verification

```bash
git diff --stat     # ← 범위 확인. 여기서 대부분 걸린다
git diff            # ← 6가지 확인 (15.5)
```

`src/network/`가 바뀌었으면 **되돌리고 다시 요청**한다.

### 5단계 · 독립 검증 — Subagent

```text
verifier 에이전트로 이번 변경을 검토해줘.
원래 요구사항은 "로그인 실패 시 에러 메시지 표시"였고,
src/network/ 는 수정 금지였어.
```

→ 작성 맥락을 모르는 상태에서 **실제 출력만** 평가한다.

### 6단계 · 반례와 승인

15.4의 반례 프롬프트 → 5문답 체크 → 커밋.

### 7단계 · 규칙 승격 — State

이번에 정한 규칙(*"에러 문구는 화면 레이어에서 만든다"*)을 `CLAUDE.md`에 추가한다. **다음부터는 설명하지 않아도 된다.**

### 이 흐름에서 쓰인 하네스

| 단계 | 하네스 |
|---|---|
| 1 | Context · Subagent |
| 2 | Debug |
| 3 | Context (Plan-First) |
| 4 | **Verification** |
| 5 | Subagent (Generation ≠ Evaluation) |
| 6 | Verification |
| 7 | State |

> Permission(15.4)은 **모든 단계에서 배경으로 작동**한다. `src/network/`를 `Edit` deny에 넣어뒀다면 4단계에서 걸릴 일도 없었다.

---

## 15.14 증상 → 원인 → 조치

| 증상 | 먼저 의심 | 조치 |
|---|---|---|
| 엉뚱한 파일 수정 | **Context** | 설명 추가 ✕ → 파일 범위 재축소 (15.3) |
| 위험한 명령 실행 | **Permission** | `settings.json` deny 추가 (15.4) |
| 수정했는데 틀림 / 테스트 실패 | **Verification** | `git diff` 확인 + 반례 요청 (15.5) |
| 같은 설명을 계속 반복 | **Tool** | Skill로 승격 (15.6) |
| "그 정보는 볼 수 없습니다" | **Tool** | MCP 연결 (15.7) |
| 하지 말라는 걸 자꾸 함 | **Tool** | CLAUDE.md ✕ → Hook (15.8) |
| 조사하다 메인 세션이 지저분해짐 | **Subagent** | 탐색을 별도 에이전트로 (15.9) |
| 병렬 작업 diff가 뒤섞임 | **Worktree** | worktree 분리 (15.10) |
| 로그를 줘도 원인 추적 실패 | **Debug** | 5단계 프롬프트 (15.11) |
| 같은 실수 반복 / 판단이 흐려짐 | **State** | `/clear` 후 좁은 범위로 (15.12) |

---

## 15.15 언제 다음 단계로 확장할지

| 지금 느끼는 불편함 | 추가할 것 | 절 |
|---|---|---|
| 매번 같은 설명 반복 | **Skill** | 15.6 |
| 외부 서비스(GitHub · DB · Jira) 조회 필요 | **MCP** | 15.7 |
| `CLAUDE.md`에 적어도 자꾸 어김 | **Hook** | 15.8 |
| 조사·구현·검토를 한 세션이 다 하다 꼬임 | **Subagent** | 15.9 |
| 여러 작업의 diff가 섞임 | **Worktree** | 15.10 |
| 며칠짜리 작업이 재시작 때마다 처음으로 | **state.md** | 15.12 |
| 팀 전체에 같은 규칙 배포 필요 | **Plugin** | 9.5 |

⚠️ **표에 있는 걸 미리 다 만들어두지 않는다.** 불편함을 먼저 겪고, **그 지점만** 확장한다.

> 잘 만든 하네스는 **처음부터 완성된 설정**이 아니라, **사고가 날 뻔할 때마다 한 줄씩 늘어난 설정**이다.
