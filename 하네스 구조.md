# 클로드 코드는 왜 "그냥 좋은 모델"이 아닐까

채팅형 클로드와 클로드 코드는 겉보기엔 둘 다 "AI한테 물어보는 것"처럼 보이지만, 실제로 하는 일은 꽤 다릅니다.

- **채팅형 클로드**: 질문을 받고, 답을 만들어서 텍스트나 코드 블록으로 보여줍니다. 여기서 끝입니다.
- **클로드 코드**: 내 프로젝트 폴더에 직접 들어가서 파일을 읽고, 명령어를 실행하고, 코드를 고치고, 테스트를 돌리고, 심지어 git 커밋까지 합니다.

즉 채팅형 클로드는 "답변 생성기"고, 클로드 코드는 "내 컴퓨터에서 실제로 작업을 대신 해주는 에이전트"입니다. 이 차이 때문에 클로드 코드는 그냥 모델 하나로 돌아가는 게 아니라, 여러 부품이 맞물려 돌아가는 하나의 시스템입니다. 이 글에서는 그 부품들을 7개로 나눠서, 최대한 실무 감각으로 풀어보겠습니다.

## 전체 그림: 한 번에 끝나는 게 아니라 계속 돌아간다

클로드 코드가 일하는 순서를 아주 단순하게 그리면 이렇습니다.

```
사용자 요청
   ↓
인터페이스 (터미널, IDE 등)
   ↓
AgentLoop (여기가 진짜 두뇌 역할)
   ↓
모델 호출 → "이 파일을 읽고 싶어요 / 이 명령을 실행하고 싶어요" (Tool 요청)
   ↓
Permission 체크 → 실행 허용된 것만 통과
   ↓
Tool 실행 (파일 수정, 명령 실행 등)
   ↓
실행 결과가 다시 AgentLoop로 돌아감
   ↓
필요하면 위 과정을 다시 반복
```

핵심은 **"한 번 딱 물어보고 끝"이 아니라는 점**입니다. 클로드 코드는 관찰하고 → 판단하고 → 실행하고 → 결과를 보고 → 그 결과를 바탕으로 다시 판단하는, 계속 도는 루프(Loop)로 동작합니다. 마치 주니어 개발자가 코드를 고치고, 테스트를 돌려보고, 실패하면 다시 원인을 찾아 고치는 과정을 반복하는 것과 비슷합니다.

이 반복 와중에 클로드 코드는 자기가 지금까지 뭘 했는지 계속 기억하고 있어야 하는데, 이걸 담당하는 게 바로 "State(상태) & Persistence(기록 유지)" 영역입니다.

## 기억은 어떻게 유지될까: State & Persistence

사람도 일을 오래 하면 "내가 아까 뭘 했더라?"를 기억해야 실수를 안 하죠. 클로드 코드도 마찬가지라서, 여러 종류의 기록을 계속 들고 다닙니다.

- **Session Transcript (세션 기록)**: 지금까지 주고받은 모든 대화와 실행 내역
- **CLAUDE.md (메모리)**: "이 프로젝트는 이런 규칙으로 짜여있어" 같은, 세션 시작할 때 미리 알려주는 컨텍스트
- **Compact Summary (압축 요약)**: 대화가 너무 길어지면 옛날 내용을 요약해서 압축 저장 (이걸 넘는 경계를 Compact Boundary라고 부름)
- **Sub-agent Sidechain (서브 에이전트)**: 특정 작업을 별도의 "보조 에이전트"에게 맡기고, 그 결과만 받아오는 곁가지 작업 흐름
- **Resume / Fork / Rewind**: 세션을 이어서 계속하거나(Resume), 특정 시점에서 갈라져 나가거나(Fork), 이전 시점으로 되돌리는(Rewind) 기능

이런 것들이 있기 때문에 클로드 코드는 긴 작업을 하다가도 "내가 왜 이 파일을 고치고 있었지"를 잃어버리지 않을 수 있습니다. 반대로 이 부분이 오염되면(예: 잘못된 가설을 계속 붙잡고 있으면) 엉뚱한 방향으로 계속 밀고 나가는 문제가 생깁니다.

## 모델은 파일을 직접 못 만진다: 하네스(Harness)라는 안전장치

여기서 많은 분들이 헷갈리는 부분인데, **모델이 직접 여러분의 파일 시스템을 건드리는 게 아닙니다.**

모델은 그냥 "이 파일을 읽고 싶어요", "이 명령어를 실행하고 싶어요", "이 코드를 이렇게 고치고 싶어요"라고 **요청만** 합니다. 실제로 그 요청을 받아서 실행해주는 건 클로드 코드의 **하네스(Harness)**라는 중간 장치입니다.

하네스가 하는 일을 순서대로 보면:

1. 모델의 Tool 요청을 받는다
2. Permission(권한)을 확인한다 — 이 작업을 해도 되는지
3. Hook(사전/사후 스크립트)을 실행한다
4. Sandbox 격리나 Deny Rule(금지 규칙)을 적용한다
5. 실제로 실행하고, 그 결과를 다시 모델에게 돌려준다

왜 이런 게 필요할까요? 모델이 아무리 똑똑해도, 그 판단을 **아무 제약 없이 바로 실행**하게 두면 실무 환경에서는 위험합니다. 잘못된 명령 하나로 파일이 삭제되거나, 민감한 데이터가 노출될 수도 있으니까요.

그래서 하네스의 역할을 한 줄로 정리하면 이렇습니다:

> **모델의 자율성을 "실행 가능한 행동"으로 바꿔주되, 그 행동을 통제 가능한 울타리 안에 가두는 것.**

## 진짜 중요한 인사이트: "실무 품질 = 모델 성능 × 하네스 품질"

여기가 이 글에서 가장 강조하고 싶은 부분입니다.

클로드 코드를 실무에서 오래 써보면, **모델 성능만으로는 설명이 안 되는 결과 차이**를 자주 마주치게 됩니다.

- 같은 모델인데 컨텍스트가 흐려지면 → 엉뚱한 파일을 고칩니다
- 같은 모델인데 Permission이 느슨하면 → 위험한 명령을 그냥 실행해버립니다
- 같은 모델인데 검증(테스트/체크) 단계가 없으면 → 틀린 수정을 자신 있게 내놓습니다
- 세션이 오염되면 → 예전의 잘못된 가설을 계속 끌고 갑니다

즉, **문제가 생겼을 때 "모델이 왜 이러지?"라고 묻기 전에, 어떤 하네스가 약했는지부터 봐야 한다**는 겁니다. 이 관점으로 문제를 나눠보면 다음과 같습니다.

| 증상 | 의심해야 할 하네스 |
|---|---|
| 관련 없는 파일을 건드림 | 컨텍스트 하네스 |
| 위험한 명령을 실행하려 함 | Permission 하네스 |
| 수정은 했는데 검증이 없음 | 검증(Certification) 하네스 |
| MCP나 Tool 연동이 꼬임 | Tool 하네스 |
| 병렬 작업이 서로 충돌함 | 서브 에이전트 / Worktree 하네스 |
| 로그를 줘도 원인을 못 찾음 | 디버깅 하네스 |

이렇게 나눠서 보면, 클로드 코드를 다루는 일이 단순히 "프롬프트를 잘 쓰는 것"이 아니라 **시스템을 설계하고 튜닝하는 것**에 가깝다는 걸 알 수 있습니다.

## 그래서 클로드 코드는 "설정"으로 동작을 조정한다

실제로 클로드 코드 공식 문서를 보면, 이런 하네스 개념이 설정 파일 형태로 구현돼 있습니다.

- **사용자 설정 / 프로젝트 설정**의 여러 계층을 지원
- **`settings.json`**: Permission(권한), 환경변수, Hook, 사용할 모델 등을 구성
- **`CLAUDE.md`**: 세션 시작할 때 클로드에게 미리 알려주는 프로젝트 컨텍스트/규칙

정리하면, 클로드 코드는 "프롬프트 한 번 잘 쓰면 끝"인 도구가 아니라, **프로젝트와 팀 단위로 정책과 설정을 조정해가며 운영하는 시스템**으로 설계되어 있습니다.

## 마무리: 문제가 생기면 이렇게 접근하자

클로드 코드를 쓰다가 뭔가 이상하게 동작한다면, 곧바로 "모델이 멍청해졌나?"라고 생각하기보다, 다음 순서로 점검해보시길 추천합니다.

1. **어떤 증상인지 먼저 분류한다** (엉뚱한 파일 수정? 위험한 명령? 검증 누락? Tool 충돌?)
2. **위 표를 참고해서 해당하는 하네스를 확인한다**
3. **`settings.json`, `CLAUDE.md` 같은 설정 파일을 점검한다** — Permission이 너무 느슨하거나 컨텍스트가 잘못 주입되고 있진 않은지

이렇게 접근하면 클로드 코드를 "가끔 헛짓하는 AI 도구"가 아니라, **내가 설정과 정책으로 통제할 수 있는 하나의 시스템**으로 다룰 수 있게 됩니다.
