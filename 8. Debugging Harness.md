# Claude Code의 디버깅 하네스(Debugging Harness), 제대로 이해하기

지난 글에서 Subagent와 Worktree로 "격리"를 다뤘다면, 이번엔 디버깅 하네스다. 결론부터: **디버깅 하네스는 Claude에게 로그를 더 많이 주는 게 아니라, "풀 수 있는 문제 형태"로 바꿔주는 시스템**이다.

## 왜 필요한가: 로그만 던지면 안 되는 이유

많은 사람이 에러 로그를 통째로 Claude에게 붙여넣고 "왜 이래?"라고 묻는다. 간단한 문제면 이것만으로도 답이 나온다. 하지만 실무에서는 상황이 다르다.

- 로그가 길고 노이즈가 많음
- 로컬/Docker/CI 환경 차이가 있음
- 재현이 불안정함
- 이전에 시도했던 여러 해결책이 뒤섞여 있음

이런 상태에서 Claude에게 그냥 로그만 던지면, 그럴듯한 일반론을 말하거나 Stack Trace의 마지막 줄만 보고 잘못된 원인으로 성급하게 수정을 시작할 수 있다.

iOS 개발자라면 이 상황이 익숙할 것이다. 크래시 로그(Crash Log)만 보고 "아 이거 이 라인 문제네"라고 단정지었다가, 알고 보니 실제 원인은 다른 스레드의 레이스 컨디션이었던 경험 말이다. 정보가 많다고 해서 정확한 진단이 나오는 게 아니라, **정보가 구조화되어 있어야** 정확한 진단이 나온다.

## 좋은 디버깅 하네스는 세 가지를 분리한다

### 1. 증상(Symptom) — 관찰된 현상
"무엇이, 언제 실패했는가"를 명확히 한다.
> 예: API 서버에서 로그인 요청 시 500 에러 발생

### 2. 증거(Evidence)
Stack Trace, 실패한 테스트 출력, Docker 로그, CI 실패 로그, 환경 변수 차이, 변경 이력 등 실제 근거 자료.

### 3. 검증 경로(Verification Path)
- 어떤 명령으로 재현할 수 있는가
- 어떤 테스트가 실패하는가
- 어떤 로그를 추가하면 가설을 확인할 수 있는가

핵심은 **"먼저 고치지 말고, 원인 후보부터 좁힌다"**는 것이다. Symptom → Evidence → 원인 후보 → Verification Loop(재현 명령, 테스트, 롤백 계획) 순서로 진행한다.

## 실전 프롬프트 구조: 5단계

실제로 Claude에게 던질 프롬프트는 이렇게 구성한다.

1. **문제(Problem)**: 무엇이 잘못됐는지
2. **환경(Environment)**: 어디서 실패하는지 (로컬은 되는데 CI는 실패, Docker 안에서는 실행 중 등)
3. **증거(Evidence)**: Stack Trace/로그 첨부
4. **제약(Constraints)**: DB 스키마 변경 금지, API 응답 형태 변경 금지, secret 파일 읽기 금지, 먼저 수정하지 말고 원인 후보부터 좁힐 것
5. **요청(Request)**: 가능성 높은 원인 후보를 순서대로 제시, 각 후보를 확인할 최소 명령/파일 목록 제시, 회귀 테스트 필요 여부 판단

이 5가지(문제-환경-증거-제약-요청)를 채워서 프롬프트를 만들면, Claude가 바로 코드를 고치는 대신 **원인 후보를 좁히는 작업**부터 시작하게 된다.

## 환경 변수 문제: 값이 아니라 "범위와 가시성"을 준다

"로컬에서는 되는데 Docker에서는 안 된다", "Docker에서는 되는데 CI에서는 안 된다", "프로덕션에서만 깨진다" — 이런 문제는 대부분 코드 버그가 아니라 **설정/스코프(scope) 문제**인 경우가 많다.

Claude Code 설정 문서에 따르면 환경 변수와 권한은 `settings.json`에서 구성할 수 있고, 민감한 파일은 `permissions.deny`로 Claude가 접근하지 못하게 막을 수 있다. 이 원칙은 디버깅할 때도 그대로 적용해야 한다. Claude에게 **값 자체를 보여주지 말고**, 다음 정보만 준다.

- 키가 존재하는가 (존재 여부)
- 어디서 왔는가 (출처)
- 어디까지 유효한가 (범위)
- 런타임에서 값이 실제로 보이는가 (가시성)

예를 들면 이런 매트릭스다.

| 환경 변수 | 로컬 | Docker | CI |
|---|---|---|---|
| `JWT_SECRET` | 존재 (.env) | ❌ 전달 안 됨 | 존재 (Secret Manager) |
| `API_BASE_URL` | 존재 | 존재 | 존재 |

Claude는 `JWT_SECRET`의 실제 값이 뭔지 몰라도, "Docker Compose에서 `JWT_SECRET`이 컨테이너로 전달되지 않는다"는 문제는 이 표만으로 찾아낼 수 있다. 값을 보여주지 않고도 문제를 풀 수 있게 해주는 **프라이버시 보존형 디버깅 하네스**다.

iOS 개발자 입장에선 `xcconfig`나 `Info.plist`에 들어가는 API 키/서명 관련 값을 Claude에게 그대로 붙여넣지 않고, "이 키가 Debug 스킴에는 있는데 Release 스킴에는 없다" 같은 구조로 정보를 정리해서 준다고 생각하면 된다.

## 마지막 단계: 반례(Counter-example) 루프

디버깅 하네스에서 마지막으로 중요한 건 **반례를 묻는 것**이다. Claude가 원인을 확신하는 순간, 이렇게 되물어야 한다.

- 이 가설이 틀릴 수 있는 조건은 무엇인가?
- 이 수정이 실패할 수 있는 입력은 무엇인가?
- 이 로그와 모순되는 다른 원인은 없는가?

이건 Claude를 못 믿어서가 아니라, 디버깅 과정을 더 논리적으로 만들기 위한 절차다. 실무 디버깅의 흐름을 정리하면:

> 가설 세우기 → 증거로 후보 좁히기 → 원인 확정 → 반례로 검증

이 흐름을 잘 설계된 디버깅 하네스가 그대로 지원해준다. Claude는 가설을 생성하고, 관찰(로그/테스트) 방법을 설계하고, 재현 조건을 정리하고, 검증 루프를 도와주는 파트너가 된다.

## 정리

문제가 생겼을 때 "어느 하네스가 약한가"부터 먼저 점검하는 습관이 중요하다. Symptom/Evidence/Verification Path를 나누지 않고 로그만 던지고 있진 않은지, 환경 변수 값을 그대로 노출하고 있진 않은지, 원인 확정 후 반례를 묻지 않고 바로 수정하고 있진 않은지 — 이 세 가지만 점검해도 디버깅 품질이 달라진다.

---

## 3줄 요약

1. 디버깅 하네스의 목적은 로그를 더 주는 게 아니라, 문제를 **Symptom(증상) → Evidence(증거) → Verification(검증 경로)**로 구조화해서 "풀 수 있는 형태"로 만드는 것이다.
2. 프롬프트는 문제-환경-증거-제약-요청 5단계로 구성하고, 환경 변수는 값이 아니라 **존재 여부·출처·범위·가시성**만 표로 정리해서 준다 (프라이버시 보존).
3. 원인을 확정하기 전에 반드시 **반례(이 가설이 틀릴 조건은?)**를 물어서 검증 루프를 거쳐야 한다.
