# Harness Engineering을 위한 SOLID 원칙

## 1. 가장 중요한 원칙

> **좋은 코드는 똑똑한 함수 하나를 구현한다고 만들어지는 게 아니다. 책임과 제약, 검증이 나뉜 구조에서 나온다.**

CodeCraft 문제를 받았을 때 "이 기능에서 자주 바뀔 수 있는 부분은 어딘가", "한 클래스가 너무 많은 일을 하고 있지는 않나", "Claude Code가 이 코드를 수정할 때 어디까지 건드리게 될까"를 판단할 수 있어야 한다. 그 기준이 되는 것이 SOLID다.

---

## 2. 미니 케이스 — 넷플릭스 시청 기록 요약 API

문제: **사용자의 넷플릭스 시청 기록 요약을 반환하는 API를 만들어라.**

사용자가 월요일에 어떤 드라마를 20분, 화요일에 같은 에피소드를 이어서 15분, 수요일에 다른 영화를 40분 봤다고 하자. 이런 시청 이벤트가 쌓이면 앱은 최근 본 콘텐츠, 이어보기, 시청 진행률, 마지막으로 본 시간 같은 화면을 보여준다.

즉 이 API는 기록을 그대로 저장·반환하는 게 아니라, **여러 시청 이벤트를 모아서 사용자가 보기 좋은 형태로 정리해주는 API**다.

* 같은 영상을 여러 번 봤다면 합쳐야 한다
* 마지막으로 본 시간이 필요하다
* 얼마나 봤는지 진행률도 계산해야 한다

---

## 3. 하나의 함수로 시작하면 생기는 문제

```python
def get_watch_history(user_id):
    events = ...      # 이벤트를 받아온다
    result = ...       # 이벤트들을 처리한다
    return result
```

처음엔 이 함수 하나로 데이터를 가져오고, 콘텐츠별로 시청 시간을 합치고, 마지막 시청 시간을 찾고, 최근 본 순서로 정렬하고, 결과를 반환해도 괜찮아 보인다. 요구사항이 "최근 본 콘텐츠를 보여준다", "콘텐츠별 진행률을 보여준다" 두 개뿐이었을 때는 문제없다.

하지만 완료된 콘텐츠 상태를 다르게 집계하거나, 국가별로 다르게 표시하거나, 오늘은 최근 본 순서로 보여주고 내일은 추천 점수를 섞어서 보여주는 식으로 요구사항이 계속 추가되면, 이 함수 안에는 `if` ... `if` ... `if` ...가 계속 쌓인다.

* 데이터 가져오기
* 권한 필터링
* 시청 시간 합치기
* 진행률 계산
* 완료 여부 판단
* 정렬

이 모든 게 하나의 함수 안에 다 들어가 있는 것이다. 코드는 점점 길어지고, Claude Code가 수정할 때도 위험해진다.

---

## 4. 목표 구조 — 책임별로 나누기

| 클래스 | 책임 |
|---|---|
| **WatchHistoryService** | 전체 흐름을 조합한다 (모든 일을 직접 하지는 않는다) |
| **WatchEventRepository** | 시청 이벤트를 가져온다 |
| **WatchEventValidator** | 잘못된 이벤트를 걸러내고, 이 사용자가 이 콘텐츠를 볼 수 있는지 판단한다 |
| **ProgressCalculator** | 진행률을 계산한다 |
| **WatchStatusPolicy** | `IN_PROGRESS` / `COMPLETED` 여부를 판단한다 |
| (정렬 기준) | 최근 본 순서, 추천 점수 등 정렬 기준을 결정한다 |
| (응답 조합) | API 응답 모양을 만든다 |

`WatchHistoryService`는 이 모든 책임을 직접 수행하지 않고, 각 클래스에 위임해서 전체 흐름만 조합한다.

---

## 5. Claude Code 구조와 SOLID의 관계

Claude Code의 기본 흐름은 User Request → Context Assembly → Model Reasoning → Tool Use Request → Permission Gate → Tool Execution → 결과 → 검증이다. 모델은 판단만 하고, 실행은 Harness가 통제한다.

CodeCraft도 똑같다. 좋은 코드는 책임과 제약, 검증이 나뉜 구조에서 나온다. 이 구조를 이해하기 위한 마지막 이론이 **SOLID**다.

---

## 6. SOLID 5원칙 한눈에

| 원칙 | 이름 | 의미 |
|---|---|---|
| **S** | Single Responsibility | 책임이 한 곳에 섞이지 않게 막는다 |
| **O** | Open-Closed | 새 정책은 추가하고, 기존 로직 흐름은 덜 수정한다 |
| **L** | Liskov Substitution | 같은 역할의 객체는 같은 방식으로 교체 가능해야 한다 |
| **I** | Interface Segregation | 필요한 기능만 가진 작은 계약을 만든다 (불필요하게 큰 계약을 만들지 않는다) |
| **D** | Dependency Inversion | 중요한 흐름이 특정 구현에 묶이지 않게 한다 |

---

## 7. SOLID 5원칙 상세 — Claude Code 관점에서

### S — Single Responsibility: 수정 범위를 좁힌다

질문: **이 클래스는 왜 바뀌는가? 그 이유가 하나인가, 여러 개인가?**

> 예: `WatchHistoryService`가 DB 조회, 이벤트 검증, 콘텐츠 숨김 처리, 진행률 계산, 완료 여부 판단, 정렬, 응답 반환을 전부 한다고 하자. DB 구조가 바뀌어도, 정책이 바뀌어도, 진행률 계산이 바뀌어도, 정렬 기준이 바뀌어도 전부 이 하나의 파일을 고쳐야 한다.

책임을 나누면 `WatchEventRepository`(이벤트 로딩), `WatchEventValidator`(이벤트 검증), `VisibilityPolicy`(사용자가 볼 수 있는 콘텐츠인지 판단), `ProgressCalculator`(진행률 계산)로 분리된다.

> Claude Code 관점에서는 **수정 범위를 좁히는 원칙**이다. "필터링 정책만 바꿔줘"라고 하면 `VisibilityPolicy`만 보면 된다. 모든 로직이 `WatchHistoryService`에 섞여 있으면 정렬·진행률·응답 생성 코드까지 Claude Code가 건드릴 위험이 있다.

### O — Open-Closed: 확장에는 열려 있고 수정에는 닫혀 있게

질문: **새로운 정책이 생겼을 때 기존 코드를 계속 고쳐야 하는가?**

> 예: 정렬 기준이 "최근 본 순서"에서 "많이 본 순서", "추천 점수 순서"로 늘어날 때, `WatchHistoryService` 안에 `if`문을 계속 추가하는 대신 `RecentWatchSortPolicy`, `MostWatchSortPolicy`처럼 `SortPolicy`를 세부적으로 나눈다.

이렇게 하면 새 정책(`RecommendationSortPolicy`)이 필요할 때, Claude Code에게 "새 정책을 추가해줘, 기존 정책은 건드리지 마"라고 Harness Engineering을 적용할 수 있다. Claude가 수정해야 할 위치와 건드리면 안 되는 위치가 명확해진다.

### L — Liskov Substitution: 같은 역할이면 서로 바꿔 끼워도 똑같이 동작해야

같은 역할이라고 부를 거면, 서로 바꿔 끼워도 같은 방식으로 동작해야 한다. 구현체를 다른 구현체로 바꿔도 서비스가 깨지지 않아야 한다.

> 예: `SortPolicy`의 약속은 "아이템들을 받아서 정렬된 아이템들을 반환한다"다. `RecentWatchSortPolicy`든 다른 `SortPolicy`든 이 약속을 지켜야 `WatchHistoryService`가 어떤 `SortPolicy`가 들어와도 같은 방식으로 사용할 수 있다. 아무것도 반환하지 않거나, 가끔 다르게 동작하면 이 룰을 위반하는 것이다.

Claude Code에게 새 정책을 만들게 할 때는 이 계약을 명시해야 한다: "아이템들을 입력받고 정렬된 새 아이템 목록을 반환해야 하고, 원본 아이템을 직접 수정하지 말고, `VisibilityPolicy`의 책임을 침범하지 마라."

### I — Interface Segregation: 큰 계약 하나보다 작은 계약 여러 개

질문: **이 인터페이스가 필요 없는 기능까지 강요하고 있지는 않은가?**

인터페이스는 "이 역할을 맡으려면 이런 기능을 제공해야 한다"는 약속(설명서)이다. 로직을 직접 갖고 있지 않고, 함수를 어떻게 써야 할지 명세만 한다.

> 예: `WatchHistoryPolicy`라는 거대한 인터페이스 하나에 `ValidateEvent`, `CheckVisibility`, `CalculateProgress`, `Sort`, `BuildResponse`를 다 넣으면, Claude Code는 필요 없는 메서드까지 구현하려다 빈 메서드나 잘못된 기본값, 책임이 섞인 코드를 만들게 된다.

작은 인터페이스로 나누면 Claude에게 "너는 visibility만 판단해", "너는 progress만 계산해", "너는 sort만 해", "너는 response만 만들어"처럼 경계를 작게 줄 수 있다. Claude Code가 실수할 공간이 작아진다.

### D — Dependency Inversion: 특정 구현에 묶이지 않게

질문: **서비스가 구체적인 특정 클래스를 직접 만들고 있는가, 아니면 필요한 역할을 외부에서 받아서 쓰고 있는가?**

> 나쁜 구조: `WatchHistoryService`가 내부에서 직접 `RecentWatchSortPolicy`, `AdultProfileVisibilityPolicy`를 생성한다.
> 좋은 구조: `WatchHistoryService`가 `SortPolicy`, `VisibilityPolicy` 객체를 외부에서 전달받아 사용한다.

서비스가 정책을 직접 만들면, Claude Code가 새 정책을 추가할 때마다 서비스 코드를 고쳐야 한다. 서비스가 정책을 외부에서 받으면, Claude Code는 새 정책 클래스를 추가하고 설정만 바꾸면 된다. 이 원칙은 Claude Code의 수정 범위를 줄이고, 결과적으로 토큰도 적게 쓰게 해준다.

---

## 3줄 요약

1. 시청 기록 요약 API처럼 하나의 함수/클래스에 조회·검증·계산·정렬·응답을 다 몰아넣으면, 요구사항이 늘어날수록 수정 범위가 걷잡을 수 없이 커지고 Claude Code가 건드릴 위험도 커진다.
2. SOLID는 이 책임을 나누는 기준이다 — 책임을 한 곳에 섞지 않아 수정 범위를 좁히고(S), 새 정책은 기존 코드를 고치지 않고 추가하고(O), 같은 역할의 구현체는 서로 바꿔 끼워도 똑같이 동작해야 하고(L), 인터페이스는 필요한 만큼만 작게 쪼개고(I), 서비스는 정책을 직접 만들지 않고 외부에서 받아 쓴다(D).
3. SOLID를 적용하면 Claude Code에게 "이 정책만 바꿔줘", "새 정책만 추가하고 기존 건 건드리지 마"처럼 구체적으로 지시할 수 있어 수정 범위와 토큰 사용을 줄일 수 있으며, 다음 Section 5부터는 이 이론을 바탕으로 CodeCraft 실습을 진행한다.
