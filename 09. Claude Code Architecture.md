# 클로드 코드는 챗봇이 아니라 "작업을 이어가는 도구"

앤스로픽 공식 문서에서는 클로드 코드를 코드베이스를 읽고, 파일을 수정하고, 명령을 실행하고, 개발 도구와 통합하는 **에이전틱(agentic) 코딩 툴**이라고 설명한다.

여기서 "에이전틱"이라는 게 핵심인데, 그냥 질문에 답만 하고 끝나는 게 아니라 **스스로 상태를 확인하고 판단하고 다시 실행하는 걸 반복하면서 작업을 끝까지 밀고 나가는 도구**라는 뜻이다. 챗봇처럼 한 번 물어보고 한 번 답 받고 끝나는 구조가 아니라, 마치 페어 프로그래밍하는 주니어 개발자한테 티켓 하나를 통째로 맡기는 느낌에 가깝다.

예를 들어 백엔드 작업에 클로드 코드를 쓴다고 하면:

1. 문제를 관찰하면서 원인을 좁히고
2. 관련 파일을 수정하고
3. 설정하고 테스트하고
4. 검토하고

이 과정을 한 번의 답변으로 끝내는 게 아니라, 중간중간 상태를 다시 읽고 다시 판단하면서 반복한다. 그래서 클로드 코드는 대화 인터페이스라기보다는 **터미널(커맨드라인) 위에서 동작하는 실행 시스템**으로 이해하는 게 맞다. 코딩뿐 아니라 문서 작성, 빌드 실행, 파일 검색, 조사 같은 커맨드라인에서 할 수 있는 작업 전반을 다룬다.

## 아키텍처를 이루는 구성 요소들

클로드 코드 아키텍처를 구성하는 요소들이 몇 가지 있는데, iOS 개발 관점에서 대응시켜보면 이렇게 이해하면 편하다.

- **CLAUDE.md**: 프로젝트 수준의 지시사항과 규칙을 유지하는 "기억 장치" 역할을 하는 파일. iOS로 치면 프로젝트 컨벤션을 정리해둔 `CONTRIBUTING.md`나 팀 코딩 스타일 가이드 문서를 클로드가 항상 참고하게 만든 버전이라고 보면 된다.
- **init**: 프로젝트 시작 시 필요한 초기 설정 파일을 만들어주는 명령. Xcode 프로젝트 템플릿을 뽑는 것과 비슷한 느낌.
- **Skills**: 반복 가능한 워크플로우를 클로드의 툴킷에 추가하는 방식. 매번 같은 작업 방식을 다시 설명 안 해도 되게끔 등록해두는 것. Fastlane의 `lane`을 미리 정의해두는 것과 비슷한 개념이라고 보면 이해하기 쉽다.
- **Hooks**: 특정 시점마다 반드시 실행되도록 강제하는 규칙/자동화 장치. Git의 `pre-commit` 훅처럼, LLM이 "알아서 하겠지" 하고 스킵하지 못하게 강제로 걸어두는 안전장치다.
- **GitHub Actions**: PR이나 이슈 흐름 안에서 클로드 코드를 실제 협업 시스템(CI/CD)에 연결하는 부분. Xcode Cloud나 Fastlane + GitHub Actions 조합으로 자동 빌드/테스트 돌리는 것과 같은 맥락.
- **Agent / Teams / Sub-Agent**: 컨텍스트를 분리하고 작업을 병렬화할 때 쓰는 구조. 하나의 작업을 여러 서브 에이전트로 나눠서 동시에 처리하는 개념으로, 멀티스레딩에서 작업을 큐로 나눠 병렬 처리하는 것과 비슷한 그림이다.

이 요소들을 종합하면 클로드 코드는 **기억 · 컨텍스트 · 실행 · 검증 · 자동화**를 함께 다루는 하나의 작업 시스템이라고 정리할 수 있다.

## 4개의 레이어로 나눠서 보기

강의에서는 이 구조를 4개 레이어로 나눠서 설명한다. 이 부분이 실제로 클로드 코드를 쓸 때 "왜 결과가 이상하게 나오지?"를 진단하는 데 제일 유용하다.

### 1. 규칙 레이어 (Rule Layer)

`CLAUDE.md`, 프로젝트 규칙, 금지 사항, 코딩 스타일 컨벤션 등 세션이 바뀌어도 계속 유지돼야 하는 기준이 여기 들어간다.

이 레이어가 약하면 **같은 프로젝트인데도 세션마다 결과가 계속 흔들린다.** iOS 개발로 치면 팀 컨벤션 문서가 없어서 PR마다 코드 스타일이 제각각인 상황과 비슷하다.

### 2. 컨텍스트 레이어 (Context Layer)

지금 해결하려는 문제, 관련 파일, 실패 로그, 수정 범위처럼 **이번 작업에만 필요한 문맥**이 들어가는 레이어다.

클로드가 아무리 똑똑해도 컨텍스트가 부족하면 엉뚱한 방향으로 나아갈 수 있다. 즉, "자꾸 관련 없는 파일을 건드린다"는 증상이 나오면 이 레이어가 부실한 거다.

### 3. 전략 레이어 (Strategy Layer)

바로 수정할지, 먼저 계획을 세울지, 서브 에이전트로 작업을 분리할지, 어디까지 자동화하고 어디서 사람이 검토할지 같은 **판단**이 이루어지는 레이어. 작업 종류에 따라 계획과 실행을 구분하는 접근을 강조한다.

수정 품질이 계속 들쭉날쭉하다면 이 레이어를 보강해야 한다.

### 4. 실행 · 검증 레이어 (Execution & Verification Layer)

파일 수정, 테스트 실행, diff 검토, Git 정리, 자동 검증, PR 생성처럼 **실제로 반영하고 검증하는 작업**이 일어나는 레이어. 앞서 나온 Hooks나 GitHub Actions가 바로 이 레이어를 강하게 만들어주는 장치다.

코드는 맞는 것 같은데 충돌이나 버그가 계속 난다면 이 레이어를 다시 봐야 한다.

## 정리하면서 느낀 점

이렇게 레이어로 나눠서 보면 클로드 코드는 프롬프트만 잘 쓴다고 해서 잘 써지는 도구가 아니라는 게 명확해진다. 결과가 이상할 때 "어느 레이어에서 병목이 생겼는지"를 진단하고 그 레이어를 보강하는 접근이 필요하다.

| 증상 | 보강해야 할 레이어 |
|---|---|
| 세션마다 결과가 계속 다름 | 규칙 레이어 |
| 자꾸 엉뚱한 파일을 건드림 | 컨텍스트 레이어 |
| 수정 품질이 들쭉날쭉함 | 전략 레이어 |
| 코드는 맞는데 충돌·버그 발생 | 실행·검증 레이어 |

그리고 각 구성 요소의 역할도 다시 정리하면:

- `CLAUDE.md`는 단순 메모가 아니라 프로젝트 규칙을 안정시키는 장치
- Skills는 기능 추가가 아니라 반복되는 워크플로우를 매번 다시 설명하지 않게 만드는 장치
- Hooks는 자동화 옵션이 아니라 중요한 검증이나 규칙을 놓치지 않게 만드는 장치
- GitHub Actions는 개인 사용을 팀 협업 수준으로 끌어올리는 장치

결국 코드베이스 이해 → 문맥 유지 → 전략 선택 → 파일 수정 → 명령 실행 → 검증 → 협업 → 자동화까지 하나의 루프로 연결돼 있고, 각 기능이 이 루프에서 어떤 병목을 줄이는 도구인지 보는 게 클로드 코드를 잘 쓰는 방법인 것 같다.

---

### 3줄 요약

1. 클로드 코드는 질문에 답만 하는 챗봇이 아니라, 상태를 확인하고 판단하고 다시 실행하는 걸 반복하며 작업을 끝까지 수행하는 에이전틱 도구다.
2. 아키텍처는 CLAUDE.md(규칙), Skills(반복 워크플로우), Hooks(강제 자동화), GitHub Actions(협업 연동), Sub-Agent(병렬 처리)로 구성된다.
3. 문제가 생기면 규칙 · 컨텍스트 · 전략 · 실행/검증 4개 레이어 중 어디서 병목이 생겼는지 진단하고 그 레이어를 보강해야 한다.
