# 클로드 코드 — "아키텍쳐" 핵심 요약

## 1. 가장 중요한 원칙

> **클로드 코드는 챗봇이 아니라 에이전틱(agentic) 코딩 툴이다.**

질문 한 번, 답 한 번으로 끝나는 구조가 아니라, **상태를 확인 → 판단 → 다시 실행**을 반복하며 작업을 끝까지 밀고 나가는 도구다. 페어 프로그래밍하는 주니어 개발자에게 티켓 하나를 통째로 맡기는 느낌에 가깝다.

---

## 2. 아키텍처 구성 요소

| 구성 요소 | 역할 |
|---|---|
| **CLAUDE.md** | 프로젝트 규칙/지시사항을 유지하는 "기억 장치" (팀 코딩 스타일 가이드) |
| **init** | 프로젝트 시작 시 초기 설정 파일 생성 |
| **Skills** | 반복 가능한 워크플로우 등록 (매번 재설명 불필요, Fastlane `lane`과 유사) |
| **Hooks** | 특정 시점마다 반드시 실행되도록 강제 (Git `pre-commit`처럼 스킵 불가능한 안전장치) |
| **GitHub Actions** | PR/이슈 흐름에서 CI/CD와 연결 |
| **Agent / Teams / Sub-Agent** | 컨텍스트 분리 + 작업 병렬화 |

> 종합하면: **기억 · 컨텍스트 · 실행 · 검증 · 자동화**를 함께 다루는 하나의 작업 시스템.

---

## 3. 4개 레이어로 진단하기

결과가 이상할 때 "어느 레이어에서 병목이 생겼는지" 진단하는 것이 핵심이다.

| 레이어 | 내용 | 문제 증상 |
|---|---|---|
| **규칙 레이어** | CLAUDE.md, 프로젝트 규칙, 금지 사항, 컨벤션 | 세션마다 결과가 계속 다름 |
| **컨텍스트 레이어** | 이번 작업에 필요한 문제/파일/로그/수정 범위 | 자꾸 엉뚱한 파일을 건드림 |
| **전략 레이어** | 바로 수정 vs 계획 우선, 서브 에이전트 분리 여부, 자동화 범위 판단 | 수정 품질이 들쭉날쭉함 |
| **실행·검증 레이어** | 파일 수정, 테스트, diff 검토, Git 정리, PR 생성 (Hooks/Actions가 강화) | 코드는 맞는데 충돌·버그 발생 |

---

## 4. 구성 요소별 진짜 역할 (오해 방지)

* `CLAUDE.md` → 단순 메모가 아니라 **프로젝트 규칙을 안정시키는 장치**
* Skills → 기능 추가가 아니라 **반복 워크플로우 재설명 방지 장치**
* Hooks → 자동화 옵션이 아니라 **중요한 검증/규칙을 놓치지 않게 하는 강제 장치**
* GitHub Actions → 개인 사용을 **팀 협업 수준으로 끌어올리는 장치**

전체 루프: 코드베이스 이해 → 문맥 유지 → 전략 선택 → 파일 수정 → 명령 실행 → 검증 → 협업 → 자동화

---

## 3줄 요약

1. 클로드 코드는 질문에 답만 하는 챗봇이 아니라, 상태를 확인하고 판단하고 다시 실행하는 걸 반복하며 작업을 끝까지 수행하는 에이전틱 도구다.
2. 아키텍처는 CLAUDE.md(규칙), Skills(반복 워크플로우), Hooks(강제 자동화), GitHub Actions(협업 연동), Sub-Agent(병렬 처리)로 구성된다.
3. 문제가 생기면 규칙 · 컨텍스트 · 전략 · 실행/검증 4개 레이어 중 어디서 병목이 생겼는지 진단하고 그 레이어를 보강해야 한다.
