# 그래프 엔지니어링 — Node, Edge, State

## 1. 가장 중요한 원칙

> **Graph를 구성하는 핵심 요소는 세 가지다 — Node, Edge, State.**

AI Workflow에서 쓰는 Graph는 복잡한 수학 개념이 아니다. 어떤 작업이 존재하고, 어떤 작업의 결과가 어디로 전달되는지 표현한 구조일 뿐이다.

---

## 2. Node — 하나의 명확한 작업 단위

좋은 Node는 입력과 출력이 분명하고, 하나의 책임만 가진다.

> 예: "주제를 조사하고 내용을 요약하고 출처를 확인한 뒤 최종 글까지 작성하라"는 하나의 Node로 보기엔 범위가 너무 넓다. 서로 다른 작업이 여러 개 섞여 있기 때문이다.

이걸 나누면 이런 Node들이 된다.

* 자료 검색
* 자료 분석(요약)
* 출처 검증
* 최종 글 작성

> Node를 설계할 때는 "이 Node는 무엇을 입력받고 어떤 결과를 만드는가"를 한 문장으로 설명할 수 있는지 확인해야 한다. 설명이 길어지거나 여러 동사가 들어간다면, 작업을 더 나눌 수 있다는 신호다.

---

## 3. Edge — 데이터 의존성

Edge는 단순한 실행 순서가 아니라 **데이터 의존성**을 의미한다. 한 Node가 만든 결과를 다른 Node가 실제로 필요로 할 때, 두 Node 사이에 Edge가 생긴다.

> 예: "파일 A를 검토하고 파일 B를 검토한다"를 문장으로 쓰면 순서대로 이어지는 것처럼 보인다. 하지만 파일 B를 검토할 때 파일 A의 검토 결과를 쓰지 않는다면, 두 작업 사이에 실제 의존성은 없다 — 동시에 실행할 수 있다.

> Edge가 필요한지 판단하려면: 앞 Node가 만든 어떤 데이터가 뒤 Node의 입력으로 전달되는지 생각해본다. 전달되는 데이터를 구체적으로 설명할 수 있다면 실제 Edge다. 설명할 데이터가 없다면, 단순히 프롬프트에 적힌 순서 때문에 기다리고 있을 가능성이 크다.

---

## 4. State — 여러 Node가 공유하는 정보

State는 여러 Node가 함께 사용하거나, 다음 실행까지 보존해야 하는 정보다.

> 예: 경쟁사 조사 Workflow라면 조사할 회사 목록, 회사별 작업 진행 상태, 수집한 출처, 검증을 통과한 주장, 실패한 Node, 남은 재시도 횟수, 현재까지 사용한 비용 등을 State로 둘 수 있다.

State가 많다고 좋은 게 아니다. 각 Node가 필요로 하는 정보만 전달해야 한다. 모든 대화 기록과 로그를 매번 넘기면 컨텍스트가 커지고, 정작 중요한 정보가 묻힐 수 있다.

> State를 설계할 때는 필드마다 "이 값이 없으면 다음 Node가 작업할 수 있는가"를 따져보고, 필요하지 않다면 공유 State에서 제외하는 편이 좋다.

---

## 5. 우리가 이미 쓰던 Linear Chain도 그래프다

멀티 스텝 프롬프트도 이미 Graph 구조를 가지고 있다. 다만 아래처럼 **Linear Chain** 형태로 작성될 뿐이다.

> Research → Summarize → Write → Check Sources → Format → Publish

각 단계가 이전 단계의 완료를 기다리는 구조라, 전체 실행 시간도 모든 단계 시간을 더한 값에 가까워진다. 그런데 실제로 모든 단계가 바로 앞 단계의 결과를 필요로 하는지 살펴봐야 한다. 여러 Source를 읽는 작업, 여러 파일을 검토하는 작업은 서로 독립적일 수 있다.

> Graph Engineering을 시작할 때 가장 먼저 해야 하는 일은 에이전트를 더 추가하는 게 아니라, **실제로 필요하지 않은 대기를 찾는 것**이다.

---

## 6. Fake Edge 테스트 — 불필요한 대기 찾기

1. Workflow의 모든 단계를 박스로 작성한다
2. 현재 실행 순서에 따라 화살표(Edge)를 연결한다
3. 모든 화살표마다 "앞 단계의 결과가 다음 단계의 입력으로 실제 사용되는가"를 분석한다 — 사용되면 Edge 유지, 사용되지 않으면 제거(**Fake Edge**)

> 예: 경쟁사 A/B/C를 조사한 뒤 세 회사를 비교하는 Workflow. B를 조사할 때 A의 결과가 필요 없고, C도 마찬가지다. 세 회사의 조사 결과는 마지막 비교 단계에서만 함께 필요하다. 그래서 A/B/C 조사를 각각 독립적으로 병렬 실행한 뒤, 마지막 비교 단계만 셋을 합친다.

각 조사가 5분씩 걸린다면: 순차 실행은 15분, 병렬 실행은 가장 늦게 끝나는 작업 기준 약 5분이면 끝난다. (실제 환경에서는 API 횟수 제한·동시 실행 제한·도구 성능에 따라 차이가 있지만, 원칙적인 개념은 같다.)

---

## 7. 다이아몬드 패턴 — 가장 많이 쓰이는 구조

불필요한 Edge를 제거하면 여러 워크플로우에서 비슷한 형태가 나타난다. **하나의 입력에서 여러 작업이 나뉘고, 각 작업의 결과를 마지막에 다시 합치는 구조**다. 모양 때문에 다이아몬드 패턴이라 부른다.

> 예: 검색 → Source 1/2/3을 독립적으로 분석 → 결과를 합쳐서 보고서 작성. Source 2를 분석할 때 Source 1의 결과가 필요 없고, Source 3도 이전 분석 결과와 무관하게 처리할 수 있다. 그래서 3개 소스는 동시에 분석하고, 최종 결과는 세 결과를 모두 써야 하므로 모든 분석이 끝날 때까지 기다린다.

> 여기서 발생하는 대기는 불필요한 지연이 아니다. 최종 결과를 만들기 위해 필요한 **실제 의존성**이다.

### 다이아몬드 패턴을 쓸 때 확인해야 할 두 가지

1. **병렬 노드들이 서로 독립적이어야 한다** — Source 2를 분석하는 데 Source 1의 중간 결론이 필요하다면 둘을 동시에 실행하면 안 된다.
2. **최종 노드가 병렬 결과 전체를 실제로 사용해야 한다** — 최종 결과에 Source 1만 필요하다면, Source 2와 Source 3 분석은 불필요한 비용이다.

> 이 패턴의 목표는 병렬화 자체가 아니라, **필요한 작업의 대기 시간을 줄이는 것**이다.

---

## 8. Barrier와 Streaming

**Barrier**는 여러 병렬 작업이 끝나기를 기다리는 지점이다. 필요할 때 써야 하지만, 모든 단계마다 넣으면 오히려 처리 시간이 길어질 수 있다.

> 예: 파일 10개를 분석 → 수정 → 테스트. Barrier 방식에서는 분석이 빨리 끝난 파일도 가장 느린 파일의 분석이 끝날 때까지 기다린다. 수정 단계에서도 같은 일이 반복된다.

**Streaming** 방식: 각 파일이 독립적이라면, 완료되는 순서대로 바로 다음 단계로 보낸다. File A가 먼저 분석되면 다른 파일을 기다리지 않고 바로 수정 단계로 이동한다. 빠른 작업이 느린 작업의 진행 속도에 맞춰 기다리지 않아도 된다.

### Barrier가 필요한 경우

* 소스 사이의 중복을 제거할 때
* 전체 후보를 한 번에 랭킹할 때
* 여러 결과를 비교해서 하나를 선택할 때
* 전체 파일의 크로스 파일 디펜던시를 분석할 때
* 모든 작업 결과가 비어 있는지 확인한 뒤 종료할 때

---

## 9. 빠른 Graph만으로는 충분하지 않다 — 결과 검증이 필요한 이유

병렬 실행을 추가하면 전체 처리 시간은 줄어든다. 하지만 결과 품질을 확인하지 않고 그대로 합치면 새로운 문제가 생긴다.

> 예: Node A(정확한 결과), Node B(정확한 결과), Node C(출처 없는 통계) — 세 리서치 노드의 결과를 Synthesis Node가 받는다. Synthesis Node는 어떤 입력이 잘못되었는지 알지 못하기 때문에, 세 결과를 자연스럽게 연결해서 하나의 보고서로 만들 수 있다. 문장은 매끄럽지만 근거가 잘못된 결과가 섞여 들어간다.

그래서 병렬 처리와 최종 종합 사이에는 **Checker(Verifier)**가 필요하다. Checker 노드는 최종 글을 작성하거나 주요 작업을 대신 수행하지 않는다. **각 결과를 다음 단계에서 써도 되는지만 판단**한다.

### Checker가 확인해야 할 것

1. **빈 결과** — 노드가 실제로 쓸 수 있는 내용을 반환했는가 (파일은 생성됐지만 내용이 없거나 요청과 무관한 문장만 있을 수도 있다)
2. **주제 일치** — 결과가 원래 작업 범위와 맞는가 (경쟁사 가격을 조사해달랬는데 회사 역사만 썼다면 통과시키면 안 된다)
3. **출력 형식** — 다음 노드가 기대하는 스키마·포맷을 만족하는가 (JSON을 받아야 하는데 마크다운이 반환되면 파싱 단계에서 오류가 난다)
4. **근거** — 중요한 주장에 확인 가능한 소스·테스트 결과·로그가 있는가
5. **상호모순** — 여러 노드의 결과가 서로 충돌하는가 (같은 제품 가격을 두 노드가 다르게 보고했다면 추가 검증이 필요하다)
6. **중복** — 동일한 파인딩이 여러 표현으로 반복되었는가 (제거하지 않으면 특정 문제가 실제보다 더 중요해 보일 수 있다)

---

## 10. 작업을 만든 에이전트가 직접 검토하면 안 되는 이유

하나의 에이전트에게 결과물을 만들고 바로 검토까지 맡기는 구조는 편리하다. 하지만 같은 컨텍스트에서 작업과 검증을 모두 수행하면 검토가 약해질 가능성이 높다. 작성 과정에서 에이전트는 특정 결정을 내린 이유와 중간 추론을 모두 알고 있어서, 결과를 다시 볼 때 실제 출력만 평가하기보다 "자신이 왜 그렇게 작성했는지"까지 함께 고려하게 된다.

따라서 **생성(Generation)과 검증(Evaluation)을 분리**하는 편이 좋다.

* **Generator Agent**: 결과물을 만들어 저장한다
* **Evaluator Agent**: 새로운 컨텍스트에서, 원래 요구사항 + 결과물 + 검증 기준 + 필요한 소스·테스트 결과만 전달받는다 (Generator의 전체 대화 기록은 전달하지 않는다)

두 역할의 지시도 다르게 구성한다.

* Generator: *"요구사항을 충족하는 가장 좋은 결과를 만드세요."*
* Evaluator: *"이 결과가 기준을 충족하지 못하는 지점을 찾아서, 구체적인 근거와 함께 보고해라."*

> 이렇게 해야 검증 과정에서 독립적인 관점을 확보할 수 있다.

---

## 11. 모델보다 스크립트가 더 잘하는 검증

Checker를 모두 AI 에이전트로 만들 필요는 없다. **명확한 규칙으로 판단할 수 있는 작업에는 일반 코드가 더 적합**하다.

* **스크립트/기존 도구가 적합한 것**: 타입 체크, Unit/Integration Test, Lint, JSON Schema Validation — 결과가 일관되고 실행 비용도 낮다.
* **에이전트가 적합한 것**: 요구사항을 제대로 해석했는지, 기술적 대안 중 어느 쪽이 더 적합한지, 소스가 충돌할 때 어떤 결론이 타당한지, 결과가 사용자 의도와 맞는지, 설계 트레이드오프가 합리적인지

> 그래프를 설계할 때 가장 먼저 따져봐야 할 질문: **이 판단을 정말 LLM이 해야 하는가, 아니면 코드로 확정할 수 있는가?**

---

## 3줄 요약

1. Graph는 Node(하나의 명확한 책임을 가진 작업 단위), Edge(실행 순서가 아니라 실제 데이터 의존성), State(여러 Node가 공유·보존해야 하는 최소한의 정보) 세 요소로 구성되며, 우리가 짜온 멀티 스텝 프롬프트도 이미 Linear Chain 형태의 그래프였다.
2. Graph Engineering의 시작은 Fake Edge 테스트로 불필요한 대기(가짜 의존성)를 찾아 제거하는 것이며, 서로 독립적인 작업은 다이아몬드 패턴으로 병렬 실행하고, Barrier는 정말 필요한 지점(중복 제거, 랭킹, 비교 선택, 크로스 파일 의존성 분석 등)에만 두고 나머지는 Streaming으로 처리한다.
3. 병렬 결과를 그냥 합치면 근거 없는 결과까지 매끄럽게 섞이므로 병렬 처리와 최종 종합 사이에 Checker/Verifier를 두고, 생성(Generator)과 검증(Evaluator)은 별도 컨텍스트로 분리해야 하며, 타입 체크·테스트·스키마 검증처럼 명확한 규칙은 스크립트로, 해석·트레이드오프 판단은 에이전트로 나눠야 한다.
