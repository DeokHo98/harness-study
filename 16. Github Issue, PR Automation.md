# GitHub 이슈-PR 자동화 (Claude) — 핵심 요약

## 1. 가장 중요한 원칙

> **자동화가 된다고 끝까지 자동으로 밀어붙이면 안 된다.**

이슈에는 애매한 요청, 외부 입력, 검증 안 된 요구사항이 섞여 있을 수 있다. 그대로 `main`까지 밀어 넣으면 속도는 빨라지지만 **제어력이 확 떨어진다.**

---

## 2. 자동화 범위

### 자동으로 되는 것

1. 이슈 내용 읽기
2. 작업용 브랜치 생성 (`feature/signup-modal`)
3. 초안 수준 코드 수정
4. 커밋 생성
5. Draft PR 오픈

### 반드시 사람이 직접 확인해야 하는 것

* **최종 코드 리뷰**
* **위험 파일 확인** (배포 설정, 인증/권한 코드, 시크릿 키 관련 코드)
* **CI 통과 여부**
* **머지 승인**

> iOS 예시: Info.plist 권한/URL Scheme, fastlane 배포 스크립트, 서명 인증서/프로비저닝, API 키 Config 파일 — 자동 수정되더라도 무조건 재검토 필요.

---

## 3. 세팅 방법 요약

1. **API 키 등록**: Settings → Secrets and variables → Actions → `ANTHROPIC_API_KEY` 등록
2. **워크플로우(YAML)**: 이슈 생성/댓글 시 트리거 — 구조는 **"이슈 읽기 → 초안 코드 수정 → Draft PR"**만 이해하면 충분 (YAML 문법 암기 불필요)
3. **실제 흐름**: 이슈 작성 → 댓글로 Claude 멘션 → Actions 트리거 → Claude가 구조 분석/구현/테스트/요약/Draft PR 생성 → 사람이 리뷰·CI 확인 후 머지

---

## 4. 언제 쓰고, 언제 피해야 하나

| 쓰기 좋은 경우 | 피해야 하는 경우 |
|---|---|
| 비슷한 이슈가 반복적으로 들어올 때 | 레포 권한 구조가 정리 안 됐을 때 |
| 브랜치 생성+초안 작업 패턴이 매번 비슷할 때 | 리뷰 문화가 없는 팀 |
| 팀에 리뷰 문화·CI 체계가 갖춰져 있을 때 | 배포/인프라/보안 관련 파일이 자주 얽히는 레포 |
| | 외부 입력을 검증 없이 믿으면 안 되는 환경 |

---

## 5. 조심해야 할 실수들

* 자동화된다고 **머지까지** 자동으로 밀어붙이기
* Claude에게 **권한을 필요 이상으로 넓게 주기**
* API 키/토큰이 **로그나 코드에 노출**되게 두기
* 이슈에 들어온 **외부 입력을 검증 없이 그대로** 프롬프트로 신뢰하기

> **본질**: "완전 자동"이 아니라 **"반복되는 초안 작업을 줄여주는 도구"**다. 최종 판단은 여전히 사람 몫.

---

## 3줄 요약

1. GitHub 이슈만 잘 써두면 Claude가 브랜치 생성부터 초안 코드 작성, Draft PR까지 자동으로 해줌.
2. 단, 최종 리뷰·위험 파일(배포/인증/시크릿) 확인·CI 통과·머지 승인은 반드시 사람이 직접 해야 함.
3. 반복되는 이슈가 많고 리뷰 체계가 잡혀있는 팀에서만 쓰는 게 좋고, 권한을 과하게 주거나 외부 입력을 그대로 믿는 실수는 조심해야 함.
