# GitHub 이슈만 잘 써도 PR까지 자동으로? Claude로 이슈-PR 자동화 해보기

Xcode에서 브랜치 파고 커밋하고 PR 올리는 거, 매번 손으로 하다 보면 은근 반복 작업 많잖아. 이번에 본 강의는 **GitHub 이슈를 잘 써두면 Claude가 알아서 브랜치 만들고, 코드 초안 짜고, Draft PR까지 올려주는** 자동화 흐름에 대한 거였어. iOS 개발자 입장에서 이해하기 쉽게 정리해봤어.

## 핵심 아이디어: 어디까지 자동화하고, 어디서 사람이 봐야 하나

일단 이 자동화가 하는 일은 이 정도야:

1. 이슈 내용 읽기
2. 작업용 브랜치 생성 (`feature/signup-modal` 이런 식)
3. 초안 수준의 코드 수정
4. 커밋 생성
5. Draft PR 오픈

여기까지는 자동으로 굴러가. 근데 강의에서 계속 강조하는 포인트가 하나 있어. **"자동화가 된다고 끝까지 자동으로 밀어붙이면 안 된다"**는 거야.

왜냐면 이슈에는 애매한 요청, 외부에서 들어온 입력, 아직 검증 안 된 요구사항 같은 게 섞여 있을 수 있거든. 이걸 그대로 `main`까지 밀어 넣으면 속도는 빨라지지만 제어력이 확 떨어져. 그래서 아래 4가지는 무조건 사람이 직접 확인해야 한다고 해:

- **최종 코드 리뷰**
- **위험 파일 확인** (배포 설정, 인증/권한 관련 코드, 시크릿 키가 얽힌 코드)
- **CI 통과 여부**
- **머지 승인**

iOS 프로젝트로 치면, Info.plist의 URL Scheme이나 권한(Permission) 설정, fastlane 배포 스크립트, 서명 인증서/프로비저닝 관련 파일, API 키 들어있는 Config 파일 같은 게 자동으로 수정되는 건 무조건 사람이 다시 봐야 한다는 얘기야. Claude가 초안은 만들어도 이런 파일이 껴 있으면 반드시 리뷰 필요.

## 실제로 어떻게 세팅하는지

### 1. API 키 등록

레포 설정에서 **Settings → Secrets and variables → Actions**로 들어가서 `ANTHROPIC_API_KEY`를 시크릿으로 등록해줘. 이게 있어야 GitHub Actions에서 Claude API를 호출할 수 있어.

### 2. 워크플로우(YAML) 파일

`Claude Issue Automation`이라는 이름의 GitHub Actions 워크플로우가 이슈가 생성되거나 이슈에 댓글이 달릴 때 트리거되도록 짜여 있어. 참고로 여기서 YAML 문법 자체를 외울 필요는 없고, **"이슈 읽기 → 초안 코드 수정 → Draft PR"**이라는 구조만 이해하면 충분해.

### 3. 실제 흐름

이슈를 하나 올려봤는데, 예시가 이런 식이었어.

> **제목:** 회원가입 버튼이 동작하지 않는 문제
> **재현:** 로그인 페이지에서 회원가입 버튼 클릭 시 아무 반응 없음 (스테이징 환경)
> **기대 동작:** 회원가입 버튼 클릭 시 모달 형태로 회원가입 UI가 떠야 함. 입력 폼, 약관 동의, 데이터 처리 정책 동의 포함.

이슈 작성 후 댓글로 Claude를 멘션하면, Actions가 트리거되면서 Claude가 작업을 시작해. iOS로 치면 Xcode Cloud나 fastlane match 돌리는 것처럼, 이슈 이벤트 → 워크플로우 트리거 → 자동 작업 실행되는 구조라고 생각하면 돼.

Claude가 진행하면서 하는 일들:

- 프로젝트 구조 분석
- 회원가입 모달 컴포넌트 설계
- 폼 UI 구현
- 테스트 진행 후 결과 요약
- 어떤 파일을 왜 수정했는지 설명
- Draft PR 생성

작업 끝나면 어떤 핸들러를 수정했고 어떻게 문제를 해결했는지까지 정리해서 알려줘. 이후에 사람이 리뷰하고 CI 통과 확인한 다음 머지하면, 실제로 회원가입 모달이 뜨고 약관 동의 UI, 입력 폼, 빈 필드 경고까지 다 동작하는 걸 확인할 수 있었어.

## 언제 쓰면 좋고, 언제는 피해야 하나

**쓰기 좋은 경우**
- 비슷한 이슈가 반복적으로 들어올 때
- 브랜치 생성 + 초안 작업 패턴이 매번 비슷할 때
- 팀에 이미 리뷰 문화랑 CI 체계가 갖춰져 있을 때

**피해야 하는 경우**
- 레포 권한 구조가 아직 정리 안 됐을 때
- 리뷰 문화가 없는 팀
- 배포/인프라/보안 관련 파일이 자주 얽히는 레포
- 외부 입력을 검증 없이 그대로 믿으면 안 되는 환경

## 조심해야 할 실수들

- 자동화된다고 머지까지 자동으로 밀어붙이기
- Claude한테 권한을 필요 이상으로 넓게 주기
- API 키나 토큰이 로그/코드에 노출되게 두기
- 이슈에 들어온 외부 입력을 검증 없이 그대로 프롬프트로 신뢰하기

결국 이 자동화의 본질은 "완전 자동"이 아니라 **"반복되는 초안 작업을 줄여주는 도구"**야. 최종 판단은 여전히 사람 몫으로 남겨두는 게 핵심이었어.

---

## 3줄 요약

1. GitHub 이슈만 잘 써두면 Claude가 브랜치 생성부터 초안 코드 작성, Draft PR까지 자동으로 해줌.
2. 단, 최종 리뷰·위험 파일(배포/인증/시크릿) 확인·CI 통과·머지 승인은 반드시 사람이 직접 해야 함.
3. 반복되는 이슈가 많고 리뷰 체계가 잡혀있는 팀에서만 쓰는 게 좋고, 권한을 과하게 주거나 외부 입력을 그대로 믿는 실수는 조심해야 함.
