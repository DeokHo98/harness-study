# CodeCraft 미니 케이스 — Harness Engineering 적용하기 (2/3)

## 1. 가장 중요한 원칙

> **Subagent Delegation은 여러 AI가 대화하는 구조가 아니라, Parent Context와 분리된 작업 단위를 만들고 permission·transcript를 별도로 다루는 구조다.**

이 "책임 분리" 원칙이 CodeCraft 설계에도 그대로 적용된다.

---

## 2. Subagent Delegation 복습

Claude Code의 서브 에이전트는 여러 명의 AI가 단체 채팅으로 대화하는 구조가 아니다. **부모 작업 공간과 분리된 작은 작업실을 따로 만들어서 일하게 하는 구조**다.

* **Parent Context**: 메인 Claude Code 세션이 보고 있는 전체 작업 맥락 — 지금까지의 대화, 프로젝트 규칙, 읽은 파일 내용, 수정한 파일 목록, 실행한 명령어 결과, 현재 작업 목표
* **Transcript**: 작업 기록 파일. 어떤 대화를 했고, 어떤 도구를 썼고, 어떤 결과가 나왔는지 기록한 로그

Parent Agent가 특정 작업만 Subagent에게 위임하면, Subagent는 분리된 작은 작업 공간에서 독립적으로 작업하고, 끝나면 **요약 결과만** Parent에게 반환한다.

> 만약 모든 에이전트가 같은 컨텍스트를 공유하면 서로를 오염시키고, 불필요한 작업 기록이 메인 세션에 계속 쌓이고, 권한(Permission) 관리가 흐려진다.

### 왜 요약만 반환하는가

* Parent Context가 너무 커지는 것을 막기 위해서다. Claude Code는 Context Window에 한계가 있다.
* Subagent가 실험적으로 여러 파일을 읽거나, 여러 추측과 실패한 시도를 하는 과정이 전부 Parent로 들어오면 메인 세션이 지저분해진다.
* Subagent는 원인을 찾을 때까지 시도를 반복한다(파일 확인 → 테스트 재실행 → 로그 확인 → 다른 가설 시도). 최종 원인과 추천만 요약해서 Parent에게 반환한다.

### Permission도 분리된다

Subagent가 생겼다고 Parent의 모든 권한을 그대로 물려받지 않는다.

| 주체 | 하는 일 |
|---|---|
| **Parent Agent** | 전체 작업 방향 결정, 사용자와 상호작용, 최종 판단 |
| **Subagent** | 제한된 작업만 수행, 필요한 Context만 사용, 별도 기록 저장, 요약만 반환 |

이렇게 하면 Subagent가 너무 많은 권한을 가지고 폭주하는 것을 막을 수 있다.

---

## 3. CodeCraft에 적용하기 — 책임 분리

Delivery Fee Calculator를 예로 들면, 좋은 설계는 책임을 모듈별로 분리하는 것이다.

* 입력값 검증
* 기본 배송비 계산
* 날씨·거리·시간대 추가 요금 계산
* 멤버십·쿠폰·무료 배송 할인 계산
* 최종 결과 반환(Result)

> Subagent가 Parent Context와 분리되어 작은 작업을 수행하듯, 좋은 설계도 큰 책임을 작은 책임 단위로 분리한다.

### 나쁜 설계 예시

하나의 함수(Calculator) 안에 이 모든 책임을 다 넣는 것이다. 함수는 입력을 받아 출력을 내는 구조인데, 그 안에서 여러 작업을 한꺼번에 수행하면 좋은 설계가 아니다. 독립된 작업들을 각각 책임 분리해서 진행해야 유지보수 비용, 확장성, 디자인 패턴 적용 측면에서 좋다.

---

## 4. Append-Only JSONL — 기록은 지우지 않고 이어붙인다

Claude Code는 대화·작업 기록을 계속 저장한다. 다만 기존 기록을 지우고 새로 쓰는 게 아니라, **기록을 계속 뒤에 이어붙이는 방식(Append-only)**으로 저장한다. 이 기록을 바탕으로 이전 세션을 다시 열거나, 복사해서 이어가거나, 특정 시점으로 되돌아갈 수 있다. 단, 이전에 허용했던 위험한 권한까지 그대로 복원되지는 않는다.

* **JSONL (JSON Lines)**: 한 줄에 하나의 기록을 JSON 형태로 저장하는 방식

> 예: 3번 결정이 마음에 안 들어도 3번을 지우고 다시 쓰지 않는다. "이전 설계를 수정하기로 결정", "정책을 분리함" 같은 새 기록을 아래에 계속 붙여 나간다.

세션 기록은 보통 User Prompt → Assistant Reasoning/Response → Tool Use → Tool Results → Assistant Response → User Feedback → New Decision 순서로, 한 줄씩 JSON으로 계속 쌓인다.

---

## 5. Human Decision Authority — 최종 판단은 사람이

Claude Code 아키텍처를 이끄는 다섯 원칙 중 하나가 **Human Decision Authority**다. 인간은 액션을 관찰하고, 승인하거나 거부하고, 진행 중인 작업을 중단하고, 사후 감사(Audit)를 수행할 수 있다. Claude Code가 아무리 강력해져도 최종 의사결정권은 인간에게 남아 있어야 한다.

> CodeCraft에서도 Claude는 설계를 제안할 수 있지만, 어떤 요구사항을 받아들이고 어떤 제약을 우선시하고 어떤 패턴이 과한지 판단하는 것은 사람의 몫이다. Harness Engineering이 필요한 이유가 여기에 있다.

Claude Code를 잘 쓰는 사람은 Claude가 만든 결과를 보고, 이 설계가 현재 요구사항에 맞는지·과하게 복잡하지는 않은지·검증 가능한지·확장성은 어떤지를 스스로 판단할 수 있는 사람이다.

---

## 6. Generation과 Evaluation 분리 — Verification Harness

Claude 에이전트는 실제 품질이 평범해도 자신의 작업을 자신감 있게 "잘했다"고 말하는 경향이 있다. 그래서 **Generation(생성)**과 **Evaluation(평가)**을 분리해야 한다. 신뢰 가능한 루프는 컨텍스트 수집 → 액션 수행 → 결과 검증으로 이어진다.

| 단계 | CodeCraft 적용 예시 |
|---|---|
| **Generation** | Policy 구현, Validator 구현, Service 구현 |
| **Evaluation** | 정상 주문 테스트, 비 오는 날 요금 테스트, 무료 배송 기준 테스트 등 |

> Claude가 "구현을 완료했다"고 말해도 그게 맞다는 뜻은 아니다. Generation과 Evaluation이 분리되어야 이 검증이 제대로 이뤄진다. 이것이 Verification Harness의 핵심이다.

---

## 3줄 요약

1. Subagent Delegation은 Parent Context와 분리된 작업 공간에서 Subagent가 독립적으로 작업하고 요약만 반환하는 구조이며, CodeCraft 설계도 이렇게 하나의 함수에 모든 책임을 몰아넣지 않고 검증·계산·할인·결과 반환처럼 책임을 모듈별로 분리해야 한다.
2. Claude Code는 기록을 지우지 않고 append-only JSONL 방식으로 계속 이어붙여 저장하므로, 이전 결정을 수정할 때도 지우는 대신 새 결정을 뒤에 덧붙인다.
3. 최종 의사결정권(Human Decision Authority)은 항상 사람에게 있어야 하고, Claude가 "완료했다"는 말을 그대로 믿지 않도록 Generation(구현)과 Evaluation(검증)을 분리하는 것이 Verification Harness의 핵심이다.
