# Claude Code 오래 쓰면 무조건 겪는 문제: "세션 오염" 정리

Claude Code로 작업하다 보면 처음엔 분명 말도 잘 듣고 똑똑했는데, 대화가 길어지고 파일도 많이 읽고 로그도 계속 붙이다 보면 어느 순간 이상하게 삐끗하는 경험, 한 번쯤 있을 거야. 분명 하지 말라고 한 파일을 건드리거나, 검증을 빼먹거나, 아까 정해준 규칙을 슬쩍 놓치거나. 이게 "모델이 갑자기 멍청해졌다"가 아니라 **세션 오염(context pollution)** 때문이라는 얘기를 정리해봤어.

## 세션 오염이 뭔데?

Claude 문서에서도 컨텍스트 윈도우는 대화가 진행될수록 이전 메시지랑 응답이 계속 누적된다고 설명해. iOS 개발자한테 익숙한 비유로 하면, 앱 메모리에 릴리즈 안 된 객체가 계속 쌓이는 느낌이랑 비슷하다고 보면 돼. 세션이 길어질수록 유용한 정보도 쌓이지만, 그만큼 필요 없는 잡음(노이즈)도 같이 쌓여. 그러다 보면 초반에 줬던 제약 조건이나 지시사항이 뒤에 쌓인 정보에 묻혀서 희석돼 버리는 거지. 그래서 작업 품질은 모델 성능만의 문제가 아니라, 컨텍스트를 얼마나 깨끗하게 유지했는지에 따라서도 크게 좌우돼.

그래서 실무에서는 세션 품질을 운에 맡기지 말고 아예 루틴으로 관리해줘야 하는데, 그 루틴이 바로 `/clear`랑 `/compact`야.

## `/clear` — 완전 초기화

`/clear`는 iOS 앱으로 치면 프로세스를 강제 종료하고 새로 실행하는 것과 비슷해. 대화 히스토리를 통째로 날리고 컨텍스트를 완전히 비워버리는 명령어야 (공식 설명도 "Clear conversation history and free up context"라고 되어 있어).

핵심은 **작업 단위(A)가 끝났으면 다음 작업(B)을 같은 컨텍스트에서 질질 끌고 가지 말라**는 거야. 이전 작업의 맥락이 다음 작업에 섞이기 시작하면 오염 속도가 훨씬 빨라지거든. 그래서 작업이 바뀌는 시점마다 `/clear`로 끊어주는 게 맞아.

## `/compact` — 리셋이 아니라 압축

`/compact`는 리셋이 아니라 요약이야. 공식 설명도 "Clear conversation history **but keep a summary in context**"로 되어 있어. 대화를 통째로 버리는 대신, 요약본만 남기고 정리해주는 거지. 긴 작업을 끊지 않고 계속 이어가야 할 때 유용해.

그런데 여기서 많은 사람들이 실수를 해. 그냥 `/compact`만 치면 알아서 잘 남겨줄 거라고 생각하는데, **아니야.** `/compact`를 쓸 때는 뭘 남길지 직접 지시를 해줘야 해. 예를 들면:

- 변경된 파일 목록
- 실행한 커맨드
- 결과 요약
- 남은 To-do 리스트

이런 걸 "이건 꼭 남겨라"라고 명시적으로 지시해야 돼. 이걸 안 하면 요약은 생기는데 정작 중요한 맥락은 빠져버려서, 다음 턴에서 또 방향이 틀어질 수 있어.

## 제일 중요한 포인트: 요약을 파일로 승격시키기

여기서 진짜 핵심은 **요약을 갱신하는 것**, 그리고 대화 중에 나온 중요한 결론·금지 사항·테스트 커맨드·결정 사항들을 `CLAUDE.md`나 `.clauderules` 같은 파일로 승격시켜주는 거야.

왜 이게 중요하냐면, 대화 안에만 있던 규칙은 `/compact` 과정에서 사라질 수 있지만, 파일로 승격된 규칙은 이후에도 계속 프로젝트 컨텍스트로 다시 읽히기 때문이야. iOS 개발로 비유하면 **대화 속 규칙은 인메모리 캐시**, **파일 속 규칙은 UserDefaults나 로컬 DB 같은 영속 저장소**라고 생각하면 이해하기 쉬울 거야. 휘발성 vs 지속성 차이지.

## 실습으로 보면 이해가 빠름

강의에서는 일부러 세션을 오염시켜보는 실습을 했어. 흐름은 이랬어:

1. 한 세션 안에서 로그인 버튼 스타일 수정, 에러 로그 확인, 테스트 파일 읽기 같은 서로 다른 종류의 작업을 계속 섞어서 요청
2. 파일 읽기, 로그 분석, UI 변경, 금지 사항, 테스트 요구가 한 세션에 뒤섞이면서 점점 대화가 지저분해지는 게 체감됨
3. 새 작업을 시작할 때 `/clear`로 리셋 → 탐색 범위를 명확하게 좁혀서 지정하니 훨씬 빠르게 작업이 진행됨
4. 애니메이션 버그 수정 같은 후속 작업을 진행하면서 다시 세션이 어느 정도 쌓임 (원인 분석 → 범위 좁히기 → 코드 수정 → 빌드로 검증)
5. 이 시점에서 `/compact` 실행 — 이때 그냥 압축하지 않고, **"변경한 파일 목록, 실행한 명령, 현재까지 통과한 테스트와 실패한 버그를 남겨줘"** 라고 커스텀 지시를 넣어서 압축
6. 이렇게 남긴 내용 중 지속적으로 유지해야 할 규칙은 다시 `CLAUDE.md`, `.clauderules` 같은 파일로 옮겨서 승격

## 정리하면

- **작업이 바뀌면** → `/clear`로 끊어줘
- **작업이 길어지면** → `/compact`로 압축하되, 남길 항목(파일 목록, 커맨드, 테스트 결과, 투두 등)을 직접 지시해줘
- **계속 유지해야 하는 규칙**이 있다면 → 대화에만 두지 말고 `CLAUDE.md`/`.clauderules` 파일로 옮겨줘

이렇게 루틴화해두면 Claude도 불필요한 컨텍스트에 토큰을 덜 쓰고, 훨씬 일관되게 작업을 이어갈 수 있어.

---

### 3줄 요약
1. 세션이 길어지면 잡음이 쌓여서 초기 지시사항이 희석되는 게 "세션 오염"이고, 작업 품질은 모델 성능뿐 아니라 컨텍스트 관리 상태에도 좌우돼.
2. 작업 단위가 바뀌면 `/clear`로 완전히 리셋하고, 작업이 이어지면 `/compact`로 압축하되 남길 항목(파일 목록·커맨드·테스트 결과·투두)을 직접 지시해줘야 해.
3. 계속 지켜야 할 규칙은 대화에만 두지 말고 `CLAUDE.md`/`.clauderules` 파일로 승격시켜야 `/compact` 이후에도 유지돼.
