# Claude Fable 5 — 장기 자율 실행과 Effort Inversion

## 1. 가장 중요한 원칙

> **Fable 5는 같은 기반 모델을 Safe Guard 유무로 다르게 배포한 모델이며, 장기 자율 실행(Long-horizon Autonomous Execution)과 Effort Inversion이 핵심 특징이다.**

2026년 6월에 나온 Claude Fable 5를 이해하려면 먼저 받아들여야 할 사실이 하나 있다. **동일 모델의 이중 배포**라는 점이다.

---

## 2. 동일 모델, 이중 배포 — Fable 5 vs Mythos

같은 기반(underlying) 모델이지만, 서로 다른 성격으로 배포된 두 제품이 있다.

| | Fable 5 | Mythos |
|---|---|---|
| Safe Guard | 있음 | 제거됨 |
| 공개 범위 | 일반 공개 | 승인된 조직에 한정 |

두 모델의 차이는 성능이 아니라 **Safe Guard의 유무**다. Anthropic도 "두 모델을 구분하는 것은 Safe Guard"라고 공식 발표했다.

> 이게 중요한 이유: 지금까지 모델을 고르던 기준(성능·가격·속도)에 이제 **"Safe Guard가 내 요청에 어떻게 개입하는가"**가 추가되었기 때문이다.

---

## 3. 가격 — Opus 5의 2배

Fable 5는 기존 최상위 모델이던 Opus 5보다 한 단계 위에 있고, 가격은 Opus 5의 2배다. 어떤 작업이 이 비용을 낼 만한지가 다음 주제다.

---

## 4. 장기 자율 실행 (Long-horizon Autonomous Execution)

작업 지속 시간에 따라 모델을 고르는 기준이 있다.

| 모델 | 작업 지속 시간 | 용도 |
|---|---|---|
| Sonnet 5 / Haiku | 수초 ~ 수분 | 단발적 응답, 분류, 요약 |
| Opus 5 | 수십 분 ~ 수시간 | 복잡한 코딩, 다중 파일 작업, 에이전트 세션(Claude Code 등) |
| **Fable 5** | 수시간 ~ 수일 | 며칠짜리 마이그레이션, 대규모 리팩토링, 사람 개입 없는 장기 작업 |

> 모델을 고르는 기준이 "얼마나 어려운가"에서 **"얼마나 오래 혼자 가야 하는가"**로 확장된다.

### Stripe 사례

Stripe는 5000만 줄 규모의 루비 코드베이스에서, 코드베이스 전반에 걸친 마이그레이션 한 건을 Fable 5로 **하루 만에** 수행했다. 사람이 수동으로 했다면 한두 달 이상 걸렸을 것으로 추정된다.

---

## 5. Safeguards 라우팅 흐름

Fable 5에는 별도의 판별 시스템이 붙어 있다.

* 사용자 요청 → **Safety Classifier**가 안전한지 판별
* 통상적인 요청 → Fable 5가 그대로 응답
* 사이버 보안, 생물·화학 등 특정 주제 감지 → **Opus 4.8로 폴백**

완전히 거부하는 대신, 위험 주제를 감지했을 때 더 안전한 모델(Opus 4.8)로 우회시켜 작업을 이어가게 하는 방식이다.

> Anthropic 공식 발표: Fable Session의 95%를 초과하는 비율에서는 폴백이 전혀 발생하지 않는다 — 즉 5% 미만에서만 폴백이 걸린다는 뜻이다.

### 클라이언트 vs API의 동작 차이

| 사용 환경 | 폴백 시 동작 |
|---|---|
| claude.ai / Claude Code / Cowork | Opus 4.8로 자동 라우팅되고 사용자에게 고지된다. 그 대화는 계속 Opus에 머무르며, Fable 5로 돌아가려면 새 대화를 시작해야 한다 |
| Messages API | HTTP 200과 함께 `Stop Reason: Refusal`로 응답한다 (에러가 아니라 정상 응답) |

> 프로덕션에서 Fable 5를 API로 쓴다면 `Stop Reason` 검사를 반드시 해줘야 한다.

---

## 6. Effort Inversion

Effort는 Low / Medium / High / X-High로 설정할 수 있다. 직관적으로는 "노력을 많이 들일수록(High, X-High) 품질이 계속 올라간다"고 생각하기 쉽지만, Fable 5·Opus 5 공식 가이드 기준으로는 그렇지 않다.

* **Low / Medium / High 구간**: 훨씬 적은 토큰과 대기 시간으로 이미 뛰어난 품질을 낸다. 이 구간에서 품질이 유지된다면, 비용과 지연 시간을 조절하는 주 수단으로 써야 한다.
* **X-High**: 까다로운 코딩·에이전트 작업에만 올려서 쓰는 수준이다. 모든 작업의 기본값이 아니다.

### 권장 워크플로우

1. 기본값 **High**로 시작한다
2. 자체 평가 결과에 따라 **Medium**으로, 품질이 유지되면 **Low**로 조정한다 — 이게 비용·응답 시간을 조절하는 주된 수단이다
3. 이전 모델에서 쓰던 Effort 값을 그대로 가져왔다면 다시 테스트하고 다시 고정해야 한다
4. 자체 평가 세트를 만들어 Low/Medium/High를 실제로 돌려보면, 상당수 작업에서 낮은 설정으로도 충분하다는 것을 발견하게 되고, 이를 통해 비용을 효율적으로 조절할 수 있다

---

## 3줄 요약

1. Fable 5와 Mythos는 같은 기반 모델을 Safe Guard 유무로 다르게 배포한 것이며, 이제 모델을 고를 때 "Safe Guard가 내 요청에 어떻게 개입하는가"까지 고려해야 한다.
2. Fable 5는 가격이 Opus 5의 2배지만 수시간~수일짜리 장기 자율 실행(며칠짜리 마이그레이션, 대규모 리팩토링)에 강하며, Stripe는 5000만 줄 루비 코드베이스 마이그레이션을 하루 만에 끝냈다.
3. Safety Classifier가 위험 주제를 감지하면 완전 거부 대신 Opus 4.8로 폴백하고(클라이언트는 자동 라우팅+고지, API는 `Stop Reason: Refusal`), Effort는 무조건 High/X-High가 아니라 Low/Medium/High 구간에서 품질이 유지되는 가장 낮은 값을 자체 평가로 찾아 비용·지연을 조절하는 게 권장된다.
