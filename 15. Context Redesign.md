# 클로드가 엉뚱한 수정을 내놓을 때 — 컨텍스트 재설계 핵심 요약

## 1. 가장 중요한 원칙

> **문제는 모델이 아니라 '지시'다.**

AI가 엉뚱하게 수정하는 건 모델 성능 문제가 아니라, **지시가 너무 넓고, 금지 범위가 없고, 완료 판단 기준이 없어서** 생기는 문제다. "로그인 버튼 고쳐주세요"처럼 넓은 지시는 어디까지 건드려도 되는지, 무엇을 확인해야 끝난 건지 아무 정보도 주지 않는다.

> **해결책은 설명을 더 길게 하는 게 아니라 컨텍스트 자체를 재설계하는 것.**

---

## 2. 컨텍스트 재설계 4가지 요소

1. **문제를 한 문장으로 정의** — 목표를 명확히 한 줄로
2. **수정 가능 범위 vs 수정 금지 범위 분리** — 건드려도 되는 파일/영역과 절대 안 되는 영역 구분
3. **절대 깨지면 안 되는 조건 명시** — 예: "버튼 위치/디자인은 바꾸지 말 것"
4. **완료 기준(검증 방법) 정하기** — 무엇을 실행해야 "다 됐다"고 볼 수 있는지 미리 정의

> 요약: "알아서 잘 고쳐주세요" → **"이 범위 안에서, 이 파일은 건드리지 말고, 이 조건을 유지하며, 이 방법으로 검증해줘"**

---

## 3. 이미 산으로 간 세션엔 설명만 덧붙이지 말 것

* AI가 엉뚱한 방향으로 작업을 시작한 뒤 그 위에 추가 설명만 붙이면 **오히려 더 위험** — 이미 오염된 컨텍스트에 좋은 지시도 희석됨
* 올바른 대처: **작업을 멈추고 방향을 끊은 뒤, 새로 좁은 범위로 다시 명확하게 지시**
* 변경 범위가 큰 작업은 바로 수정시키지 말고 **계획(plan)부터 먼저 보여달라고 요청** (PR 올리기 전 diff 리뷰하듯)

---

## 4. 실제 비교: 애매한 요청 vs 재설계한 요청

| | 애매한 요청 | 재설계한 요청 |
|---|---|---|
| 지시 | "사이드 메뉴에 로그인 버튼 추가해줘" | 목표/현재상태/수정가능범위/수정금지범위/유지조건/검증방법/보고형식 명시 |
| 결과 | 버튼은 추가됐지만 클릭 시 아무 동작 없음 | 로그인 페이지 이동까지 정상 동작 + 변경 파일 목록·검증 결과 보고 |

### 재설계 요청 예시 구조

* **목표**: 버튼 클릭 시 로그인 페이지로 이동, 기존 디자인 컨셉에 맞는 UI 구성
* **현재 상태(재현)**: 버튼은 있지만 클릭해도 반응 없음
* **수정 가능 범위**: 사이드바 관련 컴포넌트 파일
* **수정 금지 범위**: 환경 설정 파일, CI/CD 워크플로우 파일
* **유지 조건**: 버튼 위치/디자인 변경 금지
* **검증 방법**: 빌드 + 테스트 통과 확인
* **완료 보고 형식**: 변경 파일 목록, 핵심 수정 내용, 검증 결과 정리

---

## 5. 언제 써야 할까

### 재설계가 필요한 상황

* AI가 자꾸 관련 없는 파일까지 건드릴 때
* "수정 완료"라 했는데 검증(테스트)이 비어있을 때
* 작업 범위가 여러 디렉토리/모듈로 넓어질 때
* API, 인프라 설정, CI/CD workflow 등 절대 건드리면 안 되는 영역이 포함될 때

### 오버스펙인 상황

* 단순 오타 수정, 한 줄짜리 문구 수정처럼 검증 범위가 이미 명확한 작은 작업 → **짧게 요청하는 게 더 효율적**

---

## 3줄 요약

1. AI가 엉뚱하게 수정하는 건 대부분 모델 문제가 아니라 지시가 넓고 금지 범위·완료 기준이 없어서 생기는 문제다.
2. 목표를 한 문장으로 정의하고, 수정 가능/금지 범위와 절대 깨지면 안 되는 조건, 완료 검증 방법을 명확히 나눠서 지시해야 한다.
3. 이미 엉뚱한 방향으로 간 세션엔 설명만 덧붙이지 말고 방향을 끊은 뒤 좁은 범위로 다시 지시하며, 범위가 큰 작업은 바로 수정 대신 계획부터 보게 하는 게 좋다.
