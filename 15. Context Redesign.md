# 클로드가 엉뚱한 수정을 내놓을 때, 프롬프트를 어떻게 다시 짜야 할까

AI한테 "이거 고쳐줘" 하고 던졌는데, 결과물을 열어보니 뭔가 이상하게 작업된 경험 다들 있을 거야. 버그 하나만 고쳐달라고 했는데 관련도 없는 파일까지 건드려놨거나, 테스트도 안 돌리고 "완료!"라고 끝내버리거나, 심지어 건드리면 안 되는 설정 파일까지 손을 댄 경우도 있고. 이번 글에서는 이런 상황이 왜 생기는지, 그리고 프롬프트(정확히는 **컨텍스트**)를 어떻게 다시 짜야 이런 사고를 줄일 수 있는지 정리해봤어.

## 문제는 모델이 아니라 '지시'다

이런 일이 생기면 보통 "AI가 멍청하네"라고 모델 탓을 하기 쉬운데, 사실은 모델 성능 문제가 아닌 경우가 훨씬 많아. 진짜 원인은 **지시가 너무 넓고, 금지 범위가 없고, 언제 끝난 건지 판단할 기준 자체가 없다는 것**이야.

예를 들어 "로그인 버튼 고쳐주세요"라고만 말하면, AI 입장에서는 선택지가 너무 많아. 어디까지 건드려도 되는지, 어디는 절대 건드리면 안 되는지, 마지막에 뭘 확인해야 끝난 건지 아무것도 모르는 상태로 작업을 시작하게 되는 거지. Xcode에서 신입 개발자한테 "로그인 화면 좀 손봐줘"라고만 말하고 아무 컨텍스트도 안 주는 것과 비슷한 상황이라고 보면 돼.

여기서 중요한 포인트는, 해결책이 **설명을 더 길게 하는 게 아니라는 것**. 컨텍스트 자체를 다시 설계해야 해.

## 컨텍스트 재설계, 4가지 요소

지시를 다시 짤 때 아래 4가지를 채워 넣어주면 훨씬 안정적으로 작업해.

1. **문제를 한 문장으로 정의하기** — 목표가 뭔지 명확하게 한 줄로.
2. **수정 가능 범위와 수정 금지 범위 분리하기** — "이 파일/영역은 건드려도 되고, 이건 절대 건드리지 마"를 구분해서 알려주기.
3. **절대 깨지면 안 되는 조건 명시하기** — 예를 들면 "버튼 위치나 디자인은 바꾸지 말 것" 같은 제약.
4. **완료 기준(검증 방법) 정하기** — 뭘 실행해봐야 "다 됐다"고 볼 수 있는지 미리 정의하기.

정리하면 "알아서 잘 고쳐주세요" 방식이 아니라, **"이 범위 안에서 고치고, 이 파일은 건드리지 말고, 이 조건은 유지하면서, 이 방법으로 검증해줘"**로 바꿔주는 거야. 이렇게 하면 AI가 헤맬 수 있는 여지 자체가 확 줄어들어.

## 이미 산으로 간 세션에 설명만 덧붙이지 말 것

여기서 많은 사람들이 하는 실수가 있는데, AI가 이미 엉뚱한 방향으로 작업을 시작한 뒤에 그 상태에서 추가 설명만 계속 붙여주는 거야. 이건 오히려 더 위험해. 이미 컨텍스트가 오염된 상태라서, 아무리 좋은 지시를 줘도 앞의 잘못된 맥락에 희석돼버리거든.

그래서 방법은 이거야: 잘못된 방향이 보이면 **일단 작업을 멈추고 방향을 끊은 다음, 새로 좁은 범위의 작업으로 다시 명확하게 지시**를 줘야 해. 특히 변경 범위가 큰 작업이라면, 처음부터 바로 수정시키지 말고 **계획(plan)만 먼저 보여달라고 요청**하는 것도 좋은 방법이야. 마치 PR 올리기 전에 diff부터 리뷰하는 것처럼, 실제 코드를 건드리기 전에 "어떤 파일을 왜 보려고 하는지"부터 확인하는 거지.

## 실제 비교: 애매한 요청 vs 재설계한 요청

강의에서는 실제로 두 가지 방식을 비교해서 보여줘.

**애매하게 요청한 경우**: "왼쪽 사이드 메뉴에 로그인 버튼을 추가해줘"라고만 말했더니, AI는 버튼 위치는 그럭저럭 잘 잡았지만 **클릭했을 때 아무 동작도 하지 않는** 반쪽짜리 결과물을 내놨어. 지시가 넓으니 딱 "버튼 추가"까지만 하고 끝낸 거지.

**재설계해서 요청한 경우**: 같은 작업을 아래처럼 다시 짜서 줬어.

- **목표**: 로그인 버튼을 누르면 로그인 페이지로 이동하고, 그 페이지는 기존 디자인 컨셉에 맞는 로그인 UI로 구성되어야 함
- **현재 상태(재현)**: 버튼은 추가됐지만 클릭해도 아무 반응이 없음
- **수정 가능 범위**: 사이드바 관련 컴포넌트 파일 (예: `layout-sidebar.tsx`)
- **수정 금지 범위**: 환경 설정 파일, CI/CD 관련 워크플로우 파일
- **유지해야 할 조건**: 버튼의 위치나 디자인은 바꾸지 말 것
- **검증 방법**: 빌드와 테스트를 돌려서 정상 동작 확인 (iOS로 치면 Xcode에서 빌드 성공 + 유닛테스트/UI테스트 통과 확인하는 느낌)
- **완료 보고 형식**: 변경한 파일 목록, 핵심 수정 내용, 실행한 검증 결과를 마지막에 정리해서 알려줄 것

이렇게 범위를 좁혀서 주니까, AI는 더 적은 범위의 파일만 살펴보고(관련 파일 검색 후 필요한 부분만 확인), 실제로 로그인 페이지 이동까지 정상 동작하는 결과물을 만들어냈어. 그리고 요청한 대로 변경 파일 목록과 검증 결과까지 깔끔하게 정리해서 보고했고.

## 이 방식, 언제 써야 할까

이 컨텍스트 재설계 방식이 특히 필요한 상황은:

- AI가 자꾸 관련 없는 파일까지 건드릴 때
- "수정 완료"라고는 했는데 검증(테스트)이 비어있을 때
- 작업 범위가 여러 디렉토리/모듈로 넓어질 때
- 외부에 노출되는 API, 인프라 설정, CI/CD workflow처럼 절대 건드리면 안 되는 영역이 포함될 때

반대로 **너무 작은 작업에는 오버스펙**이야. 단순 오타 수정이나 한 줄짜리 문구 수정처럼 검증 범위가 이미 명확한 작은 작업까지 이렇게 길게 프롬프트를 짤 필요는 없어. 이럴 땐 그냥 짧게 요청하는 게 더 빠르고 효율적이야.

## 결론

핵심은 이거야. "설명을 길게 하면 알아서 잘 해결해주겠지"라는 믿음은 틀렸고, 실제로 중요한 건 **검증 기준과 수정 금지 범위를 먼저 명확히 주는 것**. 그리고 이미 잘못된 방향으로 간 세션 위에 설명만 계속 덧붙이는 것도, 큰 작업을 처음부터 바로 수정시키는 것도 둘 다 컨텍스트를 더 오염시킬 뿐이야. 범위가 불명확한 작업일수록 컨텍스트를 재설계해서 주고, 아주 작은 작업엔 굳이 이 방식을 과하게 쓰지 않는 게 포인트.

---

### 3줄 요약
1. AI가 엉뚱하게 수정하는 건 대부분 모델 문제가 아니라 지시가 넓고 금지 범위·완료 기준이 없어서 생기는 문제다.
2. 목표를 한 문장으로 정의하고, 수정 가능/금지 범위와 절대 깨지면 안 되는 조건, 완료 검증 방법을 명확히 나눠서 지시해야 한다.
3. 이미 엉뚱한 방향으로 간 세션엔 설명만 덧붙이지 말고 방향을 끊은 뒤 좁은 범위로 다시 지시하며, 범위가 큰 작업은 바로 수정 대신 계획부터 보게 하는 게 좋다.
