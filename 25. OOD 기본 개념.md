# OOD 기본 개념 — 요구사항을 클래스와 책임으로 바꾸기

## 1. 가장 중요한 원칙

> **OOD는 큰 문제를 작은 책임으로 분할하고, Claude Code가 안전하게 구현할 수 있는 구조를 만드는 방법이다.**

Object-Oriented Design(객체 지향 설계)을 어렵게 말하면 그렇지만, 쉽게 말하면 **큰 문제를 작은 역할들로 나누고, 각 역할이 자기 일만 하게 만드는 설계 방법**이다. CodeCraft에서 필수적으로 요구되는 지식이라 짚고 넘어간다.

---

## 2. 왜 함수 하나로 끝내면 안 되는가

배달 수수료 계산기를 Python 함수 하나로 만들면 이렇게 시작할 수 있다.

```python
def calculate_delivery_fee(order):
    return 5000
```

하지만 실제 서비스는 이렇게 단순하지 않다. 비 오는 날엔 추가 요금이 붙고, 프리미엄 고객은 할인을 받고, 주문 금액이 높으면 무료 배송이 될 수도 있다.

요구사항이 늘어날수록 함수 하나가 너무 많은 일을 하게 된다. 거리 계산, 날씨 계산, 할인, 취소 주문 검증이 전부 한 곳에 섞인다. "눈 오는 날 추가 요금도 넣어주세요", "신규 고객 첫 주문 무료 배송도 넣어주세요"처럼 요구사항이 추가될 때마다 같은 함수를 계속 고쳐야 한다. 함수 하나가 요구사항에 따라 끝없이 커지는 것이다.

---

## 3. OOD — 역할별로 나누기

학교 단체 프로젝트에 비유하면 이해가 쉽다. 한 사람이 자료 조사·발표 자료·발표·디자인·질문 답변을 전부 하면 힘들다. 그래서 역할을 나눈다. 자료 조사 담당, 발표 자료 담당, 디자인 담당처럼.

코드도 똑같다. 한 함수가 모든 일을 하게 만들지 않고, **주문 데이터 담당 / 입력 검증 / 기본 배송비 계산 / 추가 요금 계산**처럼 역할별로 나눈다. 이것이 OOD의 목적이다.

---

## 4. 책임에서 클래스로

OOD에서는 **책임이 요구사항에서 나오고, 그 책임이 클래스가 된다.** 요구사항을 읽고 필요한 책임을 찾은 뒤, 그 책임을 클래스로 바꾸는 것이다.

> 요구사항: "배달 수수료 계산기는 아래와 같은 일을 해야 한다."
> 1. 주문 정보를 받는다
> 2. 잘못된 주문인지 확인한다
> 3. 기본 배송비를 계산한다
> 4. 추가 요금(날씨·거리 등)을 계산한다
> 5. 할인을 계산한다
> 6. 최종 배송비 결과를 반환한다

이 책임들을 클래스로 바꾸면:

| 책임 | 클래스 |
|---|---|
| 주문 정보를 받는다 | `Order` |
| 잘못된 주문인지 확인한다 | `OrderValidator` |
| 기본 배송비를 계산한다 | `BaseFeePolicy` |
| 추가 요금을 계산한다 | `SurchargePolicy` |
| 할인을 계산한다 | `DiscountPolicy` |
| 최종 배송비 결과를 반환한다 | `DeliveryFeeResult` |

> 클래스 이름을 먼저 외우면 안 된다. 그전에 스스로 물어야 할 질문들이 있다: "이 기능이 해야 되는 일은 무엇인가?", "그 일들 중 서로 다른 책임은 무엇인가?", "각 책임을 누가 맡으면 좋을지?" — 그 답이 곧 클래스가 된다.

---

## 5. 클래스 vs 오브젝트

* **클래스**: 설계도
* **오브젝트**: 그 설계도로 실제로 만들어진 것

```python
class Order:
    pass

order_1 = Order()
order_2 = Order()
```

`Order` 클래스는 "주문은 이런 정보를 가져야 해"라고 알려주는 설계도다. `order_1`과 `order_2`는 둘 다 같은 설계도에서 만들어졌지만 서로 다른 실제 주문이다. 즉, 클래스는 **하나의 책임을 고정할 수 있는 역할·설계도**이고, 오브젝트는 그 설계도로 실제로 만들어진 것(예: 실제로 들어온 123번 주문)이다.

---

## 6. 객체의 종류 — Entity, Value Object, DTO

비전공자라면 완벽히 외울 필요는 없다. 아래 기준만 기억하면 된다.

| 종류 | 기준 | 예시 |
|---|---|---|
| **Entity** | ID가 중요하다 | `Order`(주문 ID), `Customer`(고객 ID), `Driver`(드라이버 ID) — 금액 등 다른 값이 바뀌어도 ID가 같으면 같은 대상 |
| **Value Object** | 값 자체(값의 묶음)가 중요하다 | `DeliveryFeeResult`(기본요금+추가요금+할인+최종요금), `Money`, `Distance` |
| **DTO** | 데이터를 주고받기 위한 전달 모양이 중요하다 | `DeliveryFeeResponseDTO` — API 응답으로 화면이나 다른 서버에 값을 전달하는 형태 |

> 간단히: ID가 중요하면 Entity, 값의 묶음이 중요하면 Value Object, 전달 모양이 중요하면 DTO.

---

## 7. 정리

* 큰 기능을 작은 책임으로 나눈다
* 각 책임에 이름을 붙인다 → **클래스**
* 실제 데이터 하나하나가 → **오브젝트**
* 바뀔 수 있는 부분은 **Policy**나 **Interface**로 분리한다
* 전체 흐름은 **Service**로 조합한다
* 마지막으로 **테스트**로 검증한다

> OOD는 요구사항에서 책임을 찾고, 그 책임을 코드 안에서 안전하게 나누는 기술이다. Claude Code에게 좋은 코드를 만들게 하려면, 먼저 사람이 이 책임 구조를 잡아줘야 한다.

---

## 3줄 요약

1. OOD는 하나의 함수에 모든 로직을 몰아넣는 대신, 큰 문제를 작은 책임으로 나누고 각 역할이 자기 일만 하게 만드는 설계 방법이다.
2. 요구사항을 읽고 필요한 책임을 찾은 뒤 그 책임을 클래스로 바꾸며, 클래스는 설계도이고 오브젝트는 그 설계도로 실제로 만들어진 것이다.
3. 객체는 ID가 중요하면 Entity, 값의 묶음이 중요하면 Value Object, 전달 모양이 중요하면 DTO로 나눌 수 있고, Claude Code에게 좋은 코드를 만들게 하려면 사람이 먼저 이 책임 구조를 잡아줘야 한다.
