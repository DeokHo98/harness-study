# Claude Code Verification Harness — 핵심 요약

## 1. 가장 중요한 원칙

> **“코드가 실행된다” ≠ “코드가 올바르다”**

AI는 좁은 Context 안에서 그럴듯한 코드를 만들 수 있지만, 프로젝트 전체의 아키텍처·API 계약·컨벤션·엣지 케이스까지 완벽하게 보장하지 않는다.

따라서 **AI가 “완료했습니다”라고 말하는 것 자체는 검증이 아니다.**

---

## 2. 검증은 3단계로 한다

### ① Automated Checks — 기계적 검증

반복 가능한 검사를 자동화한다.

* XCTest / Unit Test
* Build / Type Check
* Lint
* Smoke Test
* CI

단, **테스트 통과만으로 완료 판정을 내리면 안 된다.**

---

### ② Diff Verification — 변경 범위 검증

실제로 무엇이 바뀌었는지 `git diff`를 확인한다.

특히 확인할 것:

* 요청하지 않은 파일까지 수정했는가?
* 불필요한 리팩터링이 들어갔는가?
* Public API가 변경됐는가?
* Response Schema / Codable 모델이 변경됐는가?
* Secret / Token / Credential을 건드렸는가?
* 비즈니스 로직 변경 범위가 적절한가?

> **AI가 무엇을 만들었는지보다 실제로 무엇을 변경했는지가 중요하다.**

---

### ③ Counter-example Verification — 일부러 깨본다

“통과했으니 끝”이 아니라 **실패할 상황을 의도적으로 찾는다.**

확인 대상:

* Edge Case
* Invalid Input
* Race Condition
* Regression
* 예외 상황
* 경계값

핵심 질문:

> **“이 코드가 어떤 입력에서 깨질 수 있는가?”**

---

## 3. 테스트 통과 ≠ 검증 완료

테스트는 **충분조건이 아니다.**

왜냐하면:

* 테스트가 없는 코드 영역일 수 있다.
* 기존 테스트의 커버리지가 부족할 수 있다.
* 기존 테스트가 오래되어 현재 요구사항과 맞지 않을 수 있다.
* AI가 테스트를 통과하면서도 잘못된 요구사항을 구현할 수 있다.

따라서 항상:

```text
테스트가 무엇을 보장하는가?
+
테스트가 무엇을 보장하지 못하는가?
```

를 구분해야 한다.

---

## 4. AI에게 검증 기준까지 맡기지 않는다

단순히:

> “코드 괜찮아?”

라고 물어보는 것은 제대로 된 검증이 아니다.

검증 기준은 사람이 먼저 정의한다.

### 최소 검증 질문

1. 요청 범위를 벗어난 변경이 있는가?
2. Public API / Response Schema가 변경됐는가?
3. 테스트되지 않은 비즈니스 로직이 변경됐는가?
4. 어떤 반례에서 실패할 수 있는가?
5. Regression 테스트가 필요한가?
6. 위험도가 높은 변경은 무엇인가?

> **AI에게 검증을 시킬 수는 있지만, “무엇을 검증할 것인가”는 사람이 설계해야 한다.**

---

## 5. 코드 작성자와 리뷰어의 Context를 분리한다

같은 Claude가 작성하고 **같은 세션에서 바로 리뷰하게 하면 위험하다.**

작성 과정에서 만들어진 잘못된 가설과 편향을 리뷰 단계에서도 그대로 가져갈 수 있기 때문이다.

따라서 중요한 변경은:

* **별도 Sub-agent**
* **새 Session**
* **Git Worktree**

등으로 리뷰 Context를 분리한다.

핵심 원칙:

> **작성자와 리뷰어는 가능한 한 서로 다른 Context를 가져야 한다.**

AI 코드 리뷰에도 일반적인 코드 리뷰의 **독립성 원칙**을 적용한다.

---

## 6. Rollback까지 검증의 일부다

좋은 자동화는:

> **실패하지 않는 자동화가 아니라, 실패해도 복구할 수 있는 자동화다.**

실무에서는:

* 별도 Branch에서 작업
* 작은 Diff 단위로 변경
* 실행한 테스트 기록
* 검증 결과 기록
* PR에 변경 범위와 검증 내용 기록
* 문제 발생 시 Rollback 방법 확보

까지 고려한다.

---

## 7. AI 패치의 최종 승인 조건

Claude가 만든 변경은 최소한 다음 질문에 답할 수 있어야 한다.

```text
Claude가 무엇을 고쳤는가?
        ↓
어떤 테스트를 통과했는가?
        ↓
실제 변경 범위가 요청과 일치하는가?
        ↓
어떤 반례를 확인했는가?
        ↓
문제가 생기면 어떻게 되돌릴 것인가?
```

이 다섯 가지가 설명되지 않는다면 **“완료”가 아니라 “검증되지 않은 변경”**으로 보는 것이 안전하다.

---

# 절대 기억할 핵심 공식

> **Verification = Automated Checks + Diff Review + Counter-examples + Independent Review + Rollback**

그리고 가장 중요한 한 문장:

> **AI가 만든 코드의 품질을 믿는 것이 아니라, AI가 틀려도 발견하고 되돌릴 수 있는 시스템을 만든다.**

---

## 3줄 요약

1. **“실행된다”와 “올바르다”는 다르므로, AI의 “완료했습니다”를 검증 결과로 받아들이면 안 된다.**
2. **검증은 Automated Checks → Diff 검증 → Counter-example 검증의 3단계로 하고, 가능하면 별도 Session/Sub-agent/Worktree에서 독립 리뷰한다.**
3. **최종적으로 중요한 것은 AI가 틀리지 않는 것이 아니라, 틀려도 발견하고 Rollback할 수 있는 구조를 만드는 것이다.**
