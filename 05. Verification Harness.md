# Claude Code의 "검증 하네스(Verification Harness)" 제대로 이해하기

Claude 같은 AI 코딩 에이전트한테 코드를 맡기다 보면 이런 순간이 꼭 온다.

> "어... 실행은 되는데, 이거 진짜 맞게 고친 거 맞아?"

Claude가 실제로 틀렸는데도 **그럴듯하게 "완료했습니다"**라고 말하는 경우가 생각보다 자주 있다. 이걸 막기 위한 구조가 바로 오늘 정리할 **검증 하네스(Verification Harness)**다.

Xcode에서 빌드 성공 = 정상 동작이 아니듯이, AI가 만든 코드도 "돌아간다"와 "제대로 됐다"는 완전히 다른 이야기라는 걸 전제로 시작한다.

---

## 왜 검증이 "선택"이 아니라 "필수"인가

AI 에이전트는 **자기가 보고 있는 좁은 컨텍스트 안에서** 그럴듯한 수정안을 만든다. 문제는 그 수정이:

- 프로젝트 전체의 컨벤션(예: 팀에서 정한 네이밍 규칙, 아키텍처 패턴)을 깨뜨릴 수 있고
- 기존 테스트를 우회해버릴 수도 있고
- 요청하지도 않은 리팩터링이 슬쩍 섞여 들어갈 수도 있고
- 엣지 케이스를 놓칠 수도 있다는 것이다.

즉, **로컬 관점에서는 합리적인 판단**이 **전체 코드베이스 관점에서는 나쁜 결과**가 될 수 있다는 거다. Claude가 특정 함수 하나는 그럴듯하게 고칠 수 있어도, 프로젝트 전체의 아키텍처 경계, 테스트 전략, API 계약(Contract), 마이그레이션 정책까지 자동으로 지켜줄 거라고 기대하면 안 된다.

검증 하네스는 바로 이 "AI가 못 보는 범위"를 사람이 메꿔주는 구조다.

---

## 검증 하네스의 3단계 레이어

### 1단계 — 자동 검증 (Automated Checks)

가장 기본이 되는, 반복 가능한 기계적 검증이다.

- 유닛 테스트 (XCTest 등) 통과 여부
- Lint (SwiftLint 등)
- 타입 체크 / 빌드 성공 여부
- Smoke Test (앱을 켜서 핵심 플로우가 최소한 죽지 않고 도는지 확인하는 수준의 가벼운 테스트)

여기까지는 CI 파이프라인에서 흔히 하는 것들이라 익숙할 거다.

### 2단계 — 구조(Diff) 검증

diff, 즉 변경 사항 자체를 리뷰하는 단계다. 여기서 확인할 건:

- 수정 범위가 원래 요청한 범위 안에 있는가 (괜히 다른 파일까지 손댄 건 아닌가)
- **Public API**가 바뀌었는가 (예: 다른 모듈에서 참조하는 public 함수/프로토콜 시그니처)
- 서버 응답 모델(Codable 구조체 등) 같은 **Response Schema**를 건드렸는가
- Secret Key, 토큰 같은 민감 정보를 건드렸는가

### 3단계 — 반례(Counter-example) 검증

"통과했다"가 아니라 "일부러 깨보려고 시도했다"에 가까운 단계다.

- Edge Case (경계값, 예외 입력)
- Invalid Input (잘못된 입력값)
- Race Condition (동시성 문제 — 예를 들어 여러 스레드/비동기 작업이 겹칠 때)
- Regression Scenario (예전엔 되던 게 다시 깨지는 상황)

---

## "테스트 통과 = 검증 완료"가 아닌 이유

이게 이 강의에서 가장 중요한 포인트다.

테스트가 통과했다는 건 중요한 신호는 맞지만, **충분조건은 아니다.**

- Legacy 코드베이스일수록 테스트 커버리지가 애초에 낮아서, Claude가 **테스트가 아예 없는 영역**을 고쳤을 수도 있다.
- 반대로 테스트가 실패했는데, 알고 보니 **그 테스트 자체가 이미 outdated(만료)**된 것일 수도 있다.

그래서 검증 하네스는 "테스트를 돌렸는가"만 보는 게 아니라, **그 테스트가 정확히 무엇을 보장하고, 무엇은 보장하지 않는지**까지 파악해야 한다.

---

## 검증용 프롬프트는 이렇게 설계한다

Claude한테 "코드 괜찮아 보여?"라고 물어보면 당연히 "네, 좋아 보입니다"가 나온다. 그건 검증이 아니다.

대신 **구체적인 검토 기준**을 프롬프트에 명시해야 한다:

1. 원래 요청 범위를 벗어난 변경이 있는가?
2. Public API나 Response Schema가 바뀌었는가?
3. 기존 테스트 없이 비즈니스 로직이 바뀐 부분이 있는가?
4. 이 수정이 실패할 수 있는 반례 입력은 무엇인가?
5. 추가로 필요한 Regression 테스트가 있다면 먼저 제안해달라
6. 위험도가 높은 변경 파일을 우선순위로 정리해달라

핵심은: **AI에게 검증을 맡기더라도, 검증 기준 자체는 사람이 설계해야 한다**는 것이다.

### Claude가 만든 코드를 Claude에게 다시 검토시켜도 될까?

가능은 하다. 다만 **같은 세션, 같은 컨텍스트**에서 리뷰를 시키면, 애초에 코드를 짤 때 깔았던 것과 똑같은 가정(가설)을 리뷰할 때도 그대로 들고 가기 때문에 **같은 사각지대(Blind Spot)가 반복**될 위험이 있다.

그래서 권장하는 방법은:

- Sub-agent를 별도로 분리해서 리뷰만 맡기거나
- **새 세션**을 열어서 diff(변경 사항)만 던져주고 리뷰시키거나
- **Git Worktree**로 별도 작업공간을 분리해서 독립적으로 검증을 돌리는 것

즉, "리뷰어는 원래 작업자와 컨텍스트를 공유하지 않아야 한다"는 코드 리뷰의 기본 원칙이 AI 에이전트한테도 그대로 적용된다고 보면 된다.

---

## 마지막 단계: Rollback(되돌리기) 가능성

좋은 자동화란 "실패하지 않는 자동화"가 아니라, **"실패해도 되돌릴 수 있는 자동화"**다.

실무에 적용할 때 지켜야 할 것들:

- 브랜치를 나눠서 작업하기
- 변경을 작은 diff 단위로 쪼개기
- 어떤 테스트 커맨드를 돌렸는지 기록해두기
- 어떤 검증을 통과했는지 PR description에 남기기
- **문제가 생겼을 때 어떻게 되돌릴지(Rollback 방법)**도 함께 적어두기

즉, `claude code`를 실무에 붙일 때 최종적으로 이런 문장이 성립해야 한다:

> "Claude가 고쳤고, 이 테스트를 통과했고, 이 변경 범위 안에 있고, 이 반례들을 확인했고, 문제가 생기면 이렇게 되돌릴 수 있다."

이 5가지가 다 채워져야, AI가 만든 패치를 안전하게 다루는 개발자가 될 수 있다.

---

## 3줄 요약

1. AI는 좁은 컨텍스트 안에서 그럴듯한 코드를 짜지만, 전체 코드베이스의 컨벤션·API 계약·테스트 전략까지 알아서 지켜주진 않는다.
2. 검증 하네스는 **자동 검증 → Diff(구조) 검증 → 반례 검증**의 3단계로 설계하고, "테스트 통과"만으로 검증 끝났다고 착각하면 안 된다.
3. 리뷰는 가능하면 별도 세션/Worktree로 분리하고, 무엇보다 실패해도 **되돌릴 수 있게(Rollback 가능하게)** 작업을 남겨야 한다.
