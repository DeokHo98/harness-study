# Claude Code Permission Harness — 핵심 요약

## 1. Permission Harness의 본질

**Permission Harness = AI의 행동 반경을 설계하는 안전 경계**

Claude Code는 파일 수정, Shell 실행, Git, DB, 배포 등 실제 시스템을 변경할 수 있기 때문에 잘못된 판단이 곧 **실제 피해**로 이어질 수 있다.

따라서 Permission은 부가 기능이 아니라 **Agent 시스템의 핵심 하네스**다.

---

## 2. “Claude를 믿을 수 있나?”가 아니라 “실패 비용이 얼마인가?”

권한을 판단할 때 가장 중요한 기준:

> **신뢰도보다 실패 비용**

예:

* `grep`, 파일 읽기 → 실패 비용 낮음
* `npm test` → 비교적 안전하고 검증 목적
* 파일 수정 → 변경 발생
* `git commit` → 팀 워크플로우 영향
* `npm install` → dependency / lock 파일 변경 가능
* DB migration → 데이터 영향 가능
* `git push` → 원격 저장소 변경
* `rm -rf`, DB Drop → 치명적
* `.env`, credential 읽기 → **시크릿 노출 위험**

즉 **“AI가 실수할까?”가 아니라 “실수했을 때 얼마나 큰 피해가 발생하는가?”**를 기준으로 권한을 설계한다.

---

## 3. 권한을 3단계로 나눈다

| 단계                 | 의미         | 대표 예시                                       |
| ------------------ | ---------- | ------------------------------------------- |
| **Safe**           | 자동 실행 허용   | 읽기, 검색, 안전한 테스트                             |
| **Needs Approval** | 실행 전 사람 승인 | 파일 수정, commit, package 설치, migration        |
| **Deny**           | 자동 실행 금지   | `rm -rf`, credential 접근, DB Drop, 위험한 배포/삭제 |

핵심은 **모든 것을 막는 것이 아니다.**

> **안전한 반복 작업은 자동화하고, 실패 비용이 큰 작업만 인간의 승인 경계에 둔다.**

---

## 4. 가장 중요한 원칙: Deny는 최종 안전장치다

Permission은 단순한 Yes/No 프롬프트가 아니다.

여러 정책 레이어가 함께 행동을 제한하며, **상위 레벨에서 Deny된 행동은 다른 하위 레벨에서 Allow로 우회할 수 없도록 설계된다.**

따라서 정말 위험한 행동은:

> **“Claude에게 하지 말라고 알려주는 것”이 아니라 “실행 자체가 불가능하도록 Deny한다.”**

---

## 5. 민감한 파일은 특히 조심한다

변경하지 않는 것만 중요한 게 아니다.

**읽는 것 자체가 위험한 파일**도 있다.

예:

* `.env`
* credential 파일
* API Key / Secret
* 인증서 / private key
* 배포 자격 증명

AI가 파일을 수정하지 않았더라도 **시크릿을 Context로 가져오는 순간 노출 위험**이 발생한다.

따라서 민감 정보는 가능한 한 **Permission Deny로 접근 자체를 차단**한다.

---

## 6. 사람의 실수까지 고려해야 한다

반복되는 Permission Prompt에 사람은 익숙해진다.

```text
Allow?
→ Yes
→ Yes
→ Yes
→ Yes
```

결국 내용을 확인하지 않고 승인하는 **Approval Fatigue**가 발생할 수 있다.

따라서 좋은 Permission Harness는:

> **“사람이 매번 제대로 판단할 것”을 전제로 만들면 안 된다.**

사람이 실수해도 위험한 행동이 실행되지 않도록 **Policy + Settings + Hooks + Sandbox + Session Boundary** 등의 여러 방어선을 둔다.

---

## 7. 목표는 “AI를 막는 것”이 아니다

좋은 Permission Harness:

```text
안전한 작업
    ↓
자동화
    ↓
생산성 증가

위험한 작업
    ↓
승인 / 차단
    ↓
실패 비용 제한
```

즉,

> **AI의 자율성을 줄이는 것이 목적이 아니라, 안전한 범위에서 더 많은 작업을 위임할 수 있게 만드는 것이 목적이다.**

---

## 8. 실무에서 권한을 설계하는 기준

새로운 Tool이나 명령을 허용할 때 다음 질문만 기억하면 된다.

1. **이 작업이 실제 시스템을 변경하는가?**
2. **실패했을 때 되돌릴 수 있는가?**
3. **데이터나 시크릿에 접근하는가?**
4. **팀 / 원격 저장소 / 운영 환경에 영향을 주는가?**
5. **사람의 승인이 필요한 수준의 실패 비용인가?**

그리고 다음처럼 결정한다.

```text
실패 비용 낮음
→ Safe

실패 비용 중간 / 영향 있음
→ Needs Approval

실패 비용 매우 높음 / 시크릿 접근
→ Deny
```

---

# 절대 기억할 핵심 문장

> **Permission Harness는 AI를 믿을지 말지를 결정하는 시스템이 아니라, AI가 실수해도 피해가 제한되도록 행동의 실패 비용을 기준으로 권한을 설계하는 시스템이다.**

> **안전한 행동은 자동화하고, 위험한 행동은 승인받고, 치명적인 행동은 Deny한다.**

---

## 3줄 요약

1. **Permission Harness는 AI의 행동 범위를 정하며, 권한은 “AI를 믿을 수 있는가”가 아니라 “실패 비용이 얼마인가”를 기준으로 설계한다.**
2. **행동을 Safe / Needs Approval / Deny로 나누고, 특히 시크릿 접근이나 치명적인 삭제·배포 작업은 실행 자체를 차단한다.**
3. **좋은 Permission Harness는 AI를 막는 것이 아니라 안전한 작업은 자동화하고 위험한 작업만 인간의 승인·차단 경계에 두어 더 많은 일을 안전하게 위임하게 만든다.**
