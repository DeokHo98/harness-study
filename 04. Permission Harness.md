# 클로드 코드가 "아무거나 다 하게" 두면 안 되는 이유: 퍼미션 하네스

퍼미션 하네스(Permission Harness)는 AI의 행동 반경을 설계하는 부분이다. 클로드가 할 수 있는 행동과 할 수 없는 행동의 경계를 정의하는 구조라고 보면 된다.

클로드 코드는 파일을 읽고, 코드를 수정하고, 셸 명령어를 실행할 수 있다. 이 능력이 강력한 이유는 실제 프로젝트 작업에 직접 붙을 수 있기 때문인데, 동시에 위험한 이유도 바로 여기에 있다. AI가 파일 시스템과 터미널에 닿는 순간, 실수는 더 이상 "잘못된 답변" 수준이 아니다. **실제 변경, 실제 삭제, 실제 배포**로 이어질 수 있고, 어쩌다가는 API 키 같은 시크릿이 노출될 수도 있다.

그래서 클로드 코드에서 퍼미션은 부가 기능이 아니라 실무 하네스의 핵심이다.

## 공식 문서가 말하는 퍼미션 구조

클로드 코드 공식 퍼미션 문서에서는 클로드 코드가 **Fine-grained Permission Rules**(세밀한 권한 규칙), **Mode**(모드), **Managed Policies**(관리 정책)를 통해 에이전트가 무엇을 할 수 있고 할 수 없는지 제어한다고 설명한다.

- 읽기 전용(read-only) 작업은 승인 없이 가능할 수 있다
- 셸 명령 실행이나 파일 수정은 승인 흐름이 필요하다

그리고 중요한 규칙 하나: 퍼미션 규칙은 설정(Settings) 계층을 따르는데, **어떤 레벨에서 Deny된 툴은 다른 레벨에서 Allow로 되돌릴 수 없다.** 즉 Deny는 단순 옵션이 아니라, 상위 정책으로 행동 경계를 강제하는 수단이다. iOS로 치면 MDM(모바일 기기 관리) 프로파일로 강제한 정책을 앱 자체 설정에서 우회할 수 없는 것과 비슷한 개념이다.

## "믿을 수 있나?"가 아니라 "실패 비용이 얼마인가?"

퍼미션 하네스를 설계할 때 가장 나쁜 접근은 "클로드를 믿을 수 있나?"라는 질문으로 시작하는 것이다. 이 질문은 너무 추상적이다.

실무에서 중요한 건 **이 행동이 실패했을 때 비용이 얼마인가**다.

- `grep`으로 코드를 검색하는 행동 → 실패 비용이 낮다. 잘못 검색해도 프로젝트가 망가지지 않는다
- `npm test` 같은 테스트 명령 → 시간은 걸릴 수 있지만 되돌릴 수 없는 파일 변경은 없다
- `rm`, `git push`, DB 마이그레이션, 프로덕션 배포, 시크릿 파일 읽기 → 실패 비용이 크다

즉 퍼미션은 신뢰의 문제가 아니라, **실패 비용을 기반으로 행동 반경을 설계하는 문제**다.

## 세 단계로 나누는 퍼미션 하네스

퍼미션 하네스는 보통 세 단계로 설계한다.

| 단계 | 설명 | 예시 |
|---|---|---|
| **Safe** | 직접적으로 파일을 수정하지 않아 승인 없이 실행 가능 | 파일 읽기, `grep` 검색, `npm test` |
| **Needs Approval** | 실패 비용이 있어 승인이 필요 | 파일 수정, `git commit`, DB 마이그레이션, 패키지 설치, Docker 명령 |
| **Deny** | 절대 자동 실행하면 안 되는 명령 | `rm -rf`, 시크릿 파일 읽기/푸시, Kubernetes 리소스 삭제, DB Drop |

핵심은 **안전한 명령**과 **되돌릴 수 있는 명령**을 구분하는 것이다. 몇 가지 예시를 보면 감이 잡힌다.

- `git commit`은 되돌릴 수 있지만 팀 워크플로우에 영향을 주기 때문에 승인(ASK)이 맞다
- `npm install`은 단순해 보여도 락 파일을 바꾸고 dependency tree를 건드리기 때문에, 프로젝트에 따라 승인이 필요할 수 있다 — Xcode 프로젝트에서 `Package.resolved`나 `Podfile.lock`이 바뀌는 것과 같은 급이라 보면 된다
- `docker compose`는 로컬에서는 괜찮아 보여도 포트 충돌이나 볼륨 변경이 있을 수 있어서 상황에 따라 승인이 필요하다
- `.env` 읽기는 아무 변경이 없어도 **시크릿 노출 위험**이 있어서 Deny가 맞다 — iOS에서 서명 인증서나 프로비저닝 프로파일, `.xcconfig`에 박아둔 API 키를 함부로 노출하면 안 되는 것과 같은 맥락이다

이런 이유로 클로드 코드 설정 문서에서도 민감한 파일 접근을 막기 위해 `Permissions`의 `Deny` 규칙에 credential 파일들을 넣어두는 예시를 제시한다.

## Deny-first: 권한은 한 번의 Yes/No가 아니다

퍼미션 시스템은 기본적으로 **Deny-first**로 동작한다. 권한이 단순히 한 번의 Yes/No 프롬프트로 끝나는 게 아니라 여러 레이어에 걸쳐 적용된다는 뜻이다.

모델이 어떤 툴을 요청하더라도, 하네스는 먼저 Deny 규칙을 확인하고, 그다음 Mode와 Classifier, Sandbox 같은 다른 레이어를 통해 실행 가능 여부를 판단한다. 앱에서 네트워크 요청 하나가 인증 미들웨어 → 레이트 리밋 → 비즈니스 로직 검증을 순서대로 거치는 것과 비슷한 다단계 구조다.

이런 구조가 중요한 이유가 있다. **사람도 반복되는 권한 프롬프트에 익숙해지면 그냥 습관적으로 승인 버튼을 눌러버린다.** iOS 개발자라면 알림 권한이나 위치 권한 팝업이 뜰 때마다 내용 확인 안 하고 "허용"을 누르는 사용자 패턴을 떠올리면 바로 이해될 거다. 그래서 좋은 퍼미션 하네스는 인간의 집중력 하나에 의존하지 않는다. 사람이 실수할 수 있다는 걸 전제로 설계하는 거다.

그래서 클로드 코드는 정책(Policy), Settings, Hook, Sandbox, Session Boundary 같은 여러 장치를 함께 사용해서 이걸 관리한다.

## 목표는 "막는 것"이 아니라 "안전하게 위임하는 것"

이 설정의 목표는 "클로드가 아무것도 못 하게 막자"가 아니다. **반복적이고 안전한 검증 명령은 열어주고, 실패 비용이 큰 행동은 막는 것**이 퍼미션 하네스의 목적이다.

즉 좋은 퍼미션 하네스는 클로드의 생산성을 죽이는 규제가 아니라, 클로드가 더 안심하고 반복 작업을 수행할 수 있게 해주는 경계다.

## 이건 보안팀만의 일이 아니다

퍼미션 하네스는 보안팀만의 문제가 아니다. 중급 백엔드 개발자든, 엔지니어든, 테크리드든, 자신의 프로젝트에서 **어떤 행동을 AI에게 맡길 수 있고, 어떤 행동은 사람이 계속 쥐고 있어야 하는지** 스스로 정의해야 한다.

이 기준이 부족하면 클로드 코드는 위험한 도구가 될 수 있다. 반대로 이 기준이 잘 서 있으면, 클로드 코드는 팀의 반복 작업을 줄여주면서도 실패 비용이 큰 행동은 사람이 검토하도록 만드는 안전한 작업 시스템이 될 수 있다.

---

## 3줄 요약

1. 퍼미션 하네스는 클로드가 할 수 있는 행동과 할 수 없는 행동의 경계를 정하는 구조이며, "믿을 수 있나"가 아니라 "실패했을 때 비용이 얼마인가"를 기준으로 설계해야 한다.
2. 행동은 Safe(승인 불필요) / Needs Approval(승인 필요) / Deny(절대 금지) 세 단계로 나뉘고, 상위 레벨에서 Deny된 건 하위 레벨에서 절대 Allow로 되돌릴 수 없다.
3. 사람도 반복되는 권한 프롬프트엔 무심코 승인해버리기 때문에, 좋은 퍼미션 하네스는 사람의 집중력에 기대지 않고 Policy·Settings·Hook·Sandbox 같은 여러 레이어로 실패 비용이 큰 행동을 자동으로 막아준다.
