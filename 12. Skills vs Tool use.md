# Claude Code 제대로 쓰기: Skills vs Tool use

Skills랑 Tool use, 둘 다 "뭔가를 더 붙이는 것" 같아서 헷갈리기 쉬운데 강의 들어보니 완전히 다른 층위의 얘기더라. iOS 개발자 관점으로 정리해본다.

## 핵심 구분: "어떻게 일할지" vs "무엇으로 실행할지"

Anthropic 공식 문서 기준으로 정리하면:

- **Skills** = 반복 가능한 워크플로우랑 전문성을 Claude한테 추가하는 구조. 즉 **어떻게 일할지를 재사용**하는 것.
- **Tool use** = Claude가 도구를 호출해서 실제 작업을 수행하게 하는 별도 레이어. 즉 **무엇으로 실행할지를 확장**하는 것.

이 구분이 없으면 매번 같은 걸 다시 설명하거나, 반대로 단순 도구 호출 문제를 괜히 크게 만들 수도 있다.

iOS로 치면 Skills는 프로젝트에 있는 **코딩 컨벤션 문서 + PR 템플릿 + 배포 체크리스트** 같은 거고, Tool use는 **네트워크 레이어나 외부 API 클라이언트** 같은 거다. 컨벤션 문서 아무리 잘 써놔도 서버 통신 자체는 안 되고, 반대로 네트워크 레이어 잘 만들어놔도 코드 스타일이 매번 제각각이면 컨벤션 문서가 필요한 것처럼, 이 둘은 애초에 푸는 문제가 다르다.

## Skills: 절차가 반복되는데 실행 능력은 필요 없을 때

Skills는 **사용자 요청을 특정 작업 절차로 번역하는 전문가 계층**이라고 보면 된다. 매번 같은 종류의 로그를 분석하고, 같은 형식의 PR 설명을 쓰고, 같은 체크리스트로 배포 전 점검을 하고 있다면 이건 도구가 없어서 못 하는 게 아니라 **반복 작업이 절차로 지정 안 된 문제**다. 이럴 땐 Skills가 맞다.

Skills 안에는 설명, 절차, 그리고 허용된 도구 제한(`allowed_tools`)까지 넣을 수 있어서 Claude가 매번 새로 추측하지 않게 만들 수 있다. 즉 Skills는 단순 템플릿이 아니라 **절차 + 범위 + 컨텍스트까지 함께 제한해주는 구조**다.

iOS 프로젝트에서 CLAUDE.md에 "이 프로젝트는 TCA 쓰고, Reducer 테스트는 이런 패턴으로 짜고, PR 올릴 때는 이 포맷 지켜라"라고 규칙 박아두는 거랑 똑같은 느낌이다. 실제로 Skills로 저장하기 좋은 것들:

- 버그 리포트 정리
- 로그 요약
- PR 설명 초안
- 배포 전 체크리스트
- 레거시 코드 읽는 절차
- 특정 프레임워크용 디버깅 루틴

이건 새로운 실행 능력이 필요한 게 아니라, **좋은 절차를 매번 다시 설명하지 않게 하는 문제**다.

## Tool use: 모델 혼자 할 수 없는 실행이 필요할 때

Tool use는 완전히 다르다. 외부 시스템을 조회해야 하거나, 파일 시스템 밖의 다른 실행 주체와 연결해야 하거나, 병렬 도구 호출이 필요할 때 쓴다. Single, Parallel, Sequential 패턴으로 Claude가 도구들을 오케스트레이션하고, **코드가 도구를 호출해서 필요한 정보만 컨텍스트에 올리는 방식**이다. 즉 기능 설명이 아니라 **실행 능력 자체를 연결**해주는 거다.

Tool use가 필요한 대표 케이스:
- 이슈 트래커 조회
- 외부 문서 검색
- 사내 시스템 상태 확인
- 다중 툴 병렬 실행
- 코드 외부 데이터 접근

이런 건 Skills 안에 설명을 아무리 길게 적어놔도 Claude는 실행을 못 한다. iOS 앱에서 API 스펙 문서만 잘 써놓는다고 실제 네트워크 요청이 나가는 게 아닌 것처럼, 절차 설명과 실행 능력은 완전히 별개다.

## 선택 기준

반복되는 작업이 보이면 무조건 MCP나 Tool use부터 붙이려는 경우가 많은데, 이건 너무 비싸다. 기준은 이렇게 잡으면 된다:

- 작업 절차는 반복되는데 **외부 연결이 필요 없음** → Skills부터
- **외부 시스템 호출, 데이터 조회, 병렬 실행**이 필요함 → Tools/MCP로

실전에서는 보통 작업을 먼저 표준화된 절차(Skills)로 바꾼 다음에, 그다음 도구 연결(Tool use)을 붙이는 순서로 간다. GitHub 에이전틱 워크플로우 자료도 자연어 마크다운으로 워크플로우를 정의하고 GitHub Actions로 실행하는 구조를 보여주는데, 핵심은 처음부터 툴을 많이 붙이는 게 아니라 **작게 시작해서 실제 사용 보면서 확대하는 것**이다. 절차가 안정되지 않았는데 도구만 늘리면 오히려 유지보수 비용만 커진다.

간단한 판단법:
- 같은 설명을 3번 이상 반복하고 있다 → Skills 후보
- Claude가 절차는 아는데 외부 실행 능력이 없어서 멈춘다 → Tool use 후보
- 둘 다 필요하다 → Skills가 절차 잡고 Tool use가 실행 담당하는 구조가 안정적

절차 문제를 실행 문제로 착각하면 과설계가 되고, 실행 문제를 절차 문제로 착각하면 Claude는 그냥 멈출 수밖에 없다.

---

### 3줄 요약
1. Skills는 반복되는 작업 절차를 재사용 가능하게 만드는 구조고, Tool use는 모델이 실제로 외부 실행을 하게 해주는 별도 레이어다.
2. 외부 연결 없이 절차만 반복되면 Skills, 외부 시스템 조회·병렬 실행처럼 모델 혼자 못 하는 일이면 Tool use를 쓴다.
3. 보통은 절차를 먼저 Skills로 표준화하고 그다음 필요한 만큼만 Tool use/MCP를 붙이는 순서가 안정적이다.
