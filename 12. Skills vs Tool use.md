# Claude Code 제대로 쓰기: Skills vs Tool use — 핵심 요약

## 1. 가장 중요한 원칙

> **Skills = "어떻게 일할지"를 재사용, Tool use = "무엇으로 실행할지"를 확장**

| 구분 | Skills | Tool use |
|---|---|---|
| 정의 | 반복 가능한 워크플로우/전문성 추가 | 도구 호출로 실제 작업 수행 |
| 비유 | 코딩 컨벤션 문서 + PR 템플릿 + 배포 체크리스트 | 네트워크 레이어 / 외부 API 클라이언트 |
| 해결 문제 | 절차가 지정 안 된 문제 | 모델 혼자 할 수 없는 실행 문제 |

> 컨벤션 문서를 잘 써놔도 서버 통신은 안 되고, 네트워크 레이어가 좋아도 코드 스타일은 제각각일 수 있다 — **애초에 다른 문제를 푼다.**

---

## 2. Skills — 절차는 반복되는데 실행 능력은 필요 없을 때

Skills는 **사용자 요청을 특정 작업 절차로 번역하는 전문가 계층**이다. 설명 + 절차 + 허용 도구 제한(`allowed_tools`)까지 포함해 **절차·범위·컨텍스트를 함께 제한**한다.

### Skills로 저장하기 좋은 것들

* 버그 리포트 정리
* 로그 요약
* PR 설명 초안
* 배포 전 체크리스트
* 레거시 코드 읽는 절차
* 특정 프레임워크용 디버깅 루틴

> 이건 새 실행 능력이 필요한 게 아니라, **좋은 절차를 매번 다시 설명하지 않게 하는 문제**다.

---

## 3. Tool use — 모델 혼자 할 수 없는 실행이 필요할 때

외부 시스템 조회, 파일 시스템 밖 실행 주체 연결, 병렬 도구 호출이 필요할 때 사용. Single/Parallel/Sequential 패턴으로 오케스트레이션하며, **코드가 도구를 호출해 필요한 정보만 컨텍스트에 올린다.**

### Tool use가 필요한 대표 케이스

* 이슈 트래커 조회
* 외부 문서 검색
* 사내 시스템 상태 확인
* 다중 툴 병렬 실행
* 코드 외부 데이터 접근

> Skills 안에 설명을 아무리 길게 적어도 Claude는 실행을 못 한다 — **절차 설명과 실행 능력은 완전히 별개.**

---

## 4. 선택 기준

* 절차는 반복되는데 **외부 연결 불필요** → **Skills**부터
* **외부 시스템 호출·데이터 조회·병렬 실행** 필요 → **Tools/MCP**로

### 간단 판단법

| 상황 | 판단 |
|---|---|
| 같은 설명을 3번 이상 반복 중 | Skills 후보 |
| 절차는 아는데 외부 실행 능력이 없어 멈춤 | Tool use 후보 |
| 둘 다 필요 | Skills가 절차, Tool use가 실행 — 조합이 안정적 |

> **주의**: 절차 문제를 실행 문제로 착각하면 과설계가 되고, 실행 문제를 절차 문제로 착각하면 Claude는 그냥 멈춘다. 보통은 절차를 먼저 Skills로 표준화한 뒤, 필요한 만큼만 Tool use를 붙이는 순서가 안정적이다. **작게 시작해서 실제 사용을 보며 확대하는 것**이 핵심이며, 절차가 안정되지 않은 채 도구만 늘리면 유지보수 비용만 커진다.

---

## 3줄 요약

1. Skills는 반복되는 작업 절차를 재사용 가능하게 만드는 구조고, Tool use는 모델이 실제로 외부 실행을 하게 해주는 별도 레이어다.
2. 외부 연결 없이 절차만 반복되면 Skills, 외부 시스템 조회·병렬 실행처럼 모델 혼자 못 하는 일이면 Tool use를 쓴다.
3. 보통은 절차를 먼저 Skills로 표준화하고 그다음 필요한 만큼만 Tool use/MCP를 붙이는 순서가 안정적이다.
