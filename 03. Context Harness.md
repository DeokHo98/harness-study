# Claude Code Context Harness — 핵심 요약

## 1. 컨텍스트는 “많이”가 아니라 “정확하게”

Claude가 헛발질하는 원인이 항상 **정보 부족**은 아니다.

오히려 큰 코드베이스에서는

> **관련 없는 정보 + 오래된 대화 + 잘못된 가설 + 긴 로그 = Context Noise**

가 되어 모델의 판단을 흐릴 수 있다.

따라서 목표는 **정보를 최대한 많이 주는 것**이 아니라,

> **Claude가 올바른 첫 번째 행동을 하도록 필요한 정보만 제공하는 것**

이다.

---

## 2. Context를 4가지로 분리한다

```text
Instruction
   ↓
Context
   ↓
Evidence
   ↓
State
```

* **Instruction** → 어떻게 행동해야 하는가

  * 테스트 없이 완료 선언 금지
  * 특정 API/DB 스키마 변경 금지
  * 특정 아키텍처 규칙 준수

* **Context** → 현재 작업을 이해하는 데 필요한 배경

  * 프로젝트 구조
  * 관련 파일
  * 프레임워크 / 런타임 / 브랜치

* **Evidence** → 현재 문제를 증명하는 실제 자료

  * XCTest 실패
  * 빌드 에러
  * Crash Log
  * CI 결과

* **State** → 지금까지 진행한 작업 상태

  * 시도한 해결책
  * 실패한 가설
  * 변경 파일
  * Sub-agent 결과
  * 이전 작업 요약

**네 가지를 섞지 않고 구분하는 것이 중요하다.**

---

## 3. 좋은 Context Pack의 기본 구조

단순히

> "로그인 버그 고쳐줘."

라고 하지 않는다.

가능하면 다음 정보를 구조화한다.

```text
Objective
→ 무엇을 해결해야 하는가?

Evidence
→ 실제 에러/로그/테스트 결과는 무엇인가?

Relevant Files
→ 어떤 파일들이 관련 있는가?

Constraints
→ 절대 변경하면 안 되는 것은 무엇인가?

Do Not Touch
→ 접근하거나 수정하지 말아야 할 영역은 무엇인가?

Verification
→ 무엇을 실행해서 성공 여부를 확인할 것인가?
```

이렇게 하면 Claude가 바로 수정부터 하는 대신,

```text
Explore → Plan → Edit → Verify
```

순서로 움직이게 만들 수 있다.

---

## 4. 큰 코드베이스에서는 “탐색”이 먼저다

Claude에게 프로젝트 전체를 무작정 던지는 것이 좋은 전략이 아니다.

특히 복잡한 작업에서는:

1. 관련 파일 탐색
2. 원인 후보 축소
3. 근거 확인
4. 수정 계획 수립
5. 코드 수정
6. 테스트 / 검증

순서가 중요하다.

> **첫 번째 행동은 Edit가 아니라 Explore여야 한다.**

---

## 5. Context Window는 희소 자원이다

Claude의 Context에는 단순한 사용자 메시지만 들어가는 것이 아니다.

* System Prompt
* 프로젝트 정보
* `CLAUDE.md`
* Auto Memory
* Tool 정보
* 대화 기록
* Tool 실행 결과
* Sub-agent 결과
* Compact Summary

등이 모두 작업면을 차지한다.

따라서:

> **Context가 크다고 무조건 좋은 것이 아니다.**

특히 Context가 압축(Compact)되면 정보가 손실될 수 있으므로, 중요한 내용을 단순히 긴 대화 속에만 남겨두면 안 된다.

---

## 6. 규칙의 중요도에 따라 저장 위치를 구분한다

가장 중요한 구분:

| 필요한 것                 | 적절한 위치                  |
| --------------------- | ----------------------- |
| 반복적으로 알려줘야 하는 프로젝트 규칙 | `CLAUDE.md`             |
| 반드시 지켜야 하는 실행 규칙      | `Permissions` / `Hooks` |
| 현재 문제의 로그와 증거         | 현재 Context              |
| 현재 작업의 진행 상태          | State / Session         |
| 반복 작업 절차              | Skill                   |

### 핵심

`CLAUDE.md`는 **강제 장치가 아니다.**

단순히 Claude에게 제공되는 컨텍스트에 가깝다.

따라서 정말로 막아야 하는 행동은:

> **Context에 적지 말고 Permission / Hook 같은 실행 레벨에서 강제한다.**

---

## 7. Claude가 헛돌면 정보를 더 넣지 않는다

가장 흔한 실수:

> "설명이 부족했나?"
> → 정보를 더 추가
> → Context가 더 커짐
> → Noise 증가
> → 더 엉뚱한 결과

대신 **Context를 재설계한다.**

### 해야 할 것

* 관련 없는 파일 제거
* 불필요한 로그 제거
* 수정 금지 범위 명확화
* 현재 가설 재검토
* 잘못된 가설이면 폐기
* 필요하면 새 세션 시작
* 작업 중 실제로 어떤 파일을 보고 있는지 확인

특히 **오염된 세션을 계속 끌고 가지 않는 것**이 중요하다.

잘못된 가설을 Claude가 이미 받아들였다면, 그 위에 계속 설명을 추가하는 것보다 **새로운 Context에서 다시 탐색하는 것이 더 나을 수 있다.**

---

# 절대 기억할 핵심 공식

> **좋은 Context ≠ 많은 정보**

> **좋은 Context = 올바른 행동을 유도하는 최소한의 정확한 정보**

그리고:

> **강제해야 하는 규칙 → Context가 아니라 Permission / Hooks**

---

# 실무에서 바로 적용할 체크리스트

* [ ] 목표(Objective)가 명확한가?
* [ ] 실제 증거(Evidence)가 있는가?
* [ ] 관련 파일 범위가 제한되어 있는가?
* [ ] 수정 금지 범위가 명확한가?
* [ ] 검증 방법이 정의되어 있는가?
* [ ] Claude가 먼저 Explore하도록 했는가?
* [ ] 오래된 잘못된 가설을 계속 끌고 가고 있지 않은가?
* [ ] Context가 지나치게 커지지 않았는가?
* [ ] 강제해야 할 규칙을 `CLAUDE.md`에만 의존하고 있지 않은가?
* [ ] 필요하면 새 세션으로 Context를 초기화했는가?

---

## 3줄 요약

1. **Claude가 헛발질하는 이유는 정보 부족보다 Context Noise 때문일 수 있으므로, 많이 주기보다 필요한 정보만 정확하게 줘야 한다.**
2. **Context는 Instruction·Context·Evidence·State로 분리하고, 작업은 `Explore → Plan → Edit → Verify` 순서로 유도한다.**
3. **강제해야 할 규칙은 `CLAUDE.md`가 아니라 Permissions/Hooks로 옮기고, Context가 오염되면 설명을 더 붙이기보다 재설계하거나 새 세션을 시작한다.**


