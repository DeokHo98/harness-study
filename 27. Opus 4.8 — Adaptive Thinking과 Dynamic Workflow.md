# Opus 4.8 — Adaptive Thinking과 Dynamic Workflow

## 1. 가장 중요한 원칙

> **고정된 값(사고 예산, 실행 경로)을 사람이 미리 정해두는 대신, 모델이 요청의 난이도를 보고 스스로 조절하는 방향으로 바뀌었다.**

Opus 4.8 세대에서 달라진 세 가지 — Adaptive Thinking, Dynamic Workflow, Mid-conversation System Message — 를 다룬다.

---

## 2. 기존 방식의 문제 — 고정된 Thinking Budget

LLM API로 프로덕션을 만들 때 보통 이런 코드를 쓴다.

```
model: "..."
thinking: { type: "enabled", budget_tokens: 10000 }
messages: [...]
```

문제는 `budget_tokens` 값이 대부분 **감**으로 정해진다는 것이다. "5천은 부족한 것 같고 2만은 비싼 것 같으니 그 중간"처럼. 그리고 이 숫자는 쉬운 질문에도 어려운 질문에도 똑같이 적용된다. "안녕"이라는 인사 한마디에도 1만 토큰의 사고 예산이 잡힌다.

> 문제의 난이도는 요청마다 다른데, 예산은 코드에 고정되어 있어서 모델이 "이건 쉬우니까 조금만 생각할게"라고 스스로 판단할 방법이 없었다.

---

## 3. Adaptive Thinking — 모델이 사고량을 스스로 조절한다

Opus 4.8에서는 **모델이 요청을 보고 사고량을 스스로 조절**한다. 고정 예산이 아니라 적응형이다.

* 쉬운 요청 → 모델이 짧게 생각 → 토큰 절약
* 어려운 요청 → 모델이 길게 생각 → 품질 확보

개발자는 이제 정확한 예산이 아니라 **"이 정도까지는 써도 된다"는 허용 범위(Limit)**만 관리하면 되고, 그 안에서 얼마나 쓸지는 모델이 판단한다.

### 달라지는 것들

* **비용 구조**: 고정 예산에서는 쉬운 요청과 어려운 요청이 같은 비용을 냈지만, 적응형에서는 트래픽의 난이도 분포에 따라 비용이 자연스럽게 조절된다.
* **지연 시간**: 쉬운 요청은 빨라지고 어려운 요청은 더 걸린다. 타임아웃을 최악의 케이스 기준으로 재설정해야 한다.
* **프롬프트 설계**: "충분히 생각하라" 같은 지시는 이제 효용이 줄어든다. 모델이 이미 알아서 하는 일을 프롬프트로 또 지시하면, 개선 없이 비용만 중복으로 늘어난다.

---

## 4. Dynamic Workflow — 정적 파이프라인에서 동적 파이프라인으로

### 기존: 정적 파이프라인(Static Pipeline)

지금까지 짜온 LLM 파이프라인은 대부분 정적이었다. 개발자가 경로를 미리 설정한다.

> 입력 → 분류 → 단계 A → 단계 B → 단계 C → 출력

모든 요청이 같은 경로를 통과한다. 문제는 단계 B가 불필요한 요청도 어쨌든 B를 지나야 하고, 예상하지 못한 경우의 수는 이 파이프라인 밖으로 새어 나간다는 것이다.

### 새로운 방식: Dynamic Workflow

모델이 경로를 스스로 판단하고 구성한다.

> 입력 → 모델이 필요한 단계를 판단·구성 → (쉬운 요청은 한 단계로 종료 / 어려운 요청은 필요한 만큼 단계를 확장) → 출력

> Adaptive Thinking의 확장이라고 볼 수 있다. 사고량을 모델이 정하던 것에서, 실행 경로까지 모델이 정하는 쪽으로 주도권이 넘어간 것이다.

### 정적 vs 동적 비교

| | 정적 파이프라인 | Dynamic Workflow |
|---|---|---|
| 예측 가능성 | 높음 | 낮음 |
| 비용 예측 | 쉬움 | 어려움 |
| 예외 처리 | 미리 정의한 것만 처리 | 모델이 유연하게 대응 |
| 디버깅 | 단계별 추적 용이 | 실행 경로 로깅이 필수 |
| 적합한 상황 | 요청 유형이 좁고 균일할 때 | 요청이 다양하고 비정형일 때 |

> Dynamic Workflow가 항상 더 낫다는 뜻은 아니다. 예측 가능성이 요구되는 규제·결제처럼, "이 요청이 어떤 경로를 탔는지" 증명해야 하는 도메인에서는 정적 파이프라인이 여전히 더 적절하다.

---

## 5. Mid-Conversation System Message — 대화 중간에 정책 교체하기

### 기존 구조

시스템 프롬프트는 대화 시작 시 한 번만 고정된다. User ↔ Model이 반복되는 동안 이 시스템 프롬프트가 계속 유지된다. 작업 도중 다른 정책을 써야 한다면, 대화를 아예 새로 시작하거나 유저 메시지 안에 시스템 지시를 몰래 끼워 넣는 편법을 써야 했다. 구조적인 문제였다.

### 새로운 구조

대화 중간에 시스템 수준의 지시(system message)를 넣을 수 있다. User → Model → User → Model로 작업하다가 정책이 바뀌면, 그 자리에 정책 변경 시스템 프롬프트를 주입한다.

> **맥락은 유지한 채, 정책만 교체한다.** 이후 User ↔ Model 작업은 이 새 정책이 적용된 상태로 계속 이어진다.

---

## 3줄 요약

1. Opus 4.8부터는 고정된 사고 예산(`budget_tokens`) 대신 모델이 요청 난이도를 보고 스스로 사고량을 조절하는 Adaptive Thinking이 적용되어, 개발자는 정확한 예산이 아니라 상한(Limit)만 관리하면 된다.
2. 정적 파이프라인(개발자가 미리 정한 고정 경로)과 달리 Dynamic Workflow는 모델이 요청마다 필요한 단계를 스스로 판단·구성하며, 예측 가능성·비용 예측이 중요한 규제·결제 도메인에서는 여전히 정적 파이프라인이 더 적합하다.
3. Mid-conversation System Message를 쓰면 대화를 새로 시작하거나 유저 메시지에 시스템 지시를 끼워 넣는 편법 없이, 맥락을 유지한 채 대화 중간에 정책만 교체할 수 있다.
