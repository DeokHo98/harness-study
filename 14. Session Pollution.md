# Claude Code "세션 오염(Context Pollution)" — 핵심 요약

## 1. 가장 중요한 원칙

> **모델이 갑자기 멍청해진 게 아니라 "세션 오염" 때문이다.**

세션이 길어질수록 유용한 정보와 함께 잡음(노이즈)도 쌓이고, 초반에 준 제약 조건/지시사항이 뒤에 쌓인 정보에 묻혀 희석된다. 작업 품질은 모델 성능뿐 아니라 **컨텍스트를 얼마나 깨끗하게 유지했는가**에도 좌우된다.

---

## 2. `/clear` — 완전 초기화

* 대화 히스토리를 통째로 날리고 컨텍스트를 완전히 비움 ("Clear conversation history and free up context")
* **작업 단위(A)가 끝났으면 다음 작업(B)을 같은 컨텍스트에서 끌고 가지 말 것**
* 이전 작업 맥락이 섞이면 오염 속도가 빨라짐 → **작업이 바뀌는 시점마다 `/clear`로 끊기**

---

## 3. `/compact` — 리셋이 아니라 압축

* 대화를 버리지 않고 **요약본만 남기고 정리** ("keep a summary in context")
* 긴 작업을 끊지 않고 이어갈 때 유용

> **주의**: `/compact`만 치면 알아서 잘 남겨줄 거라 생각하면 안 된다. **뭘 남길지 직접 지시**해야 한다.

### 명시적으로 지시해야 할 항목

* 변경된 파일 목록
* 실행한 커맨드
* 결과 요약
* 남은 To-do 리스트

이걸 안 하면 요약은 생기지만 중요한 맥락이 빠져서 다음 턴에서 방향이 틀어질 수 있다.

---

## 4. 가장 중요한 포인트 — 요약을 파일로 승격시키기

> 대화 속에만 있던 규칙은 `/compact` 과정에서 사라질 수 있지만, **파일로 승격된 규칙은 이후에도 계속 프로젝트 컨텍스트로 다시 읽힌다.**

* 대화 속 규칙 = **인메모리 캐시** (휘발성)
* 파일(`CLAUDE.md`, `.clauderules`) 속 규칙 = **UserDefaults/로컬 DB** (지속성)

→ 대화 중 나온 중요한 결론·금지 사항·테스트 커맨드·결정 사항은 `CLAUDE.md`나 `.clauderules`로 옮겨야 한다.

---

## 5. 실전 루틴 정리

| 상황 | 행동 |
|---|---|
| **작업이 바뀌면** | `/clear`로 끊기 |
| **작업이 길어지면** | `/compact`로 압축 (남길 항목: 파일 목록·커맨드·테스트 결과·투두를 직접 지시) |
| **계속 유지해야 하는 규칙이 있으면** | 대화에 두지 말고 `CLAUDE.md`/`.clauderules` 파일로 승격 |

---

## 3줄 요약

1. 세션이 길어지면 잡음이 쌓여서 초기 지시사항이 희석되는 게 "세션 오염"이고, 작업 품질은 모델 성능뿐 아니라 컨텍스트 관리 상태에도 좌우돼.
2. 작업 단위가 바뀌면 `/clear`로 완전히 리셋하고, 작업이 이어지면 `/compact`로 압축하되 남길 항목(파일 목록·커맨드·테스트 결과·투두)을 직접 지시해줘야 해.
3. 계속 지켜야 할 규칙은 대화에만 두지 말고 `CLAUDE.md`/`.clauderules` 파일로 승격시켜야 `/compact` 이후에도 유지돼.
