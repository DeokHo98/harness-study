# Claude Code의 CLAUDE.md, 제대로 알고 쓰자

Claude Code로 작업하다 보면 `CLAUDE.md`라는 파일을 한 번쯤은 보게 된다. 근데 이게 정확히 뭐 하는 녀석인지 모르고 그냥 "AI한테 주는 메모장"쯤으로 생각하고 넘어가는 경우가 많다. 오늘 강의 내용을 iOS 개발자 기준으로 다시 정리해봤다.

## CLAUDE.md는 메모장이 아니라 "법전"이다

먼저 오해부터 풀고 가자. `CLAUDE.md`는 Claude가 뭔가 기억해두는 메모장이 아니다. Claude Code에서 세션이 시작될 때마다 자동으로 로드되는 **Context Harness**의 핵심 파일이다. 여기서 Context Harness는 쉽게 말하면 AI가 매번 새로 물어보지 않아도 되도록 프로젝트 규칙, 코딩 스타일, 하지 말아야 할 것들을 미리 못박아두는 장치라고 보면 된다.

iOS 개발로 치면 `.swiftlint.yml`이나 프로젝트 컨벤션 문서를 하나로 합쳐서, 매 빌드(세션)마다 Xcode가 자동으로 읽어들이는 것과 비슷한 느낌이다. 다만 사람이 아니라 AI가 읽는다는 게 다르지.

`CLAUDE.md`가 하는 역할은 크게 세 가지다.

- **반복 제거**: 같은 규칙을 세션마다 다시 설명 안 해도 됨
- **아키텍처 경계 지정**: 어떤 파일을 왜 건드리면 안 되는지 명시
- **검증 기준 제공**: 테스트 명령이랑 완료 조건을 알려줘서 Claude가 스스로 검증하게 함

## 3-Layer 구조: 회사 규칙, 내 취향, 프로젝트 규칙, 로컬 메모

`CLAUDE.md`는 하나의 파일이 아니라 계층 구조로 되어 있다. 우선순위 순서대로 정리하면:

1. **Enterprise** — 회사 전체의 보안 정책이나 표준. 조직 레벨.
2. **User (`~/CLAUDE.md` 같은 개인 전역 설정)** — 내가 개인적으로 선호하는 스타일, 자주 쓰는 명령어 같은 것들. Xcode의 개인 설정이나 dotfiles 개념이랑 비슷하다.
3. **Project (`CLAUDE.md`)** — 팀이 공통으로 쓰는 프로젝트 규칙. 아키텍처 경계, 테스트 명령, 코딩 컨벤션 등. 이건 Git으로 팀원들과 공유된다.
4. **Local (`CLAUDE.local.md`)** — 나만 쓰는 로컬 실험 설정이나 개인 메모. 공유하면 안 되는 내용. `.gitignore`에 들어가는 개인 설정 파일이라고 생각하면 된다.

정리하면 Project `CLAUDE.md`는 팀 전체가 보는 소스 컨트롤 대상이고, User/Local 쪽은 내 개인 환경에만 적용되는 거다.

## 짧을수록 잘 먹힌다 — Auto Memory 로딩 방식

여기서 은근 중요한 포인트. `CLAUDE.md`는 세션 시작 시 전체가 로드되긴 하지만, **길게 쓴다고 더 잘 지켜지는 게 아니다.** 오히려 길면 준수율이 떨어진다.

그리고 Auto Memory라는 관련 메커니즘이 있는데, 이건 `CLAUDE.md`와는 별개로 세션 시작 시 **처음 200줄 또는 25KB 중 더 작은 쪽까지만** 로드된다. 나머지 토픽별 파일들은 필요할 때 그때그때 읽힌다.

iOS 개발자 감각으로 비유하면, 앱 시작 시 전체 리소스를 다 로드하는 게 아니라 초기 화면에 필요한 것만 우선 로드하고 나머지는 지연 로딩(lazy loading)하는 것과 비슷한 구조다. 그러니까 `CLAUDE.md`는 짧고 임팩트 있게, 핵심 규칙 위주로 작성하는 게 정석이다.

## Worktree를 쓸 때 조심해야 할 것

Git worktree로 브랜치별 병렬 작업을 할 때, `CLAUDE.md`는 worktree마다 따로 둘 수 있다. 근데 **Auto Memory는 같은 Git 저장소의 모든 worktree와 서브 디렉토리가 하나의 메모리 디렉토리를 공유**한다. 즉 worktree를 나눴다고 해서 메모리까지 완전히 격리된다고 착각하면 안 된다.

세션/에이전트/워크트리 개념을 구분하면:
- **세션** = 하나의 대화
- **에이전트** = 실행 단위
- **워크트리** = 파일 시스템 + 브랜치 단위 구분

iOS 개발에서 여러 브랜치를 worktree로 나눠서 동시에 작업하는 상황을 생각하면, 각 worktree는 독립된 작업 공간처럼 보여도 백그라운드에서 공유되는 메모리가 있다는 걸 알아둬야 한다.

## Guidance vs Enforcement: 진짜 막고 싶으면 다른 설정을 써야 한다

여기가 오늘 강의에서 제일 실용적인 부분이라고 생각한다.

`CLAUDE.md`에 "`.env` 파일은 읽지 마"라고 써두면, 이건 어디까지나 **가이드라인(Guidance)**이다. Claude가 대부분 따르긴 하지만 소프트한 규칙이라 간혹 적용이 안 될 수도 있다. 강제성이 없다는 뜻이다.

진짜로 파일 접근을 차단하고 싶으면 `settings.json`의 `permissions.deny` 설정을 써야 한다. 이건 **Enforcement(강제)**다. Claude가 실행하려고 시도해도 실제로 차단된다.

정리하면:
- `CLAUDE.md` → 판단 기준, 가이드라인 (소프트, 어길 수도 있음)
- `settings.json`의 `permissions.deny` → 실제 차단 (하드, 무조건 막힘)

그리고 팀이 공유해야 할 `permissions.deny`/`permissions.allow` 정책은 `settings.json`에 넣고, 개인적인 실험 설정은 `settings.local.json`(gitignore 대상)에 분리해서 넣는 게 맞는 사용법이다. iOS 프로젝트에서 `.xcconfig`를 팀 공용/개인용으로 나누는 것과 비슷한 패턴이라고 보면 이해가 빠를 거다.

단, `permissions.deny`도 만능 보안장치는 아니다. Claude에게 프로젝트를 설명해주는 설정 파일 체계일 뿐, 모든 위험을 막아주는 진짜 보안 솔루션은 아니라는 점은 기억해두자.

## 핵심 원칙 요약

- `CLAUDE.md`는 팀의 판단 기준을 고정하는 규칙이자 프로젝트 설명 파일이다.
- Enterprise → User → Project → Local 순서의 3-Layer 계층 구조를 가진다.
- 짧을수록 더 잘 지켜진다. 길게 쓰면 오히려 준수율이 떨어진다.
- 정말 강제로 막아야 하는 규칙은 `CLAUDE.md`가 아니라 `settings.json`의 `permissions.deny`로 설정해야 한다.
- Worktree를 쓸 때는 Auto Memory가 저장소 전체에서 공유된다는 점을 주의해야 한다.

---

### 3줄 요약
1. `CLAUDE.md`는 메모장이 아니라 Claude Code가 매 세션마다 자동으로 읽는 프로젝트 규칙집이고, Enterprise/User/Project/Local 4단계 계층으로 나뉜다.
2. 길게 쓸수록 오히려 준수율이 떨어지므로 짧고 핵심만 담아야 하며, Auto Memory는 처음 200줄/25KB만 로드되고 worktree 간에 공유된다는 점을 유의해야 한다.
3. `CLAUDE.md`는 어디까지나 소프트한 가이드라인이고, 진짜로 강제 차단이 필요하면 `settings.json`의 `permissions.deny`를 써야 한다.
