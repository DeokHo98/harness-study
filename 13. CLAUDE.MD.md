# Claude Code의 CLAUDE.md — 핵심 요약

## 1. 가장 중요한 원칙

> **CLAUDE.md는 메모장이 아니라 "법전"이다.**

Claude Code 세션 시작 시 자동으로 로드되는 **Context Harness**의 핵심 파일이며, 다음 세 가지 역할을 한다.

* **반복 제거**: 같은 규칙을 세션마다 다시 설명 안 해도 됨
* **아키텍처 경계 지정**: 어떤 파일을 왜 건드리면 안 되는지 명시
* **검증 기준 제공**: 테스트 명령/완료 조건을 알려줘 스스로 검증하게 함

---

## 2. 4-Layer 계층 구조 (우선순위 순)

| 레이어 | 위치 | 내용 | 공유 여부 |
|---|---|---|---|
| **Enterprise** | 조직 레벨 | 회사 전체 보안 정책/표준 | 조직 공유 |
| **User** | `~/CLAUDE.md` | 개인 선호 스타일, 자주 쓰는 명령어 | 개인 전역 |
| **Project** | `CLAUDE.md` | 팀 공통 규칙, 아키텍처 경계, 테스트 명령 | Git으로 팀 공유 |
| **Local** | `CLAUDE.local.md` | 나만의 실험 설정/메모 | `.gitignore` 대상, 비공유 |

---

## 3. 짧을수록 잘 먹힌다 — Auto Memory 로딩 방식

> **길게 쓴다고 더 잘 지켜지는 게 아니다. 오히려 길면 준수율이 떨어진다.**

* **Auto Memory**는 `CLAUDE.md`와 별개의 메커니즘이며, 세션 시작 시 **처음 200줄 또는 25KB 중 더 작은 쪽까지만** 로드
* 나머지 토픽별 파일은 필요할 때 지연 로딩(lazy loading)

→ `CLAUDE.md`는 **짧고 임팩트 있게, 핵심 규칙 위주**로 작성해야 한다.

---

## 4. Worktree 사용 시 주의할 점

* `CLAUDE.md`는 worktree마다 따로 둘 수 있음
* 하지만 **Auto Memory는 같은 Git 저장소의 모든 worktree/서브 디렉토리가 하나의 메모리 디렉토리를 공유**함

→ worktree를 나눴다고 메모리까지 완전히 격리된다고 착각하면 안 된다.

개념 구분: **세션** = 하나의 대화 / **에이전트** = 실행 단위 / **워크트리** = 파일 시스템+브랜치 단위 구분

---

## 5. Guidance vs Enforcement — 진짜 막고 싶으면 다른 설정을 써야 한다

| 구분 | 설정 위치 | 성격 |
|---|---|---|
| **Guidance (가이드라인)** | `CLAUDE.md` | 소프트, 어길 수도 있음 (강제성 없음) |
| **Enforcement (강제)** | `settings.json`의 `permissions.deny` | 하드, 실행 시도해도 실제로 차단됨 |

* 팀 공유 정책(`permissions.deny`/`allow`) → `settings.json`
* 개인 실험 설정 → `settings.local.json` (gitignore 대상)

> **주의**: `permissions.deny`도 만능 보안장치는 아니다. Claude에게 프로젝트를 설명해주는 설정 체계일 뿐, 모든 위험을 막는 진짜 보안 솔루션은 아니다.

---

## 3줄 요약

1. `CLAUDE.md`는 메모장이 아니라 Claude Code가 매 세션마다 자동으로 읽는 프로젝트 규칙집이고, Enterprise/User/Project/Local 4단계 계층으로 나뉜다.
2. 길게 쓸수록 오히려 준수율이 떨어지므로 짧고 핵심만 담아야 하며, Auto Memory는 처음 200줄/25KB만 로드되고 worktree 간에 공유된다는 점을 유의해야 한다.
3. `CLAUDE.md`는 어디까지나 소프트한 가이드라인이고, 진짜로 강제 차단이 필요하면 `settings.json`의 `permissions.deny`를 써야 한다.
