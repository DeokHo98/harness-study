# Part 7. 실무 체크리스트 — 개념을 프로젝트에 붙이기

---

# 15장. 실무 하네스 엔지니어링 체크리스트

> 1~14장의 개념을 실제 프로젝트에 붙이기 위한 실행 가이드.
> **한 번에 다 하지 않는다.** 15.1부터 순서대로, 필요할 때만 다음 단계로 넘어간다.

## 15.1 셋업 우선순위 (처음 하루)

| 순서 | 할 일 | 목적 | 소요 |
|---|---|---|---|
| 1 | `CLAUDE.md` 작성 | 반복 설명 제거 | 15분 |
| 2 | `settings.json`에 `deny` 3~4개 | 사고 방지 | 10분 |
| 3 | 요청 템플릿으로 작업 1회 시도 | 습관화 | — |
| 4 | 결과 나오면 `git diff --stat` 확인 | 검증 습관화 | 5분 |

⚠️ Skill · MCP · Subagent · Worktree는 **오늘 만들지 않는다.** 3번 이상 같은 문제가 반복될 때 그때 만든다. (9.7 안티패턴 · 11.4 "역할이 겹치면 만들지 않는다")

## 15.2 CLAUDE.md 템플릿 (프로젝트 루트)

```markdown
# 프로젝트 규칙

## 개요
- 스택: (예: Next.js + TypeScript)
- 실행: npm run dev
- 테스트: npm run test
- 빌드: npm run build

## 절대 규칙 (Instruction)
- 테스트 통과 확인 없이 "완료"라고 말하지 않는다
- DB 마이그레이션은 먼저 계획을 보고한 뒤 실행한다
- src/legacy/ 는 읽기만 하고 수정하지 않는다

## 컨벤션
- 함수형 컴포넌트만 사용
- 상태관리는 zustand만 사용, redux 금지
```

⚠️ **짧게 유지한다.** 200줄 / 25KB를 넘으면 일부만 로드된다 (5.2). 특정 작업에만 필요한 절차는 여기 넣지 말고 15.5(Skill)로 뺀다.

## 15.3 settings.json 템플릿 (`.claude/settings.json`)

```json
{
  "permissions": {
    "allow": ["Bash(npm run test *)", "Bash(npm run lint)", "Bash(git log *)"],
    "ask":   ["Bash(git commit *)", "Bash(npm install *)"],
    "deny":  ["Read(./.env)", "Read(./secrets/**)", "Bash(rm -rf *)", "Bash(git push *)"]
  }
}
```

* 이 파일 하나로 6장의 **Safe / Needs Approval / Deny** 3단계가 실제로 구현된다.
* 처음엔 `deny` 3~4줄만. 위험한 명령이 시도되는 걸 보면서 하나씩 추가한다.
* 팀 공유 규칙은 `settings.json`(git commit), 개인 실험은 `settings.local.json`(gitignore)로 분리한다 (5.4).

## 15.4 요청 템플릿 (Context Pack)

작업이 파일 2개 이상 얽히거나 범위가 애매하면 이 형식으로 요청한다. (4.2)

```text
목표: (무슨 일이 일어나야 하는가)
현재 상태(재현): (지금 뭐가 잘못됐는가)
수정 가능 범위: (어떤 파일 / 디렉토리)
수정 금지 범위: (환경설정, CI, 인증 로직 등)
유지 조건: (디자인 / API 스펙 등 깨지면 안 되는 것)
검증 방법: (무엇을 실행하면 성공 확인이 되는가)
완료 보고 형식: (변경 파일 목록 + 테스트 결과)
```

⚠️ **오버스펙 주의** — 오타 수정, 한 줄짜리 문구 변경까지 이 양식을 채울 필요는 없다 (4.3). 간단한 건 그냥 짧게 요청한다.

## 15.5 반복 작업 → Skill 승격

같은 지시를 3번 이상 반복하고 있다면 `.claude/skills/<이름>/SKILL.md`로 만든다. (9.1)

```markdown
---
description: 버그를 증상/증거/검증경로로 구조화한다. 에러·크래시·테스트 실패를 들고 올 때 사용.
allowed-tools: Read Grep Glob
---

## 현재 변경사항

!`git diff HEAD --stat`

## 지시

먼저 고치지 말고 아래 순서로만 답한다.
1. 증상: 무엇이 언제 실패하는가 (한 문장)
2. 증거: 로그에서 확인 가능한 사실만
3. 원인 후보: 가능성 순 최대 3개
4. 확인 방법: 각 후보 확인용 최소 명령어
5. 반례: 1순위 가설이 틀릴 수 있는 조건
```

> `description`이 가장 중요하다. Claude가 이걸 보고 **언제 이 Skill을 꺼낼지** 판단한다.

## 15.6 증상 → 원인 → 조치 대응표

| 증상 | 먼저 의심 | 조치 |
|---|---|---|
| 엉뚱한 파일 수정 | **Context** | 설명 추가 말고 파일 범위 재축소 |
| 위험한 명령 실행 | **Permission** | `settings.json`에 deny 추가 |
| 수정했지만 틀림 / 테스트 실패 | **Verification** | `git diff` 확인 + 테스트 재실행 요청 |
| MCP · Tool 문제 | **Tool** | 권한 범위(allow/ask/deny) 재점검 |
| 병렬 작업 충돌 | **Sub-agent / Worktree** | 세션을 worktree로 분리 |
| 로그를 줘도 원인 추적 실패 | **Debug** | 8장 5단계 프롬프트로 재요청 |
| 같은 실수 반복 | **State / Session** | `/clear`로 새 세션 시작 |

(1.3 증상별 진단표 + 4.3 증상별 대처를 하나로 합친 표)

## 15.7 작업 규모별 도구 선택

| 상황 | 선택 |
|---|---|
| 문제 단순, 범위 명확 | 그냥 바로 요청 |
| 범위 애매, 파일 여러 개 | 15.4 Context Pack 템플릿 사용 |
| 영향 범위 불확실, 여러 디렉토리 얽힘 | **Plan-First** (먼저 계획만 요청) |
| 작업 성격이 서로 다름 (탐색 / 구현 / 검증) | **Sub-agent** 분리 |
| 동시에 여러 기능 작업 | **Worktree** 분리 (`claude --worktree`) |

(13.3)

## 15.8 최종 승인 전 체크리스트

작업이 끝났다고 넘어가기 전에 아래 5개에 스스로 답해본다. 하나라도 막히면 "완료"가 아니라 **"검증 안 된 변경"**이다. (7.6)

- [ ] 뭘 고쳤는가?
- [ ] `git diff --stat`로 확인했을 때 요청 범위와 일치하는가?
- [ ] 어떤 테스트를 통과했는가?
- [ ] 어떤 예외 상황(반례)을 확인했는가?
- [ ] 문제가 생기면 어떻게 되돌리는가?

## 15.9 언제 다음 단계로 확장할지

| 지금 느끼는 불편함 | 추가할 것 |
|---|---|
| 매번 같은 설명 반복 중 | **Skill** (9.1) |
| 외부 서비스(GitHub · DB · Jira) 조회 필요 | **MCP** (9.2) |
| "이거 하지 마세요"를 `CLAUDE.md`에 적어도 자꾸 어김 | **Hook** (9.3) |
| 여러 파일이 동시에 얽혀서 자꾸 부딪힘 | **Worktree** (12장) |
| 조사 · 구현 · 검토를 한 세션이 다 하다가 꼬임 | **Subagent** 역할 분리 (11장) |
| 팀 전체에 같은 규칙 배포 필요 | **Plugin** (9.5) |

⚠️ **표에 있는 걸 미리 다 만들어두지 않는다.** 불편함을 먼저 느끼고, **그 지점만** 확장한다.
