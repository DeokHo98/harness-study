# Part 3. Permission Harness

---

# 6장. 실패 비용으로 설계하는 권한

> **Permission Harness = AI의 행동 반경을 설계하는 안전 경계**

Claude Code는 파일 수정·Shell 실행·Git·DB·배포 등 실제 시스템을 바꿀 수 있다. 잘못된 판단이 곧 **실제 피해**다. Permission은 부가 기능이 아니라 **Agent 시스템의 핵심 하네스**다.

## 6.1 기준은 신뢰도가 아니라 실패 비용

> **"AI가 실수할까?"가 아니라 "실수했을 때 얼마나 큰 피해가 발생하는가?"**

| 낮음 → 높음 | 행동 |
|---|---|
| 낮음 | `grep`, 파일 읽기 |
| 낮음 | `npm test` (안전하고 검증 목적) |
| 중간 | 파일 수정 (변경 발생) |
| 중간 | `git commit` (팀 워크플로우 영향) |
| 중간 | `npm install` (dependency/lock 변경) |
| 높음 | DB migration (데이터 영향) |
| 높음 | `git push` (원격 저장소 변경) |
| 치명 | `rm -rf`, DB Drop |
| 치명 | `.env`·credential 읽기 (**시크릿 노출**) |

## 6.2 권한 3단계

| 단계 | 의미 | 예시 |
|---|---|---|
| **Safe** | 자동 실행 | 읽기, 검색, 안전한 테스트 |
| **Needs Approval** | 실행 전 사람 승인 | 파일 수정, commit, package 설치, migration |
| **Deny** | 자동 실행 금지 | `rm -rf`, credential 접근, DB Drop, 위험한 배포/삭제 |

> 모든 것을 막는 게 아니다. **안전한 반복 작업은 자동화하고, 실패 비용이 큰 작업만 승인 경계에 둔다.**

**설계 5문항** — 새 Tool·명령을 허용할 때

1. 실제 시스템을 변경하는가?
2. 실패했을 때 되돌릴 수 있는가?
3. 데이터나 시크릿에 접근하는가?
4. 팀/원격 저장소/운영 환경에 영향을 주는가?
5. 사람의 승인이 필요한 수준의 실패 비용인가?

```text
낮음 → Safe    중간·영향 있음 → Needs Approval    매우 높음·시크릿 → Deny
```

> 💡 **실물 — `.claude/settings.json`** (원본 강의 밖 보충 · Claude Code 공식 문서 기준)
> 위 3단계가 실제로는 이 파일 하나다.
>
> ```json
> {
>   "permissions": {
>     "allow": ["Bash(npm run lint)", "Bash(npm run test *)", "Bash(git log *)"],
>     "ask":   ["Bash(git commit *)", "Bash(npm install *)"],
>     "deny":  ["Read(./.env)", "Read(./secrets/**)", "Bash(git push *)", "Bash(rm -rf *)"]
>   }
> }
> ```
>
> **규칙 문법**
>
> | 쓰고 싶은 것 | 규칙 |
> |---|---|
> | 정확히 이 명령만 | `Bash(npm run build)` — `npm run build --watch`는 매칭 안 됨 |
> | 접두사 매칭 | `Bash(npm run *)` = `Bash(npm run:*)` — `*` 앞 단어는 그대로 매칭 |
> | 파일 읽기 차단 | `Read(./.env)`, `Read(./secrets/**)` |
> | 파일 수정 범위 | `Edit(src/**)` — ⚠️ **경로 규칙은 `Read`/`Edit`만 검사됨** (`Write(...)`·`Glob(...)`는 무시) |
> | 도메인 제한 | `WebFetch(domain:github.com)` |
> | MCP 도구 | `mcp__github__get_*` — allow에서는 **서버 이름 자리에 와일드카드 불가** |
> | 도구 자체를 안 보이게 | deny에 괄호 없이 `"Bash"` (= 6.4의 Tool Pre-filtering) |
>
> ⚠️ `Bash(curl http://x/ *)` 식으로 인자를 제한하려는 시도는 `-X GET`·`https`·`URL=... && curl $URL`에 뚫린다. **`curl`/`wget`을 deny하고 `WebFetch(domain:...)`로 여는 편이 낫다.**
>
> **파일 우선순위(위가 강함)**: 관리형 설정 > `claude --settings` > `.claude/settings.local.json`(개인) > `.claude/settings.json`(팀 공유, git commit) > `~/.claude/settings.json`
> 로드 확인은 세션 안에서 `/status` → `Setting sources` 줄.

## 6.3 Deny First Rule

Permission은 단순한 Yes/No 프롬프트가 아니다. 여러 정책 레이어가 함께 작동하며 **상위에서 Deny된 행동은 하위에서 Allow로 우회할 수 없다.**

> 정말 위험한 행동은 **"하지 말라고 알려주는 것"이 아니라 "실행 자체가 불가능하도록 Deny한다."**

**Deny First Rule** — 모르는 액션은 그냥 허용하지 않는다. 위험한 액션은 차단하거나 사용자에게 묻는다.

> 💡 실제 평가 순서는 **deny → ask → allow**이고 먼저 매칭된 것이 이긴다. 그래서 좁은 `Bash(aws s3 ls)` allow가 넓은 `Bash(aws *)` deny를 못 이긴다 — 이것이 "상위 Deny는 하위 Allow로 우회 불가"의 실제 구현이다.

이 원칙은 코드 설계에도 그대로 옮겨진다 (→ 20장).

* 모르는 요구사항은 가정하지 않는다
* 위험한 입력은 허용하지 않는다
* 애매한 정책은 코드 안에 숨기지 않는다

> 좋은 시스템은 정상 상황에서만 잘 되는 시스템이 아니다. **이상한 입력이 들어왔을 때 어떻게 행동할지 정해진 시스템**이다.

## 6.4 Layered Control System

위험한 행동은 하나의 장치로만 막지 않는다. 처음부터 안 보이게 하고, 보여도 권한을 확인하고, 권한이 있어도 Sandbox 안에서 실행하고, Hook이 중간에서 막는다.

```text
User Prompt → Tool Pre-filtering → Permission Rule → Auto Safety Classifier
  → Pre-tool-use Hooks → Operational Shell Sandbox → Tool Execution → Session Transcript
```

| 계층 | 역할 |
|---|---|
| **Tool Pre-filtering** | 위험·비허용 도구를 모델에 아예 안 보여줌 |
| **Permission Rule** | 액션마다 Allow / Ask / Deny |
| **Auto Safety Classifier** | ML 기반으로 위험 여부 추가 판단 |
| **Hooks** | 도구 실행 전후 중간 개입 |
| **Shell Sandbox** | 실행되더라도 파일시스템·네트워크 범위 제한 |
| **Session Permission** | 이전 세션의 임시 권한을 자동 승계하지 않음 |

> **공항 보안 검색**과 같다 — 항공권·여권·수하물·몸수색·탑승구·최종 확인. 액션 하나를 여러 단계에서 반복 확인한다.

## 6.5 민감한 파일 — 읽는 것 자체가 위험

`.env` · credential 파일 · API Key/Secret · 인증서/private key · 배포 자격 증명

⚠️ 수정하지 않았더라도 **시크릿을 Context로 가져오는 순간 노출 위험**이 발생한다. → 가능한 한 **Deny로 접근 자체를 차단**한다.

## 6.6 Approval Fatigue — 사람의 실수까지 고려

반복되는 Permission Prompt에 사람은 익숙해진다. `Allow? → Yes → Yes → Yes...` 결국 내용을 확인하지 않고 승인하게 된다.

> **"사람이 매번 제대로 판단할 것"을 전제로 설계하면 안 된다.**

사람이 실수해도 위험한 행동이 실행되지 않도록 **Policy + Settings + Hooks + Sandbox + Session Boundary**의 여러 방어선을 둔다.

## 6.7 목표는 AI를 막는 것이 아니다

```text
안전한 작업 → 자동화 → 생산성 증가
위험한 작업 → 승인/차단 → 실패 비용 제한
```

> **자율성을 줄이는 게 목적이 아니라, 안전한 범위에서 더 많은 작업을 위임할 수 있게 만드는 것이 목적이다.**
