# Part 7. 하네스 적용하기 

## 1. Context CLAUDE.md 만들기

### 1-1. CLAUDE.md
프로젝트 루트에서:
```bash
claude
> /init
```
Claude가 저장소를 스캔해서 `my-app/CLAUDE.md` 초안을 자동 생성함. 생성된 내용을 열어서 다듬기:

```md
# my-app/CLAUDE.md
## Repo
- app/android: Kotlin + Jetpack Compose
- app/ios: Swift + SwiftUI

## Common Rules
- API 스펙 변경 시 반드시 두 플랫폼 모두 수정
- 절대 release 브랜치에 직접 커밋 금지
```
→ **이 파일은 Claude Code 실행할 때마다 프로젝트 루트에서 자동으로 읽힘.** 별도 로드 명령 필요 없음.


## 2. Permission: settings.json 만들기

### 2-1. 폴더/파일 생성
```bash
mkdir -p .claude
touch .claude/settings.json
touch .claude/settings.local.json
```

### 2-2. `.claude/settings.json` (팀 전체 공유, git에 커밋)
```json
{
  "permissions": {
    "allow": ["Bash(./gradlew *)", "Bash(xcodebuild *)", "Edit(app/**)"],
    "deny": [
      "Edit(app/android/app/google-services.json)",
      "Edit(app/ios/GoogleService-Info.plist)",
      "Bash(fastlane deploy*)"
    ]
  }
}
```

### 2-3. `.claude/settings.local.json` (나만의 설정, git 제외)
```json
{
  "permissions": {
    "allow": ["Bash(git log*)"]
  }
}
```

### 2-4. `.gitignore`에 추가
```bash
echo ".claude/settings.local.json" >> .gitignore
```
→ `settings.json`은 팀 규칙이라 커밋, `settings.local.json`은 개인 취향이라 제외. **Claude Code가 이 두 파일을 자동으로 합쳐서 읽음** (local이 우선순위 높음).

### 2-5. 위험 명령 차단 스크립트
```bash
mkdir -p scripts
touch scripts/block-release-push.sh
chmod +x scripts/block-release-push.sh
```
`scripts/block-release-push.sh`:
```bash
#!/bin/bash
if echo "$1" | grep -q "push.*release/"; then
  echo "release 브랜치 직접 push 금지" >&2
  exit 2
fi
exit 0
```
`.claude/settings.json`에 hook 등록 추가:
```json
{
  "hooks": {
    "PreToolUse": [{
      "matcher": "Bash",
      "hooks": [{"type": "command", "command": "./scripts/block-release-push.sh"}]
    }]
  }
}
```
→ Bash 도구 실행 **직전에** 이 스크립트가 먼저 실행됨. exit code 2를 반환하면 Claude Code가 명령 실행을 취소함.

---

## 3. Tool: Skill / MCP 만들기

### 3-1. Skill 폴더 구조
```bash
mkdir -p .claude/skills/release-notes
touch .claude/skills/release-notes/SKILL.md
```
`.claude/skills/release-notes/SKILL.md`:
```md
---
name: release-notes
description: 커밋 로그를 보고 Android(Google Play)/iOS(App Store) 릴리즈노트를 각 스토어 양식에 맞게 작성. "릴리즈노트 써줘" 요청 시 사용.
---
# 릴리즈노트 작성 규칙
- Google Play: 500자 이내, 이모지 사용 가능
- App Store: 4000자 이내, 격식체
- git log에서 feat/fix 태그만 추려서 사용자 관점 문장으로 변환
```
→ 폴더명 = skill 이름. `SKILL.md`라는 파일명 고정. 이후 "릴리즈노트 써줘"라고 하면 Claude가 이 파일을 자동으로 찾아서 참고함. 별도 등록 명령 없음.

### 3-2. MCP 연결
파일 직접 안 만들고 명령으로 등록:
```bash
claude mcp add firebase -- npx -y @modelcontextprotocol/server-firebase
```
→ 실행하면 프로젝트 루트에 `.mcp.json`이 자동 생성됨:
```json
{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-firebase"]
    }
  }
}
```
이 파일을 git에 커밋하면 팀원도 `claude mcp add` 없이 동일 도구를 씀 (단, 각자 인증 필요할 수 있음).

---

## 4. Sub-agent / Worktree

### 4-1. Subagent 파일 만들기
```bash
mkdir -p .claude/agents
touch .claude/agents/android-reviewer.md
touch .claude/agents/ios-reviewer.md
```
`.claude/agents/android-reviewer.md`:
```md
---
name: android-reviewer
description: Android PR 리뷰 전담, Compose 관례 위반만 지적
tools: Read, Grep, Bash
---
너는 Android 시니어 리뷰어다. app/android 하위 diff만 본다.
```
`.claude/agents/ios-reviewer.md`도 동일 형식으로 iOS 버전 작성.

→ 파일명(확장자 제외)이 곧 subagent 이름. 대화 중 `> android-reviewer 서브에이전트로 이 PR 리뷰해줘` 라고 부르면 별도 컨텍스트로 실행됨. 등록 명령 없음, 파일만 있으면 인식.

### 4-2. Worktree로 병렬 작업
파일이 아니라 git 명령으로 진행:
```bash
git worktree add ../app-ios-darkmode feature/ios-darkmode
git worktree add ../app-android-widget feature/android-widget
```
→ `my-app` 옆에 `app-ios-darkmode/`, `app-android-widget/` 폴더가 새로 생김. 각 폴더에서 **따로** `claude`를 실행하면 완전히 독립된 세션 두 개가 동시에 돌아감 (서로 파일 안 겹침).
```bash
cd ../app-ios-darkmode && claude   # 터미널 탭 1
cd ../app-android-widget && claude # 터미널 탭 2
```

---

## 5. Verification: 자동 검증 Hook

`scripts/lint-changed-platform.sh` 생성:
```bash
touch scripts/lint-changed-platform.sh
chmod +x scripts/lint-changed-platform.sh
```
```bash
#!/bin/bash
FILE="$1"
if [[ "$FILE" == app/android/* ]]; then
  ./gradlew ktlintCheck
elif [[ "$FILE" == app/ios/* ]]; then
  swiftlint
fi
```
`.claude/settings.json`의 hooks에 추가:
```json
{
  "hooks": {
    "PostToolUse": [{
      "matcher": "Edit",
      "hooks": [{"type": "command", "command": "./scripts/lint-changed-platform.sh"}]
    }]
  }
}
```
→ Claude가 파일을 **수정한 직후** 자동 실행. 사람이 "린트 돌려봐"라고 매번 말할 필요 없음.

---

## 6. 실패 로그 남기기

폴더는 미리 만들 필요 없음 (첫 실행 시 자동 생성). hook만 추가:
```json
{
  "hooks": {
    "PostToolUse": [{
      "matcher": "Bash",
      "hooks": [{"type": "command", "command": "mkdir -p .claude/logs && echo \"$CLAUDE_TOOL_OUTPUT\" >> .claude/logs/build.log"}]
    }]
  }
}
```
→ 이후 `.claude/logs/build.log`에 모든 Bash 실행 결과가 누적됨. 나중에 "왜 계속 빌드 실패했지" 물어보면 Claude가 이 파일을 읽고 패턴을 찾아줌.
```bash
echo ".claude/logs/" >> .gitignore   # 로그는 git에 안 올림
```

---

## 7. Persistence: 세션 이어가기

파일 만들 필요 없음, 명령만 알면 됨:
```bash
claude --continue          # 가장 최근 세션 이어가기
claude --resume            # 세션 목록 보고 선택해서 이어가기
```
대화 중:
```
> /cost     # 지금까지 토큰 사용량/비용 확인
> /compact  # 컨텍스트가 길어졌을 때 요약해서 압축
```

---
