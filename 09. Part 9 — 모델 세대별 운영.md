# Part 9. 모델 세대별 운영

> 하네스는 모델이 바뀌면 함께 바뀌어야 한다. 이전 세대에 필요했던 장치가 다음 세대에선 낭비가 되고, 새로 필요해지는 통제 지점이 생긴다.

---

# 22장. Opus 4.8 — Adaptive Thinking과 Dynamic Workflow

> **고정된 값(사고 예산, 실행 경로)을 사람이 미리 정해두는 대신, 모델이 요청 난이도를 보고 스스로 조절하는 방향으로 바뀌었다.**

달라진 세 가지: **Adaptive Thinking · Dynamic Workflow · Mid-conversation System Message**

## 22.1 기존 문제 — 고정된 Thinking Budget

```
model: "..."
thinking: { type: "enabled", budget_tokens: 10000 }
messages: [...]
```

`budget_tokens`는 대부분 **감**으로 정해진다("5천은 부족하고 2만은 비싸니 그 중간"). 그리고 이 숫자가 쉬운 질문에도 어려운 질문에도 똑같이 적용된다. **"안녕"이라는 인사에도 1만 토큰의 사고 예산이 잡힌다.**

> 난이도는 요청마다 다른데 예산은 코드에 고정되어 있어서, 모델이 "이건 쉬우니 조금만 생각할게"라고 판단할 방법이 없었다.

## 22.2 Adaptive Thinking

**모델이 요청을 보고 사고량을 스스로 조절한다.** 쉬운 요청은 짧게(토큰 절약), 어려운 요청은 길게(품질 확보).

> 개발자는 정확한 예산이 아니라 **"이 정도까지는 써도 된다"는 허용 범위(Limit)만 관리**하고, 그 안에서 얼마나 쓸지는 모델이 판단한다.

**달라지는 것 3**

* **비용 구조** — 고정 예산에선 쉬운 요청과 어려운 요청이 같은 비용. 적응형에선 **트래픽의 난이도 분포에 따라 자연스럽게 조절**된다.
* **지연 시간** — 쉬운 건 빨라지고 어려운 건 더 걸린다. ⚠️ **타임아웃을 최악 케이스 기준으로 재설정**해야 한다.
* **프롬프트 설계** — **"충분히 생각하라"는 효용이 줄어든다.** 이미 알아서 하는 일을 또 지시하면 개선 없이 비용만 중복된다.

(13장 Thinking Mode의 판단 기준이 이 세대에서 상당 부분 모델 쪽으로 옮겨간다.)

## 22.3 Dynamic Workflow

**기존 — 정적 파이프라인**: `입력 → 분류 → 단계 A → B → C → 출력`. 모든 요청이 같은 경로를 통과한다. **B가 불필요한 요청도 어쨌든 B를 지나야 하고, 예상 못 한 경우의 수는 파이프라인 밖으로 새어 나간다.**

**새 방식 — Dynamic Workflow**: `입력 → 모델이 필요한 단계를 판단·구성 → (쉬우면 한 단계로 종료 / 어려우면 필요한 만큼 확장) → 출력`

> Adaptive Thinking의 확장이다. 사고량을 모델이 정하던 것에서 **실행 경로까지 모델이 정하는 쪽으로 주도권이 넘어갔다.**

| | 정적 파이프라인 | Dynamic Workflow |
|---|---|---|
| 예측 가능성 | 높음 | 낮음 |
| 비용 예측 | 쉬움 | 어려움 |
| 예외 처리 | 미리 정의한 것만 | 모델이 유연하게 대응 |
| 디버깅 | 단계별 추적 용이 | **실행 경로 로깅 필수** |
| 적합한 상황 | 요청 유형이 좁고 균일 | 요청이 다양·비정형 |

⚠️ 항상 동적이 낫다는 뜻은 아니다. **"이 요청이 어떤 경로를 탔는지" 증명해야 하는 규제·결제 도메인에서는 정적이 여전히 적절하다.** (27장 Static vs Dynamic Graph와 같은 판단 축)

## 22.4 Mid-Conversation System Message

**기존** — 시스템 프롬프트는 대화 시작 시 한 번만 고정된다. 도중에 다른 정책을 쓰려면 대화를 새로 시작하거나 **유저 메시지에 시스템 지시를 몰래 끼워 넣는 편법**을 써야 했다. 구조적 문제였다.

**지금** — 대화 중간에 시스템 수준 지시를 주입할 수 있다.

> **맥락은 유지한 채, 정책만 교체한다.** 이후 작업은 새 정책이 적용된 상태로 이어진다.

---
---

# 23장. Opus 5 프롬프트 가이드 — 지울 것과 새로 필요한 것

> **Opus 5는 시키지 않아도 스스로 검증한다 — 그래서 기존에 당연히 넣던 검증 지시를 오히려 지워야 한다.**

"마지막에 검증 단계를 넣어라", "응답 전에 답을 다시 확인해라", "하위 에이전트로 검증해라"는 모델이 성급하지 않도록 강제하던 익숙한 관행이었다. 하지만 공식 가이드는 이를 **지우라**고 말한다 — 이중 검증이 되어 **지나친 검증과 토큰 낭비**만 부르기 때문이다.

## 23.1 지워야 할 것

| 지울 것 | 이유 |
|---|---|
| 검증 지시 ("검증해라") | 스스로 검증하므로 이중검증 |
| 마지막 검증 단계 | 토큰만 낭비, 지워도 품질 손실 없음 |
| 하위 에이전트로 검증하라는 지시 | 위와 동일 |
| 재확인 지시 ("답을 다시 확인해라") | 스스로 실수를 찾아 고치는 행동과 겹쳐 비용만 증가 |
| 이미지 처리 우회책 | 성능이 개선되어 불필요 |
| **"생각하지 말라"는 규칙** | 사고를 끈 상태에선 오히려 **내부 thinking 태그 유출**을 늘릴 수 있음 |
| **별도 검증 단계를 붙이는 Harness 장치** | 같은 이유로 비용만 증가 |

⚠️ **프롬프트만 고치고 파이프라인(Harness)은 그대로 두면 절반만 고친 것이다.**

## 23.2 새로 필요해진 것 ① 장황함(Verbosity)

**흔한 오해: Effort를 낮추면 응답도 짧아진다?** → 아니다.

> **Effort = "얼마나 생각하는가"(내부) / 응답 길이 = "얼마나 말하는가"(출력).** 서로 다른 축이다. 생각을 덜 할 뿐 말을 덜 하는 게 아니다. **길이는 프롬프트에 명시적으로 적어야 한다.**

> *"Keep responses focused and concise. Keep disclaimers brief — spend most of the response on the core answer. If asked to explain, go deep on exactly what was asked. Don't offer a big-picture summary unless requested."*

**배치 기법** — 긴 시스템 프롬프트는 앞머리 지시의 영향력이 뒤로 갈수록 희석된다. **끝부분에 같은 지시를 짧게 한 번 더** 배치하면 회복된다. 간단하지만 효과가 크다.

모델이 디스크에 쓰는 **보고서·마크다운 문서도 길어지는 경향**이 있어 같은 기법이 통한다.

> *"프롬프트로 문서 길이를 작업에 필요한 만큼 맞춰라. 군더더기 절이나 겹치는 요약, 상투적인 문구로 채우지 마라."*

## 23.3 새로 필요해진 것 ② 범위 확장(Scope Creep)

요청하지 않은 단계를 더하거나 나름의 판단을 적용한다. **명세를 온전히 주고 맡기는 큰 작업에선 강점**이지만, 범위가 좁은 작업에선 **범위를 고정**해줘야 한다.

> *"Deliver what was asked, at the scope asked. Make routine judgment calls yourself within the intended scope; only check in when a different reading of the request would meaningfully change the deliverable. If you think the request is wrong or there's a better approach, say so in one sentence — but don't silently narrow, expand, or change the request. Keep working within the requested scope; don't take actions that clearly fall outside it."*

가장 좋은 부분은 **"조용히 범위를 바꾸지 말라"**다. 더 나은 방법을 알아도 말없이 다른 걸 하지 않고 **한 문장으로 말한 뒤 시킨 대로** 한다 — **판단은 공유하되 결정권은 사용자에게.** (4장의 수정 가능/금지 범위 재설계를 프롬프트 레벨에서 하는 것)

## 23.4 서브 에이전트 위임

| 값어치 있는 경우 | 손해인 경우 |
|---|---|
| 여러 파일을 넓게 조사 | 도구 몇 번이면 끝날 일 |
| 진짜 독립적인 작업 | **자기 작업 검증·재확인용** |
| 병렬 처리 가능 | 하나면 되는데 여러 개 (비용·시간 몇 배) |

> *"Delegate to subagents only for genuinely independent, parallelizable work — like broad research across many files. Don't delegate work that could be finished directly with a few tool calls. Don't use subagents to verify or double-check your own work. If one subagent can finish the job, use one instead of several, and keep the spawn count low."*

## 23.5 Thinking은 끄지 말고 Effort만 낮춰라

공식 권고는 **사고 기능을 켠 채로 유지하고 Effort 수준만 낮추라**는 것. 끄면 **도구 호출을 그대로 써버리거나 XML 태그가 유출**될 수 있다. High → Medium/Low가 **Thinking을 꺼버리는 것보다 성능이 좋다.**

---
---

# 24장. Claude Fable 5 — 장기 자율 실행과 Effort Inversion

> **같은 기반 모델을 Safe Guard 유무로 다르게 배포한 모델이며, 장기 자율 실행(Long-horizon Autonomous Execution)과 Effort Inversion이 핵심이다.** (2026년 6월 출시)

## 24.1 동일 모델, 이중 배포

| | Fable 5 | Mythos |
|---|---|---|
| Safe Guard | 있음 | **제거됨** |
| 공개 범위 | 일반 공개 | 승인된 조직 한정 |

차이는 성능이 아니라 **Safe Guard의 유무**다 (Anthropic 공식 발표).

> 중요한 이유: 모델 선택 기준(성능·가격·속도)에 **"Safe Guard가 내 요청에 어떻게 개입하는가"**가 추가되었다.

**가격은 Opus 5의 2배.**

## 24.2 작업 지속 시간으로 모델 고르기

| 모델 | 지속 시간 | 용도 |
|---|---|---|
| Sonnet 5 / Haiku | 수초 ~ 수분 | 단발 응답, 분류, 요약 |
| Opus 5 | 수십 분 ~ 수시간 | 복잡한 코딩, 다중 파일, 에이전트 세션(Claude Code) |
| **Fable 5** | **수시간 ~ 수일** | 며칠짜리 마이그레이션, 대규모 리팩토링, 사람 개입 없는 장기 작업 |

> 선택 기준이 "얼마나 어려운가"에서 **"얼마나 오래 혼자 가야 하는가"**로 확장된다.

**Stripe 사례** — 5000만 줄 규모 루비 코드베이스의 전면 마이그레이션 한 건을 **하루 만에** 수행. 수동이었다면 한두 달 이상으로 추정된다. (장시간 실행의 상태 유지는 → 27장 State 영속화)

## 24.3 Safeguards 라우팅

요청 → **Safety Classifier** 판별 → 통상적이면 Fable 5가 응답 / **사이버 보안·생물·화학 등 감지 시 Opus 4.8로 폴백**

완전 거부 대신 **더 안전한 모델로 우회시켜 작업을 이어가게** 하는 방식이다.

> 공식 발표: **Fable Session의 95%를 초과하는 비율에서 폴백이 전혀 발생하지 않는다** (= 5% 미만에서만 발생).

| 환경 | 폴백 시 동작 |
|---|---|
| claude.ai / Claude Code / Cowork | Opus 4.8로 자동 라우팅 + 사용자 고지. 그 대화는 계속 Opus에 머물며, **Fable 5로 돌아가려면 새 대화를 시작**해야 함 |
| Messages API | **HTTP 200 + `Stop Reason: Refusal`** (에러가 아니라 정상 응답) |

⚠️ **프로덕션에서 API로 쓴다면 `Stop Reason` 검사는 필수.**

## 24.4 Effort Inversion

Effort는 **Low / Medium / High / X-High**. 직관적으론 "높일수록 품질이 계속 올라간다"고 생각하기 쉽지만 공식 가이드 기준으로는 아니다.

* **Low / Medium / High** — 훨씬 적은 토큰과 대기 시간으로 **이미 뛰어난 품질**. 이 구간에서 품질이 유지된다면 **비용·지연 조절의 주 수단**으로 쓴다.
* **X-High** — 까다로운 코딩·에이전트 작업에만. **모든 작업의 기본값이 아니다.**

**권장 워크플로우**

1. 기본값 **High**로 시작
2. 자체 평가에 따라 **Medium** → 품질이 유지되면 **Low** — 이게 비용·응답 시간 조절의 주된 수단
3. 이전 모델의 Effort 값을 그대로 가져왔다면 **다시 테스트하고 다시 고정**
4. 자체 평가 세트로 Low/Medium/High를 실제로 돌려보면 **상당수 작업은 낮은 설정으로도 충분**하다는 걸 발견하게 된다

> 💡 **실물 — 자체 평가(eval) 세트** (원본 강의 밖 보충 · Kotlin / Swift)

"자체 평가로 찾아라"의 그 세트. **케이스 3~5개로 시작해도 충분하다.**
세 번 돌려서 비교한다 — **24.4의 권장 순서 그대로다.**

```bash
run high && run medium && run low
```

**좋은 케이스의 조건은 25장의 "좋은 검증 조건"과 같다** — `mustInclude`처럼 **기계적으로 채점 가능**해야 한다. "답이 충분히 좋은가"는 케이스가 아니다.
통과 수가 같은데 시간이 절반이면 그 작업은 Low로 고정한다. 단 **Effort를 낮춰도 응답 길이는 안 줄어드니**(23.2) 길이는 프롬프트로 따로 잡는다.
