# Code Craft — 실무형 문제 해결 전략 핵심 요약

## 1. 가장 중요한 원칙

> **Code Craft는 알고리즘 테스트가 아니라, 제품과 시스템 관점에서 문제를 풀어가는 실무형 문제 해결 전략이다.**

정답 코드를 빠르게 짜는 능력이 아니라, 요구사항을 해석하고 구조를 설계하고 트레이드오프를 판단해서 확장 가능하게 구현하는 능력을 기르는 방식이다. DoorDash, Uber, Stripe, Netflix, Amazon 같은 기업들이 실제 서비스에서 마주치는 문제를 이런 방식으로 다룬다.

---

## 2. 알고리즘 문제 풀이와 무엇이 다른가

| | 알고리즘 문제 풀이 | Code Craft |
|---|---|---|
| 목적 | 제한 시간 안에 정답 코드 작성 | 문제를 해석하고 설계·구현·검증 |
| 초점 | 투 포인터, BFS, DFS, DP, HashMap 같은 패턴 | 요구사항 파악, 구조 설계, 트레이드오프, 확장성 |
| 결과물 | 정답 코드 하나 | 코딩 실력 + 설계 사고 + 논리적 설명 능력 |

Code Craft가 확인하는 것들:

* 요구사항을 제대로 이해했는가
* 애매한 부분을 명확하게 만들 수 있는가
* 바로 코딩하지 않고 구조를 먼저 설계하는가
* 기능 추가·수정에 유연한 구조와 유지보수성을 고려하는가
* 예외 상황을 생각하는가
* 코드 작성 후 검증할 수 있는가
* 자신의 설계를 논리적으로 설명할 수 있는가

---

## 3. 하나의 카테고리로 끝나지 않는다 — OOD부터 System Design까지

Code Craft는 OOD(객체 지향 설계), API 디자인, Low-Level Design, Product Design, System Design이 서로 연결된 사고 과정이다.

### 예시 1 — DoorDash Payout 계산

Dasher(배달원)의 배달 요금(Payout)을 계산하는 기능을 만든다고 하자. 바로 구현하지 말고 먼저 물어야 할 것들이 있다.

* Payout은 어떤 요소로 구성되는가 (거리, 시간, 보너스, Peak Time, 팁 등)
* 환불·취소된 주문은 어떻게 처리하는가
* 정산 금액의 소수점은 어떻게 처리하는가
* 정책이 자주 바뀔 수 있는가

> **이 질문들이 중요한 이유**: 실제 회사에서는 요구사항이 처음부터 완벽하게 주어지지 않는다. 애매한 부분을 구조화하고, 변경 가능성이 높은/낮은 부분을 나눠서 우선순위를 판단할 수 있어야 한다.

### 예시 2 — Spotify 플레이리스트 매니저

겉보기엔 단순한 CRUD(추가·삭제·순서 변경)처럼 보인다. 하지만 조금만 깊이 들어가면 API 디자인 문제가 된다.

* 플레이리스트는 어떻게 생성되는가
* 곡은 어떤 입력·방식으로 추가되는가
* 순서를 바꾸는 API는 어떤 방식이 좋은가
* 같은 곡을 중복으로 넣을 수 있는가
* 수백만 명이 동시에 쓸 때 순서 정보는 어떻게 저장하는가, 캐시는 어떻게 하는가, 동시 추가 충돌은 어떻게 처리하는가

> **핵심**: 올바른 질문을 던져야 올바른 답을 얻는다. 스스로 질문하고 판단하는 능력이 먼저다.

---

## 4. Code Craft의 5단계 프로세스

| 단계 | 하는 일 |
|---|---|
| **Explore** | 요구사항 재확인, 애매한 부분 질문, 입력/출력·제약조건·예외 상황 정리 |
| **Brainstorm** | 어떤 데이터 구조·패턴을 쓸지, 각각의 장단점(트레이드오프) 검토 |
| **Plan** | 필요한 클래스/객체, 데이터 흐름 설계, 각 컴포넌트에 책임(responsibility) 부여 |
| **Implement** | 읽기 쉽고 테스트 가능하고 확장 가능한 코드 작성 |
| **Verify** | Edge Case·예외 입력·빈 데이터·중복·실패 상황 검증 |

### 단계별 세부 내용

**Explore**
* 제품 요구사항 (기능적 요구사항)
* 애매한 부분에 대한 명확성 (비즈니스 정책, UI/UX 판단)
* 입력과 출력 (어떤 데이터가 들어가야 어떤 결과가 나오는가)
* 제약조건 (최댓값/최솟값 등 데이터 제한)
* 예외 상황 (예상치 못한 입력/상태)

**Plan — 책임(Responsibility) 배분**

> 결제 모듈이 결제 말고 다른 것까지 책임지면 좋은 설계가 아니다. 각 컴포넌트가 자기 책임만 지도록 나누는 것도 Plan 단계의 핵심이다.

**Verify — Edge Case 대응**
* 유효하지 않은 입력, 빈 데이터, 중복 처리, 기능 실패 상황을 어떻게 처리할지 결정
* 예: 숫자 입력란에 문자가 들어오면 → 재입력 요청, 알림, 값 초기화 등으로 대응

---

## 5. 실무에서 왜 중요한가

* 요구사항을 잘못 이해한 채 구현부터 들어가면, 아무리 정교한 코드를 짜도 소용없다.
* Code Craft가 없으면 어떤 게 좋은 코드인지, 어떤 게 제품에 좋은 기능인지 스스로 판단할 수 없다.
* 이 과정을 훈련하면 비전공자든 전공자든 Claude Code를 Harness Engineering으로 활용해서 빅테크 수준의 기능을 스스로 판단하고 구현할 수 있게 된다.

---

## 3줄 요약

1. Code Craft는 알고리즘 정답 속도를 재는 게 아니라, DoorDash·Uber·Stripe 같은 회사가 실제 서비스를 만들 때 쓰는 "요구사항 해석 → 설계 → 트레이드오프 판단 → 확장 가능한 구현" 문제 해결 전략이다.
2. Explore(탐색) → Brainstorm(구조/패턴 검토) → Plan(클래스·데이터 흐름·책임 설계) → Implement(읽기 쉽고 테스트 가능한 구현) → Verify(예외 상황 검증)의 5단계로 진행된다.
3. OOD, API 디자인, Low-Level Design, Product Design, System Design이 서로 연결되어 있어서, 이 훈련을 하면 비전공자든 전공자든 빅테크 수준의 문제를 스스로 판단하고 해결할 수 있게 된다.
