# Part 9. Graph Engineering

---

# 24장. 왜 그래프가 필요한가 — 루프의 한계

> **그래프 엔지니어링은 하나의 큰 작업을 여러 작업 단위로 나누고, 어떤 작업은 동시에 실행하고 어떤 작업은 앞선 결과를 기다리게 할지 설계하는 방식이다.**

여기에 검증·실패 처리·상태 저장·사람의 승인까지 연결하면 장시간 실행되는 에이전트 시스템을 훨씬 안정적으로 운영할 수 있다. 그래프 이론이나 복잡한 멀티 에이전트를 떠올리기 쉽지만 실제로는 어렵지 않다.

**Part 9에서 다루는 것**: 루프와 그래프의 차이 · Node/Edge/State의 역할 · 병렬 가능한 작업 구분법 · 여러 에이전트 결과 검증 · 중단돼도 이어서 작업하는 법 · 그래프를 쓸 때와 쓰지 말아야 할 때

**다 읽으면**: 워크플로우를 Node/Edge/State로 그리고 · Barrier와 스트리밍 지점을 구분하고 · State를 파일로 설계하고 · 불필요한 Edge를 제거하고 · 스크립트로 검사할 것과 에이전트가 판단할 것을 나누고 · 그래프가 필요한지 판단할 수 있게 된다.

## 24.1 문제 상황 — 작업 범위가 넓어질 때

에이전트는 범위가 좁은 작업(함수 구현, 타입 오류 수정, 정해진 형식의 요약)은 잘 처리한다. 문제는 **40개 파일 동시 수정, 여러 패키지 마이그레이션, 수십 개 자료로 보고서 만들기** 같은 작업부터다.

하나의 에이전트에게 다 맡기면 시간이 지날수록:

1. 처음 합의한 **규칙을 뒤쪽 작업에서 놓친다**
2. 파일 간 **의존성을 확인하지 못한다**
3. **일부만 처리하고 완료했다고 판단**한다
4. **재시작하면 이미 끝낸 작업을 반복**한다

> 모델 성능 문제로 보기 쉽지만, 더 직접적인 원인은 **작업 구조**에 있다.

**왜 무너지는가** — 한 에이전트가 **전체 계획 세우기 · 개별 작업 수행 · 진행 상황 기억 · 결과물 검토 · 오류 수정 · 완료 판단**을 하나의 긴 대화에서 모두 처리하기 때문이다. Context는 계속 커지고 초반 기준은 희석된다. (4장 세션 오염과 같은 메커니즘)

## 24.2 AI 작업 구조가 확장되어 온 흐름

| 단계 | 설계 대상 | 이 책 |
|---|---|---|
| **Prompt Engineering** | 한 번의 요청(메시지) — 역할·요구사항·출력 형식·예시를 명확히 | (전제) |
| **Context Engineering** | 모델이 참고할 정보의 범위 — 무엇을 보여주고 요약하고 제외할지 | Part 2 |
| **Harness Engineering** | 한 번의 실행 환경 — 읽고 쓸 파일, 실행 가능한 명령, 네트워크 접근, 민감 파일 보호, 완료 인정 기준 | Part 1·3·4·5 |
| **Loop Engineering** | 반복 실행 — 수정 → 테스트 → 실패 시 재수정 → 통과 시 종료를 사람 개입 없이 | 24.3 |
| **Graph Engineering** | 여러 작업과 실행 **사이의 관계** | Part 9 |

**Graph Engineering이 구분해서 설계하는 것**

* 독립적이어서 동시에 실행할 수 있는 작업
* 반드시 앞 단계 결과를 받아야 시작할 수 있는 작업
* 결과를 합쳐야 하는 지점 / 검증이 필요한 지점
* 실패했을 때 이동할 경로 / 사람이 승인할 지점
* 다음 실행을 위해 저장할 상태

## 24.3 루프부터 이해하기

**행동(Action) → 결과(Result) → 검증(Verify) → 반복 또는 종료(Repeat/Terminate)**

> 예: 코드 수정 → 테스트 실행 → 결과 확인 → 실패하면 다시 수정, 통과하면 종료

구조는 단순하다. 중요한 건 코드 수정이 아니라 **검증 조건**이다. **루프의 품질은 검증 단계가 결정한다.**

| 나쁜 검증 조건 | 좋은 검증 조건 |
|---|---|
| "코드가 충분히 좋아졌다고 판단되면 종료하세요" | 모든 유닛 테스트 통과 |
| (무엇을 기준으로 좋아졌는지 정해져 있지 않음) | TypeScript 컴파일러 오류 0 / 오류 없이 종료 / 지정된 JSON 스키마 만족 |

> 루프 하나는 **단일 목표를 반복해서 개선하는 작업**에 적합하다.

## 24.4 루프만으로 부족해지는 4가지

1. **동시 처리가 어렵다** — 서로 관련 없는 파일도 하나씩 순서대로
2. **분기 흐름이 복잡해진다** — 네트워크 오류와 테스트 실패를 같은 방식으로 재시도하게 된다
3. **자기 결과물에 관대해진다** — 하나의 긴 컨텍스트에서 작업과 검증을 함께 하면
4. **세션 간 상태가 유실된다** — 중간 상태가 대화 안에만 있으면

> 반복만 설계할 게 아니라 **여러 작업 사이의 관계까지 나눠야 한다.** 그래서 그래프가 필요하다.

### 24장 정리

1. 범위가 넓어지면 규칙 희석·부분 완료 오판·의존성 누락·재시작 중복이 나타나며, 이는 모델 성능이 아니라 **작업 구조**의 문제다.
2. AI 작업 설계는 Prompt → Context → Harness → Loop → Graph 순으로 확장돼 왔다.
3. 루프는 행동→결과→검증→반복/종료이고 품질은 **명확한 검증 조건**에 달렸지만, 병렬·분기·자기 판단의 관대함·상태 유지 4가지 한계 때문에 그래프가 필요해진다.

---
---

# 25장. Node, Edge, State

> **Graph의 핵심 요소는 셋 — Node, Edge, State.**
> 복잡한 수학 개념이 아니라 **어떤 작업이 존재하고, 어떤 결과가 어디로 전달되는지** 표현한 구조일 뿐이다.

## 25.1 Node — 하나의 명확한 작업 단위

좋은 Node는 **입력과 출력이 분명하고 하나의 책임만** 가진다.

> "주제를 조사하고 요약하고 출처를 확인한 뒤 최종 글까지 작성하라"는 범위가 너무 넓다.
> → **자료 검색 / 자료 분석(요약) / 출처 검증 / 최종 글 작성**으로 나눈다.

> **"이 Node는 무엇을 입력받고 어떤 결과를 만드는가"를 한 문장으로 설명할 수 있는지** 확인한다. 설명이 길어지거나 **동사가 여럿이면 더 나눌 수 있다는 신호**다. (18~19장 단일 책임과 같은 기준)

## 25.2 Edge — 실행 순서가 아니라 데이터 의존성

한 Node가 만든 결과를 다른 Node가 **실제로 필요로 할 때** Edge가 생긴다.

> "파일 A를 검토하고 파일 B를 검토한다"는 순서대로 이어지는 것처럼 보인다. 하지만 B를 검토할 때 A의 결과를 쓰지 않는다면 **실제 의존성은 없고 동시에 실행할 수 있다.**

> **전달되는 데이터를 구체적으로 설명할 수 있다면 실제 Edge다.** 설명할 데이터가 없다면 단순히 **프롬프트에 적힌 순서 때문에 기다리는 것**일 가능성이 크다.

## 25.3 State — 여러 Node가 공유하는 정보

여러 Node가 함께 쓰거나 다음 실행까지 보존해야 하는 정보.

> 예(경쟁사 조사): 조사할 회사 목록 · 회사별 진행 상태 · 수집한 출처 · 검증 통과한 주장 · 실패한 Node · 남은 재시도 횟수 · 현재까지 사용한 비용

⚠️ **State가 많다고 좋은 게 아니다.** 모든 대화 기록과 로그를 매번 넘기면 컨텍스트가 커지고 중요한 정보가 묻힌다.

> 필드마다 **"이 값이 없으면 다음 Node가 작업할 수 있는가"**를 따져보고, 아니면 공유 State에서 제외한다.

## 25.4 Linear Chain도 이미 그래프다

> Research → Summarize → Write → Check Sources → Format → Publish

각 단계가 이전 단계의 완료를 기다리므로 **전체 시간 = 모든 단계 시간의 합**에 가까워진다. 그런데 실제로 모든 단계가 앞 결과를 필요로 하는지 봐야 한다. 여러 Source를 읽는 작업, 여러 파일을 검토하는 작업은 독립적일 수 있다.

> 시작할 때 가장 먼저 할 일은 에이전트를 추가하는 게 아니라 **실제로 필요하지 않은 대기를 찾는 것**이다.

## 25.5 Fake Edge 테스트

1. 모든 단계를 박스로 작성한다
2. 현재 실행 순서에 따라 화살표(Edge)를 연결한다
3. 화살표마다 **"앞 단계의 결과가 다음 단계의 입력으로 실제 사용되는가"**를 확인 → 사용되면 유지, 아니면 제거(**Fake Edge**)

> 예: 경쟁사 A/B/C 조사 → 비교. B를 조사할 때 A의 결과가 필요 없고 C도 마찬가지다. 세 결과는 **마지막 비교 단계에서만** 함께 필요하다 → A/B/C를 병렬 실행하고 비교 단계만 합친다.
> 각 조사가 5분이면 **순차 15분 → 병렬 약 5분.** (실제로는 API 횟수 제한·동시 실행 제한·도구 성능에 따라 차이가 있지만 개념은 같다.)

## 25.6 다이아몬드 패턴

**하나의 입력에서 여러 작업이 나뉘고, 각 결과를 마지막에 다시 합치는 구조.** 불필요한 Edge를 제거하면 여러 워크플로우에서 이 형태가 나타난다.

> 예: 검색 → Source 1/2/3 독립 분석 → 합쳐서 보고서 작성. Source 2를 분석할 때 Source 1의 결과가 필요 없다. 그래서 셋을 동시에 분석하고, 최종 결과는 셋을 다 써야 하므로 모든 분석을 기다린다.
> **여기서의 대기는 불필요한 지연이 아니라 실제 의존성이다.**

**확인할 두 가지**

1. **병렬 노드들이 서로 독립적인가** — Source 2에 Source 1의 중간 결론이 필요하면 동시 실행하면 안 된다
2. **최종 노드가 병렬 결과 전체를 실제로 쓰는가** — Source 1만 필요하다면 2·3 분석은 불필요한 비용이다

> 목표는 병렬화 자체가 아니라 **필요한 작업의 대기 시간을 줄이는 것.**

## 25.7 Barrier와 Streaming

**Barrier** — 여러 병렬 작업이 끝나기를 기다리는 지점. 필요할 때 써야 하며, **모든 단계마다 넣으면 오히려 처리 시간이 길어진다.**

> 예: 파일 10개 분석 → 수정 → 테스트. Barrier 방식에선 분석이 빨리 끝난 파일도 **가장 느린 파일을 기다린다.** 수정 단계에서도 반복된다.

**Streaming** — 각 파일이 독립적이면 **완료되는 순서대로 바로 다음 단계로** 보낸다. File A가 먼저 분석되면 기다리지 않고 수정 단계로 이동한다.

**Barrier가 필요한 경우**: 소스 간 중복 제거 · 전체 후보를 한 번에 랭킹 · 여러 결과를 비교해 하나 선택 · 전체 파일의 크로스 파일 디펜던시 분석 · 모든 결과가 비어 있는지 확인 후 종료

## 25.8 Checker — 빠른 그래프만으로는 부족하다

병렬 실행으로 시간은 줄지만, **품질을 확인하지 않고 합치면** 새 문제가 생긴다.

> Node A(정확), Node B(정확), Node C(출처 없는 통계)의 결과를 Synthesis Node가 받는다. Synthesis는 **어떤 입력이 잘못됐는지 알지 못하므로** 셋을 자연스럽게 연결한다 → **문장은 매끄럽지만 근거가 잘못된 결과**가 섞여 들어간다.

그래서 병렬 처리와 최종 종합 사이에 **Checker(Verifier)**를 둔다. Checker는 글을 쓰거나 주요 작업을 대신하지 않는다. **각 결과를 다음 단계에서 써도 되는지만 판단**한다.

**Checker의 6항목**

1. **빈 결과** — 실제로 쓸 수 있는 내용을 반환했는가 (파일은 생겼는데 내용이 없거나 무관한 문장만 있을 수 있다)
2. **주제 일치** — 원래 작업 범위와 맞는가 (경쟁사 가격을 요청했는데 회사 역사만 썼다면 통과시키면 안 된다)
3. **출력 형식** — 다음 노드가 기대하는 스키마·포맷을 만족하는가 (JSON을 받아야 하는데 마크다운이면 파싱 오류)
4. **근거** — 중요한 주장에 확인 가능한 소스·테스트 결과·로그가 있는가
5. **상호모순** — 여러 노드의 결과가 충돌하는가 (같은 제품 가격을 다르게 보고했다면 추가 검증)
6. **중복** — 동일한 파인딩이 여러 표현으로 반복되었는가 (제거하지 않으면 특정 문제가 실제보다 중요해 보인다)

## 25.9 만든 에이전트가 직접 검토하면 안 되는 이유

같은 컨텍스트에서 작업과 검증을 모두 하면 **검토가 약해진다.** 작성 과정에서 에이전트는 **왜 그렇게 결정했는지와 중간 추론을 모두 알고 있어서**, 결과를 다시 볼 때 실제 출력만 평가하지 않고 자신의 이유까지 함께 고려하게 된다.

* **Generator Agent** — 결과물을 만들어 저장한다
* **Evaluator Agent** — **새로운 컨텍스트**에서 원래 요구사항 + 결과물 + 검증 기준 + 필요한 소스·테스트 결과만 받는다 (**Generator의 전체 대화 기록은 전달하지 않는다**)

| 역할 | 지시 |
|---|---|
| Generator | *"요구사항을 충족하는 가장 좋은 결과를 만드세요."* |
| Evaluator | *"이 결과가 기준을 충족하지 못하는 지점을 찾아서, 구체적인 근거와 함께 보고해라."* |

(7장의 독립 리뷰 원칙을 그래프에서 노드로 구현한 것)

## 25.10 모델보다 스크립트가 더 잘하는 검증

Checker를 모두 에이전트로 만들 필요는 없다.

| 스크립트·기존 도구 | 에이전트 |
|---|---|
| 타입 체크 | 요구사항을 제대로 해석했는지 |
| Unit/Integration Test | 기술적 대안 중 어느 쪽이 적합한지 |
| Lint | 소스가 충돌할 때 어떤 결론이 타당한지 |
| JSON Schema Validation | 결과가 사용자 의도와 맞는지 |
| → 결과가 일관되고 실행 비용도 낮다 | 설계 트레이드오프가 합리적인지 |

> 가장 먼저 따질 질문: **이 판단을 정말 LLM이 해야 하는가, 아니면 코드로 확정할 수 있는가?**

### 25장 정리

1. Graph = Node(하나의 책임) + Edge(**실행 순서가 아니라 데이터 의존성**) + State(최소한의 공유·보존 정보). 멀티스텝 프롬프트도 이미 Linear Chain 형태의 그래프다.
2. 시작은 **Fake Edge 제거** → 독립 작업은 다이아몬드 패턴으로 병렬 → Barrier는 정말 필요한 지점에만, 나머지는 Streaming.
3. 병렬 결과는 **Checker**를 통과시키고, **Generator와 Evaluator는 별도 컨텍스트**로 분리하며, 명확한 규칙은 스크립트로 해석·판단은 에이전트로 나눈다.

---
---

# 26장. 실전 패턴과 운영

> **핵심은 에이전트 수를 늘리는 게 아니다. 각 작업이 왜 분리되어야 하고, 무엇을 기다려야 하고, 어떤 조건을 통과해야 다음으로 갈 수 있는지 명확히 설계하는 것이다.**

## 26.1 자주 쓰는 패턴 7

| 패턴 | 구조 / 쓰임 |
|---|---|
| **① Router** | 입력을 분류해 처리 경로로 (Support Ticket → 결제/계정/기술). **분류는 모델이, 경로 선택은 구조화된 출력(카테고리 + confidence)을 읽은 일반 코드가** 담당하는 편이 안전하다 |
| **② Orchestrator-Worker** | Orchestrator가 작업을 나눠 Worker들에게 → 각자 제한된 범위만 → Synthesis에서 합침. **대규모 마이그레이션·레포 분석**처럼 처리 단위가 많을 때 |
| **③ Fan-Out / Fan-In** | 독립 작업 병렬 후 결과 수집. 다이아몬드와 유사. 여러 소스 분석·파일별 리뷰·후보 생성처럼 **같은 형태 작업이 반복될 때** |
| **④ Evaluator-Optimizer** | Generator 생성 → Evaluator 검토 → 미충족 시 **구체적 피드백과 함께** 재생성, Pass면 종료 |
| **⑤ Generate & Filter** | 후보 20개 생성 → Rubric 평가 → 상위 3개. 제목·마케팅 문구·UI 대안·기술 접근 비교에 적합 |
| **⑥ 토너먼트** | Judge가 두 개씩 비교 (A vs B → 승자1, C vs D → 승자2, 승자1 vs 승자2). **절대 점수는 어렵지만 상대 비교는 가능한** 작업에 |
| **⑦ Human-in-the-loop Gate** | 되돌리기 어렵거나 책임이 큰 작업(프로덕션 배포·고객 메시지 발송·결제 실행·데이터 삭제·권한 변경·중요 PR 머지)은 **작업 준비 → 자동 검증 → Human Approval** |

⚠️ **④에는 반드시 실행 한도가 필요하다.** 완벽한 결과는 없어서 계속 Fail할 수 있다.
> 최대 수정 3회 / 점수 90점 이상 / 모든 필수 항목 충족 / 비용 한도 도달 시 사람에게 전달
> **종료 기준 없이 "좋아질 때까지"로 두면 비용과 시간이 계속 늘어난다.**

## 26.2 Static vs Dynamic Graph

| | Static | Dynamic |
|---|---|---|
| 구조 | 실행할 때마다 같음 | 실행 결과에 따라 새 노드·경로 추가 |
| 예측 가능성 | 실행 노드·비용·시간 추정이 좋음 | 노드 수·전체 비용 예측 어려움 |
| 디버깅 | 실행 기록 검토·오류 위치 파악 쉬움 | 실행 경로가 매번 달라짐 |
| 예시 | 경쟁사 3곳 매주 조사 → 체크 → 비교 → 보고서 | 코드 리뷰 중 예상보다 복잡한 문제 발견 → 추가 분석·보안 리뷰를 동적으로 추가 |

> **처음에는 Static으로 시작**하고, 고정 구조로 처리 못 하는 예외가 발생했을 때만 Dynamic을 고려한다. Dynamic은 **무한 생성 제한 + 실행 계획·생성 이유 기록**이 필요하다.

## 26.3 State 영속화 — 대화가 아니라 파일에

장시간 에이전트가 이전 결정을 잊는 가장 큰 이유는 **진행 상태가 대화 안에만 있기 때문**이다. 컨텍스트가 요약되거나 세션이 끝나면 완료·실패 작업, 이전 결정, 다음 항목, 반복하면 안 되는 실패가 사라진다. (4장 "요약을 파일로 승격"의 확장)

* **`state.md`** — 현재 진행 상황(In Progress / Failed / Next / Budget / Completed). 다음 실행은 이 파일을 읽고 **멈춘 위치부터** 이어간다
* **`vision.md`** — 최종 목적과 완료 조건. state.md가 "지금 어디까지"라면 vision.md는 **"처음 정한 목표"**를 유지시킨다. 긴 작업엔 둘 다
* **Queue도 파일 시스템 기준** — 대기 목록을 채팅 메모리에 두지 않는다. 예: **"소스 파일은 있는데 아웃풋 파일이 없는 항목"만 재처리**하면, 60% 진행 상태에서 중단돼도 재시작 시 완료분을 건너뛴다. 이전 대화를 기억할 필요 없이 **파일 시스템으로 완료 여부를 다시 계산**하기 때문이다

## 26.4 Worker보다 Judge를 먼저

많은 사람이 Worker 프롬프트부터 쓴다. 하지만 **완료 조건(Judge)부터 만들어야** 안정성이 올라간다.

> 예: 문서 검사 Judge — Empty File, Missing Requirements, Missing Architecture 검사. 코드 작업이면 실행 명령어와 연결할 수도 있다.

⚠️ **Judge가 항상 Pass만 반환하면 의미가 없다.** 일부러 잘못 만든 `broken.md`와 정상 `good.md`를 **둘 다 넣어봐서** 정상은 통과·잘못된 건 거절하는지 확인해야 실제 게이트로 쓸 수 있다.

> 💡 **실물 — 프레임워크 없이 40줄** (원본 강의 밖 보충 · Kotlin / Swift)

Node/Edge/State · Fan-out · Checker · State 영속화 · 실행 한도가 이 정도로 구현된다.

<details open>
<summary><b>Kotlin</b></summary>

```kotlin
import java.io.File
import java.util.concurrent.Executors

val outDir = File("out").apply { mkdirs() }
val stateFile = File("state.txt")

// 26.3 상태는 대화가 아니라 파일에 · 4.6 지우지 않고 이어붙인다(append-only)
fun loadDone(): MutableSet<String> =
    if (stateFile.exists()) stateFile.readLines().filter { it.isNotBlank() }.toMutableSet()
    else mutableSetOf()

fun markDone(name: String) = stateFile.appendText("$name\n")

// ---- Node: 하나의 명확한 작업 단위 (25.1) ----
fun research(company: String): File {
    val path = File(outDir, "$company.md")
    ProcessBuilder(
        "claude", "-p",
        "${company}의 가격/기능/최근 변화를 조사해서 마크다운으로 ${path.path}에 저장해줘.",
        "--allowedTools", "WebFetch,Write",
    ).inheritIO().start().waitFor()
    return path
}

// ---- Checker: 다음 단계로 보내도 되는지만 판단 (25.8) ----
fun check(path: File): Pair<Boolean, String> {
    if (!path.exists()) return false to "파일 없음"
    val text = path.readText()
    if (text.length < 200) return false to "빈 결과"
    if ("가격" !in text)   return false to "주제 불일치: 가격 정보 없음"
    if ("http" !in text)  return false to "근거 없음: 출처 링크 없음"
    return true to "ok"
}

fun runGraph(companies: List<String>, maxRetry: Int = 2) {
    val done = loadDone()
    val todo = companies.filter { it !in done }            // 재시작 시 완료분 건너뜀
    val failed = mutableMapOf<String, Int>()

    // ---- Fan-out: Edge가 없으므로 병렬 (25.5 Fake Edge 제거) ----
    val pool = Executors.newFixedThreadPool(3)
    val jobs = todo.map { c -> c to pool.submit<File> { research(c) } }
    pool.shutdown()

    for ((company, job) in jobs) {
        val (ok, reason) = check(job.get())
        if (ok) {
            done += company
            markDone(company)                              // 매 노드마다 저장
        } else {
            val n = failed.merge(company, 1, Int::plus)!!
            if (n >= maxRetry)                             // 26.11 실행 한도
                println("[HUMAN] $company ${n}회 실패($reason) — 사람 확인 필요")
        }
    }

    // ---- Barrier: 전부 통과했을 때만 종합 (25.7) ----
    if (done.containsAll(companies)) {
        ProcessBuilder("claude", "-p", "out/ 의 파일들을 읽고 비교 보고서를 report.md로 작성해줘.")
            .inheritIO().start().waitFor()
    } else {
        println("미완료: ${companies - done}")
    }
}

fun main() = runGraph(listOf("A사", "B사", "C사"))
```

</details>

<details>
<summary><b>Swift</b></summary>

```swift
import Foundation

let outDir = URL(fileURLWithPath: "out")
try? FileManager.default.createDirectory(at: outDir, withIntermediateDirectories: true)
let stateFile = URL(fileURLWithPath: "state.txt")

// 26.3 상태는 대화가 아니라 파일에 · 4.6 지우지 않고 이어붙인다(append-only)
func loadDone() -> Set<String> {
    guard let t = try? String(contentsOf: stateFile, encoding: .utf8) else { return [] }
    return Set(t.split(separator: "\n").map(String.init))
}

func markDone(_ name: String) {
    let line = "\(name)\n"
    if let h = try? FileHandle(forWritingTo: stateFile) {
        h.seekToEndOfFile(); h.write(line.data(using: .utf8)!); try? h.close()
    } else {
        try? line.write(to: stateFile, atomically: true, encoding: .utf8)
    }
}

func claude(_ args: [String]) {
    let p = Process()
    p.executableURL = URL(fileURLWithPath: "/usr/bin/env")
    p.arguments = ["claude"] + args
    try? p.run(); p.waitUntilExit()
}

// ---- Node: 하나의 명확한 작업 단위 (25.1) ----
func research(_ company: String) -> URL {
    let path = outDir.appendingPathComponent("\(company).md")
    claude(["-p", "\(company)의 가격/기능/최근 변화를 조사해서 마크다운으로 \(path.path)에 저장해줘.",
            "--allowedTools", "WebFetch,Write"])
    return path
}

// ---- Checker: 다음 단계로 보내도 되는지만 판단 (25.8) ----
func check(_ path: URL) -> (Bool, String) {
    guard let text = try? String(contentsOf: path, encoding: .utf8) else { return (false, "파일 없음") }
    if text.count < 200          { return (false, "빈 결과") }
    if !text.contains("가격")     { return (false, "주제 불일치: 가격 정보 없음") }
    if !text.contains("http")    { return (false, "근거 없음: 출처 링크 없음") }
    return (true, "ok")
}

func runGraph(_ companies: [String], maxRetry: Int = 2) {
    var done = loadDone()
    let todo = companies.filter { !done.contains($0) }      // 재시작 시 완료분 건너뜀
    var failed: [String: Int] = [:]

    // ---- Fan-out: Edge가 없으므로 병렬 (25.5 Fake Edge 제거) ----
    var paths: [String: URL] = [:]
    let lock = NSLock()
    DispatchQueue.concurrentPerform(iterations: todo.count) { i in
        let path = research(todo[i])
        lock.lock(); paths[todo[i]] = path; lock.unlock()
    }

    for company in todo {
        let (ok, reason) = check(paths[company]!)
        if ok {
            done.insert(company)
            markDone(company)                               // 매 노드마다 저장
        } else {
            failed[company, default: 0] += 1
            if failed[company]! >= maxRetry {               // 26.11 실행 한도
                print("[HUMAN] \(company) \(failed[company]!)회 실패(\(reason)) — 사람 확인 필요")
            }
        }
    }

    // ---- Barrier: 전부 통과했을 때만 종합 (25.7) ----
    if Set(companies).isSubset(of: done) {
        claude(["-p", "out/ 의 파일들을 읽고 비교 보고서를 report.md로 작성해줘."])
    } else {
        print("미완료: \(Set(companies).subtracting(done))")
    }
}

runGraph(["A사", "B사", "C사"])
```

</details>

| 코드 | 대응하는 원칙 |
|---|---|
| `loadDone()` / `markDone()` | State 영속화 — 중단돼도 이어서 실행 (append-only) |
| `Executors` / `concurrentPerform` | Fan-out — Fake Edge가 없으니 병렬 |
| `check()`가 **일반 함수** | 25.10 "이 판단을 정말 LLM이 해야 하는가?" — 빈 결과·형식·출처는 코드로 충분 |
| `maxRetry` | 종료 조건 없는 사이클 방지 |
| `[HUMAN]` 출력 | 실패 경로 분기 → 사람에게 전달 |
| 마지막 `containsAll` / `isSubset` | Barrier — 전부 통과해야 종합 |

**Judge를 먼저 만든다는 건 이런 뜻이다** — `check()`를 먼저 쓰고, 일부러 망가뜨린 파일로 테스트한다.

<details open>
<summary><b>Kotlin</b></summary>

```kotlin
@Test fun `망가진 결과는 거절한다`() {
    val p = File(outDir, "broken.md").apply { writeText("회사 역사만 잔뜩...") }
    assertFalse(check(p).first)
}

@Test fun `정상 결과는 통과시킨다`() {
    val p = File(outDir, "good.md").apply {
        writeText("가격: 월 10달러 " + "설명 ".repeat(80) + "출처 https://x.com")
    }
    assertTrue(check(p).first)
}
```

</details>

<details>
<summary><b>Swift</b></summary>

```swift
func testCheckerRejectsBroken() throws {
    let p = outDir.appendingPathComponent("broken.md")
    try "회사 역사만 잔뜩...".write(to: p, atomically: true, encoding: .utf8)
    XCTAssertFalse(check(p).0)
}

func testCheckerAcceptsGood() throws {
    let p = outDir.appendingPathComponent("good.md")
    try ("가격: 월 10달러 " + String(repeating: "설명 ", count: 80) + "출처 https://x.com")
        .write(to: p, atomically: true, encoding: .utf8)
    XCTAssertTrue(check(p).0)
}
```

</details>

이 두 테스트가 통과해야 `check()`를 실제 게이트로 쓸 수 있다.

## 26.5 Rulebook — 같은 실수는 규칙으로 고친다

리뷰어가 같은 문제를 반복 발견하면 **결과물을 하나씩 고치지 말고 규칙을 고친다.**

> 3개 문서 모두에서 소스에 없는 숫자가 추가됐다면, 그 문서들만 고치는 게 아니라 Rulebook에 규칙을 추가한다. **새 파일만 고치면 다음 배치에서 같은 문제가 반복된다.**

| 항목 | 내용 예시 |
|---|---|
| **Output Format** | 입력 하나당 출력 하나, Markdown으로 작성, 전체 결과를 Code Fence로 감싸지 않는다 |
| **Convention** | 날짜는 YYYY-MM-DD, 소스에 없는 숫자는 생성하지 않는다, 모든 핵심 주장에 근거를 연결, 제목이 없으면 파일명을 제목으로, 5000단어 이상이면 상위 헤딩 기준 분할 |
| **Cannot Proceed** | 확인되지 않은 값은 임의로 작성하지 않는다, 처리 불가 항목은 `TODO(agent): <이유>`로 표시 |

> 핵심은 개별 결과가 아니라 **같은 결과를 반복 생산하는 규칙과 과정까지 수정하는 것.** 영향받은 배치는 규칙 수정 후 재실행한다.

## 26.6 실패 종류에 따라 경로를 나눈다

단순 루프는 실패하면 무조건 재시도한다. 하지만 **원인이 다르면 처리도 달라야 한다.**

> Research 노드의 실패 원인: 일시적 네트워크 오류 / 소스 접근 불가 / 소스 간 정보 충돌(판단 불가) / Budget 초과. **모두 같은 프롬프트로 재실행하면 같은 문제가 반복된다.**

노드마다 미리 정할 것: 어떤 실패를 재시도할지 · 최대 몇 번까지 · 대체 툴·소스가 있는지 · 어느 시점에 사람에게 넘길지 · 언제 안전하게 종료할지 → 이것이 **복구 설계**다.

## 26.7 검사 비용에 따라 실행 시점을 나눈다

| 검사 | 예시 | 실행 시점 |
|---|---|---|
| **빠름** (몇 초 이내) | Type Check, Lint, 작은 유닛 테스트, JSON 구조 검사, 필수 파일 존재 여부 | **작업 직후 바로** — 잘못된 결과가 다음 작업으로 넘어가는 것을 막는다 |
| **느림** (몇 분 이상) | 전체 Workspace Build, 전체 Integration Test, Repository 단위 Security Scan | 여러 변경을 모아 **배치 단위로** |

> 여러 에이전트가 각자 전체 빌드를 돌리면 같은 비용을 반복 지불한다. 워커는 **빌드 요청만 큐에 남기고, 하나의 "빌드 게이트"가 모아서 한 번에** 처리하는 편이 낫다.

## 26.8 병렬 에이전트에는 독립 워크스페이스

두 종류의 실패를 구분한다.

1. **에이전트 하나의 작업 실패** — 하나의 실패가 전체 배치를 중단시키지 않게 (`error_code`로 구분)
2. **여러 에이전트가 같은 파일을 동시 수정** — 각자는 성공해도 **서로의 변경을 덮어쓴다**

> 코드를 수정하는 병렬 에이전트에는 **별도 Git Worktree나 Sandbox**를 준다. 각자 독립적으로 수정·테스트한 뒤 **검증을 통과한 결과만 Merge.** (12장과 같은 해법)

## 26.9 모델도 역할에 따라 나눠 쓴다

* **작고 반복적인 작업** (티켓 분류, 파일별 정형 작업, 형식 변환) → 빠르고 저렴한 모델
* **Rulebook 작성, 아키텍처 검토, 복잡한 실패 원인 분석** → 더 높은 판단력

> 특히 Rulebook 노드에는 성능을 충분히 투자한다. **Worker 하나의 실수는 결과 하나에 영향을 주지만, 잘못된 규칙 하나는 이후 수백 개의 결과에 같은 문제를 만든다.** (모델 선택 기준 → Part 8)

## 26.10 로깅과 트레이싱은 처음부터

Linear Prompt는 순서대로라 추적이 쉽지만, Graph는 여러 노드가 동시에 서로 다른 경로를 거친다.

**최소 기록 항목**: RunID · NodeID · 시작·종료 시간 · 입력·출력 위치 · 출력 파일 · 사용 모델 · 토큰 사용량 · 툴 호출 기록 · Retry 횟수 · 검증 결과

> 이런 기록이 있으면 "Review 노드가 두 번째 실행에서 테스트 3개가 실패했고 이후 Fix 경로로 이동했다"처럼 **정확한 실패 지점**을 확인할 수 있다. 그래프가 커질수록 **관찰 가능성(Observability)**이 중요해진다.

## 26.11 안티패턴 8

* **Fake Edge** — 데이터 의존성이 없는데 프롬프트 순서만으로 연결 → 독립 작업이 불필요하게 대기
* **불필요한 Barrier** — 다음 단계가 전체 결과를 필요로 하지 않는데 전부 기다림 → 빠른 작업이 가장 느린 것에 맞춰짐
* **단순 데이터 처리에 에이전트** — 배열 정렬·중복 제거처럼 코드로 되는 일에 쓰면 비용만 증가
* **리뷰어가 같은 컨텍스트** — 독립 검증이 아니라 기존 결정을 재확인하는 과정이 된다
* **종료 조건 없는 사이클** — 새 결과가 없는데 계속 검색하거나, Reject한 파인딩을 기억 못 해 다시 발견 → 파인딩 상태를 **Confirmed / Rejected / Duplicated / Unresolved**로 저장
* **검증되지 않은 병렬 결과를 바로 합치기** — 하나의 잘못된 결과가 최종 신뢰도를 낮춘다
* **State를 채팅에만 저장** — 세션 종료 시 이어가기 어려움
* **실행 한도 없음** — **최대 Turn · Retry 수 · 실행당 Token Budget · 하루 비용 한도**를 제한한다

## 26.12 그래프의 숨은 비용

* **리뷰 병목** — 생성 속도가 검토 속도보다 빠르면 미검토 결과가 쌓인다. **PR 100개를 만들어도 20개만 검토되면 실제 처리량은 20개**이고 자동 생성량만 늘어난 것이다.
* **Cognitive Surrender** — 자동화가 안정적일수록 사람은 덜 확인하고, 확인이 줄면 **잘못된 결정을 식별하는 능력도 약해진다.** 실행은 맡기되 **중요한 기술적 판단과 책임은 넘기지 않는다.** 정기적으로 **머지된 diff를 직접 읽고** 왜 이 구조가 선택됐는지 확인한다.
* **Token Blowout** — 병렬 실행 + 재시도 + 소스·컨텍스트 반복 읽기.
  전체 비용 = **(실행 횟수 × 노드별 평균 리트라이 × 노드별 토큰 비용) + 툴 비용 + CI 비용 + 사람 리뷰 비용**. 실행 전에 Budget과 Retry 제한을 정한다.

**의미 없는 지표**: 생성된 코드 줄 수 · 만들어진 PR 수 · 실행된 에이전트 수 · 생성된 토큰 수 · 자동화된 워크플로우 수 → **작업량**만 보여줄 뿐이다.

> **진짜 지표는 "승인된 변경 한 건당 전체 비용"이다.** 100개를 만들었는데 20개만 승인됐다면 나머지 80개의 생성·검토 비용까지 포함해 계산한다.
> **목표는 많이 만드는 게 아니라, 검증을 통과하는 결과를 더 낮은 비용과 짧은 시간으로 만드는 것.**

## 26.13 그래프를 쓰지 않는 편이 나은 경우

* **한 번만 수행하는 짧은 작업** — 설계 시간이 실제 작업 시간보다 길어질 수 있다
* **낯선 코드베이스 탐색·원인 불명 디버깅** — 고정 그래프를 너무 빨리 만들지 말고 **먼저 자유롭게 탐색**하며 실제 구조를 파악한다
* **완료를 기계적으로 판단할 수 없는 작업** — Gate를 넣어도 형식적 승인이 되기 쉽다
* **Payment·인증·데이터 삭제·보안·권한 변경** — **Human Gate 없이 완전 자동화 금지**

> 좋은 그래프 아키텍트는 복잡한 그래프를 만드는 사람만이 아니다. **"이건 스크립트나 루프로 충분하다"고 판단하는 것도 실력이다.**

## 26.14 현실적인 도입 순서 9단계

| 단계 | 내용 |
|---|---|
| 1 | Claude와 함께 직접 해보며 **어떤 지식이 필요한지, 어디서 자주 실패하는지, 완료를 어떻게 판단할지** 확인 |
| 2 | 반복 절차를 **Rulebook·Skill**로 정리하고 자동 통과 판정을 하는 **Judge**를 만든다 |
| 3 | **단일 루프** — 실패 시 피드백 반영해 재작업, **최대 Retry와 Budget**을 함께 설정 |
| 4 | **State 저장** — state.md / vision.md / DB에 기록해 중간에 멈춰도 이어서 실행 |
| 5 | 독립 작업을 찾아 **Fan-out** 도입 |
| 6 | **Checker + Synthesis** — 병렬 결과를 검증하고 통과분만 합침 |
| 7 | **Router + Failure Path** — 입력 종류와 실패 원인에 따라 경로 분리 |
| 8 | 충분히 검증된 워크플로우에 **Human Gate + 예약 실행** 추가 |
| 9 | Static으로 처리 어려운 예외가 **반복 확인됐을 때만** Dynamic Graph 허용 |

## 26.15 종합 예시 — 경쟁사 비교 보고서

목표: 경쟁사 세 곳의 **가격·기능·최근 변화·시장 포지셔닝**을 조사해 비교 보고서 작성.

기존 Linear(A조사 → B조사 → C조사 → 비교 → 초안 → 사실 확인 → 형식 정리)는 순차라 느리고, **잘못된 조사 결과가 검증 없이 비교 단계로 넘어간다.**

**그래프 구조**

```text
Input → Task Setup
  → (A조사 / B조사 / C조사 병렬·독립 → 각각 검증 → 실패 시 수정 단계로 개별 이동)
  → Normalize (데이터 형식 통일, 회사 간 정보 모순 확인)
  → (비교문 작성 / Pro Format — 서로 독립이면 동시 실행)
  → Final Judge (전체 검증)
  → Human Approval (게시·고객 전달 전)
```

* **Node 계약(Contract)** — 회사 조사 노드의 입력: `Company`, `Fields`, `As of Date` / 출력: **다음 노드가 바로 쓸 수 있도록 구조화** (자유형식 긴 문장을 다시 해석하지 않아도 되게)
* **Checker 검증 항목**: 필수 필드를 모두 포함하는가 · 핵심 주장마다 소스가 있는가 · 서로 충돌하는 정보가 있는가
* **실패 원인별 경로**: 소스 부족 → 추가 리서치 / 정보 충돌 → Independent Reviewer 투입 / 접근 불가 → 대체 소스
* **Final Judge**: 모든 회사를 같은 기준으로 비교했는가 · 핵심 주장에 근거가 연결됐는가 · **사실과 추정이 구분됐는가** — 모두 충족해야 완료

## 26.16 Part 9 정리

긴 작업에서 일관성을 잃는 이유는 모델 성능만으로 설명되지 않는다. **하나의 에이전트와 하나의 컨텍스트에 계획·구현·검증·진행 관리·완료 판단을 모두 맡긴 구조**에도 원인이 있다.

* **루프**는 행동과 검증을 반복하게 해준다
* **그래프**는 한 단계 더 나아가 **의존성·병렬 실행·검증·실패 처리·상태 저장**을 명시적으로 설계한다
* 기본 요소는 **Node**(하나의 명확한 작업) · **Edge**(실제 데이터 의존성) · **State**(다음 실행까지 보존할 진행 상황과 결정)

**설계할 때 가장 먼저 할 일은 에이전트를 늘리는 게 아니다.**

1. 실제 데이터가 이동하지 않는 **Fake Edge를 찾아낸다**
2. 독립 작업은 병렬로, **전체 결과가 필요한 지점에만 Barrier**를 둔다
3. 병렬 결과는 바로 합치지 말고 **Checker를 통과**시킨다
