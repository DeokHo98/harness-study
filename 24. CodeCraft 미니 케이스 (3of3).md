# CodeCraft 미니 케이스 — Harness Engineering 적용하기 (3/3)

## 1. 가장 중요한 원칙

> **안전장치는 하나에만 의존하면 안 된다 — Claude Code처럼 여러 겹의 검증 레이어(Layered Control)를 쌓아야 한다.**

CodeCraft에서도 Verification을 하나의 검사에만 기대면 안 되고, 여러 메커니즘을 레이어링해야 한다.

---

## 2. Delivery Fee Calculator — 5단계 검증 레이어

| 레이어 | 검증 내용 |
|---|---|
| **1. Type** | 타입을 제한 |
| **2. Input 검증** | 음수 금액 금지, 음수 거리 금지, city 필수 등 |
| **3. 정책 검증** | 무료 배송 기준, 할인 적용 순서, 요금 적용 조건 |
| **4. 결과 검증** | 최종 요금이 음수가 될 수 없다 등 |
| **5. 테스트** | Happy Path + 예외 케이스 검증 |

> 이렇게 여러 레이어의 안전장치를 만들어두는 것이 **Safety Harness**다.

---

## 3. Claude Code의 Layered Control System

Claude Code는 위험한 행동을 하나의 장치로만 막지 않는다. 위험한 행동은 처음부터 안 보이게 하고, 보이더라도 권한을 확인하고, 권한이 있어도 Sandbox 안에서 실행하고, Hook이 중간에서 막는 식으로 여러 겹을 겹친다.

> User Prompt → Tool Pre-filtering → Permission Rule → Auto Safety Classifier → Pre-tool-use Hooks → Operational Shell Sandbox → Tool Execution → Session Transcript

| 계층 | 역할 |
|---|---|
| Tool Pre-filtering | 위험하거나 허용되지 않은 도구를 모델에 아예 안 보여줌 |
| Permission Rule | 액션마다 Allow / Ask / Deny 판단 |
| Auto Safety Classifier | 머신러닝 기반으로 행동이 위험한지 추가 판단 |
| Hooks | 도구 실행 전후에 중간 개입 |
| Shell Sandbox | 실행되더라도 파일 시스템·네트워크 접근 범위 제한 |
| Session Permission | 이전 세션에서 허용한 임시 권한을 다음 세션으로 자동 이어주지 않음 |

> **공항 보안 검색**에 비유할 수 있다. 항공권 확인, 여권 확인, 수하물 검사, 몸수색, 탑승구 확인, 탑승 전 최종 확인 — 액션 하나를 한 번만 검사하는 게 아니라 여러 단계에서 반복적으로 확인한다.

---

## 4. CodeCraft에도 같은 레이어를 적용한다

Delivery Fee Calculator를 레이어드해서 검증하면: **Layer 1(Type 제한) → Layer 2(입력 검증) → Layer 3(정책 검증) → Layer 4(결과 검증) → Layer 5(테스트)**.

* **테스트**: Happy Path + 예외 케이스 검증
* **결과 검증**: 최종 요금은 음수가 될 수 없다는 것을 검증 기준으로 명시

---

## 5. 미니 케이스를 관통하는 원칙들

* 모델보다 Harness가 중요하고, 코드보다 요구사항(Requirements) Harness가 먼저다. 그 위에 다양한 Harness들이 겹겹이 적용되어야 한다.
* **Reasoning(판단)과 Execution(실행)은 분리되어야 한다** — 설계 판단과 구현·실행을 분리한다.
* **모호한 요구사항과 위험한 능력을 그대로 허용해서는 안 된다** — Deny First 룰.
* **텍스트(컨텍스트)는 희소 자원이다** — 문제 맥락을 선별하고 명시해야 한다.
* **디자인 패턴은 상황별로 써야 한다** — 모든 문제에 하나의 패턴을 억지로 끼워 맞추지 않는다.
* **책임을 분리해야 한다** — 하나의 함수나 모듈에 여러 역할을 몰아넣지 않는다.
* **정책도 격리해야 한다** — 바뀔 수 있는 정책은 로직과 분리해서 관리한다.
* **왜 이 설계를 선택했는지 설명 가능해야 한다** — 트레이드오프를 근거로 판단을 설명할 수 있어야 한다.
* **구현과 테스트를 분리해야 한다** — Generation(생성)과 Evaluation(평가)이 분리되어야 검증이 제대로 이뤄진다.

---

## 6. CodeCraft 문제를 풀 때 실제로 적용해야 하는 것들

CodeCraft를 Harness Engineering으로 풀 때는 에이전트 루프를 사용해서 **요구사항 분석 → 설계 → 구현 → 테스트**를 반복하게 만든다. 그 루프 위에 아래 Harness들이 함께 적용되어야 한다.

| Harness | Delivery Fee Calculator에서의 역할 |
|---|---|
| **Context Harness** | 주문 금액, 거리, 도시, 날씨, 고객 타입 등 문제 맥락을 수집·검증 |
| **Permission(제약조건) Harness** | 음수 거리 금지, 취소 주문 제외 같은 제약조건을 검증하는 게이트 |
| 설계 방안 | 상황에 맞는 디자인 패턴을 선택 |
| **Verification Harness** | Compaction, Subagent, Test 등 구현 결과를 검증 |

이 외에도 Tool Harness, Persistence Harness, Subagent Harness, 그리고 최소 구현·테스트까지 — 다양한 Harness가 CodeCraft 문제를 풀면서 함께 적용되어야 한다는 점을 기억해야 한다.

---

## 7. 다음 강의 예고 — OOD

다음 강의에서는 지금까지 다룬 것들을 실제로 어떻게 구현할지 알기 위해 **OOD(객체 지향 설계)** 수업을 진행한다. OOD를 알아야 다이어그램을 설계하고, 구현 직전 단계에서 설계를 할 수 있다. 그래야 Claude Code가 잘 구현하고 있는지 판단할 수 있는 기준이 생긴다.

---

## 3줄 요약

1. Claude Code가 위험한 행동을 Tool Pre-filtering → Permission → Safety Classifier → Hooks → Sandbox → Session Permission처럼 여러 겹으로 통제하듯, CodeCraft도 Type 제한 → 입력 검증 → 정책 검증 → 결과 검증 → 테스트의 5단계 레이어로 검증해야 한다.
2. 안전장치는 하나에만 기대면 안 되고(Layered Control), 공항 보안처럼 같은 액션을 여러 단계에서 반복적으로 확인해야 하며, 모델보다 Harness·코드보다 요구사항이 먼저라는 것, Reasoning과 Execution 분리, Deny First, 텍스트는 희소 자원, 디자인 패턴은 상황별로, 책임·정책 분리, 설계에 대한 설명 가능성, 구현과 테스트(Generation/Evaluation) 분리가 CodeCraft를 관통하는 원칙이다.
3. CodeCraft 문제는 요구사항 분석 → 설계 → 구현 → 테스트를 반복하는 에이전트 루프 위에 Context·Permission·Tool·Verification·Persistence·Subagent Harness를 함께 적용해서 풀어야 하며, 다음 강의에서는 이를 실제로 구현하기 위한 OOD(객체 지향 설계)를 다룬다.
