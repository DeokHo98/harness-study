# Part 2. Context Harness

---

# 3장. 컨텍스트는 "많이"가 아니라 "정확하게"

> **클로드가 헛도는 건 대부분 모델 성능 문제가 아니라 컨텍스트를 잘못 준 것이다.**

큰 코드베이스에서는 정보 부족보다 **컨텍스트가 너무 넓고 흐려서** 실패하는 경우가 더 많다.

```text
관련 없는 정보 + 오래된 대화 + 잘못된 가설 + 긴 로그 = Context Noise
```

관련 없는 파일이 딸려 들어오면 모델이 좋아도 "지금 뭘 고쳐야 하는지" 자체가 흐려진다.

> **좋은 Context ≠ 많은 정보**
> **좋은 Context = 올바른 첫 번째 행동을 유도하는 최소한의 정확한 정보**
> AI가 똑똑해질수록 사람이 할 일은 **더 정확한 경계 설정**이다.

## 3.1 Context를 4가지로 분리한다

`Instruction → Context → Evidence → State` — **섞지 않는 것이 중요하다.**

| 구분 | 무엇 | 예 |
|---|---|---|
| **Instruction** | 어떻게 행동해야 하는가 | 테스트 없이 완료 선언 금지, 특정 API/DB 스키마 변경 금지, 아키텍처 규칙 준수 |
| **Context** | 작업 이해에 필요한 배경 | 프로젝트 구조, 관련 파일, 프레임워크/런타임/브랜치 |
| **Evidence** | 문제를 증명하는 실제 자료 | XCTest 실패, 빌드 에러, Crash Log, CI 결과 |
| **State** | 지금까지의 진행 상태 | 시도한 해결책, 실패한 가설, 변경 파일, Sub-agent 결과, 이전 작업 요약 |

## 3.2 Context Pack

"로그인 버그 고쳐줘"가 아니라 이렇게 구조화한다.

```text
Objective       무엇을 해결해야 하는가?
Evidence        실제 에러/로그/테스트 결과는?
Relevant Files  어떤 파일들이 관련 있는가?
Constraints     절대 변경하면 안 되는 것은?
Do Not Touch    접근·수정하지 말아야 할 영역은?
Verification    무엇을 실행해서 성공을 확인하는가?
```

→ 바로 수정하는 대신 **`Explore → Plan → Edit → Verify`** 순서로 움직이게 된다.

## 3.3 탐색이 먼저다

복잡한 작업의 순서: **관련 파일 탐색 → 원인 후보 축소 → 근거 확인 → 수정 계획 → 코드 수정 → 테스트·검증**

> **첫 번째 행동은 Edit가 아니라 Explore.**
> 새 코드베이스 투입 순서도 같다: **구조 파악 → 특정 영역 탐색 → 수정**

## 3.4 범위 줄이기 3단계

| 단계 | 내용 |
|---|---|
| **1. 문제 범위** | 지금 뭘 고치는지 **한 문장**으로 요약 가능해야 함 |
| **2. 파일 범위** | 관련 파일 후보를 모은 뒤 실제 수정할 파일로 다시 좁힘 (전체를 한꺼번에 열지 않음) |
| **3. 세션 범위** | 조사 / 수정 / 리뷰 세션을 섞지 않음 (익스플로러·플랜 모드·서브에이전트로 분리) |

## 3.5 Context Window는 희소 자원

Context를 차지하는 것: System Prompt · 프로젝트 정보 · `CLAUDE.md` · Auto Memory · Tool 정보 · 대화 기록 · Tool 실행 결과 · Sub-agent 결과 · Compact Summary

⚠️ 압축(Compact)되면 정보가 손실될 수 있으므로 **중요한 내용을 긴 대화 속에만 남겨두면 안 된다.**

**Context Reduction Pipeline** — 모델 호출 전에 불필요하거나 큰 정보를 줄이는 5단계

```text
Budget Reduction → Harness Snip → Micro Compact → Context Collapse → Auto Compact
```

그 밖의 절감 방식: `.md` 문서를 필요한 시점에만 로딩 · Tool Schema 지연 로딩 · Subagent는 전체 대화가 아니라 **요약만** 부모에 반환

## 3.6 규칙의 저장 위치

| 필요한 것 | 위치 |
|---|---|
| 반복적으로 알려줘야 하는 프로젝트 규칙 | `CLAUDE.md` |
| **반드시 지켜야 하는 실행 규칙** | **Permissions / Hooks** |
| 현재 문제의 로그와 증거 | 현재 Context |
| 현재 작업의 진행 상태 | State / Session |
| 반복 작업 절차 | Skill |

⚠️ `CLAUDE.md`는 **강제 장치가 아니다.** 정말 막아야 하는 행동은 Context에 적지 말고 **실행 레벨에서 강제한다.** (→ 5·6·9장)

## 3.7 에이전트는 "잘 훈련된 인턴"

좋은 인턴에게도 처음부터 다 맡기지 않는다. 알려줘야 할 것: **지금 뭘 해야 하는지 · 범위가 어디까지인지 · 어떤 기준으로 검증할지.** 그리고 작업 중 흐름을 지켜보고, 맥락을 보충하고, 핵심 정보가 유지되도록 관리한다.

> **에이전트를 잘 쓰는 사람 = 많이 만드는 사람이 아니라, 충분한 컨텍스트를 유지한 채 작업을 넘기는 사람**

## 3.8 에이전트 친화적 코드베이스 5기준

1. 테스트 커버리지가 충분한가
2. README와 실제 코드베이스가 일치하는가
3. 설계 패턴이 프로젝트 전반에서 일관적인가
4. 빌드/실행 방식이 명확한가
5. 코드 스타일/컨벤션이 흔들리지 않는가

약하면 악순환: **잘못된 이해 → 잘못된 수정안 → 새 버그 → 그 버그도 잘못 해석 → 혼란 증폭**

> **코드베이스가 약하면 에이전트는 그 약점을 그대로 증폭시킨다.**
> 컨텍스트 컨트롤은 프롬프트 스킬이 아니라 **설계·테스트·문서화 수준과 직결된 엔지니어링 문제**다.

### 3장 정리

1. 헛발질의 원인은 정보 부족보다 Context Noise — 많이 주기보다 **필요한 것만 정확히**.
2. Instruction·Context·Evidence·State로 분리하고, 문제·파일·세션 범위를 줄인 뒤 `Explore → Plan → Edit → Verify`로 유도한다.
3. Context Window는 희소 자원이고, 강제할 규칙은 Permissions/Hooks로 옮기며, 근본 해결책은 에이전트 친화적 코드베이스다.

---
---

# 4장. 컨텍스트 재설계와 세션 위생

> **문제는 모델이 아니라 '지시'다.**
> 지시가 넓고 · 금지 범위가 없고 · 완료 판단 기준이 없어서 생긴다. "로그인 버튼 고쳐주세요"는 어디까지 건드려도 되는지, 무엇을 확인해야 끝인지 아무 정보도 주지 않는다.

**가장 흔한 실수**: "설명이 부족했나?" → 정보 추가 → Context 커짐 → Noise 증가 → 더 엉뚱한 결과

> **해결책은 설명을 늘리는 게 아니라 컨텍스트를 재설계하는 것.**

## 4.1 헛돌 때 해야 할 것

* 관련 없는 파일·불필요한 로그 제거
* 수정 금지 범위 명확화
* 현재 가설 재검토 → 잘못됐으면 **폐기**
* 실제로 어떤 파일을 보고 있는지 확인
* 필요하면 **새 세션 시작**

⚠️ 이미 산으로 간 세션에 설명만 덧붙이면 **더 위험하다** — 오염된 컨텍스트에서는 좋은 지시도 희석된다.
→ **작업을 멈추고 방향을 끊은 뒤, 좁은 범위로 다시 지시.**
→ 변경 범위가 큰 작업은 수정 대신 **계획(plan)부터 받는다** (PR 전 diff 리뷰하듯).

## 4.2 재설계 4요소

1. **문제를 한 문장으로 정의**
2. **수정 가능 범위 vs 수정 금지 범위 분리**
3. **절대 깨지면 안 되는 조건 명시** (예: 버튼 위치/디자인 변경 금지)
4. **완료 기준(검증 방법) 정의**

> "알아서 잘 고쳐주세요" → **"이 범위 안에서, 이 파일은 건드리지 말고, 이 조건을 유지하며, 이 방법으로 검증해줘"**

| | 애매한 요청 | 재설계한 요청 |
|---|---|---|
| 지시 | "사이드 메뉴에 로그인 버튼 추가해줘" | 목표/현재상태/수정가능/수정금지/유지조건/검증방법/보고형식 |
| 결과 | 버튼은 추가됐지만 클릭해도 무반응 | 페이지 이동까지 정상 + 변경 파일·검증 결과 보고 |

**요청 템플릿 7칸**

* **목표**: 버튼 클릭 시 로그인 페이지로 이동, 기존 디자인 컨셉에 맞는 UI
* **현재 상태(재현)**: 버튼은 있지만 클릭해도 반응 없음
* **수정 가능 범위**: 사이드바 관련 컴포넌트 파일
* **수정 금지 범위**: 환경 설정 파일, CI/CD 워크플로우 파일
* **유지 조건**: 버튼 위치/디자인 변경 금지
* **검증 방법**: 빌드 + 테스트 통과 확인
* **완료 보고 형식**: 변경 파일 목록, 핵심 수정 내용, 검증 결과

## 4.3 언제 재설계하고, 언제 오버스펙인가

| 재설계가 필요 | 오버스펙 |
|---|---|
| 관련 없는 파일까지 건드림 | 단순 오타 수정 |
| "수정 완료"인데 검증이 비어있음 | 한 줄짜리 문구 수정 |
| 범위가 여러 디렉토리/모듈로 확대 | → 검증 범위가 이미 명확하면 **짧게 요청하는 게 효율적** |
| API·인프라·CI/CD 등 금지 영역 포함 | |

**증상별 대처**

| 증상 | 대처 |
|---|---|
| 엉뚱한 파일을 보기 시작 | 설명을 붙이지 말고 **파일 범위 재축소** |
| 수정 범위가 계속 확대 | 밀어붙이지 말고 **조사 세션과 수정 세션 분리** |
| 세션이 길어져 판단이 흐려짐 | 억지로 잇지 말고 **핵심만 요약해 새 세션** |

## 4.4 세션 오염(Context Pollution)

> **모델이 갑자기 멍청해진 게 아니라 세션 오염이다.**

세션이 길어질수록 노이즈가 쌓이고 **초반에 준 제약 조건이 뒤에 쌓인 정보에 묻혀 희석된다.** 작업 품질은 모델 성능뿐 아니라 **컨텍스트를 얼마나 깨끗하게 유지했는가**에 좌우된다.

| 명령 | 성격 | 언제 |
|---|---|---|
| **`/clear`** | 대화 히스토리를 통째로 날려 컨텍스트를 완전히 비움 | **작업이 바뀔 때** — 작업 A가 끝났으면 B를 같은 컨텍스트로 끌고 가지 않는다 |
| **`/compact`** | 버리지 않고 **요약본만 남김** | **작업이 길어질 때** — 끊지 않고 이어갈 때 |

⚠️ `/compact`만 치면 알아서 잘 남겨줄 거라 생각하면 안 된다. **남길 항목을 직접 지시**해야 한다.

> 변경된 파일 목록 · 실행한 커맨드 · 결과 요약 · 남은 To-do

안 하면 요약은 생기지만 중요한 맥락이 빠져 다음 턴에서 방향이 틀어진다.

## 4.5 요약을 파일로 승격시키기

> 대화 속 규칙은 `/compact`에서 사라질 수 있지만, **파일로 승격된 규칙은 계속 다시 읽힌다.**

* 대화 속 규칙 = **인메모리 캐시** (휘발성)
* `CLAUDE.md` / `.clauderules` 속 규칙 = **UserDefaults/로컬 DB** (지속성)

→ 대화 중 나온 중요한 결론·금지 사항·테스트 커맨드·결정 사항은 파일로 옮긴다.

**실전 루틴**

| 상황 | 행동 |
|---|---|
| 작업이 바뀌면 | `/clear` |
| 작업이 길어지면 | `/compact` + 남길 항목 직접 지시 |
| 계속 유지할 규칙이 있으면 | `CLAUDE.md` / `.clauderules`로 승격 |

## 4.6 Append-Only JSONL

Claude Code는 기록을 지우고 새로 쓰지 않고 **뒤에 계속 이어붙인다(Append-only).** 이 기록으로 이전 세션을 다시 열거나, 복사해서 이어가거나, 특정 시점으로 되돌아갈 수 있다. 단 **이전에 허용했던 위험한 권한까지 복원되지는 않는다.**

* **JSONL (JSON Lines)** — 한 줄에 하나의 기록을 JSON으로 저장
* 쌓이는 순서: User Prompt → Assistant Reasoning/Response → Tool Use → Tool Results → Assistant Response → User Feedback → New Decision

> 예: 3번 결정이 마음에 안 들어도 3번을 지우지 않는다. "이전 설계를 수정하기로 결정" 같은 새 기록을 아래에 붙인다.

이 원칙은 26장의 **State 영속화**로 이어진다 — 진행 상태를 대화가 아니라 파일에 남기는 것이 장시간 작업의 핵심이다.

### 4장 정리

1. 엉뚱한 수정의 원인은 넓은 지시·금지 범위 없음·완료 기준 없음 → **목표 한 문장 + 가능/금지 범위 + 유지 조건 + 검증 방법**으로 재설계.
2. 산으로 간 세션엔 설명을 덧붙이지 말고 끊는다. 작업이 바뀌면 `/clear`, 길어지면 `/compact`(남길 항목 직접 지시).
3. 계속 지킬 규칙은 파일로 승격해야 `/compact` 이후에도 살아남고, 기록 자체는 append-only라 재개·분기·복원이 가능하다.

---
---

# 5장. CLAUDE.md와 규칙의 저장 위치

> **CLAUDE.md는 메모장이 아니라 "법전"이다.**

세션 시작 시 자동 로드되는 **Context Harness의 핵심 파일**. 세 가지 역할을 한다.

* **반복 제거** — 같은 규칙을 세션마다 다시 설명하지 않음
* **아키텍처 경계 지정** — 어떤 파일을 왜 건드리면 안 되는지 명시
* **검증 기준 제공** — 테스트 명령/완료 조건을 알려줘 스스로 검증하게 함

## 5.1 4-Layer 계층 (우선순위 순)

| 레이어 | 위치 | 내용 | 공유 |
|---|---|---|---|
| **Enterprise** | 조직 레벨 | 회사 전체 보안 정책/표준 | 조직 공유 |
| **User** | `~/CLAUDE.md` | 개인 선호 스타일, 자주 쓰는 명령어 | 개인 전역 |
| **Project** | `CLAUDE.md` | 팀 공통 규칙, 아키텍처 경계, 테스트 명령 | Git 공유 |
| **Local** | `CLAUDE.local.md` | 나만의 실험 설정/메모 | `.gitignore`, 비공유 |

## 5.2 짧을수록 잘 먹힌다 — Auto Memory

> **길게 쓴다고 더 잘 지켜지지 않는다. 오히려 길면 준수율이 떨어진다.**

* **Auto Memory**는 `CLAUDE.md`와 **별개 메커니즘**이며, 세션 시작 시 **처음 200줄 또는 25KB 중 더 작은 쪽까지만** 로드
* 나머지 토픽별 파일은 필요할 때 **지연 로딩(lazy loading)**

→ `CLAUDE.md`는 **짧고 핵심 규칙 위주로.** 특정 작업에만 필요한 절차는 몰아넣지 말고 **Skill로 분리**한다. (→ 9장)

## 5.3 Worktree 함정

* `CLAUDE.md`는 worktree마다 따로 둘 수 있다
* ⚠️ **그러나 Auto Memory는 같은 Git 저장소의 모든 worktree/서브 디렉토리가 하나의 메모리 디렉토리를 공유한다**

→ worktree를 나눴다고 메모리까지 격리된다고 착각하면 안 된다. 한쪽에서 학습한 게 다른 쪽 작업에 은근슬쩍 영향을 준다. (→ 12장)

**개념 구분**: 세션 = 하나의 대화 / 에이전트 = 실행 단위 / 워크트리 = 파일 시스템 + 브랜치 단위

## 5.4 Guidance vs Enforcement

| 구분 | 위치 | 성격 |
|---|---|---|
| **Guidance** | `CLAUDE.md` | 소프트 — 어길 수도 있음 |
| **Enforcement** | `settings.json`의 `permissions.deny` | 하드 — 실행 시도해도 실제 차단 |

* 팀 공유 정책(`permissions.deny`/`allow`) → `settings.json`
* 개인 실험 설정 → `settings.local.json` (gitignore)

⚠️ `permissions.deny`도 만능 보안장치는 아니다. 프로젝트를 설명해주는 설정 체계일 뿐 모든 위험을 막는 보안 솔루션은 아니므로, **여러 겹의 방어선으로 설계한다.** (→ 6장)
