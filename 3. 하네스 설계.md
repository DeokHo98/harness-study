
# 클로드가 헛발질할 때, 진짜 원인은 "정보 부족"이 아닐 수도 있다

클로드 코드 같은 AI 코딩 도구를 쓰다 보면 다들 한 번쯤 겪는 상황이 있다. 로그인 버그 하나 고쳐달라고 했는데, 갑자기 엉뚱한 파일을 건드리고 있거나 관련도 없는 API 스키마를 손대고 있는 경우.

이럴 때 보통 "아 모델이 오늘 좀 멍청하네"라고 넘어가기 쉬운데, 실무에서는 원인이 다른 데 있는 경우가 더 많다. **모델이 보고 있는 작업면(context) 자체가 흐려진 거다.** 오늘은 이 문제를 구조적으로 푸는 방법, 컨텍스트 하네스(Context Harness) 설계에 대해 정리해본다.

## 컨텍스트는 많이 준다고 좋은 게 아니다

직관적으로는 "정보를 더 많이 주면 더 잘하겠지"라고 생각하기 쉽다. 근데 큰 코드베이스에서는 오히려 **컨텍스트 과잉**이 컨텍스트 부족보다 더 자주 문제를 일으킨다.

관련 없는 파일, 이미 끝난 대화 기록, 예전에 틀렸던 가설, 지나치게 긴 로그, 애매한 요구사항 — 이런 게 뒤섞여서 컨텍스트에 들어가면 모델은 그럴듯하지만 틀린 방향으로 흘러갈 확률이 높아진다. 겉보기엔 정보가 풍부해 보여도, 실제로는 신호 대비 잡음이 너무 많은 상태인 거다.

Claude Code 공식 메모리 문서에서도 각 세션은 **깨끗한 컨텍스트 윈도우(fresh context)**로 시작한다고 설명한다. 세션을 넘어 지식을 이어가는 수단은 사용자가 직접 쓰는 `CLAUDE.md`랑, 클로드가 스스로 쓰는 Auto Memory 두 가지다.

여기서 중요한 포인트 하나. `CLAUDE.md`랑 Auto Memory는 **강제 규칙이 아니라 "제공되는 컨텍스트"**라는 점이다. `CLAUDE.md`에 뭔가 적었다고 클로드가 무조건 그걸 지키는 게 아니다. 그냥 "이런 참고 정보 있어"라고 흘려주는 것뿐이다. 그래서 진짜로 특정 행동을 막아야 한다면, 그건 컨텍스트 레벨이 아니라 **Permissions**나 **Hooks** 같은 실행 하네스 쪽으로 옮겨야 한다.

## 컨텍스트를 4가지로 쪼개서 보기

컨텍스트 하네스를 설계할 때 가장 먼저 해야 할 일은, 뭉뚱그려진 "정보"를 아래 네 가지로 구분하는 거다.

```
Instruction (지시사항)
   ↓
Context (배경 정보)
   ↓
Evidence (증거)
   ↓
State (진행 상태)
```

**Instruction** — 클로드가 어떻게 행동해야 하는지에 대한 규칙
- 테스트 없이 완료를 선언하지 말 것
- API 응답 스키마를 임의로 바꾸지 말 것
- 컨트롤러에 비즈니스 로직을 직접 작성하지 말 것

**Context** — 현재 작업을 이해하는 데 필요한 배경
- 프로젝트 구조, 관련 파일
- 현재 브랜치, 프레임워크, 런타임 환경

**Evidence** — 지금 문제가 뭔지 보여주는 관찰 자료
- 실패한 테스트 아웃풋, 스택 트레이스
- 빌드 에러, CI 로그

**State** — 지금까지 어떤 작업이 있었는지 기록
- 이미 시도한 해결책, 실패한 가설
- 변경된 파일 목록, 압축된 요약, 서브 에이전트 결과

이 네 가지가 구분 안 된 채로 뭉쳐서 던져지면, 클로드한테 정보를 많이 준 것 같아도 실제로는 흐릿한 정보를 준 거랑 다를 게 없다.

## 예시로 비교해보면 확실히 느껴진다

"이 로그인 버그를 고쳐줘" 같은 한 줄 지시랑, 아래처럼 구조화된 지시를 비교해보면 차이가 확 느껴진다.

> 현재 API 로그인에서 500 에러가 발생하고 있고, 관련 로그는 다음과 같다. 인증 관련 파일 후보는 `Service`, `Controller`, `Strategy`, `Test`로 구성되어 있다. 이번 작업에서는 DB 스키마랑 공개 API 응답 형태는 변경하지 말고, 먼저 원인 후보를 3개 이하로 좁힌 다음 수정 전에 어떤 파일을 볼지 제한해라.

여기엔 Objective(목표), Evidence(에러/로그), Relevant Files(관련 파일 후보), Constraints(제약), Do Not Touch(건드리면 안 되는 범위), Verification Command(검증 방법)가 다 들어가 있다. 이렇게 컨텍스트 팩(Context Pack)이 잘 짜여 있으면, 작업 실패 확률이 확 줄어든다.

이런 구조가 잡히면 세션도 자연스럽게 이렇게 흘러간다.

```
Explore → Plan → Edit → Verify
```

핵심은, 프로젝트 전체를 던지는 게 아니라 **지금 필요한 작업면만 잘라서 주는 것**이다.

## 컨텍스트는 무한한 게 아니라 희소 자원이다

클로드 코드 입장에서 컨텍스트 윈도우는 매번 모델 호출 전에 조립되는 **제한된 작업면**이다. 시스템 프롬프트, 환경 정보, `CLAUDE.md` 계층, Auto Memory, 툴 메타데이터, 대화 히스토리, 툴 실행 결과, 서브 에이전트 요약, 압축된 요약 — 이게 다 합쳐져서 하나의 컨텍스트 윈도우가 만들어진다.

컨텍스트가 커지면 결국 압축(compact)이 일어나는데, **압축은 항상 손실을 동반한다.** 그래서 팀 전체가 지켜야 할 규칙이나 반복되는 아키텍처 원칙 같은 걸 그냥 대화 컨텍스트 안에 흘려 넣어두면, 압축 이후에 흐려져서 사라져버릴 수 있다.

이럴 땐 원칙이 단순하다.

| 상황 | 옮길 곳 |
|---|---|
| 반복되는 규칙 | `CLAUDE.md`에 넣기 |
| 반드시 강제해야 하는 규칙 | `Permissions`나 `Hooks`로 옮기기 |

## 클로드가 헛돌 때, 설명부터 더 붙이지 말자

클로드가 엉뚱한 결과를 내놓으면 흔히 "설명이 부족했나?" 하면서 정보를 더 구조 없이 쏟아붓게 된다. 근데 실제로는 반대인 경우가 많다.

클로드가 이상한 수정안을 낸다면, 해야 할 일은 설명 추가가 아니라 **컨텍스트 재설계**다.

- 관련 파일이랑 무관한 파일을 다시 나눈다
- 수정 금지 범위를 명확히 한다
- 지금 붙잡고 있는 가설이 틀렸으면 버리고 새 가설로 갈아탄다
- 필요하면 아예 새 세션을 연다

컨텍스트가 한 번 오염된 상태로 대화를 계속 이어가면, 클로드는 이전의 잘못된 가설을 대화 흐름으로 받아들이고 그 위에 더 정교한 오답을 쌓아 올린다. 그래서 작업 로그를 자주, 상세히 들여다보면서 "지금 어떤 파일 보고 있는지, 어떤 수정하고 있는지"를 체크하는 습관이 중요하다.

## 정리

컨텍스트 하네스 설계의 핵심은 **클로드한테 정보를 많이 주는 게 아니라, 올바른 첫 번째 행동을 하게 만드는 것**이다.

큰 코드베이스에서 클로드의 첫 번째 행동은 보통 "수정"이 아니라 "탐색"이어야 한다. 관련 파일을 찾고, 원인 후보를 세우고, 검증 명령으로 확인한 다음에야 수정으로 들어가는 거다. 이 흐름의 기준이 잡히면, 그냥 "고쳐줘"라고 던지는 대신 뭘 먼저 살펴봐야 하는지, 어떤 문제가 있는지, 원인을 어떻게 분석하고 계획을 세울지를 컨텍스트로 설계해나갈 수 있다.

---


<img width="922" height="506" alt="스크린샷 2026-07-29 오후 1 48 12" src="https://github.com/user-attachments/assets/90a699cb-64d3-46a6-866f-8d9938fcbefa" />
