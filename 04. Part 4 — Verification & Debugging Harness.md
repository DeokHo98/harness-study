# Part 4. Verification & Debugging Harness

---

# 7장. 검증 하네스

> **"코드가 실행된다" ≠ "코드가 올바르다"**

AI는 좁은 Context 안에서 그럴듯한 코드를 만들지만, 프로젝트 전체의 아키텍처·API 계약·컨벤션·엣지 케이스까지 보장하지 않는다. 게다가 **품질이 평범해도 자신감 있게 "잘했다"고 말하는 경향**이 있다.

> **AI의 "완료했습니다"는 검증이 아니다.**

## 7.1 검증 3단계

**① Automated Checks** — 반복 가능한 검사를 자동화
XCTest/Unit Test · Build/Type Check · Lint · Smoke Test · CI
⚠️ 단, **테스트 통과만으로 완료 판정을 내리면 안 된다.**

**② Diff Verification** — `git diff`로 실제 변경 범위 확인

* 요청하지 않은 파일까지 수정했는가?
* 불필요한 리팩터링이 들어갔는가?
* Public API가 변경됐는가?
* Response Schema / Codable 모델이 변경됐는가?
* Secret / Token / Credential을 건드렸는가?
* 비즈니스 로직 변경 범위가 적절한가?

> **무엇을 만들었는지보다 실제로 무엇을 변경했는지가 중요하다.**

**③ Counter-example Verification** — 일부러 깨본다
Edge Case · Invalid Input · Race Condition · Regression · 예외 상황 · 경계값

> 핵심 질문: **"이 코드가 어떤 입력에서 깨질 수 있는가?"**

## 7.2 테스트 통과 ≠ 검증 완료

테스트는 충분조건이 아니다.

* 테스트가 없는 코드 영역일 수 있다
* 커버리지가 부족할 수 있다
* 기존 테스트가 오래되어 현재 요구사항과 안 맞을 수 있다
* **테스트를 통과하면서도 잘못된 요구사항을 구현할 수 있다**

→ 항상 **"테스트가 보장하는 것"과 "보장하지 못하는 것"**을 구분한다.

## 7.3 검증 기준은 사람이 정의한다

"코드 괜찮아?"는 검증이 아니다.

**최소 검증 6문항**

1. 요청 범위를 벗어난 변경이 있는가?
2. Public API / Response Schema가 변경됐는가?
3. 테스트되지 않은 비즈니스 로직이 변경됐는가?
4. 어떤 반례에서 실패할 수 있는가?
5. Regression 테스트가 필요한가?
6. 위험도가 높은 변경은 무엇인가?

> **검증을 시킬 수는 있어도, "무엇을 검증할 것인가"는 사람이 설계한다.**

## 7.4 Generation과 Evaluation 분리

신뢰 가능한 루프: **컨텍스트 수집 → 액션 수행 → 결과 검증**

| 단계 | 예 (Delivery Fee Calculator) |
|---|---|
| **Generation** | Policy 구현, Validator 구현, Service 구현 |
| **Evaluation** | 정상 주문 / 비 오는 날 요금 / 무료 배송 기준 테스트 |

> "구현을 완료했다"는 말이 맞다는 뜻은 아니다. **둘이 분리돼야 검증이 성립한다.**

**작성자와 리뷰어의 Context를 분리한다** — 같은 Claude가 쓰고 **같은 세션에서 바로 리뷰하면 위험하다.** 작성 중 만들어진 잘못된 가설과 편향을 리뷰에도 그대로 가져간다.

→ 중요한 변경은 **별도 Sub-agent / 새 Session / Git Worktree**로 리뷰 Context를 분리한다. AI 코드 리뷰에도 일반 코드 리뷰의 **독립성 원칙**이 적용된다. (→ 25장 Generator/Evaluator)

## 7.5 Rollback도 검증의 일부

> **좋은 자동화는 실패하지 않는 자동화가 아니라, 실패해도 복구할 수 있는 자동화다.**

별도 Branch · 작은 Diff 단위 · 실행한 테스트 기록 · 검증 결과 기록 · PR에 변경 범위와 검증 내용 기록 · Rollback 방법 확보

## 7.6 최종 승인 5문답

```text
무엇을 고쳤는가? → 어떤 테스트를 통과했는가? → 변경 범위가 요청과 일치하는가?
→ 어떤 반례를 확인했는가? → 문제가 생기면 어떻게 되돌리는가?
```

답할 수 없으면 "완료"가 아니라 **"검증되지 않은 변경"**이다.

> **Verification = Automated Checks + Diff Review + Counter-examples + Independent Review + Rollback**
> AI가 만든 코드의 품질을 믿는 게 아니라, **틀려도 발견하고 되돌릴 수 있는 시스템**을 만든다.

### 7장 정리

1. "실행된다"와 "올바르다"는 다르다. AI의 완료 선언은 검증 결과가 아니다.
2. Automated Checks → Diff → Counter-example 3단계로 검증하고, Generation과 Evaluation을 별도 Session/Sub-agent/Worktree로 분리한다.
3. 중요한 건 AI가 틀리지 않는 게 아니라, **틀려도 발견하고 Rollback할 수 있는 구조**다.

---
---

# 8장. 디버깅 하네스

> **로그를 더 많이 주는 게 아니라, "풀 수 있는 문제 형태"로 바꿔주는 시스템이다.**

로그만 통째로 던지면 Stack Trace 마지막 줄만 보고 잘못된 원인으로 성급히 수정한다. **정보가 많아야가 아니라 정보가 구조화되어야** 정확한 진단이 나온다.

## 8.1 세 가지를 분리한다

| 구성 요소 | 내용 |
|---|---|
| **증상 (Symptom)** | 무엇이, 언제 실패했는가 (예: 로그인 요청 시 500 에러) |
| **증거 (Evidence)** | Stack Trace, 실패 테스트 출력, 로그, 환경 변수 차이, 변경 이력 |
| **검증 경로 (Verification Path)** | 재현 명령, 실패하는 테스트, 가설 확인용 추가 로그 |

> **먼저 고치지 말고 원인 후보부터 좁힌다.**
> Symptom → Evidence → 원인 후보 → Verification Loop(재현/테스트/롤백 계획)

## 8.2 프롬프트 5단계

1. **문제** — 무엇이 잘못됐는가
2. **환경** — 어디서 실패하는가 (로컬/Docker/CI 차이)
3. **증거** — Stack Trace·로그 첨부
4. **제약** — 스키마 변경 금지, API 응답 변경 금지, secret 접근 금지, **먼저 수정하지 말고 원인 후보부터 좁힐 것**
5. **요청** — 원인 후보 순위, 후보 확인용 최소 명령/파일, 회귀 테스트 필요 여부

## 8.3 환경 변수는 값이 아니라 "범위와 가시성"

"로컬은 되는데 Docker/CI는 안 됨"은 대부분 코드 버그가 아니라 **설정/스코프 문제**다. 값 자체를 보여주지 말고 네 가지만 정리한다.

**존재 여부 · 출처 · 유효 범위 · 런타임 가시성**

| 환경 변수 | 로컬 | Docker | CI |
|---|---|---|---|
| `JWT_SECRET` | 존재 (.env) | ❌ 전달 안 됨 | 존재 (Secret Manager) |
| `API_BASE_URL` | 존재 | 존재 | 존재 |

→ 실제 값을 몰라도 이 표만으로 문제 위치를 찾는 **프라이버시 보존형 디버깅**. (시크릿을 Context에 올리지 않는다는 점에서 6장과 이어진다.)

## 8.4 반례 루프

원인을 확신하는 순간 되묻는다.

* 이 가설이 틀릴 수 있는 조건은?
* 이 수정이 실패할 수 있는 입력은?
* 이 로그와 모순되는 다른 원인은 없는가?

> **가설 → 증거로 후보 좁히기 → 원인 확정 → 반례로 검증**
