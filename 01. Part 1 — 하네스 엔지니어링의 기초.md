# Part 1. 하네스 엔지니어링의 기초

---

# 1장. 하네스란 무엇인가

> **"AI가 왜 이러지?" → "어느 하네스가 약하지?"**
> 실무 품질 = **모델 성능 × 하네스 품질**. 같은 모델이어도 하네스가 다르면 결과가 크게 달라진다.

## 1.1 Claude Code는 모델이 아니라 에이전트 시스템

* 채팅형 Claude = **답변 생성**
* Claude Code = **관찰 → 판단 → Tool 실행 → 결과 확인 → 재판단**을 반복하는 Agent Loop

> **Claude Code = Model + Agent Loop + Harness + State/Persistence + Tools**

모델은 파일이나 터미널을 직접 조작하지 않는다. `"이 파일을 읽고 싶다"`고 **요청할 뿐**이고, 실행 전에 Harness가 다음을 수행한다.

```text
Permission 확인 → Hook 실행 → Sandbox/Deny Rule 적용 → Tool 실행 → 결과를 모델에 전달
```

> **Harness = AI의 자율성을 실제 행동으로 바꾸면서도 통제 가능한 범위 안에 가두는 시스템**

## 1.2 하네스의 7축

| 하네스 | 역할 |
|---|---|
| **Context** | 필요한 정보만 정확하게 — 무엇을 볼 것인가 |
| **Permission** | 위험한 행동·접근 범위 제한 — 무엇을 해도 되는가 |
| **Verification** (Certification) | 테스트·diff로 결과 검증 |
| **Tool** | 어떤 도구를 어떻게 쓸지 설계 |
| **Sub-agent / Worktree** | 작업 분리와 병렬화 |
| **Debug** | 실패 원인을 추적 가능하게 설계 |
| **Persistence / State** | Session과 기록을 남김 |

## 1.3 증상 → 의심할 하네스

| 증상 | 먼저 의심할 것 |
|---|---|
| 엉뚱한 파일 수정 | **Context** |
| 위험한 명령 실행 | **Permission** |
| 수정했지만 틀림 / 테스트 실패 | **Verification** |
| MCP / Tool 문제 | **Tool** |
| 병렬 작업 충돌 / 세션 간 충돌 | **Sub-agent / Worktree** |
| 로그를 줘도 원인 추적 실패 | **Debug** |
| 이전 잘못된 가설을 계속 유지 | **State / Session** |

## 1.4 프롬프트보다 시스템 설계

AI를 잘 쓰는 핵심은 프롬프트가 아니라 **Context + Permission + Tools + 검증 + 작업 분리 + Debugging을 하나의 시스템으로 설계하는 것**이다. 팀 단위라면 프롬프트가 아니라 **설정과 정책**을 관리해야 한다.

| 무엇을 | 어디에 |
|---|---|
| 프로젝트 컨텍스트 / 개발 규칙 | `CLAUDE.md` |
| Permission·Hook·환경·모델 등 실행 정책 | `settings.json` |
| 반복 작업 표준화 | **Skill** |
| 외부 서비스 연결 | **MCP** |
| 반드시 지켜야 할 실행 규칙 | **Hooks** |

> Claude Code는 AI 도구가 아니라 **프로젝트 개발 환경에 통합해서 운영하는 시스템**이다.

## 1.5 "기억"도 시스템의 일부

긴 작업에서는 State & Persistence가 중요하다.

* **Session Transcript** — 지금까지의 작업 기록
* **CLAUDE.md** — 지속적으로 필요한 규칙·컨텍스트
* **Compact Summary** — 긴 대화를 요약해 맥락 유지
* **Sub-agent Sidechain** — 특정 작업을 별도 에이전트에 위임
* **Resume / Fork / Rewind** — 이어가기·분기·복원

⚠️ **잘못된 가설이 세션 상태에 남으면 같은 방향으로 계속 삽질한다.** (→ 4장)

## 1.6 자율성은 목표가 아니다

좋은 Agent 시스템의 목표는 AI를 최대한 자유롭게 만드는 것이 아니다.

> **검증 가능한 경계(Boundary) 안에서 안전하게 자율성을 발휘하게 만드는 것**

권한을 줄수록 실패 비용도 커진다 → **권한의 범위와 검증 장치는 함께 설계한다.**

## 1.7 문제가 생겼을 때의 순서

```text
문제 발생 → 증상 분류
   → Context / Permission / Verification / Tool / Sub-agent / Debug / State 중 원인 추정
   → CLAUDE.md / settings.json / 세션 상태 점검
   → 하네스 수정 → 재실행 + 검증
```

> 프롬프트를 더 길게 쓰는 것보다 **실패한 하네스의 구조를 고치는 것**이 근본적이다.

### 1장 정리

1. AI 코딩의 실패는 모델보다 하네스(Context·Permission·검증·Tool·작업 분리·Debug·State)의 문제일 수 있다.
2. 모델은 행동을 **요청**할 뿐이고, 실제 실행·권한·검증은 Harness가 담당한다.
3. 좋은 Agent는 무제한 자율성이 아니라 **검증 가능한 경계 안의 자율성**으로 설계된 시스템이다.

---
---

# 2장. Claude Code 아키텍처와 코어 루프

> **챗봇이 아니라 에이전틱(agentic) 코딩 툴이다.**
> 질문 한 번·답 한 번이 아니라 **상태 확인 → 판단 → 재실행**을 반복해 작업을 끝까지 밀고 나간다. 주니어 개발자에게 티켓 하나를 통째로 맡기는 느낌에 가깝다.

## 2.1 코어 루프

> **모델 호출 → 도구 사용 요청 → 도구 실행 → 결과를 다시 모델에게**

이 반복 주위를 하네스가 감싼다.

| Harness | 역할 |
|---|---|
| Context | 어떤 정보를 볼지 정함 |
| Permission | 어떤 행동을 해도 되는지 제안 |
| Tool | 어떤 도구를 어떻게 쓸지 정함 |
| Verification | 결과를 테스트·실행 결과로 확인 |
| Subagent | 작업을 분리 |
| Persistence | Session과 기록을 남김 |

모델은 파일 시스템·Shell·네트워크에 **직접 접근하지 않는다.** 모델이 Tool Use를 만들면 Harness가 Parse → Permission 확인 → Tool Implementation에 Dispatch한다. 이 분리 덕분에 모델이 잘못 판단하거나 조작되더라도 **Sandboxing·Permission Check·Deny First**로 실행을 통제할 수 있다.

| 주체 | 하는 일 |
|---|---|
| **모델** | 판단, 다음 액션 제안, Tool Use 요청 |
| **Harness** | 요청 검증, 권한 확인, 도구 실행, 결과를 루프에 되돌림 |

```text
User Request → Context Assembly → Model Reasoning → Tool Use Request
   → Permission Gate → Tool Execution → 결과 → 검증
```

> **모델은 판단만 하고, 실행은 Harness가 통제한다.**

## 2.2 구성 요소

| 구성 요소 | 역할 | 오해하지 말 것 |
|---|---|---|
| **CLAUDE.md** | 프로젝트 규칙을 유지하는 기억 장치 | 메모가 아니라 **규칙을 안정시키는 장치** |
| **init** | 프로젝트 초기 설정 파일 생성 | |
| **Skills** | 반복 워크플로우 등록 (Fastlane `lane`과 유사) | 기능 추가가 아니라 **재설명 방지 장치** |
| **Hooks** | 특정 시점마다 강제 실행 (Git `pre-commit`처럼 스킵 불가) | 자동화 옵션이 아니라 **놓침 방지 강제 장치** |
| **GitHub Actions** | PR/이슈 흐름에서 CI/CD 연결 | 개인 사용을 **팀 협업 수준으로** |
| **Agent / Teams / Sub-Agent** | 컨텍스트 분리 + 병렬화 | |

> 종합하면 **기억 · 컨텍스트 · 실행 · 검증 · 자동화**를 함께 다루는 하나의 작업 시스템.
> 전체 루프: 코드베이스 이해 → 문맥 유지 → 전략 선택 → 파일 수정 → 명령 실행 → 검증 → 협업 → 자동화

## 2.3 4개 레이어로 진단하기

| 레이어 | 내용 | 문제 증상 |
|---|---|---|
| **규칙** | CLAUDE.md, 프로젝트 규칙, 금지 사항, 컨벤션 | 세션마다 결과가 계속 다름 |
| **컨텍스트** | 이번 작업의 문제/파일/로그/수정 범위 | 자꾸 엉뚱한 파일을 건드림 |
| **전략** | 바로 수정 vs 계획 우선, 서브에이전트 분리, 자동화 범위 | 수정 품질이 들쭉날쭉 |
| **실행·검증** | 파일 수정, 테스트, diff 검토, Git 정리, PR (Hooks/Actions가 강화) | 코드는 맞는데 충돌·버그 |

> 1.3의 "증상 → 하네스" 표와 같은 문제를 다른 해상도로 본 것이다. 증상으로 하네스를 좁히고, 그 하네스가 속한 레이어를 보강한다.

## 2.4 Human Decision Authority

아키텍처를 이끄는 다섯 원칙 중 하나. 인간은 액션을 **관찰·승인/거부·중단·사후 감사(Audit)**할 수 있어야 하며, Claude Code가 아무리 강력해져도 최종 의사결정권은 인간에게 남는다.

> Claude는 설계를 제안할 수 있지만, 어떤 요구사항을 받아들이고 어떤 제약을 우선하고 어떤 패턴이 과한지 판단하는 것은 사람의 몫이다. **Harness Engineering이 필요한 이유가 여기에 있다.**

Claude Code를 잘 쓰는 사람 = 결과를 보고 **요구사항에 맞는지 · 과하게 복잡하지 않은지 · 검증 가능한지 · 확장성은 어떤지**를 스스로 판단할 수 있는 사람.

### 2장 정리

1. 상태를 확인하고 판단하고 다시 실행하는 걸 반복해 작업을 끝까지 수행하는 에이전틱 도구다.
2. 코어 루프(모델 호출 → 도구 요청 → 실행 → 결과)를 6개 하네스가 감싸며, 모델은 판단만 하고 실행은 하네스가 통제한다.
3. 문제가 생기면 규칙·컨텍스트·전략·실행/검증 4레이어 중 병목을 찾아 보강하고, 최종 판단권은 항상 사람이 갖는다.
