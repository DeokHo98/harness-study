# Claude Code — 절대 잊으면 안 되는 핵심 요약

## 1. Claude Code는 “모델”이 아니라 “에이전트 시스템”이다

* 채팅형 Claude = **답변 생성**
* Claude Code = **관찰 → 판단 → Tool 실행 → 결과 확인 → 재판단**을 반복하는 Agent Loop
* 모델이 파일이나 터미널을 직접 조작하는 것이 아니라, **Harness가 모델의 요청을 실제 행동으로 변환한다.**

> **Claude Code = Model + Agent Loop + Harness + State/Persistence + Tools**

---

## 2. 모델보다 중요한 것이 Harness다

모델은 `"이 파일을 읽고 싶다"`, `"이 명령을 실행하고 싶다"`라고 요청할 뿐이다.

실제 실행 전에는 Harness가:

1. Permission 확인
2. Hook 실행
3. Sandbox / Deny Rule 적용
4. Tool 실행
5. 결과를 다시 모델에 전달

한다.

즉,

> **Harness = AI의 자율성을 실제 행동으로 바꾸면서도 통제 가능한 범위 안에 가두는 시스템**

---

## 3. 실무 품질은 “모델 성능 × 하네스 품질”

같은 모델이어도 하네스가 다르면 결과가 크게 달라질 수 있다.

| 증상               | 먼저 의심할 것                 |
| ---------------- | ------------------------ |
| 엉뚱한 파일 수정        | **Context**              |
| 위험한 명령 실행        | **Permission**           |
| 수정했지만 틀림         | **Verification**         |
| MCP / Tool 문제    | **Tool**                 |
| 병렬 작업 충돌         | **Sub-agent / Worktree** |
| 원인 분석 실패         | **Debug**                |
| 이전 잘못된 가설을 계속 유지 | **State / Session**      |

따라서 문제가 생기면:

> **“모델이 왜 이러지?”가 아니라 “어느 하네스가 약하지?”**

라고 생각한다.

---

## 4. Claude Code의 “기억”도 시스템의 일부다

긴 작업에서는 State & Persistence가 중요하다.

* **Session Transcript** → 지금까지의 작업 기록
* **CLAUDE.md** → 프로젝트 규칙과 지속적으로 필요한 컨텍스트
* **Compact Summary** → 긴 대화를 요약해 맥락 유지
* **Sub-agent Sidechain** → 특정 작업을 별도 에이전트에 위임
* **Resume / Fork / Rewind** → 작업을 이어가거나 분기·복원

특히 **잘못된 가설이 세션 상태에 계속 남으면 AI가 같은 방향으로 계속 삽질할 수 있다.**

---

## 5. Claude Code는 “프롬프트”보다 “프로젝트 정책”이 중요하다

팀 단위로 제대로 사용하려면 프롬프트만 관리할 게 아니라 **설정과 정책을 관리해야 한다.**

* `CLAUDE.md` → **프로젝트 컨텍스트 / 개발 규칙**
* `settings.json` → **Permission / Hook / 환경 / 모델 등 실행 정책**
* **Skill** → 반복 작업을 표준화
* **MCP** → 외부 서비스와 Tool 연결
* **Hooks** → 반드시 지켜야 하는 실행 규칙 자동화

즉 Claude Code는 단순한 AI 도구가 아니라 **프로젝트의 개발 환경에 통합해서 운영하는 시스템**으로 보는 것이 맞다.

---

## 6. 문제가 생겼을 때의 디버깅 순서

```text
문제 발생
   ↓
증상 분류
   ↓
Context / Permission / Verification / Tool /
Sub-agent / Debug / State 중 원인 추정
   ↓
CLAUDE.md / settings.json / 세션 상태 점검
   ↓
하네스 수정
   ↓
다시 실행 + 검증
```

**AI에게 프롬프트를 더 길게 쓰는 것보다, 실패가 발생한 하네스의 구조를 고치는 것이 더 근본적인 해결책일 수 있다.**

---

# 반드시 기억할 한 문장

> **Claude Code의 성능은 모델 하나로 결정되는 것이 아니라, 모델이 어떤 Context를 보고 어떤 Permission으로 어떤 Tool을 사용하며 어떤 방식으로 검증받는지를 설계한 Harness에 의해 크게 결정된다.**

---

# 3줄 요약

1. **Claude Code는 단순한 LLM이 아니라 Model + Agent Loop + Harness + State + Tools로 구성된 에이전트 시스템이다.**
2. **모델은 행동을 요청할 뿐이고, 실제 실행·권한·검증은 Harness가 담당한다.**
3. **문제가 생기면 모델을 탓하기 전에 Context·Permission·Verification·Tool·Sub-agent·Debug·State 중 어디가 약한지부터 찾는다.**

<img width="1358" height="673" alt="스크린샷 2026-07-23 오후 2 08 30" src="https://github.com/user-attachments/assets/2bc85fc4-82b7-4d82-8ba1-2340663908f8" />
<img width="1223" height="641" alt="스크린샷 2026-07-23 오후 2 13 16" src="https://github.com/user-attachments/assets/8bdaac7e-6f3f-4432-9555-c1bef6838871" />

<img width="1358" height="673" alt="스크린샷 2026-07-23 오후 2 08 30" src="https://github.com/user-attachments/assets/2bc85fc4-82b7-4d82-8ba1-2340663908f8" />
<img width="1223" height="641" alt="스크린샷 2026-07-23 오후 2 13 16" src="https://github.com/user-attachments/assets/8bdaac7e-6f3f-4432-9555-c1bef6838871" />
