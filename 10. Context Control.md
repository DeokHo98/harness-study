# 큰 코드베이스에서 클로드가 헛도는 이유 — "컨텍스트 컨트롤" 정리

큰 프로젝트에서 클로드 코드 쓰다 보면 이런 경험 다들 있을 거야.

- 분명 A 파일만 고치라고 했는데 엉뚱한 파일까지 건드림
- 수정 범위가 자기 맘대로 넓어짐
- 필요도 없는 설명을 장황하게 늘어놓음

이럴 때 보통 "아 모델 성능이 떨어졌나?" 싶은데, 사실 원인은 다른 데 있는 경우가 많아. 바로 **컨텍스트를 잘못 준 것**. 이번 글에서는 그 이유랑 해결 방법을 정리해볼게.

## 프롬프트보다 중요한 건 "범위 관리"

핵심부터 말하면, 클로드 코드를 잘 쓰는 건 프롬프트를 정교하게 쓰는 기술이 아니라 **작업 범위와 맥락을 어떻게 통제하느냐**의 문제야.

큰 코드베이스에서는 정보가 부족해서 실패하는 경우보다, 오히려 **컨텍스트가 너무 넓고 흐려서** 실패하는 경우가 더 많아. iOS 프로젝트로 치면, 모듈 하나 수정하는데 프로젝트 전체 타겟을 다 열어주는 느낌이라고 생각하면 돼. 관련 없는 파일이 잔뜩 딸려 들어오면 아무리 모델이 좋아도 "지금 뭘 고쳐야 하는지"에 대한 문제 정의 자체가 흐려져.

반대로 범위를 좁혀주면, 모델은 오히려 어떤 코드가 진짜 관련 코드인지 더 정확하게 짚어낼 수 있어. 즉, AI가 똑똑해질수록 사람이 해줘야 할 일은 **더 정확한 경계 설정**이라는 거지.

## 컨텍스트 컨트롤 3단계

새 코드베이스에 클로드를 투입할 때는 이 순서를 지키는 게 좋아: **구조 파악 → 특정 영역 탐색 → 수정**. 프로젝트 전체를 한 번에 던져주는 것보다, 탐색-범위선정-수정 순서를 지키는 게 핵심이야.

이걸 3단계로 나눠서 볼 수 있어.

### 1. 문제 범위 줄이기

지금 뭘 고치는 건지 한 문장으로 요약이 안 되면, 클로드도 같이 헤맬 수밖에 없어. "OwnedBannerView 캐러셀 드래그 제스처가 iOS 17에서 onEnded 안 먹는 버그 고치기" 처럼 한 문장으로 딱 떨어지게 정의하는 게 첫 단계.

### 2. 파일 범위 줄이기

관련 있을 만한 파일 후보를 먼저 모으고, 거기서 실제로 수정할 파일을 다시 좁혀가는 방식이야. TCA 프로젝트라면 Reducer, View, Dependency를 한꺼번에 다 열어주지 말고, 지금 작업에 진짜 필요한 것만 골라주는 느낌.

### 3. 세션 범위 줄이기

조사(탐색)하는 세션, 수정하는 세션, 리뷰하는 세션을 섞지 않는 게 품질을 확 올려줘. 클로드 공식 문서에서도 **빌트인 익스플로러(탐색 기능), 플랜 모드, 서브 에이전트** 같은 패턴으로 컨텍스트를 분리해서 관리하라고 반복해서 강조하고 있어.

> **참고**: 여기서 "서브 에이전트"는 클로드 코드 안에서 특정 작업(조사, 리뷰 등)만 맡아서 독립적인 컨텍스트로 돌아가는 하위 에이전트를 말해. iOS로 치면 메인 스레드 작업이랑 백그라운드 작업 분리하듯, 역할별로 컨텍스트 자체를 나눠서 굴린다고 보면 이해가 쉬워.

## 에이전트는 "잘 훈련된 인턴"처럼 다뤄라

여기서 중요한 개념이 하나 더 나오는데, 바로 **컨텍스트를 전환하는 능력**이야. 클로드나 서브 에이전트를 좋은 인턴 다루듯이 투입하는 게 맞다는 얘기야.

좋은 인턴한테도 처음부터 모든 걸 한 번에 맡기지 않잖아. 지금 뭘 해야 하는지, 범위가 어디까지인지, 어떤 기준으로 결과를 검증할지 알려줘야 하지. 클로드도 똑같아.

- 에이전트가 작업하는 동안 터미널에서 흐름을 지켜보고
- 필요하면 맥락을 보충해주고
- 다음 단계로 넘어갈 때 핵심 정보가 유지되도록 관리해줘야 함

즉, 에이전트를 잘 쓰는 사람은 에이전트를 많이 만드는 사람이 아니라, **에이전트마다 충분한 컨텍스트를 유지한 채로 작업을 넘겨주는 사람**이야.

## "에이전트 친화적 코드베이스"란

아무리 클로드를 잘 써도 코드베이스 자체가 에이전트한테 안 친화적이면 계속 흔들릴 수밖에 없어. 에이전트 친화적 코드베이스인지는 이런 기준으로 판단할 수 있어.

1. 테스트 커버리지가 충분한가
2. README와 실제 코드베이스가 일치하는가
3. 설계 패턴이 프로젝트 전반에서 일관적인가
4. 빌드/실행 방식이 명확한가
5. 코드 스타일과 컨벤션이 흔들리지 않는가

이 조건들이 약하면 무슨 일이 생기냐면 — 에이전트가 코드를 처음부터 잘못 이해 → 그 잘못된 이해로 수정안 생성 → 그 수정안이 새 버그를 만듦 → 그 버그를 또 잘못 해석 → 혼란이 계속 증폭되는 악순환이 벌어져. **코드베이스가 약하면 에이전트는 그 약점을 그대로 증폭시키는 존재**라고 보면 돼.

그래서 컨텍스트 컨트롤은 단순 프롬프트 스킬이 아니라, 설계·테스트·문서화 수준과 직결된 **엔지니어링 문제**야. TCA로 치면 Reducer 단위 테스트가 탄탄하고, 의존성 주입 구조가 일관되게 유지되고 있는 프로젝트일수록 클로드가 훨씬 덜 헛돈다는 뜻이지.

## 결국 디테일 싸움

"AI 시대엔 큰 그림만 잡으면 된다"고 생각하기 쉬운데, 실제로는 반대야. 디테일이 단단해야 새 기능 추가든 버그 수정이든 자동화든 안정적으로 나올 수 있어. AI 네이티브 엔지니어는 도구를 이것저것 많이 붙이는 사람보다, **설계·테스트·빌드의 기본기를 탄탄히 만든 다음 그 위에 에이전트를 얹는 사람**에 가까워.

그리고 한 번에 완벽한 에이전트 워크플로를 만들려는 건 대부분 실패해. 단계적으로 만들고, 실험하고, 피드백 보고, 다시 반영하는 과정을 반복하는 게 맞는 접근이야.

## 문제 상황별 대처법

- **클로드가 엉뚱한 파일을 보기 시작하면** → 설명을 더 붙이지 말고 관련 파일 범위를 다시 좁혀라
- **수정 범위가 계속 넓어지면** → 밀어붙이지 말고 조사 세션과 수정 세션을 분리해라
- **세션이 길어져서 판단이 흐려지면** → 이전 대화를 억지로 이어가지 말고 핵심만 요약해서 새 세션으로 시작해라

---

## 3줄 요약

1. 큰 코드베이스에서 클로드가 헛도는 건 대부분 모델 문제가 아니라 **컨텍스트 범위(문제/파일/세션)가 흐린 게 원인**이다.
2. 클로드는 좋은 인턴처럼 **범위와 검증 기준을 명확히 주고, 컨텍스트를 유지한 채 작업을 넘겨야** 잘 쓸 수 있다.
3. 테스트 커버리지, README-코드 일치, 일관된 설계 패턴을 갖춘 **에이전트 친화적 코드베이스**가 결국 근본 해결책이다.
