# 큰 코드베이스에서의 "컨텍스트 컨트롤" — 핵심 요약

## 1. 가장 중요한 원칙

> **클로드가 헛도는 건 대부분 모델 성능 문제가 아니라 컨텍스트를 잘못 준 것이다.**

큰 코드베이스에서는 정보 부족보다 **컨텍스트가 너무 넓고 흐려서** 실패하는 경우가 더 많다. 관련 없는 파일이 딸려 들어오면 모델이 좋아도 "지금 뭘 고쳐야 하는지" 자체가 흐려진다.

> **핵심**: AI가 똑똑해질수록 사람이 해야 할 일은 **더 정확한 경계 설정**이다.

---

## 2. 컨텍스트 컨트롤 3단계

새 코드베이스 투입 순서: **구조 파악 → 특정 영역 탐색 → 수정**

| 단계 | 내용 |
|---|---|
| **1. 문제 범위 줄이기** | 지금 뭘 고치는지 **한 문장**으로 요약 가능해야 함 |
| **2. 파일 범위 줄이기** | 관련 파일 후보를 모은 뒤, 실제 수정할 파일로 다시 좁힘 (전체를 한꺼번에 열지 않음) |
| **3. 세션 범위 줄이기** | 조사(탐색) / 수정 / 리뷰 세션을 섞지 않음 (빌트인 익스플로러, 플랜 모드, 서브 에이전트로 분리) |

---

## 3. 에이전트는 "잘 훈련된 인턴"처럼 다뤄라

좋은 인턴에게도 처음부터 모든 걸 맡기지 않듯, 클로드에게도:

* 지금 뭘 해야 하는지
* 범위가 어디까지인지
* 어떤 기준으로 검증할지

를 알려줘야 한다. 그리고 작업 중 흐름을 지켜보고, 필요하면 맥락을 보충하고, 핵심 정보가 유지되도록 관리해야 한다.

> **에이전트를 잘 쓰는 사람 = 에이전트를 많이 만드는 사람이 아니라, 충분한 컨텍스트를 유지한 채 작업을 넘겨주는 사람**

---

## 4. "에이전트 친화적 코드베이스" 판단 기준

1. 테스트 커버리지가 충분한가
2. README와 실제 코드베이스가 일치하는가
3. 설계 패턴이 프로젝트 전반에서 일관적인가
4. 빌드/실행 방식이 명확한가
5. 코드 스타일/컨벤션이 흔들리지 않는가

이 조건이 약하면 다음 악순환이 발생한다:

> 잘못된 이해 → 잘못된 수정안 생성 → 새 버그 발생 → 그 버그도 잘못 해석 → 혼란 증폭

> **코드베이스가 약하면 에이전트는 그 약점을 그대로 증폭시키는 존재다.**

컨텍스트 컨트롤은 프롬프트 스킬이 아니라 **설계·테스트·문서화 수준과 직결된 엔지니어링 문제**다.

---

## 5. 문제 상황별 대처법

| 증상 | 대처법 |
|---|---|
| 엉뚱한 파일을 보기 시작함 | 설명을 더 붙이지 말고 **관련 파일 범위를 다시 좁히기** |
| 수정 범위가 계속 넓어짐 | 밀어붙이지 말고 **조사 세션과 수정 세션을 분리** |
| 세션이 길어져 판단이 흐려짐 | 이전 대화 억지로 잇지 말고 **핵심만 요약해서 새 세션 시작** |

---

## 3줄 요약

1. 큰 코드베이스에서 클로드가 헛도는 건 대부분 모델 문제가 아니라 **컨텍스트 범위(문제/파일/세션)가 흐린 게 원인**이다.
2. 클로드는 좋은 인턴처럼 **범위와 검증 기준을 명확히 주고, 컨텍스트를 유지한 채 작업을 넘겨야** 잘 쓸 수 있다.
3. 테스트 커버리지, README-코드 일치, 일관된 설계 패턴을 갖춘 **에이전트 친화적 코드베이스**가 결국 근본 해결책이다.
