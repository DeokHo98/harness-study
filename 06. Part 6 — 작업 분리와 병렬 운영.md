# Part 6. 작업 분리와 병렬 운영

> **"병렬 처리(속도)"가 목표가 아니라 "격리(충돌 방지)"가 목표다.**

격리 없이 여러 세션/Subagent를 동시에 돌리면: 같은 파일 동시 수정 · 테스트 DB 결과 꼬임 · 브랜치 충돌 · 세션 간 컨텍스트 혼입 · 머지 불가능한 변경 누적

**격리는 3계층**

| 계층 | 제공 주체 | 상태 |
|---|---|---|
| **대화(컨텍스트)** | 별도 세션 / 서브에이전트 | 세션만 나눠도 자연히 됨 |
| **파일 시스템** | Worktree | Worktree가 직접 제공 (diff/충돌 분리) |
| **메모리** | `CLAUDE.local.md` | ⚠️ **불완전** — 오토메모리는 같은 레포에서 공유 |

---

# 11장. Subagent — 컨텍스트 격리

## 11.1 Subagent란

여러 AI가 단체 채팅하는 구조가 아니다. **부모 작업 공간과 분리된 작은 작업실.**

* Main 대화와 **분리된 별도 컨텍스트**
* 필요한 **도구만** 접근 권한 부여
* Main 세션에는 조사 과정 전체가 아니라 **요약/최종 보고만** → Main 컨텍스트 오염 방지

| 용어 | 뜻 |
|---|---|
| **Parent Context** | 메인 세션의 전체 작업 맥락 — 지금까지의 대화, 프로젝트 규칙, 읽은 파일, 수정 파일 목록, 명령어 결과, 현재 목표 |
| **Transcript** | 작업 기록 파일 — 어떤 대화·도구·결과가 있었는지의 로그 |

⚠️ 모든 에이전트가 같은 컨텍스트를 공유하면 서로를 오염시키고, 불필요한 기록이 메인에 쌓이고, **권한 관리가 흐려진다.**

## 11.2 왜 요약만 반환하는가

* Parent Context가 커지는 것을 막기 위해 (Context Window에 한계가 있다)
* Subagent의 여러 추측과 **실패한 시도**가 전부 Parent로 들어오면 메인 세션이 지저분해진다
* Subagent는 원인을 찾을 때까지 반복한다(파일 확인 → 테스트 재실행 → 로그 확인 → 다른 가설). **최종 원인과 추천만** 요약해 반환한다

## 11.3 역할과 권한을 함께 나눈다

| 역할 | 하는 일 | 권한 |
|---|---|---|
| **Explorer (탐색)** | 코드 탐색, 구조 파악 | 읽기 전용 |
| **Implement (구현)** | 실제 코드 수정 | 편집/쓰기 |
| **Verify (검증)** | 테스트 실행, 변경 리뷰 | 테스트 실행만, 쓰기 제한 |

> **모든 Subagent에게 모든 도구를 주지 않는다.**

**Permission도 분리된다** — Subagent는 Parent의 권한을 그대로 물려받지 않는다.

| 주체 | 하는 일 |
|---|---|
| **Parent** | 전체 방향 결정, 사용자와 상호작용, 최종 판단 |
| **Subagent** | 제한된 작업만, 필요한 Context만, 별도 기록, 요약만 반환 |

→ Subagent가 과도한 권한으로 폭주하는 것을 막는다.

## 11.4 역할이 겹치면 만들지 않는다

빌트인 3종: `explore` · `plan` · `general-purpose` (커스텀 가능)

> **description이 곧 역할 경계다.** Claude는 이 description을 보고 언제 위임할지 판단한다.

* 역할이 겹치면 **만들지 않는다**
* 조사용/수정용/검토용처럼 역할이 뚜렷할 때만 분리 가치가 있다
* 작업 경계도 안 정해졌는데 서브에이전트부터 여러 개 만들면 혼란만 커진다

> 많이 돌리는 게 아니라 **작게 시작해서 측정하고 반복적으로 키운다.** (모델 세대별 위임 판단은 → 23장)

> 💡 **실물 — `.claude/agents/*.md`** (원본 강의 밖 보충 · 공식 문서 기준)
> 11.3의 역할·권한 분리가 파일 두 개가 된다.
>
> `.claude/agents/explorer.md`
>
> ```markdown
> ---
> name: explorer
> description: 코드베이스에서 관련 파일을 찾아 구조만 요약한다. 수정 범위를 정하기 전에 사용.
> tools: Read, Grep, Glob
> model: sonnet
> ---
>
> 너는 탐색 전용이다. 코드를 절대 수정하지 않는다.
> 1. 질문과 관련된 파일을 찾는다
> 2. 각 파일이 어떤 책임을 갖는지 한 줄로 정리한다
> 3. 수정 후보 파일과 건드리면 안 될 파일을 구분해서 제시한다
>
> 출력은 15줄 이내로 요약한다. 읽은 파일 전문을 붙여넣지 않는다.
> ```
>
> `.claude/agents/verifier.md`
>
> ```markdown
> ---
> name: verifier
> description: 변경된 코드를 독립적으로 검증한다. 구현이 끝난 뒤 리뷰가 필요할 때 사용.
> tools: Read, Grep, Glob, Bash
> permissionMode: plan
> model: opus
> ---
>
> 너는 이 코드를 작성하지 않았다. 작성 의도를 추측하지 말고 실제 출력만 평가한다.
> 1. `git diff`로 실제 변경 범위를 확인한다
> 2. 요청 범위를 벗어난 변경을 지적한다
> 3. 이 코드가 깨질 수 있는 입력(반례)을 3개 제시한다
> 4. 테스트를 실행하고 결과를 그대로 보고한다
>
> "괜찮아 보인다"는 결론은 금지한다.
> ```
>
> | 프론트매터 | 대응하는 원칙 |
> |---|---|
> | `description` | 11.4 "description이 곧 역할 경계" |
> | `tools` | 11.3 "모든 Subagent에게 모든 도구를 주지 않는다" |
> | `permissionMode` | 11.3 "Parent 권한을 그대로 물려받지 않는다" |
> | 별도 파일 = 별도 컨텍스트 | 7·26장 "작성자와 리뷰어는 다른 Context" |
> | `isolation: worktree` | 12장 Worktree 격리를 에이전트 단위로 |
>
> 그 밖에 쓸 만한 필드: `maxTurns`(폭주 방지) · `effort`(24장) · `disallowedTools` · `skills`(스킬 미리 로드)
> 위치: 프로젝트 `.claude/agents/` · 개인 `~/.claude/agents/`

---

# 12장. Worktree — 파일 시스템 격리와 머지

## 12.1 Git 기능이 아니라 격리 도구

> **Worktree = "컨텍스트 + 파일 시스템을 함께 격리하는 도구"**

* `git worktree`로 하나의 저장소에서 여러 작업 디렉터리 생성
* 각 worktree가 서로 다른 브랜치를 독립 체크아웃
* `.git` 히스토리는 공유, 디스크는 효율적

같은 디렉토리에서 세션을 여러 개 띄우면 **대화창은 분리돼도 파일 시스템은 공유**된다.

* 세션 A가 고친 파일을 세션 B가 모르고 또 건드림
* diff가 뒤섞여 어떤 작업 때문인지 추적 불가
* 테스트 결과가 어떤 세션 때문인지 불명확
* 리뷰·머지가 지옥

**분리하면**: 세션 A `bugfix/auth` / 세션 B `refactor/network` / 세션 C 실험 브랜치 → Main worktree(baseline)는 오염되지 않고 각 작업의 변경·테스트 결과를 비교할 수 있다.

⚠️ **주의 1** — 같은 브랜치를 여러 세션이 동시 편집하면 병렬이 아니라 **혼란**이다. 누가 뭘 바꿨는지 추적 불가능해진다.
⚠️ **주의 2** — Worktree는 충돌을 없애는 마법이 아니라 충돌이 **"언제, 얼마나 비싸게" 일어날지 통제하는 도구**다. 실시간 충돌 대신 머지 시점의 정리된 충돌로 바꿔준다.
⚠️ **주의 3 (함정)** — `CLAUDE.local.md`는 worktree별로 분리되지만 **오토메모리는 같은 레포의 모든 worktree가 하나의 디렉터리를 공유**한다. 한쪽에서 학습한 게 다른 쪽에 영향을 준다. (→ 5장)

## 12.2 명령어 워크플로우

1. `claude --worktree` → Worktree 자동 생성, 독립 브랜치에서 세션 시작
2. `git status`, `git remote set-head origin -a` → 기준점(origin 헤드) 재동기화
3. Worktree 관련 경로를 `.gitignore`에 추가
4. 작업 완료 후 `worktree remove`로 개별 제거

**작업 쪼개기** — 각 Worktree는 **딱 하나의 책임(concern)**만 진다.
예: `auth-validate`(로그인 입력 검증) / `auth-errors`(로그인 실패 에러 메시지 구조)

## 12.3 진짜 중요한 건 생성이 아니라 머지

* 각 Worktree는 자기 diff와 자기 테스트를 **스스로 책임**진다
* 메인 Worktree에서 **순차적으로** 머지 (동시에 우르르 합치지 않는다)
* 충돌 시 **바로 "고쳐줘"라고 시키지 않는다** — 한쪽을 기계적으로 덮어쓸 위험

**충돌 해결 프롬프트 순서 (필수)**

1. **원인 분석** — 왜 충돌했는지, 양쪽이 같은 로직 영역을 어떻게 다르게 고쳤는지 설명시키기
2. **머지 계획 제시** — 어떻게 합칠지 계획을 먼저 받기
3. **계획대로 수정 + 테스트**

> 이 순서를 지켜야 "아무거나 골라서 덮어쓰기" 사고를 막는다.

## 12.4 둘을 함께 써야 진짜 병렬 격리

| | 격리 대상 | 혼자 쓸 때 |
|---|---|---|
| **Subagent** | 컨텍스트(대화) | 대화는 깨끗해도 **파일 충돌은 여전** |
| **Worktree** | 파일 시스템 | 파일은 분리돼도 **역할이 섞일 수 있음** |

**안전한 워크플로우**: 탐색(Explorer, 읽기 전용) → 구현(Implement, 별도 worktree에서 작은 단위 패치) → 검증(Verify, 변경분·테스트 결과 리뷰)

---
---

# 13장. Plan-First, Thinking Mode, Sub-Agent 선택

## 13.1 Plan-First — Read-Only로 먼저 훑기

**Read-Only 기반으로 코드베이스를 분석해 계획을 세우는 모드.** 복잡한 변경일수록 바로 수정에 들어가면 실패하기 쉽다.

**계획부터 세워야 하는 신호 3**

1. 영향 범위를 확신할 수 없을 때
2. 여러 디렉토리·서비스가 얽혀 있을 때
3. 테스트가 약하거나 레거시라 연쇄 영향이 우려될 때

**필수 4요소**: 지금 해결할 문제 정의 · 가능한 원인 후보 · 수정 범위 · 테스트 검증 순서 + **반영 전에 멈춰서 확인할 지점**

⚠️ Plan 모드를 켜놓고 대충 훑고 바로 수정으로 넘어가면 그건 계획이 아니라 **워밍업**이다.

## 13.2 Thinking Mode — 불확실성이 클 때만

> "더 똑똑한 답"용이 아니라 **즉시 실행보다 판단 품질이 중요한 작업에 계산 자원을 더 쓰는 전략**이다.

| 유용 | 과함 |
|---|---|
| 영향 범위 넓은 수정 | 로그 한 줄 파싱 |
| 설계 대안 비교 | 파일 경로 찾기 |
| 원인 후보가 여러 개인 디버깅 | |

(모델 세대에 따라 사고량 조절 방식이 달라진다 — Adaptive Thinking → 22장, Effort → 23·24장)

## 13.3 언제 뭘 쓸지

| 상황 | 선택 |
|---|---|
| 문제 단순, 수정 범위 명확 | 바로 실행 |
| 문제 복잡, 범위 넓음 | **Plan-First** |
| 작업 유형이 서로 다름 | **Sub-Agent** |
| 결과 품질 중요한데 범위 애매 | **Thinking Mode** |

* **Plan-First**는 느린 게 아니라 실패 비용을 줄이는 방식
* **Thinking Mode**는 불확실성 큰 문제에 계산을 더 쓰는 전략
* **Sub-Agent**는 장식이 아니라 작업 경계를 분리해 결과 품질을 높이는 구조

---
---

# 14장. GitHub Issue-PR 자동화

> **자동화된다고 끝까지 자동으로 밀어붙이면 안 된다.**
> 이슈에는 애매한 요청·외부 입력·검증 안 된 요구사항이 섞여 있다. 그대로 `main`까지 밀면 속도는 빨라지지만 **제어력이 확 떨어진다.**

## 14.1 자동화 범위

| 자동으로 되는 것 | 반드시 사람이 확인 |
|---|---|
| 이슈 내용 읽기 | **최종 코드 리뷰** |
| 작업 브랜치 생성 (`feature/signup-modal`) | **위험 파일** (배포 설정·인증/권한·시크릿 키) |
| 초안 수준 코드 수정 | **CI 통과 여부** |
| 커밋 생성 | **머지 승인** |
| Draft PR 오픈 | |

> iOS 예: Info.plist 권한/URL Scheme, fastlane 배포 스크립트, 서명 인증서/프로비저닝, API 키 Config — 자동 수정돼도 무조건 재검토.

## 14.2 세팅

1. **API 키 등록** — Settings → Secrets and variables → Actions → `ANTHROPIC_API_KEY`
2. **워크플로우(YAML)** — 이슈 생성/댓글 시 트리거. 구조는 **"이슈 읽기 → 초안 코드 수정 → Draft PR"**만 이해하면 충분 (YAML 문법 암기 불필요)

> 💡 **실물 — `.github/workflows/claude.yml`** (원본 강의 밖 보충 · 공식 문서 기준)
> "YAML은 외울 필요 없다"고 한 그 파일. 실제로 이 정도가 전부다.
>
> ```yaml
> name: Claude Code
> on:
>   issue_comment:
>     types: [created]
>   pull_request_review_comment:
>     types: [created]
> jobs:
>   claude:
>     if: contains(github.event.comment.body, '@claude')
>     runs-on: ubuntu-latest
>     permissions:
>       contents: write
>       pull-requests: write
>       issues: write
>       id-token: write
>       actions: read
>     steps:
>       - uses: actions/checkout@v6
>         with:
>           fetch-depth: 1
>       - uses: anthropics/claude-code-action@v1
>         with:
>           anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
> ```
>
> **읽는 법**
>
> * `if: contains(...'@claude')` — 멘션이 있을 때만 러너가 뜬다 (비용 통제)
> * `permissions:` 블록 — 14.4의 "권한을 필요 이상으로 넓게 주지 마라"의 실물. 필요 없으면 `contents: read`로 낮춘다
> * **`prompt` 입력이 없으므로 대화형 모드** — 사람이 `@claude ...`라고 써야 움직인다
> * **`prompt`를 주면 자동 모드**가 되어 멘션 없이 돈다 — 위에서 경고한 "끝까지 자동으로 밀어붙이기"가 시작되는 지점
>
> 세팅은 `claude` 안에서 `/install-github-app`을 실행하면 앱 설치 + 시크릿 + 이 워크플로 PR까지 자동 생성된다.
> 비용 통제는 `claude_args: "--max-turns 5"`와 워크플로 timeout으로.
> ⚠️ **머지 승인은 이 워크플로가 아니라 브랜치 보호 규칙으로 강제한다.** 워크플로 자체는 머지 권한을 갖지 않게 둔다.
3. **흐름** — 이슈 작성 → 댓글로 Claude 멘션 → Actions 트리거 → 구조 분석/구현/테스트/요약/Draft PR → 사람이 리뷰·CI 확인 후 머지

## 14.3 언제 쓰고, 언제 피하나

| 쓰기 좋은 경우 | 피해야 하는 경우 |
|---|---|
| 비슷한 이슈가 반복적으로 들어옴 | 레포 권한 구조가 정리 안 됨 |
| 브랜치 생성+초안 작업 패턴이 매번 비슷 | 리뷰 문화가 없는 팀 |
| 리뷰 문화·CI 체계가 갖춰짐 | 배포/인프라/보안 파일이 자주 얽히는 레포 |
| | 외부 입력을 검증 없이 믿으면 안 되는 환경 |

## 14.4 조심할 실수 4

* 자동화된다고 **머지까지** 밀어붙이기
* Claude에게 **권한을 필요 이상으로 넓게** 주기
* API 키/토큰이 **로그나 코드에 노출**되게 두기
* 이슈에 들어온 **외부 입력을 검증 없이** 프롬프트로 신뢰하기

> **본질**: "완전 자동"이 아니라 **"반복되는 초안 작업을 줄여주는 도구"**. 최종 판단은 사람 몫이다. (→ 2장 Human Decision Authority, 27장 Human-in-the-loop Gate)
