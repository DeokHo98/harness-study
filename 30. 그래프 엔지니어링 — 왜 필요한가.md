# 그래프 엔지니어링 — 왜 필요한가

## 1. 가장 중요한 원칙

> **그래프 엔지니어링은 하나의 큰 작업을 여러 작업 단위로 나누고, 어떤 작업은 동시에 실행하고 어떤 작업은 앞선 결과를 기다리게 할지 설계하는 방식이다.**

여기에 검증, 실패 처리, 상태 저장, 사람이 하는 승인까지 연결해주면, 장시간 실행되는 AI 에이전트 시스템을 훨씬 안정적으로 운영할 수 있다. 그래프 이론이나 복잡한 멀티 에이전트 시스템을 떠올리기 쉽지만, 실제로는 그렇게 어렵지 않다.

---

## 2. 이 시리즈에서 다루는 것

* 루프와 그래프가 어떻게 다른지
* Node, Edge, State가 각각 어떤 역할을 하는지
* 병렬로 실행해도 되는 작업을 구분하는 법
* 여러 에이전트의 결과를 검증하는 방식
* 중간에 실행이 중단되어도 이어서 작업하게 만드는 방법
* 그래프를 사용해야 할 때와 사용하지 않아야 할 때

이 시리즈를 다 들으면: 지금 쓰는 워크플로우를 Node/Edge/State로 그려내고, Barrier를 둘 지점과 스트리밍으로 보낼 지점을 구분하고, 중단돼도 이어서 실행할 수 있도록 State를 파일로 설계하고, 데이터가 오가지 않는 불필요한 Edge를 찾아 제거하고, 스크립트로 검사할 항목과 에이전트가 판단할 항목을 나누고, 그래프가 필요한 작업과 필요 없는 작업을 판단할 수 있게 된다.

---

## 3. 문제 상황 — 왜 그래프 엔지니어링이 필요한가

AI 에이전트는 짧고 범위가 명확한 작업(함수 구현, 파일의 타입 오류 수정, 정해진 형식으로 문서 요약)은 꽤 잘 처리한다. 문제는 작업 범위가 넓어질 때부터 시작된다.

> 예: 40개의 파일을 함께 수정해야 하거나, 여러 패키지를 새로운 구조로 마이그레이션하거나, 수십 개의 자료를 조사해서 하나의 보고서를 만들어야 하는 경우.

이런 작업을 하나의 에이전트에게 한 번에 맡기면 시간이 지날수록:

* 처음에 합의했던 규칙을 뒤쪽 작업에서 놓친다
* 일부 파일만 처리하고 전체 작업을 마쳤다고 판단한다
* 한 파일의 수정이 다른 파일에 어떤 영향을 주는지(의존성)를 확인하지 못한다
* 세션을 다시 시작하면 진행 상황을 정확히 이어가지 못하고, 이미 끝낸 작업을 반복할 가능성도 있다

> 이런 현상을 모델의 성능 문제로만 보기 쉽지만, 많은 경우 더 직접적인 원인은 **작업 구조**에 있다.

### 왜 하나의 에이전트에게 다 맡기면 무너지는가

하나의 에이전트에게 전부 맡기면, 전체 작업 계획 세우기·개별 작업 수행하기·진행 상황 기억하기·결과물 검토하기·오류 수정·완료 여부 판단이라는 여러 책임을 하나의 긴 대화 안에서 모두 처리하게 된다. 그러면 Context는 계속 커지고, 초반에 정한 기준은 점점 희석된다.

그 결과로 관찰되는 네 가지:

1. 초반 규칙이 뒤쪽에서 적용되지 않는다
2. 파일 간 영향(의존성)을 확인하지 못한다
3. 일부만 처리하고 완료로 보고한다
4. 재시작하면 이미 한 작업을 다시 하게 된다

---

## 4. AI 작업 구조가 확장되어 온 흐름

| 단계 | 설계 대상 |
|---|---|
| **Prompt Engineering** | 한 번의 요청(메시지) |
| **Context Engineering** | 모델이 참고할 정보의 범위 |
| **Harness Engineering** | 한 번의 실행 환경(권한, 도구, 완료 기준) |
| **Loop Engineering** | 반복 실행 |
| **Graph Engineering** | 여러 작업과 실행 사이의 관계 |

### Prompt Engineering

하나의 메시지를 잘 작성하는 데 집중한다. 역할, 요구사항, 출력 형식, 예시를 명확하게 작성해서 한 번에 응답 품질을 높이는 방식이다.

### Context Engineering

작업이 복잡해지면 프롬프트 하나만으로 부족해진다. 모델이 참고해야 할 코드, 문서, 규칙, 이전에 했던 결정도 함께 제공해야 한다. 어떤 정보를 보여주고, 무엇을 요약하고, 무엇을 제외할지가 핵심 질문이다. 정보가 부족하면 중요한 내용을 놓치고, 너무 많으면 핵심을 찾기 어려워진다.

### Harness Engineering

한 번의 에이전트 실행 전체를 설계한다. 어떤 파일을 읽고 수정할 수 있는지, 어떤 명령어를 실행할 수 있는지, 외부 네트워크에 접근해도 되는지, 민감한 파일은 어떻게 보호하는지, 테스트를 실행하지 않은 상태에서 완료로 인정할 수 있는지 — 이런 것들을 미리 정한다. 에이전트에게 자유롭게 일을 시키는 게 아니라, 안전하게 일할 수 있는 실행 환경과 규칙을 함께 제공하는 방식이다.

### Loop Engineering

한 번의 실행을 넘어서 반복을 설계한다. 예: 코드를 수정한 뒤 테스트를 실행하고, 실패하면 원인을 분석해서 다시 수정하고, 통과하면 종료한다. 사람이 매번 다음 프롬프트를 입력하지 않아도 시스템이 스스로 다음 행동을 결정한다.

### Graph Engineering

여러 작업과 루프의 관계를 설계하는 단계다. 모든 작업을 한 줄로 연결하지 않고, 아래를 구분해서 설계한다.

* 서로 독립적이어서 동시에 실행할 수 있는 작업
* 반드시 앞 단계의 결과를 받아야 시작할 수 있는 작업
* 결과를 합쳐야 하는 지점
* 검증이 필요한 지점
* 실패했을 때 이동해야 하는 경로
* 사람이 승인해야 하는 지점
* 다음 실행을 위해 저장해야 하는 상태

---

## 5. 그래프 전에 루프부터 이해해야 하는 이유

그래프는 여러 작업과 루프를 연결한 구조이기 때문에, 먼저 루프의 기본 원리를 이해해야 한다.

### 루프의 4단계

**행동(Action) → 결과(Result) → 검증(Verify) → 반복 또는 종료(Repeat/Terminate)**

> 예: 테스트를 통과할 때까지 코드를 수정하는 과정. 코드 수정 → 테스트 실행 → 결과 확인(검증) → 실패하면 다시 수정, 통과하면 종료.

구조 자체는 단순하다. 여기서 가장 중요한 부분은 코드 수정이 아니라 **검증 조건**이다. 루프의 품질은 이 검증 단계가 결정한다.

### 나쁜 검증 조건 vs 좋은 검증 조건

* **나쁨**: "코드가 충분히 좋아졌다고 판단되면 종료하세요" — 무엇을 기준으로 좋아졌다고 판단하는지 정해져 있지 않다.
* **좋음**: 명령어로 확인 가능한 조건들 — 모든 유닛 테스트 통과, TypeScript 컴파일러 오류 0, 오류 없이 종료, 결과가 지정된 JSON 스키마를 만족한다.

> 루프 하나는 단일 목표를 반복해서 개선하는 작업에 적합하다.

---

## 6. 루프만으로 부족해지는 지점 — 작업이 커질 때 생기는 4가지 어려움

1. **여러 작업을 동시에 처리하기 어렵다** — 서로 관련 없는 파일도 하나씩 순서대로 실행해야 한다
2. **결과에 따라 여러 경로로 분기하는 흐름이 복잡해진다** — 예: 네트워크 오류와 테스트 실패를 같은 방식으로 재시도하게 될 수도 있다
3. **하나의 긴 컨텍스트에서 작업과 검증을 함께 처리하면, 자기 결과물에 관대한 판단이 나올 가능성이 높다**
4. **중간 상태가 대화 안에만 남아 있으면, 세션이 종료된 뒤 이전 진행 상황을 정확히 이어가기 어렵다**

> 이 문제를 해결하려면 반복만 설계할 게 아니라, 여러 작업 사이의 관계까지 명확히 나눠야 한다. 그래서 그래프가 필요하다.

---

## 3줄 요약

1. 함수 구현이나 타입 오류 수정처럼 범위가 좁은 작업은 에이전트 하나가 잘 처리하지만, 40개 파일 수정이나 대규모 마이그레이션처럼 범위가 넓어지면 규칙 희석·부분 완료 오판·의존성 누락·재시작 시 중복 작업 같은 문제가 나타나며, 이는 대부분 모델 성능이 아니라 작업 구조의 문제다.
2. AI 작업 설계는 Prompt Engineering(한 메시지) → Context Engineering(참고 정보 범위) → Harness Engineering(실행 환경 전체) → Loop Engineering(반복) → Graph Engineering(여러 작업·실행 사이의 관계) 순으로 확장되어 왔다.
3. 루프는 행동→결과→검증→반복/종료의 4단계이며 품질은 명확한 검증 조건(유닛 테스트 통과, 컴파일 오류 0 등)에 달려 있지만, 병렬 처리·분기·자기 판단의 관대함·세션 간 상태 유지라는 4가지 한계 때문에 작업이 커지면 루프만으로는 부족해져 그래프가 필요해진다.
