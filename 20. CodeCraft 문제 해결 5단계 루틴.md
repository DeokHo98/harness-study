# CodeCraft 문제 해결 5단계 루틴 — 핵심 요약

## 1. 가장 중요한 원칙

> **어떤 문제가 나오더라도 Explore → Brainstorm → Plan → Implement → Verify, 이 5단계 루틴만 따라가면 풀 수 있다.**

문제를 듣자마자 코딩부터 하지 않는다. 방향부터 확실히 잡고, 옵션을 비교하고, 구조를 짠 다음, 구현하고, 마지막에 검증한다.

---

## 2. 5단계 한눈에 보기

| 단계 | 하는 일 |
|---|---|
| **Explore** | 요구사항과 제약사항 확인 |
| **Brainstorm** | 가능한 설계 옵션들의 트레이드오프 비교 |
| **Plan** | 선택한 설계를 구조화 |
| **Implement** | 설계한 대로 코드 구현 |
| **Verify** | 테스트와 Edge Case 검증 |

---

## 3. Explore — 방향부터 확실히 잡기

문제를 듣자마자 코딩하지 않고, 방향을 확실히 잡는 단계다.

* **Clarifying Question**: 조심해야 할 설계 사항이나 애매한 엣지 케이스를 명확한 질문으로 다시 체크한다
* **입력/출력 정의**: 입력 데이터는 어디서 오는가, 출력 형식은 무엇인가
* **변경 가능성이 높은 부분 찾기**
* **최소 구현 범위 정하기** (MVP를 만든다고 생각하면 된다)
* **예외 상황은 무엇인가**
* **확장 가능성 고려**

> 이 단계의 목표는 "코딩"이 아니라 "방향"을 확실히 잡는 것이다.

---

## 4. Brainstorm — 설계 옵션 비교하기

하나의 답만 내는 게 아니라, 여러 옵션을 비교하고 트레이드오프를 알고 설계안을 제시하는 사고를 하는 단계다.

> 예: Option A와 Option B가 있다고 하자. Option A는 "빠르고 읽기 쉽다"는 장점이 있지만 "규칙이 늘어나면 복잡해진다"는 단점이 있다. Option B도 똑같이 장단점을 정리해서 서로 비교한다.

이렇게 옵션별 트레이드오프를 비교한 다음, 현재 요구사항에 맞는 합리적인 옵션을 선택한다.

---

## 5. Plan — 선택한 설계를 구조화하기

설계를 선택하고 나서 어떻게 구현할지 계획을 세우는 단계다.

> 예: 핵심 도메인 객체를 A, B, C로 나눈다면, A는 데이터를 표현하고, B는 계산을 담당하고, C는 정책을 캡슐화하는 식으로 구현 계획을 다룬다.

변경 가능성이 높은 부분을 어떻게 처리할지도 이 단계에서 함께 계획한다.

---

## 6. Implement — 코드 구현

설계한 대로 코드로 옮기는 단계다.

* 어떤 언어로 구현할지
* Class, Method, Enum, Data Type 설계
* 예외 처리
* 테스트 가능한 구조로 작성

---

## 7. Verify — 요구사항을 만족하는지 증명하기

"코드가 돌아간다"가 아니라, **요구사항을 만족하는지 증명**하는 단계다.

* **Happy Path** 테스트
* **Edge Case** 테스트
* **Invalid Input**(유효하지 않은 입력)이 들어왔을 때 대응 검증
* **성능**, **확장성** 검증

---

## 3줄 요약

1. CodeCraft 문제는 Explore(요구사항 확인) → Brainstorm(설계 옵션 비교) → Plan(구조화) → Implement(구현) → Verify(검증)의 5단계 루틴으로 어떤 문제든 풀 수 있다.
2. Explore는 코딩하지 않고 Clarifying Question·입출력 정의·변경 가능성·MVP 범위·예외 상황·확장성을 먼저 확인하는 단계이고, Brainstorm은 하나의 답이 아니라 여러 설계 옵션의 트레이드오프를 비교하는 단계다.
3. Verify의 핵심은 "코드가 돌아가는가"가 아니라 "요구사항을 만족하는가"이며, Happy Path·Edge Case·잘못된 입력·성능·확장성까지 검증해야 한다.
