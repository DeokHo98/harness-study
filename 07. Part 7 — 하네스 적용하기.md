# Part 7. 하네스 적용하기 — 처음 쓰는 사람을 위한 순서

---

# 15장. 7개 하네스, 하나씩 붙이기

Claude Code를 막 쓰기 시작했다면 **7개를 한꺼번에 만들면 안 된다.**
불편을 겪은 순서대로 하나씩 붙인다. 겪지 않은 불편은 만들지 않는다.

| 하네스 (1.2의 7축) | 이런 일이 생기면 | 절 |
|---|---|---|
| **Context** | 매번 프로젝트 설명을 다시 해야 한다 / 엉뚱한 파일을 고친다 | 15.2 |
| **Permission** | 위험한 걸 건드릴까 봐 불안하다 | 15.3 |
| **Verification** | 고쳤다는데 뭘 고쳤는지 모르겠다 | 15.4 |
| **Tool** | 같은 말을 반복한다 / 외부 정보가 필요하다 / 말을 안 듣는다 | 15.5 |
| **Sub-agent / Worktree** | 세션이 지저분해진다 / 여러 작업이 섞인다 | 15.6 |
| **Debug** | 버그를 물어보면 엉뚱한 답이 온다 | 15.7 |
| **Persistence / State** | 며칠짜리 작업이 재시작마다 처음으로 돌아간다 | 15.8 |

> 순서대로 읽을 필요는 없다. **"이런 일"에 해당하는 절만 찾아서 보면 된다.**

**각 절은 똑같은 4칸이다.**

```text
① 이런 일이 생긴다   ← 실제 대화 예시
② 왜 생기나          ← 한 줄
③ 이렇게 한다         ← 그대로 따라 하면 되는 것
④ 됐는지 확인         ← 이걸 해봐야 만든 게 아니다
```

---

## 15.1 시작 전에 — 세 가지만 확인

### ① 지금 어떤 모드인지

Claude Code는 **파일을 고치기 전에 물어보는 모드**와 **안 물어보고 고치는 모드**가 있다. 플랜에 따라 **안 물어보는 모드로 시작될 수 있다.**

`Shift + Tab`을 누르면 모드가 바뀐다. 화면에 현재 모드가 표시된다.

> **익숙해지기 전까지는 물어보는 모드로 쓴다.** 무엇을 하려는지 눈으로 보는 게 먼저다.

### ② git이 안전망이다

```bash
git status
```

**변경사항 없는 깨끗한 상태**에서 시작한다. Claude가 뭘 하든 `git checkout .` 한 줄로 전부 되돌릴 수 있다.

### ③ 우리 프로젝트 명령 3개

빌드·테스트·린트 명령을 확인해둔다. 뒤에서 계속 쓴다.

```bash
cat README.md | head -40
cat .github/workflows/*.yml 2>/dev/null    # CI가 돌리는 명령이 가장 정확하다
```

| | 웹 · Node | 서버 · Python | Android | iOS |
|---|---|---|---|---|
| 빌드 | `npm run build` | (없음) | `./gradlew assembleDebug` | `xcodebuild -scheme App` |
| 테스트 | `npm test` | `pytest` | `./gradlew test` | `xcodebuild test -scheme AppTests` |
| 린트 | `npm run lint` | `ruff check .` | `./gradlew ktlintCheck` | `swiftlint` |

---

## 15.2 Context — 무엇을 보게 할 것인가

### ① 이런 일이 생긴다

```text
나: 로그인 화면에 에러 메시지 좀 띄워줘

Claude: (에러 처리는 됐는데 네트워크 레이어까지 리팩터링했다)
```

그리고 다음 날 새 세션에서 **프로젝트 설명을 처음부터 또 한다.**

### ② 왜 생기나

두 가지가 없어서다.

* **프로젝트 규칙**이 사람 머릿속에만 있다 → 매번 다시 말해야 한다
* **이번 작업의 범위**를 안 줬다 → 어디까지 건드려도 되는지 모른다

앞은 `CLAUDE.md`로, 뒤는 **요청 템플릿**으로 해결한다.

### ③-A 프로젝트 규칙 — `CLAUDE.md`

**프로젝트 루트에 `CLAUDE.md`를 만든다.** 세션이 시작될 때 자동으로 읽힌다.

빈 화면부터 쓰지 말고 **Claude에게 초안을 시킨다.**

```text
이 저장소를 훑어보고 CLAUDE.md 초안을 만들어줘.

- 빌드/테스트/린트 명령은 실제 파일(README, package.json, CI 설정 등)에서
  찾아서 넣어. 못 찾은 건 "확인 필요"라고 표시해.
- 코드에서 반복적으로 보이는 규칙을 5개만 뽑아줘. 추측한 건 표시해.
- 전체 40줄 이내로.
```

초안이 나오면 **"지켜야 할 것"만 내가 직접 쓴다.**

```markdown
# 프로젝트 규칙

## 개요
- 스택: (Claude가 채워줌 — 맞는지만 확인)
- 빌드: (15.1 ③에서 확인한 명령)
- 테스트: (15.1 ③에서 확인한 명령)

## 지켜야 할 것            ← ★ 여기는 내가 직접 쓴다
- 테스트를 돌려보지 않고 "완료"라고 말하지 않는다
- src/network/ 는 읽기만 하고 수정하지 않는다

## 컨벤션
- (Claude가 뽑아준 것 중 맞는 것만 남긴다)
```

**"지켜야 할 것"에 뭘 쓸지 모르겠다면** 이 질문에 답해본다.

| 질문 | 답이 곧 규칙 |
|---|---|
| 신입이 오면 반드시 알려주는 것은? | → 컨벤션 |
| 코드 리뷰에서 세 번 이상 지적한 것은? | → 컨벤션 |
| 건드리면 다른 팀에 영향 가는 폴더는? | → 수정 금지 |
| 우리 팀이 "다 됐다"고 부르는 조건은? | → 완료 기준 |

⚠️ **길게 쓰지 않는다.** 40~60줄이면 충분하다. 길면 뒷부분이 읽히지 않는다 (5.2).

### ③-B 작업 요청 — 6칸 템플릿

**파일 2개 이상이 얽히거나 범위가 애매하면** 이 형식으로 준다. 앞의 대화에서 네트워크 레이어까지 고쳐진 이유가 이게 없어서다.

```text
목표: 로그인 실패 시 사용자에게 에러 메시지를 보여준다
현재 상태: 잘못된 비밀번호를 넣으면 아무 반응이 없다
수정 가능 범위: src/features/login/
수정 금지 범위: src/network/
유지 조건: 화면 레이아웃과 기존 성공 플로우는 그대로
검증 방법: <테스트 명령> 통과 + 잘못된 비밀번호로 직접 확인
```

**여섯 칸 중 가장 중요한 두 개**

| 칸 | 없으면 생기는 일 |
|---|---|
| **수정 금지 범위** | 시키지 않은 폴더까지 "하는 김에" 고친다 |
| **검증 방법** | 뭘 돌려봐야 끝인지 몰라서 "완료했습니다"로 끝난다 |

> 이 두 칸이 **15.4 diff 검증의 판정 기준**이 된다. 금지 범위를 안 적으면 diff를 봐도 잘못됐는지 알 수 없다.

⚠️ **오버스펙 주의** — 오타 수정, 문구 한 줄 변경까지 이 양식을 채우지 않는다. 그냥 짧게 말하는 게 빠르다.

### ③-C 세션이 길어지면

| 신호 | 조치 |
|---|---|
| 작업이 바뀐다 (로그인 → 결제) | **`/clear`** — 이전 맥락을 끌고 가지 않는다 |
| 같은 작업이 길어져 느려진다 | **`/compact`** + 남길 것을 직접 지시 |
| 엉뚱한 파일을 보기 시작한다 | 설명을 더 붙이지 말고 **범위를 좁혀 다시 요청** |
| 같은 실수를 반복한다 | 이미 오염됐다 → **`/clear` 후 짧게 다시** |

```text
/compact 변경한 파일, 실행한 명령과 결과, 확인된 원인, 남은 할 일만 남겨줘
```

### ④ 됐는지 확인

**새 세션을 열고** 묻는다.

```text
이 프로젝트 규칙을 요약해봐.
```

| 결과 | 판정 |
|---|---|
| 내가 쓴 내용이 나온다 | ✅ 됐다 |
| 엉뚱한 소리를 한다 | ❌ 파일이 프로젝트 **루트**에 있나 확인 |
| 앞부분만 안다 | ❌ 너무 길다. 줄인다 |

---

## 15.3 Permission — 무엇을 하게 할 것인가

### ① 이런 일이 생긴다

```text
나: 이 브랜치 정리 좀 해줘

Claude: (git push --force 를 실행하려고 한다)
```

안 물어보는 모드였다면 — **이미 실행됐다.**

### ② 왜 생기나

Claude는 **무엇이 되돌릴 수 없는 일인지 모른다.** `CLAUDE.md`에 "하지 마세요"라고 써도 **부탁일 뿐 강제가 아니다.**

### ③ 이렇게 한다

**먼저 우리 프로젝트에서 위험한 걸 찾는다.**

```bash
# 시크릿·인증서
find . -type f \( -name "*.env*" -o -name "*.pem" -o -name "*.key" \
  -o -name "*.keystore" -o -name "*.p12" \) -not -path "./.git/*"

# 배포·CI 설정
ls -d .github/workflows fastlane .circleci Dockerfile 2>/dev/null
```

**판단 기준 한 줄**: *잘못 건드리면 되돌리는 데 **하루 이상** 걸리나?*

**`.claude/settings.json`을 만든다.**

```json
{
  "permissions": {
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Bash(git push *)",
      "Bash(rm -rf *)"
    ]
  }
}
```

⚠️ **처음엔 `deny` 네 줄이면 충분하다.** 위에서 찾은 것 중 **가장 무서운 것**만.

**스택별로 더 넣을 것**

| 스택 | 추가 |
|---|---|
| 웹 · Node | `"Bash(npm publish *)"` |
| 서버 | `"Edit(k8s/**)"`, `"Bash(* migrate *)"` |
| Android | `"Read(**/*.keystore)"`, `"Edit(**/AndroidManifest.xml)"` |
| iOS | `"Read(**/*.p12)"`, `"Edit(**/Info.plist)"`, `"Edit(fastlane/**)"` |

**쓰다 보면 두 가지를 추가하게 된다.**

* 승인 창이 **자꾸 뜨는** 안전한 명령 → `allow` (테스트·린트·읽기)
* **"어? 이걸 하려고 했다고?"** 싶은 순간 → `deny`

```json
{
  "permissions": {
    "allow": ["Bash(npm test *)", "Bash(git diff *)", "Bash(git status)"],
    "ask":   ["Bash(git commit *)", "Bash(npm install *)"],
    "deny":  ["Read(./.env)", "Bash(git push *)", "Bash(rm -rf *)"]
  }
}
```

| 칸 | 뜻 | 여기에 넣을 것 |
|---|---|---|
| `allow` | 안 물어보고 실행 | 되돌릴 필요조차 없는 것 (읽기·테스트·린트) |
| `ask` | 실행 전에 물어봄 | 되돌릴 수는 있지만 흔적이 남는 것 (커밋·설치) |
| `deny` | **아예 실행 안 됨** | 되돌릴 수 없는 것 |

**꼭 알아야 할 두 가지**

⚠️ **평가 순서는 `deny → ask → allow`.** 먼저 매칭된 것이 이긴다. `deny`에 `Bash(git *)`가 있으면 `allow`에 `Bash(git status)`를 넣어도 **안 통한다.** 넓은 `deny`는 좁은 `allow`로 뚫을 수 없다.

⚠️ **파일 경로 규칙은 `Read`와 `Edit`만** 통한다. `Write(...)`나 `Glob(...)`으로 쓰면 **조용히 무시된다.**

### ④ 됐는지 확인

**만들었다고 되는 게 아니다.** 새 세션에서 **일부러 시켜본다.**

| 시켜볼 것 | 기대 |
|---|---|
| `.env 파일 읽어줘` | **막힌다** |
| `git push 해줘` | **막힌다** |
| `테스트 돌려줘` | 물어보지 않고 실행 (allow를 넣었다면) |

**막히지 않으면 설정이 안 먹는 것이다.**

1. 파일이 `.claude/settings.json`인가 (프로젝트 루트 아래)
2. JSON 문법 — 마지막 항목 뒤 **쉼표**가 흔한 실수
3. 세션에서 `/status` → 로드된 설정 파일이 표시된다
4. 넓은 `deny`가 `allow`를 덮고 있지 않은가

> ⚠️ **승인 창이 계속 뜨면 사람은 내용을 안 읽고 누르게 된다.** 안전한 명령을 `allow`로 올려서 **승인 창 자체를 줄이는 것**도 안전장치다.

---

## 15.4 Verification — 결과를 어떻게 확인할 것인가

### ① 이런 일이 생긴다

```text
Claude: 수정을 완료했습니다. 테스트도 통과했습니다. ✅
```

그래서 그냥 커밋했다. **다음 날 다른 화면이 깨졌다.**

### ② 왜 생기나

**"완료했습니다"는 검증이 아니다.** 시킨 것 말고 다른 파일도 고쳤을 수 있고, 테스트를 실제로 돌렸는지도 확인하지 않았다.

### ③ 이렇게 한다 — 세 단계

**1단계 · 내가 직접 돌린다**

"테스트 통과했습니다"를 그대로 믿지 않는다. **터미널에서 직접 친다.**

```bash
<테스트 명령>      # 15.1 ③에서 확인한 것
<린트 명령>
```

**2단계 · `--stat`으로 범위를 본다**

```bash
git diff --stat
```

```text
 src/features/login/LoginForm       |  12 ++++--
 src/features/login/LoginViewModel  |  34 ++++++++++---
 src/network/ApiClient              |  47 ++++++++++++-------   ← ⚠️ 어?
 3 files changed, 93 insertions(+), 22 deletions(-)
```

**여기서 이미 멈춘다.** 15.2 ③-B에서 `수정 금지 범위: src/network/`라고 적었는데 그 폴더가 바뀌었다.

> **`--stat`은 파일 목록만 보여준다. 10초면 본다.** 범위가 다르면 본문을 읽을 필요조차 없다.

```bash
git checkout .        # 되돌리고 범위를 더 좁혀 다시 요청
```

**3단계 · 본문에서 6가지를 본다**

```bash
git diff
```

| 확인 | 찾는 것 |
|---|---|
| 시키지 않은 파일 | 지정하지 않은 폴더가 바뀌었나 |
| **불필요한 리팩터링** | 시킨 것 외에 "하는 김에 정리한" 코드 |
| 공용 함수 변경 | 다른 화면도 쓰는 함수의 시그니처가 바뀌었나 |
| **응답 모델 변경** | 서버와 주고받는 데이터 구조(DTO)의 필드가 바뀌었나 |
| 키·토큰 | 코드에 하드코딩되거나 로그로 찍히나 |
| **지워진 코드 (`-` 줄)** | 조용히 사라진 예외 처리·검사 |

⚠️ **`-` 줄을 특히 본다.** 추가된 코드는 눈에 띄지만 **없어진 코드는 안 보인다.**

**마지막으로 반례를 묻는다.**

```text
지금 수정한 코드가 깨질 수 있는 입력을 3개 알려줘.
각각 현재 코드가 그 경우를 처리하는지도 같이 알려줘.
```

⚠️ **"이 diff 괜찮아?"라고 묻지 않는다.** 자기가 쓴 코드를 자기가 검토하면 통과시킨다. 제대로 된 검토는 **다른 세션**이 해야 한다 (→ 15.6).

> **테스트 통과 ≠ 검증 완료.** 테스트가 없는 영역일 수도, 커버리지가 부족할 수도, **테스트를 통과하면서 잘못된 요구사항을 구현했을 수도** 있다.

### ④ 됐는지 확인

커밋 전에 이 다섯 개에 답할 수 있으면 된다.

- [ ] 뭘 고쳤는가?
- [ ] `git diff --stat`이 내가 준 **수정 가능 범위**와 같은가?
- [ ] 테스트를 **내가** 돌려봤는가?
- [ ] 어떤 예외 상황을 확인했는가?
- [ ] 문제 생기면 어떻게 되돌리는가?

> 하나라도 막히면 아직 **"완료"가 아니라 "검증 안 된 변경"**이다.

---

## 15.5 Tool — 셋 중 하나만 고른다

| 불편 | 만들 것 |
|---|---|
| 같은 지시를 계속 반복한다 | **Skill** |
| "그 정보는 볼 수 없습니다"라고 한다 | **MCP** |
| `CLAUDE.md`에 썼는데 자꾸 어긴다 | **Hook** |

⚠️ **외부 접근이 필요 없는 반복 절차에 MCP를 붙이지 않는다.** 그건 Skill이면 된다.

### Skill — 반복하는 지시를 파일로

`.claude/skills/이름/SKILL.md`. **폴더 이름이 곧 `/이름` 명령**이 된다.

```markdown
---
description: 버그를 증상·증거·원인후보로 정리한다. 에러나 테스트 실패를 들고 올 때 사용.
---

먼저 고치지 말고 아래 순서로만 답한다.

1. 증상: 무엇이 언제 실패하는가 (한 문장)
2. 증거: 로그에서 확인 가능한 사실만
3. 원인 후보: 가능성 순 3개
4. 확인 방법: 각 후보를 확인할 명령어
```

**`description`이 제일 중요하다.** Claude가 이걸 보고 **언제 꺼낼지** 정한다.

**확인**: `/이름`으로 실행되는지, 해당 상황을 말했을 때 알아서 꺼내는지.

### MCP — 외부 시스템 연결

GitHub 이슈나 DB를 **직접 읽게** 하고 싶을 때만. 프로젝트 루트에 `.mcp.json`.

```json
{
  "mcpServers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": { "Authorization": "Bearer ${GITHUB_PAT}" }
    }
  }
}
```

⚠️ **붙였으면 권한을 좁힌다.** 도구 이름은 `mcp__서버__도구` 형식이다.

```json
{ "permissions": { "deny": ["mcp__github__delete_*"] } }
```

**확인**: 세션에서 `/mcp` → 연결 상태가 보인다.

### Hook — 진짜로 막기 / 뒷정리

`CLAUDE.md`는 부탁이고 Hook은 **강제**다.

**막을 때는 `PreToolUse`** — `.claude/settings.json`

```json
{
  "hooks": {
    "PreToolUse": [
      { "matcher": "Edit|Write",
        "hooks": [{ "type": "command",
                    "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/protect.sh" }] }
    ]
  }
}
```

`.claude/hooks/protect.sh` (만든 뒤 `chmod +x`)

```bash
#!/bin/bash
FILE=$(jq -r '.tool_input.file_path // empty')
case "$FILE" in
  *.env|*/.github/workflows/*)
    echo "보호된 파일입니다: $FILE" >&2
    exit 2 ;;          # exit 2 = 차단
  *) exit 0 ;;         # 그 외는 통과
esac
```

**뒷정리는 `PostToolUse`** — 저장할 때마다 포매터를 돌린다.

```json
{
  "hooks": {
    "PostToolUse": [
      { "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "<포매터 명령> $CLAUDE_FILE_PATHS" }] }
    ]
  }
}
```

⚠️ **막는 건 `PreToolUse`만 된다.** `PostToolUse`는 **이미 실행된 뒤**라 되돌릴 수 없다.

**확인**: 보호 파일을 일부러 고쳐보라고 시킨다. 차단되면 정상. **테스트 안 한 Hook은 없는 것과 같다.**

---

## 15.6 Sub-agent / Worktree — 섞이지 않게

### ① 이런 일이 생긴다

* 파일을 여기저기 뒤지느라 대화가 길어져서 **정작 중요한 지시가 묻힌다**
* 두 가지를 동시에 하는데 **`git diff`가 뒤섞여** 어느 작업 변경인지 모르겠다

### ② 왜 생기나

**Subagent는 대화를, Worktree는 파일을** 격리한다. 둘 다 없으면 하나의 세션·하나의 폴더에 전부 섞인다.

### ③-A 역할을 나눈다

에이전트는 **역할마다 권한이 다르다.** 이게 핵심이다.

| 역할 | 하는 일 | 줄 권한 |
|---|---|---|
| **Explorer** | 코드 탐색, 구조 파악 | 읽기 전용 |
| **Implement** | 실제 코드 수정 | 편집·쓰기 (보통 메인 세션이 한다) |
| **Verify** | 테스트 실행, 변경 리뷰 | 테스트만, 쓰기 금지 |

`.claude/agents/explorer.md`

```markdown
---
name: explorer
description: 관련 파일을 찾아 구조만 요약한다. 수정 범위를 정하기 전에 사용.
tools: Read, Grep, Glob
---

너는 탐색 전용이다. 코드를 절대 수정하지 않는다.
관련 파일과 각 파일의 역할을 찾아서 15줄 이내로 요약한다.
수정 후보와 건드리면 안 될 파일을 구분해서 알려준다.
```

`.claude/agents/verifier.md`

```markdown
---
name: verifier
description: 변경된 코드를 독립적으로 검증한다. 구현이 끝난 뒤 리뷰가 필요할 때 사용.
tools: Read, Grep, Glob, Bash
permissionMode: plan
maxTurns: 20
---

너는 이 코드를 작성하지 않았다. 작성 의도를 추측하지 말고 실제 출력만 평가한다.

1. `git diff`로 실제 변경 범위를 확인한다
2. 요청 범위를 벗어난 변경을 지적한다
3. 이 코드가 깨질 수 있는 입력을 3개 제시한다
4. 테스트를 실행하고 결과를 그대로 보고한다

"괜찮아 보인다"는 결론은 금지한다.
```

| 프론트매터 | 왜 넣나 |
|---|---|
| `description` | **이게 곧 역할 경계다.** Claude가 이걸 보고 위임을 판단한다 |
| `tools` | 탐색용에 편집 도구를 주지 않는다 |
| `permissionMode: plan` | 검증용은 **읽기·계획만.** 고치지 못하게 |
| `maxTurns` | 폭주 방지 |

⚠️ **역할이 겹치면 만들지 않는다.** 조사용·검증용처럼 **권한이 다를 때만** 나누는 의미가 있다.

**부르는 법** — 이름을 말하거나, 상황이 맞으면 알아서 불린다.

```text
explorer 로 로그인 관련 파일 먼저 찾아줘
```

**받는 것** — 뒤진 과정 전부가 아니라 **요약만** 돌아온다.

```text
관련 파일 3개
- src/features/login/LoginForm       : 화면. 에러 표시 없음
- src/features/login/LoginViewModel  : 에러 처리가 비어 있음  ← 원인 후보
- src/network/ApiClient              : 401을 그대로 던짐. 수정 금지 범위
```

> 이 요약이 그대로 **15.2 ③-B 요청 템플릿의 재료**가 된다.

**전체 흐름**

```text
explorer (읽기 전용)  →  메인 세션 (수정)  →  verifier (검증)
```

②와 ③을 **같은 세션에서 하지 않는 것**이 핵심이다. 15.4에서 *"이 diff 괜찮아? 묻지 마라"*고 한 이유가 이것이다.

### ③-B 여러 작업을 동시에 — Worktree

```bash
claude --worktree                    # 별도 폴더 + 별도 브랜치로 시작
git remote set-head origin -a        # 기준 브랜치 재동기화
# worktree 경로를 .gitignore에 추가
git worktree remove <경로>            # 작업 끝나면 개별 제거
```

* worktree 하나에 **작업 하나**
* 합칠 때는 **하나씩 순서대로** (동시에 우르르 ✕)
* 충돌하면 **바로 "고쳐줘" 하지 않는다** — 한쪽을 통째로 덮어쓸 위험이 있다

**충돌 해결은 이 순서로**

```text
1. 지금 충돌이 왜 났는지 설명해줘.
   양쪽이 같은 로직을 어떻게 다르게 고쳤는지 알려주고, 아직 수정하지는 마.

2. 두 변경을 어떻게 합칠지 계획을 먼저 제시해줘.

3. 그 계획대로 수정하고 관련 테스트를 실행해줘.
```

⚠️ **함정** — worktree를 나눠도 **오토메모리는 같은 저장소 전체가 공유한다.** 한쪽에서 학습한 게 다른 쪽 작업에 영향을 준다. **파일은 격리돼도 기억은 격리되지 않는다** (5.3).

### ④ 됐는지 확인

```bash
git worktree list                 # 브랜치가 서로 다른가
git -C <worktree경로> diff --stat   # 각자 자기 파일만 바뀌었나
```

두 worktree의 `--stat`에 **같은 파일이 겹쳐 나오면** 분리에 실패한 것이다.

---

## 15.7 Debug — 실패 원인을 어떻게 좁힐 것인가

### ① 이런 일이 생긴다

```text
나: (에러 로그 전체를 붙여넣음) 이거 고쳐줘

Claude: (로그 마지막 줄만 보고) null 체크를 추가했습니다.

나: (고쳐졌는데 원인이 아니었다. 다음 날 또 터진다)
```

### ② 왜 생기나

**로그를 통째로 주면 마지막 줄만 본다.** 정보가 부족한 게 아니라 **정리가 안 된 것**이다.

### ③ 이렇게 한다

**로그를 붙여넣기 전에 다섯 칸으로 나눈다.**

```text
[문제] 로그인에서 잘못된 비밀번호를 넣으면 아무 반응이 없다.
       에러 메시지도, 로딩 종료도 안 된다.

[환경] 스테이징에서만 발생. 로컬에서는 정상.

[증거] 로그에 401이 한 줄 찍히고 그 뒤로 아무것도 없음
       src/features/login/ 에 관련 테스트 없음

[제약] src/network/ 는 수정 금지
       ★ 먼저 고치지 말고 원인 후보부터 좁혀줘

[요청] 1. 원인 후보를 가능성 순으로 3개
       2. 각 후보를 확인할 방법
```

⚠️ **`[제약]`의 ★ 줄이 핵심이다.** 이게 없으면 **첫 번째 그럴듯한 원인을 잡고 바로 고치기 시작한다.**

**"로컬은 되는데 서버는 안 됨"이라면** 대개 코드가 아니라 설정 문제다. **값을 보여주지 말고 네 가지만** 표로 준다.

**존재 여부 · 어디서 왔는지(출처) · 어디까지 유효한지(범위) · 실행 시점에 실제로 보이는지(가시성)**

```text
| 설정값         | 로컬            | 스테이징        |
|----------------|-----------------|-----------------|
| API_BASE_URL   | 있음 (.env)     | 있음 (CI 주입)  |
| AUTH_CLIENT_ID | 있음 (.env)     | ❌ 전달 안 됨    |
```

실제 값을 안 줘도 **문제 위치가 표에서 드러난다.** 시크릿을 대화에 올리지 않아도 된다.

### ④ 됐는지 확인

원인을 찾았다고 하면 **반드시 되묻는다.**

```text
이 가설이 틀릴 수 있는 조건은 뭐야?
```

여기서 말이 막히면 **아직 원인을 못 찾은 것이다.**

---

## 15.8 Persistence / State — 무엇을 기록으로 남길 것인가

### ① 이런 일이 생긴다

이틀째 작업. 새 세션을 열었더니 **어제 정한 걸 하나도 모른다.** 이미 끝낸 걸 또 하려고 한다.

### ② 왜 생기나

**대화는 저장되지 않는다.** 진행 상황이 대화 안에만 있으면 세션이 끝날 때 같이 사라진다.

### ③ 이렇게 한다

**작업이 이틀 이상 걸릴 때만** `state.md`를 만든다.

```markdown
# 진행 상황

## 완료
- 로그인 화면 에러 처리 (PR #412)

## 진행 중
- 회원가입 화면에 같은 패턴 적용

## 다음
- 나머지 화면 5개

## 결정한 것
- 에러 문구는 화면 쪽에서 만든다. 네트워크 레이어는 그대로 던지기만.

## 반복하면 안 되는 실패
- 네트워크 레이어에서 문구를 만들려다 되돌림
```

새 세션은 **이 파일부터 읽고** 시작한다.

**계속 지킬 규칙이 나오면 `CLAUDE.md`로 옮긴다.**

```text
이번에 정한 것 중에 앞으로도 계속 지켜야 할 규칙이 있으면
CLAUDE.md에 추가할 문장으로 정리해줘.
```

내가 확인하고 커밋한다. **대화에만 있는 규칙은 사라지고, 파일에 있는 규칙은 남는다.**

### ④ 됐는지 확인

**새 세션을 열고 아무 설명 없이** 묻는다.

```text
state.md 읽고 지금 이어서 할 작업이 뭔지 알려줘.
```

다음 작업을 정확히 짚으면 성공. **되묻거나 끝낸 일을 다시 하려 하면** 파일이 부실한 것이다.

---

## 15.9 다 만들면 이렇게 된다

```text
내프로젝트/
├── CLAUDE.md                    ← 15.2
├── .mcp.json                    ← 15.5  필요할 때만
├── state.md                     ← 15.8  긴 작업일 때만
└── .claude/
    ├── settings.json            ← 15.3  (권한 + Hook)
    ├── skills/이름/SKILL.md      ← 15.5  필요할 때만
    ├── agents/explorer.md       ← 15.6  필요할 때만
    ├── agents/verifier.md       ← 15.6  필요할 때만
    └── hooks/protect.sh         ← 15.5  필요할 때만
```

`.gitignore`에 한 줄 추가한다. (개인 설정은 팀과 공유하지 않는다)

```gitignore
.claude/settings.local.json
```

> **`CLAUDE.md`와 `settings.json` 두 개만 있는 상태도 정상이다.** 나머지가 없다고 잘못된 게 아니다.

---

## 15.10 안 될 때

| 증상 | 확인할 것 |
|---|---|
| `CLAUDE.md`를 모르는 것 같다 | 프로젝트 **루트**에 있나 · 너무 길지 않나(40~60줄) |
| `deny`가 안 막는다 | 파일 위치 · JSON 쉼표 · `/status` 로드 확인 · **넓은 `deny`가 `allow`를 덮고 있나** |
| 경로 규칙이 안 먹는다 | `Write(...)`·`Glob(...)`은 무시된다. **`Read`/`Edit`으로** |
| 자꾸 엉뚱한 파일을 본다 | 설명을 **더** 하지 말고 **수정 가능 범위를 좁혀** 다시 요청 (15.2 ③-B) |
| 시키지 않은 폴더가 바뀐다 | 요청에 **수정 금지 범위**를 안 적었다 |
| 같은 실수를 반복한다 | 대화가 오염됐다 → `/clear` 후 짧게 다시 |
| 대화가 길어져 느려진다 | `/compact` + 남길 것 지시 |
| Skill을 안 꺼낸다 | `description`이 모호하다. "언제 쓰는지"를 더 구체적으로 |
| 뭘 했는지 모르겠다 | `git diff --stat` (15.4) |
| 다 꼬였다 | `git checkout .` 로 되돌리고 처음부터 |

---

## 15.11 지금까지 만든 게 뭐였나

7개를 따로 외울 필요는 없다. **위 순서대로 했다면 이미 만든 것이다.**

| 하네스 (1.2의 7축) | 내가 만든 것 | 절 |
|---|---|---|
| **Context** | `CLAUDE.md` · 6칸 요청 템플릿 | 15.2 |
| **Permission** | `.claude/settings.json` | 15.3 |
| **Verification** | 직접 테스트 · `git diff --stat` · 승인 전 5문답 | 15.4 |
| **Tool** | Skill · MCP · Hook | 15.5 |
| **Sub-agent / Worktree** | `explorer.md` · `verifier.md` · `claude --worktree` | 15.6 |
| **Debug** | 5칸 프롬프트 | 15.7 |
| **Persistence / State** | `state.md` · `CLAUDE.md`로 승격 | 15.8 |

> 좋은 하네스는 **처음부터 완성된 설정**이 아니라, **사고가 날 뻔할 때마다 한 줄씩 늘어난 설정**이다.
