# Part 7. 하네스 적용하기

## 1. Context Harness 필요한 정보만 정확하게 — 무엇을 볼 것인가

### 1-1. CLAUDE.md

CLAUDE.md는 Claude Code가 세션마다 자동으로 읽는 컨텍스트 주입 파일이다. 규칙을 강제하는 장치가 아니라, **모델이 우선적으로 볼 정보를 정의**하는 것이 본질이다.    
회사로 치자면 개발 가이드 + 신입사원 온보딩 문서같은거다.    

**핵심 원칙**

1. 문서가 아니라 계약이다 — 행동을 바꾸지 않는 문장은 삭제한다.
2. 짧을수록 잘 지켜진다 — 200줄이 넘어가면 준수도가 떨어진다. 코드에서 유추 가능한 내용(디렉토리 구조, 의존성 목록)은 빼고, 함정·근거·컨벤션만 남긴다.
3. 강제가 아니다 — 정말 막아야 할 행동은 CLAUDE.md가 아니라 훅(hook)으로 처리한다.

**예시**

```markdown
# CLAUDE.md

## 아키텍처
MVVM + Combine. View는 상태를 갖지 않는다. ViewModel이 단일 진실 소스.

## 규칙
- 강제 언래핑(!) 금지, guard let / if let만 사용
- 네트워킹은 반드시 Repository 계층을 거친다 (ViewModel에서 직접 URLSession 호출 금지)
- UI 문자열은 하드코딩 금지 → Localizable.strings 사용
```

### 1-2. Rules
 
`.claude/rules/*.md`는 CLAUDE.md의 확장판이다. CLAUDE.md가 "항상 켜져 있는 지식"이라면, Rules는 **특정 경로의 파일을 건드릴 때만 조건부로 로드되는 지식**이다.
회사로 치자면 전 직원 공통 사규(CLAUDE.md)와 부서별 업무 매뉴얼(Rules)의 관계다.
 
**핵심 원칙**
 
1. 상시 로드가 아니라 조건부 로드다 — 파일 상단 frontmatter의 glob 패턴이 매칭되는 파일을 다룰 때만 컨텍스트에 주입된다. 매칭되지 않으면 아예 로드되지 않아 토큰을 쓰지 않는다.
2. frontmatter가 없으면 전역 규칙이 된다 — glob을 지정하지 않은 rule 파일은 CLAUDE.md처럼 항상 로드된다.
3. CLAUDE.md보다 더 짧아야 한다 — 커뮤니티 실무 가이드는 rules 파일 한 개당 100줄 이하를 권장한다. 조건부로 켜지는 파일인데도 길면, 매칭되는 순간 컨텍스트를 그대로 잠식한다.

**예시**
```yaml
# .claude/rules/networking.md
---
globs: "Sources/Networking/**/*.swift", "Sources/Repository/**/*.swift"
---
# Networking Rules
- 모든 API 응답은 Decodable로 파싱, 실패 시 커스텀 NetworkError로 매핑한다
- 토큰 재발급 로직은 반드시 AuthInterceptor를 거친다 (Repository에서 직접 refresh 호출 금지)
- 응답 매핑 실패를 무시하고 빈 배열/nil로 흘려보내지 않는다 (실패는 반드시 상위로 전파)
```
 
```yaml
# .claude/rules/swiftui-view.md
---
globs: "Sources/Views/**/*.swift"
---
# View Rules
- View 안에서 @State로 비즈니스 로직 상태를 두지 않는다 (ViewModel로 이동)
- List/ForEach의 id는 옵셔널 프로퍼티 대신 반드시 고유 식별자를 사용한다
- .onAppear에서 네트워크 호출 직접 트리거 금지 → ViewModel.load() 경유
```

## 2. Permission	위험한 행동·접근 범위 제한 — 무엇을 해도 되는가

## 3. Verification (Certification)	테스트·diff로 결과 검증

## 4. Tool	어떤 도구를 어떻게 쓸지 설계

## 5. Sub-agent / Worktree	작업 분리와 병렬화

## 6. Debug	실패 원인을 추적 가능하게 설계

## 7. Persistence / State	Session과 기록을 남김

