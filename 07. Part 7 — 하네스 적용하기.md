# Part 7. 하네스 적용하기

---

## 7-1. CLAUDE.md

CLAUDE.md는 Claude Code가 세션마다 자동으로 읽는 **프로젝트 컨텍스트와 행동 지침을 정의하는 파일**이다. 규칙을 강제로 실행하는 장치가 아니라, 모델이 프로젝트를 이해하고 올바른 판단을 내릴 수 있도록 필요한 정보를 제공하는 것이 본질이다.

회사로 치자면 **개발 가이드 + 신입사원 온보딩 문서** 같은 거다.

Claude Code 공식 문서에서도 CLAUDE.md는 프로젝트 구조, 코딩 컨벤션, 빌드 명령어처럼 매 세션 알아야 하는 정보를 담는 곳으로 설명한다. 반대로 특정 파일에만 필요한 절차나 지식은 Rules나 Skills로 분리하는 것이 좋다.

**핵심 원칙**

1. 문서가 아니라 계약이다 — 모델의 행동에 영향을 주지 않는 문장은 삭제한다.
2. 짧을수록 좋다 — Claude Code 공식 문서도 CLAUDE.md를 200줄 이하로 유지할 것을 권장한다.
3. 코드에서 쉽게 알 수 있는 내용은 넣지 않는다 — 디렉토리 구조나 단순한 의존성 목록을 장황하게 설명할 필요가 없다.
4. 강제가 아니다 — 정말 막아야 하는 행동은 CLAUDE.md가 아니라 Permissions나 Hook으로 처리한다.
5. 반복해서 같은 실수를 한다면 그 내용을 CLAUDE.md에 추가한다.

**예시**

```text
# CLAUDE.md

## 아키텍처
MVVM + Combine.
View는 비즈니스 로직을 갖지 않는다.
ViewModel이 화면 상태의 단일 진실 공급원이다.

## 규칙
- 강제 언래핑(!) 금지
- 네트워킹은 반드시 Repository 계층을 거친다
- ViewModel에서 URLSession 직접 호출 금지
- UI 문자열 하드코딩 금지 → Localizable.strings 사용

## 검증
변경 후 다음 명령어를 실행한다.

- swift test
- swiftlint
- xcodebuild test
```

CLAUDE.md에 모든 것을 집어넣는 것이 좋은 하네스는 아니다.

**항상 필요한 지식 → CLAUDE.md**

**특정 경로에서만 필요한 규칙 → Rules**

**특정 작업을 수행하는 절차 → Skills**

**반드시 실행되어야 하는 검증/차단 → Hooks**

이렇게 역할을 분리해야 컨텍스트가 불필요하게 커지지 않는다.

---

## 7-2. Rules

`.claude/rules/*.md`는 CLAUDE.md의 확장판이다.

CLAUDE.md가 **항상 필요한 프로젝트 공통 지식**이라면, Rules는 **특정 파일이나 디렉토리를 다룰 때 필요한 조건부 규칙**이다.

회사로 치자면 전 직원 공통 사규(CLAUDE.md)와 **부서별 업무 매뉴얼(Rules)​**의 관계다.

Claude Code는 `paths` frontmatter를 이용해 특정 파일에만 적용되는 규칙을 만들 수 있다. 따라서 프로젝트 전체 컨텍스트를 불필요하게 차지하지 않으면서 특정 영역의 규칙을 적용할 수 있다.

**핵심 원칙**

1. 특정 파일에만 필요한 규칙은 Rules로 분리한다.
2. `paths`를 사용해 적용 범위를 좁힌다.
3. 전역적으로 항상 필요한 규칙이라면 CLAUDE.md에 두는 것을 먼저 고려한다.
4. Rule 하나에는 하나의 관심사만 넣는다.
5. 코드에서 자동으로 검사할 수 있는 규칙이라면 나중에 Hook이나 테스트로 옮기는 것을 고려한다.

**예시**

```text
.claude/
└── rules/
    ├── networking.md
    ├── swiftui-view.md
    ├── database.md
    └── testing.md
```

```text
# .claude/rules/networking.md

---
paths:
  - "Sources/Networking/**/*.swift"
  - "Sources/Repository/**/*.swift"
---

# Networking Rules

- 모든 API 응답은 Decodable로 파싱한다
- 네트워크 에러는 NetworkError로 변환한다
- 토큰 재발급은 AuthInterceptor를 통해서만 수행한다
- 응답 매핑 실패를 nil이나 빈 배열로 무시하지 않는다
```

```text
# .claude/rules/swiftui-view.md

---
paths:
  - "Sources/Views/**/*.swift"
---

# View Rules

- View 안에 비즈니스 로직을 작성하지 않는다
- ViewModel을 통해 상태를 관리한다
- .onAppear에서 네트워크 API를 직접 호출하지 않는다
- List/ForEach의 id는 고유 식별자를 사용한다
```

이렇게 하면 `Sources/Views/`를 수정할 때는 View 규칙이, `Sources/Networking/`을 수정할 때는 Networking 규칙이 적용된다.

**필요한 순간에 필요한 지식만 가져오는 것**이 Rules의 핵심이다.

---

## 7-3. Skills

Skills(`SKILL.md`)는 CLAUDE.md·Rules와 달리 **특정 작업을 수행하기 위한 절차와 도구를 패키징한 것**이다.

CLAUDE.md가 "무엇을 지켜야 하는가"라면,

Rules는 "특정 영역에서 무엇을 지켜야 하는가",

Skills는 **"이 작업을 어떻게 수행하는가"​**다.

Claude Code 공식 문서에서도 Skill은 재사용 가능한 지식이나 workflow를 담는 기능으로 설명하며, 필요할 때 `/skill-name`으로 직접 실행하거나 Claude가 작업에 맞는 Skill을 선택하도록 할 수 있다.

**핵심 원칙**

1. 설명서가 아니라 실행 절차다.
2. 반복되는 특정 작업을 Skill로 만든다.
3. 여러 프로젝트에서 반복해서 사용하는 절차도 Skill로 만든다.
4. 부작용이 있는 작업은 자동 호출을 제한하고 명시적으로 호출하게 만들 수 있다.
5. Skill이 너무 커지면 references나 scripts로 분리한다.

**예시**

```text
.claude/
└── skills/
    └── viewmodel-test/
        ├── SKILL.md
        ├── references/
        │   └── test_template.swift
        └── scripts/
            └── create_test.sh
```

```text
---
name: viewmodel-test
description: ViewModel의 유닛 테스트를 작성할 때 사용하는 Skill
---

# ViewModel 테스트 작성

1. 대상 ViewModel을 읽는다.
2. @Published 프로퍼티를 확인한다.
3. public 메서드를 확인한다.
4. Repository와 UseCase 의존성을 확인한다.
5. 실제 네트워크 대신 Mock/Stub을 사용한다.
6. Given-When-Then 구조로 테스트를 작성한다.
7. 다음 케이스를 반드시 포함한다.
   - 정상 응답
   - 에러 응답
   - 빈 응답
   - 로딩 상태 변화
8. 테스트를 실행한다.
9. 실패하면 원인을 분석하고 수정한다.
```

사용자는:

```text
/viewmodel-test UserViewModel
```

처럼 직접 호출할 수도 있고, Skill의 설명이 작업과 잘 맞으면 Claude가 자동으로 선택할 수도 있다.

---

## 7-4. MCP

MCP(Model Context Protocol)는 Claude Code가 **외부 시스템과 상호작용할 수 있도록 연결하는 표준 인터페이스**다.

Claude Code 자체가 프로젝트 파일과 터미널을 다룰 수 있다면, MCP를 통해 GitHub, 데이터베이스, Jira, Slack 등의 외부 시스템까지 연결할 수 있다.

회사로 치자면 **사내 시스템에 접근할 수 있는 업무용 API**라고 생각하면 쉽다.

**핵심 원칙**

1. Agent에게 필요한 도구만 제공한다.
2. 읽기와 쓰기 권한을 구분한다.
3. 민감한 시스템은 최소 권한으로 연결한다.
4. 사용하지 않는 MCP 서버는 연결하지 않는다.
5. MCP가 제공하는 기능을 CLAUDE.md에 장황하게 설명하기보다 도구 자체가 명확한 인터페이스를 갖도록 한다.

**예시**

```text
Claude Code
    │
    ├── GitHub MCP
    │     ├── PR 조회
    │     └── Issue 조회
    │
    ├── Jira MCP
    │     ├── 티켓 조회
    │     └── 상태 변경
    │
    └── Database MCP
          └── 읽기 전용 조회
```

예를 들어 요구사항이 Jira에 있다면 Claude가 직접 Jira를 검색하고 작업 내용을 가져올 수 있다.

하지만 **MCP를 많이 연결하는 것이 좋은 하네스는 아니다.**

Agent가 사용할 필요가 없는 도구는 오히려 판단해야 할 공간과 권한만 늘린다.

---

## 7-5. Subagents

Subagent는 **특정 역할을 맡은 독립적인 작업자**다.

일반적인 Claude Code 세션이 모든 일을 처리하는 대신, 특정 작업을 별도의 context에서 처리하도록 위임할 수 있다.

회사로 치면:

```text
팀장
 ├── 설계 담당
 ├── 개발 담당
 ├── 테스트 담당
 └── 보안 담당
```

과 비슷하다.

Claude Code의 Subagent는 별도의 context window에서 실행되며, 각각 다른 prompt, tool, permission, model, skill 등을 지정할 수 있다.

**핵심 원칙**

1. 같은 종류의 작업을 반복해서 맡긴다면 Subagent로 만든다.
2. 많은 파일을 탐색해야 하는 작업은 context isolation을 위해 Subagent로 분리한다.
3. 역할에 필요한 Tool만 제공한다.
4. 읽기 전용 Agent와 수정 가능한 Agent를 구분한다.
5. 모든 작업을 Subagent로 쪼개지는 않는다.

**예시**

```text
.claude/
└── agents/
    ├── architect.md
    ├── implementer.md
    ├── tester.md
    ├── reviewer.md
    └── security-reviewer.md
```

### Architect

```text
---
name: architect
description: 새로운 기능의 구조와 의존성을 분석하고 설계를 작성하는 Agent
tools:
  - Read
  - Grep
  - Glob
---

# Architecture Agent

코드를 직접 수정하지 않는다.

1. 요구사항을 분석한다.
2. 기존 Architecture를 확인한다.
3. 영향을 받는 모듈을 찾는다.
4. 의존성 방향을 확인한다.
5. 구현 계획을 작성한다.
```

### Tester

```text
---
name: tester
description: 구현된 기능의 테스트를 작성하고 실행하는 Agent
tools:
  - Read
  - Grep
  - Glob
  - Bash
---

# Test Agent

1. 변경된 코드를 분석한다.
2. 기존 테스트 패턴을 확인한다.
3. 필요한 테스트를 작성한다.
4. 테스트를 실행한다.
5. 실패 원인을 정리한다.
```

### Reviewer

```text
---
name: reviewer
description: 구현된 코드의 품질과 아키텍처를 검토하는 Agent
tools:
  - Read
  - Grep
  - Glob
---

# Code Review Agent

코드를 직접 수정하지 않는다.

다음 순서로 검토한다.

1. 요구사항 충족 여부
2. Architecture 위반 여부
3. 중복 코드
4. 에러 처리
5. 테스트 부족
6. 보안 문제

발견한 문제를 심각도 순으로 정리한다.
```

Subagent의 핵심은 **역할 분리보다 Context와 권한의 분리**다.

예를 들어 Reviewer에게 `Edit` 권한을 주지 않으면 리뷰어는 코드를 직접 수정할 수 없다. Claude Code는 Subagent마다 사용할 Tool과 Permission을 별도로 제한할 수 있다.

---

## 7-6. Permissions

Permissions는 Claude가 **무엇을 할 수 있는지**를 제한하는 장치다.

CLAUDE.md가 "이렇게 해라"라고 알려주는 것이라면,

Permissions는 **"이건 할 수 있다 / 이건 할 수 없다"​**를 시스템 수준에서 제한한다.

회사로 치면:

```text
개발자
 ├── 개발 서버 접근 가능
 ├── 테스트 DB 접근 가능
 └── 운영 DB 접근 불가
```

와 같다.

**핵심 원칙**

1. 최소 권한 원칙을 적용한다.
2. 위험한 명령어는 deny한다.
3. Agent별로 필요한 권한만 제공한다.
4. 운영 환경과 개발 환경의 권한을 분리한다.
5. "하지 마라"가 아니라 시스템적으로 실행할 수 없도록 만든다.

**예시**

```text
허용
- Read
- Edit
- Git status
- 테스트 실행

차단
- Production DB 변경
- .env 수정
- SSH 접속
- 위험한 시스템 명령
```

Claude Code에서는 Permission 규칙으로 특정 Tool이나 Subagent 사용을 허용하거나 차단할 수 있다.

---

## 7-7. Hooks

Hooks는 특정 이벤트가 발생했을 때 **항상 실행되어야 하는 자동화**다.

이 부분이 중요하다.

CLAUDE.md:

```text
.env 파일을 수정하지 마라.
```

는 **모델에게 부탁하는 것**이다.

Hook:

```text
.env 수정 시 → 실행 차단
```

은 **시스템이 강제하는 것**이다.

회사로 치면 사내 규정을 읽게 하는 것과 출입통제 시스템을 설치하는 것의 차이다.

Claude Code 공식 문서에서도 Hook은 특정 lifecycle event에 따라 실행되는 자동화로 설명하며, 반드시 동일하게 실행되어야 하는 검증이나 차단에는 Hook을 사용하는 것이 적합하다고 설명한다.

**핵심 원칙**

1. 반드시 실행되어야 하는 검증은 Hook으로 만든다.
2. 위험한 Tool 사용은 PreToolUse에서 차단한다.
3. 코드 변경 후 자동 검증은 PostToolUse에서 실행한다.
4. 로깅이나 알림도 Hook으로 처리할 수 있다.
5. Claude의 판단이 필요 없는 것은 Hook으로 옮긴다.

**예시**

```text
Claude
  │
  │ 파일 수정
  ↓
PostToolUse Hook
  │
  ├── SwiftLint
  ├── Type Check
  └── Unit Test
        │
        ↓
      결과
        │
        ↓
     Claude가 확인
```

또 다른 예:

```text
Claude
  │
  │ Bash("rm -rf ...")
  ↓
PreToolUse Hook
  │
  ├── 안전 → 실행
  │
  └── 위험 → BLOCK
```

**Instruction은 행동을 유도하고, Hook은 행동을 강제한다.**

이 차이를 이해하는 것이 하네스에서 매우 중요하다.

---

## 7-8. Tests

Tests는 Agent가 작성한 코드가 **실제로 올바른지를 검증하는 장치**다.

Agent가:

```text
"기능 구현 완료했습니다."
```

라고 말하는 것은 성공 조건이 아니다.

성공 조건은:

```text
테스트 통과
+
타입 검사 통과
+
Lint 통과
+
Architecture 검사 통과
```

처럼 **기계적으로 확인할 수 있어야 한다.**

**핵심 원칙**

1. 모든 중요한 기능에는 자동화된 테스트를 만든다.
2. Agent의 설명보다 실행 결과를 신뢰한다.
3. Unit → Integration → E2E 순으로 필요한 수준까지 검증한다.
4. Architecture도 테스트 대상으로 만든다.
5. 반복되는 실수는 테스트로 고정한다.

**예시**

```text
변경
 ↓
Unit Test
 ↓
Integration Test
 ↓
Lint
 ↓
Type Check
 ↓
Architecture Test
 ↓
PASS
```

예를 들어:

```text
ViewModel
   ↓
Repository Mock
   ↓
Unit Test
```

와 같이 외부 시스템에 의존하지 않는 테스트를 만들 수 있다.

그리고 Architecture Test:

```text
View
 ↓
ViewModel
 ↓
Repository
 ↓
Network
```

라는 규칙을 검사해:

```text
View → Network
```

같은 잘못된 의존성을 자동으로 잡아낼 수 있다.

---

## 7-9. Evals

Tests가 **코드의 정답을 검증**한다면, Evals는 **Agent가 작업을 제대로 수행했는지를 검증**한다.

이 둘은 비슷해 보이지만 다르다.

```text
Test
→ 코드가 올바른가?

Eval
→ Agent가 올바른 작업을 수행했는가?
```

Anthropic은 Agent 평가에서 최종 답변만 보는 것이 아니라 **Agent가 수행한 trajectory와 최종 환경 상태(outcome)​**까지 평가해야 한다고 설명한다. 또한 evaluation harness는 작업을 실행하고, 과정을 기록하고, 결과를 평가하는 infrastructure라고 설명한다.

**예시**

```text
Task:
"로그인 실패 시 에러 메시지를 표시하도록 수정하라."

Agent
 ↓
파일 탐색
 ↓
ViewModel 수정
 ↓
Test 작성
 ↓
Test 실행
 ↓
완료
```

Eval은 다음을 검사할 수 있다.

```text
✓ 요구사항을 구현했는가?
✓ 올바른 파일을 수정했는가?
✓ 테스트를 작성했는가?
✓ 테스트를 실행했는가?
✓ 기존 Architecture를 위반하지 않았는가?
✓ 불필요한 파일을 수정하지 않았는가?
```

즉 **코드 결과뿐 아니라 Agent의 작업 과정과 행동까지 평가하는 것**이다.

---

## 7-10. Verification

Tests와 Evals를 하나의 **Verification Layer**로 묶는다.

Agent에게 중요한 것은:

```text
"완료했다고 말했는가?"
```

가 아니라

```text
"완료 조건을 통과했는가?"
```

다.

**예시**

```text
Implementation
      ↓
┌─────────────────────┐
│    Verification     │
├─────────────────────┤
│ Unit Test            │
│ Integration Test     │
│ Type Check            │
│ Lint                  │
│ Architecture Check   │
│ Security Check        │
│ Agent Eval            │
└──────────┬──────────┘
           ↓
       PASS / FAIL
```

여기서 하나라도 실패하면 Agent가 다시 수정하도록 만들 수 있다.

이렇게 되면:

```text
구현 → 검증 → 실패 → 수정 → 검증
```

이라는 **feedback loop**가 만들어진다.

OpenAI의 Harness Engineering 사례에서도 테스트, 검증, 리뷰, 피드백, 복구를 시스템에 지속적으로 넣으면서 Agent가 더 많은 개발 과정을 자율적으로 수행할 수 있도록 발전시켰다.

---

## 7-11. Orchestration

여기서부터가 **하네스의 다음 단계**다.

앞에서 만든 요소들을 연결해서 **어떤 작업을 어떤 순서로 수행할지 관리하는 것**이 Orchestration이다.

회사로 치면 팀장이:

```text
요구사항 분석
 ↓
설계
 ↓
개발
 ↓
테스트
 ↓
리뷰
 ↓
배포
```

를 조율하는 것과 비슷하다.

**핵심 원칙**

1. 작업을 단계별로 나눈다.
2. 각 단계에 적절한 Agent를 배치한다.
3. 각 단계의 입력과 출력을 정의한다.
4. 검증 결과에 따라 다음 단계를 결정한다.
5. 실패하면 이전 단계로 돌아갈 수 있어야 한다.
6. 성공 조건을 만족했을 때만 작업을 종료한다.

**예시**

```text
User
 ↓
Orchestrator
 ↓
Architect
 ↓
Implementer
 ↓
Tester
 ↓
Reviewer
 ↓
Security Reviewer
 ↓
Verification
 ↓
PASS?
 ├─ NO → Implementer
 └─ YES → 완료
```

여기서 중요한 것은 **Orchestration과 Subagent가 같은 개념이 아니라는 것**이다.

Subagent:

```text
"누가 일을 하는가?"
```

Orchestration:

```text
"누가 언제 무엇을 하는가?"
```

이다.

---

## 7-12. State

Orchestration을 제대로 만들려면 **현재 작업 상태(State)​**가 필요하다.

단순히:

```text
Architect
 ↓
Developer
 ↓
Tester
```

라고만 하면 고정된 Pipeline이다.

하지만 실제 개발에서는 테스트가 실패하면 다시 Developer에게 돌아가야 한다.

따라서 작업 상태를 기록한다.

**예시**

```text
.task/
└── feature-123/
    ├── state.json
    ├── requirements.md
    ├── design.md
    ├── test-report.md
    └── review.md
```

```json
{
  "task": "feature-123",
  "status": "TEST_FAILED",
  "current_stage": "implementation",
  "attempt": 2,
  "completed": [
    "requirements",
    "architecture"
  ]
}
```

그러면:

```text
TEST_FAILED
      ↓
원인 분석
      ↓
Implementation
      ↓
Test
      ↓
PASS
      ↓
Review
```

처럼 workflow를 다시 진행할 수 있다.

**State가 있기 때문에 Agent가 장시간 작업을 수행하더라도 현재 어디까지 진행되었는지를 알 수 있다.**

---

## 7-13. Feedback Loop

하네스에서 가장 중요한 구조 중 하나다.

Agent가 실패했을 때 사람이 직접 코드를 고치는 것이 아니라,

**"왜 Agent가 실패했는가?"**

를 찾는다.

```text
Agent
 ↓
작업
 ↓
Verification
 ↓
FAIL
 ↓
원인 분석
 ↓
┌──────────────────────┐
│ 무엇이 부족했는가?   │
├──────────────────────┤
│ Context?             │
│ Rule?                │
│ Skill?               │
│ Tool?                │
│ Permission?          │
│ Test?                │
│ Eval?                │
└──────────┬───────────┘
           ↓
       Harness 개선
           ↓
       다시 실행
```

예를 들어 Claude가 계속 Repository를 거치지 않고 API를 직접 호출한다면:

```text
나쁜 해결책
→ CLAUDE.md에 같은 문장을 한 번 더 적는다.

좋은 해결책
→ Architecture Rule 추가
→ Architecture Test 추가
→ 필요하면 Hook 추가
```

OpenAI의 Harness Engineering 사례도 Agent의 실패를 단순히 개별 코드 문제로 보지 않고 **도구·가드레일·문서 등 Harness 자체에 무엇이 부족했는지 찾아 다시 시스템에 반영하는 방식**을 사용한다.

---

## 7-14. 전체 구조

지금까지 만든 요소를 모두 합치면 다음 구조가 된다.

```text
                         USER
                           │
                           ▼
                    ┌─────────────┐
                    │ Orchestrator│
                    └──────┬──────┘
                           │
                           ▼
                     Task State
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
          Architect    Implementer    Tester
              │            │            │
              └────────────┼────────────┘
                           ▼
                        Reviewer
                           │
                           ▼
                     Verification
                           │
                    ┌──────┴──────┐
                    │             │
                  FAIL           PASS
                    │             │
                    ▼             ▼
               Orchestrator     DONE
                    │
                    └──────→ Retry
```

그리고 이 Agent들을 둘러싸고:

```text
              ┌─────────────────────────┐
              │        CLAUDE.md        │
              │         Rules           │
              │         Skills          │
              │          MCP            │
              │     Permissions         │
              │         Hooks            │
              │      Tests / Evals       │
              └─────────────────────────┘
```

가 전체 Harness를 구성한다.

결국 구조는 다음과 같다.

```text
Context
   ↓
Rules
   ↓
Skills
   ↓
Tools / MCP
   ↓
Subagents
   ↓
Permissions / Hooks
   ↓
Tests / Evals
   ↓
Verification
   ↓
Orchestration
   ↓
State
   ↓
Feedback Loop
   ↓
Harness 개선
```

**하네스의 목적은 Claude에게 일을 잘하라고 부탁하는 것이 아니다.**

Claude가 실수하더라도:

```text
실수
 ↓
탐지
 ↓
차단 또는 검증 실패
 ↓
원인 파악
 ↓
수정
 ↓
재검증
```

할 수 있는 **환경 자체를 만드는 것**이다.

이것이 Claude Code를 단순한 AI 코딩 도구가 아니라 **Agent가 안정적으로 소프트웨어를 개발할 수 있는 환경으로 만드는 과정**이다.
