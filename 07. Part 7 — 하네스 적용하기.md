# Part 7. 하네스 적용하기

> Part 1~6이 **"왜 하네스가 필요한가"**였다면, Part 7은 **"그래서 어떤 파일을 어디에 만드는가"**다.
> 각 구성요소가 무엇이고 어떻게 만드는지만 훑는다. 세부 문법은 필요할 때 공식 문서를 찾아보면 된다.

## 무엇이 어느 축에 붙는가

| 하네스 축 | 붙이는 것 | 섹션 |
|---|---|---|
| **Context** | CLAUDE.md · Rules · Skills | 7-1 · 7-2 · 7-5 |
| **Permission** | settings.json `permissions` · Hooks | 7-3 · 7-4 |
| **Verification** | Stop 훅 · verifier 에이전트 | 7-11 |
| **Tool** | Skills · MCP · Slash Commands | 7-5 · 7-7 · 7-8 |
| **Sub-agent / Worktree** | `.claude/agents/` · `git worktree` | 7-6 · 7-10 |
| **Debug** | 세션 로그 | 7-9 |
| **State / Persistence** | `/clear` · `/compact` · CLAUDE.md 승격 | 7-12 |

Hooks는 여러 축에 걸쳐 쓰인다. 위험한 행동을 막으면 Permission 축이고(7-4), 테스트를 강제하면 Verification 축이다(7-11).

**전체 구조**

```text
my-project/
├── CLAUDE.md                   # 7-1
├── .mcp.json                   # 7-7
└── .claude/
    ├── settings.json           # 7-3 권한 · 7-4 훅 · 7-9 로그
    ├── settings.local.json     # 개인용, git ignore
    ├── rules/                  # 7-2
    ├── skills/                 # 7-5
    ├── agents/                 # 7-6
    ├── commands/               # 7-8
    └── hooks/                  # 7-4
```

---

## 7-1. CLAUDE.md

CLAUDE.md는 Claude Code가 세션마다 자동으로 읽는 **프로젝트 컨텍스트와 행동 지침을 정의하는 파일**이다. 규칙을 강제로 실행하는 장치가 아니라, 모델이 프로젝트를 이해하고 올바른 판단을 내릴 수 있도록 필요한 정보를 제공하는 것이 본질이다.

회사로 치자면 **개발 가이드 + 신입사원 온보딩 문서** 같은 거다.

**핵심 원칙**

1. 문서가 아니라 계약이다 — 모델의 행동에 영향을 주지 않는 문장은 삭제한다.
2. 짧을수록 좋다 — 200줄 이하로 유지한다.
3. 코드에서 쉽게 알 수 있는 내용은 넣지 않는다.
4. 강제가 아니다 — 정말 막아야 하는 행동은 Permissions나 Hook으로 처리한다.
5. 반복해서 같은 실수를 한다면 그 내용을 CLAUDE.md에 추가한다.

**예시**

```text
# CLAUDE.md

## 아키텍처
MVVM + Combine.
View는 비즈니스 로직을 갖지 않는다.
ViewModel이 화면 상태의 단일 진실 공급원이다.

## 규칙
- 강제 언래핑(!) 금지
- 네트워킹은 반드시 Repository 계층을 거친다
- ViewModel에서 URLSession 직접 호출 금지
- UI 문자열 하드코딩 금지 → Localizable.strings 사용

## 검증
변경 후 다음 명령어를 실행한다.

- swift test
- swiftlint
- xcodebuild test
```

**어디에 두는가**

| 위치 | 범위 |
|---|---|
| `~/.claude/CLAUDE.md` | 나의 모든 프로젝트 (개인 취향) |
| `CLAUDE.md` (레포 루트) | 팀 공통 — git 커밋 |
| `CLAUDE.local.md` | 나만의 실험 설정 — git ignore |

길어지면 `@docs/architecture.md`처럼 다른 파일을 불러올 수 있다.

CLAUDE.md에 모든 것을 집어넣는 것이 좋은 하네스는 아니다.

**항상 필요한 지식 → CLAUDE.md**

**특정 경로에서만 필요한 규칙 → Rules**

**특정 작업을 수행하는 절차 → Skills**

**반드시 실행되어야 하는 검증/차단 → Hooks**

---

## 7-2. Rules

`.claude/rules/*.md`는 CLAUDE.md의 확장판이다.

CLAUDE.md가 **항상 필요한 프로젝트 공통 지식**이라면, Rules는 **특정 파일이나 디렉토리를 다룰 때 필요한 조건부 규칙**이다. `paths` frontmatter로 적용 범위를 지정한다.

**핵심 원칙**

1. 특정 파일에만 필요한 규칙은 Rules로 분리한다.
2. `paths`를 사용해 적용 범위를 좁힌다.
3. 전역적으로 항상 필요한 규칙이라면 CLAUDE.md에 두는 것을 먼저 고려한다.
4. Rule 하나에는 하나의 관심사만 넣는다.
5. 코드에서 자동으로 검사할 수 있는 규칙이라면 Hook이나 테스트로 옮기는 것을 고려한다.

**예시**

```text
.claude/
└── rules/
    ├── networking.md
    ├── swiftui-view.md
    ├── database.md
    └── testing.md
```

`.claude/rules/networking.md`

```text
---
paths:
  - "Sources/Networking/**/*.swift"
  - "Sources/Repository/**/*.swift"
---

# Networking Rules

- 모든 API 응답은 Decodable로 파싱한다
- 네트워크 에러는 NetworkError로 변환한다
- 토큰 재발급은 AuthInterceptor를 통해서만 수행한다
- 응답 매핑 실패를 nil이나 빈 배열로 무시하지 않는다
```

이렇게 하면 `Sources/Networking/`을 수정할 때만 이 규칙이 적용된다.

**필요한 순간에 필요한 지식만 가져오는 것**이 Rules의 핵심이다.

---

## 7-3. Permissions (settings.json)

`settings.json`은 **어떤 도구를 자유롭게 실행할 수 있고, 어떤 도구는 물어봐야 하고, 어떤 도구는 절대 실행하면 안 되는지**를 정의하는 파일이다.

CLAUDE.md·Rules·Skills가 **권고**라면, Permissions는 모델이 무시할 수 없는 **강제 규칙**이다. 설정 자체가 실행 여부를 결정하기 때문에 모델의 판단과 무관하게 동작한다.

**핵심 원칙**

1. 기본값은 보수적으로 — 위험한 작업은 opt-in, 안전한 작업만 opt-out.
2. `allow`는 반복 승인이 귀찮은 안전한 명령어 위주로 (`git status`, `npm test`).
3. `ask`는 막을 정도는 아니지만 눈으로 보고 싶은 것 (`git commit`, `npm install`).
4. `deny`는 되돌릴 수 없는 명령어와 민감 파일 읽기 (`rm -rf`, `.env`).
5. 목록에 없는 도구는 기본적으로 승인을 요구하도록 둔다.
6. 팀 공통은 `.claude/settings.json`, 개인용은 `.claude/settings.local.json`(git ignore).

**예시**

`.claude/settings.json`

```json
{
  "permissions": {
    "allow": [
      "Bash(git status)",
      "Bash(git diff *)",
      "Bash(npm test)",
      "Bash(npm run lint)"
    ],
    "ask": [
      "Bash(git commit *)",
      "Bash(npm install *)"
    ],
    "deny": [
      "Read(./.env)",
      "Read(./secrets/**)",
      "Bash(rm -rf *)",
      "Bash(sudo *)",
      "Bash(git push --force*)"
    ]
  }
}
```

지금 어떤 설정 파일이 로드됐는지는 세션 안에서 `/status`로 확인한다.

**CLAUDE.md와의 관계**

- "이렇게 일해줘" → CLAUDE.md
- "이건 절대 안 돼" → Permissions

---

## 7-4. Hooks

Hooks는 Claude Code의 **생명주기 특정 시점에 자동으로 실행되는 스크립트**다.

Permissions가 "이 도구를 쓸 수 있냐 없냐"를 결정한다면, Hooks는 "이 도구를 쓰기 직전/직후에 무엇을 추가로 실행할 것인가"를 결정한다. 모델의 판단을 거치지 않는 **결정론적 처리**라는 점이 핵심이다.

**핵심 원칙**

1. 모델의 협조 없이도 반드시 실행되어야 하는 작업에 사용한다 (포맷팅, 린트, 시크릿 스캔).
2. `PreToolUse` 훅이 exit code 2를 반환하면 해당 도구 호출은 차단된다.
3. 반복 실수가 있다면 CLAUDE.md에 문장을 추가하기 전에 Hook으로 막을 수 있는지 먼저 본다.
4. 스크립트는 짧고 빠르게 — 매 도구 호출마다 실행된다.

**이벤트**

| 이벤트 | 시점 | 용도 |
|---|---|---|
| `PreToolUse` | 도구 실행 전 | 검사·차단 |
| `PostToolUse` | 도구 실행 후 | 포매터·린터 |
| `Stop` | 응답 종료 전 | 테스트 강제 (7-11) |
| `SessionStart` | 세션 시작 | 컨텍스트 주입 |
| `PreCompact` | 압축 전 | 보존할 정보 지정 (7-12) |

**예시**

`.claude/settings.json`

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": ".claude/hooks/pre-commit-check.sh" }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": ".claude/hooks/post-edit-lint.sh" }
        ]
      }
    ]
  }
}
```

`matcher`에는 도구 이름이나 정규식을 쓴다 (`Bash`, `Edit|Write`). 훅은 실행될 때 **stdin으로 JSON**을 받고, `Bash`라면 `.tool_input.command`에 실행하려는 명령이 들어 있다.

`.claude/hooks/pre-commit-check.sh`

```bash
#!/bin/bash
# git commit 시, 스테이징된 변경에 시크릿이 있으면 커밋을 차단한다.

CMD=$(cat | jq -r '.tool_input.command // empty')
case "$CMD" in
  *"git commit"*) ;;
  *) exit 0 ;;
esac

if git diff --cached | grep -qE "(api_key|secret|password)[[:space:]]*="; then
  echo "시크릿으로 의심되는 값이 포함되어 있습니다." >&2
  exit 2   # 차단. stderr 내용이 이유로 전달된다
fi
exit 0
```

스크립트에는 실행 권한이 필요하다 — `chmod +x`.

---

## 7-5. Skills

Skills(`SKILL.md`)는 **특정 작업을 수행하기 위한 절차와 지식을 폴더 단위로 패키징한 것**이다.

- CLAUDE.md — "무엇을 지켜야 하는가" (항상 로드)
- Rules — "특정 영역에서 무엇을 지켜야 하는가" (경로 매칭 시 로드)
- **Skills — "이 작업을 어떻게 수행하는가" (필요할 때만 로드)**

세션 시작 시에는 각 Skill의 `name`과 `description`만 읽고, 실제로 관련 있다고 판단될 때 본문 전체를 로드한다. 그래서 **많이 등록해도 안 쓰면 비용이 거의 없다.**

**핵심 원칙**

1. 설명서가 아니라 실행 절차로 쓴다 — "이렇게 순서대로 하라".
2. **`description`에 가장 공을 들인다.** 트리거 여부를 결정하는 유일한 요소다.
3. 여러 세션·여러 프로젝트에서 반복되는 절차만 Skill로 만든다.
4. 부작용이 있는 작업은 `allowed-tools`로 제한하거나 `/skill-name` 명시 호출만 허용한다.
5. 본문은 간결하게, 세부 내용은 `references/`로 분리한다.

**구조**

```text
.claude/
└── skills/
    └── viewmodel-test/
        ├── SKILL.md          # 필수
        ├── references/       # 선택: 상세 문서
        └── scripts/          # 선택: 실행 스크립트
```

**예시**

```markdown
---
name: viewmodel-test
description: ViewModel의 유닛 테스트를 작성한다. 사용자가 "ViewModel 테스트 작성해줘", "테스트 커버리지 추가" 등을 요청할 때 사용한다.
allowed-tools: Read, Write, Bash
---

# ViewModel 테스트 작성

1. 대상 ViewModel을 읽는다.
2. @Published 프로퍼티와 public 메서드를 확인한다.
3. Repository와 UseCase 의존성을 확인한다.
4. 실제 네트워크 대신 Mock/Stub을 사용한다.
5. Given-When-Then 구조로 작성한다. 템플릿은 references/test_template.swift 참고.
6. 다음 케이스를 반드시 포함한다 — 정상 응답 / 에러 응답 / 빈 응답 / 로딩 상태 변화.
7. 테스트를 실행하고, 실패하면 원인을 분석해 수정한다.
```

**frontmatter**

| 필드 | 설명 |
|---|---|
| `name` | 필수. `/skill-name`으로 호출할 때 쓰는 이름 |
| `description` | 필수. **무엇을 하는지 + 언제 쓰는지** 둘 다 적는다 |
| `allowed-tools` | 선택. 이 Skill이 쓸 수 있는 도구 제한 |
| `disable-model-invocation` | 선택. `true`면 `/skill-name`으로만 실행 |

`description`이 애매하면 Skill은 영영 트리거되지 않는다.

```yaml
# 나쁜 예
description: PDF 관련 작업을 도와줍니다.

# 좋은 예
description: PDF에서 텍스트와 표를 추출하고 양식을 채운다. 사용자가 PDF, 양식, 문서 추출을 언급할 때 사용한다.
```

**저장 위치**

| 위치 | 범위 |
|---|---|
| `~/.claude/skills/` | 나의 모든 프로젝트 |
| `.claude/skills/` | 이 프로젝트 팀 전체 (git 커밋) |

**호출**

```text
/viewmodel-test UserViewModel        # 직접 호출
UserViewModel 테스트 작성해줘.        # description 매칭 시 자동 선택
```

본문에 `` !`git diff HEAD` ``처럼 적으면 Claude가 읽기 전에 명령이 실행되어 결과가 그 자리에 들어간다.

---

## 7-6. Sub-agents (.claude/agents/)

Sub-agent는 **특정 역할을 위해 격리된 컨텍스트에서 동작하는 별도의 Claude 인스턴스**다. 해당 작업에 필요한 컨텍스트만 가지고 독립적으로 실행한 뒤 **결과만** 메인 세션에 반환한다.

**핵심 원칙**

1. 반복적으로 위임하는 역할(리뷰, 테스트 작성, 리서치)을 분리한다.
2. 필요한 도구만 제한적으로 부여한다 — 리뷰 에이전트에는 쓰기 권한을 주지 않는다.
3. system prompt는 역할을 하나로 좁힌다.
4. 병렬 실행 시 각 에이전트가 겹치지 않는 범위를 갖게 한다.
5. 역할이 겹치면 만들지 않는다. 빌트인 `explore` · `plan` · `general-purpose`로 안 되는 것만 만든다.

**역할별 도구 배분**

| 역할 | 하는 일 | `tools` |
|---|---|---|
| Explorer | 코드 탐색, 구조 파악 | `Read, Grep, Glob` |
| Implement | 실제 코드 수정 | `Read, Edit, Write, Bash` |
| Verify | 테스트 실행, 변경 리뷰 | `Read, Grep, Glob, Bash` |

**예시**

`.claude/agents/code-reviewer.md`

```markdown
---
name: code-reviewer
description: 코드 변경사항을 리뷰할 때 사용. 파일을 수정하지 않고 리뷰 코멘트만 남긴다.
tools: Read, Grep, Glob
model: opus
---

# Code Reviewer

당신은 코드 리뷰만 담당하는 에이전트입니다.

- 파일을 수정하지 마세요. 읽기 전용으로만 동작합니다.
- 다음 기준으로 리뷰하세요: 가독성, 에러 처리, 테스트 커버리지, 보안.
- 발견한 문제를 심각도(Critical/Major/Minor)로 분류해서 보고하세요.
- 출력은 요약으로만. 읽은 파일 전문을 붙여넣지 마세요.
```

**주요 frontmatter** — `tools`(도구 제한) · `model` · `permissionMode`(`plan` 등) · `maxTurns`(폭주 방지) · `isolation: worktree`(파일까지 격리, 7-10)

**저장 위치** — 프로젝트 `.claude/agents/` · 개인 `~/.claude/agents/`

**호출**

```text
code-reviewer 에이전트로 방금 작성한 PaymentService.swift를 리뷰해줘.
```

---

## 7-7. MCP (Model Context Protocol)

MCP는 Claude Code를 **외부 시스템과 연결하는 표준 인터페이스**다. 기본적으로 Claude Code는 파일 시스템과 Bash만 다루지만, MCP 서버를 연결하면 GitHub·Slack·DB·사내 API를 Tool로 노출시켜 직접 호출할 수 있다.

Bash로는 `git status` 같은 로컬 정보만 볼 수 있지만, GitHub MCP를 연결하면 Issue 조회·PR 리뷰처럼 **서버 쪽 정보**까지 다룰 수 있다.

**핵심 원칙**

1. 필요한 Tool만 최소 권한으로 — 도구가 많을수록 잘못 호출될 위험도 커진다.
2. 팀 전체용만 `project` scope로 커밋하고, 개인 실험용은 `local`에 둔다.
3. 자격증명은 환경변수로. 프로덕션 DB는 애초에 연결하지 않는다.
4. `npx` 기반 서버는 버전을 고정한다.
5. 연결했으면 반드시 `permissions`로 범위를 좁힌다.

**Scope**

| Scope | 범위 | 저장 위치 |
|---|---|---|
| `local` (기본) | 나만, 현재 프로젝트 | 로컬 사용자 설정 |
| `project` | 팀 전체 | `.mcp.json` (git 커밋) |
| `user` | 나의 모든 프로젝트 | 전역 사용자 설정 |

**Transport** — `stdio`(로컬 프로세스) · `HTTP`(원격 SaaS) · `SSE`(레거시, 신규 비권장)

**예시**

`.mcp.json`

```json
{
  "mcpServers": {
    "github": { "type": "http", "url": "https://api.githubcopilot.com/mcp/" },
    "postgres": {
      "type": "stdio",
      "command": "npx",
      "args": ["@modelcontextprotocol/server-postgres", "postgresql://localhost:5432/mydb"],
      "env": { "PGPASSWORD": "${DB_PASSWORD}" }
    }
  }
}
```

비밀번호는 `${DB_PASSWORD}`처럼 환경변수로 참조하면 `.mcp.json` 자체는 커밋해도 된다.

**권한 좁히기** — MCP 도구도 Tool이므로 7-3이 그대로 적용된다. 이름은 `mcp__<서버>__<도구>` 형식이다.

```json
{ "permissions": {
    "allow": ["mcp__github__get_*"],
    "ask":   ["mcp__github__create_pull_request"],
    "deny":  ["mcp__github__delete_*"] } }
```

**실습**

```bash
claude mcp add --scope project github --transport http \
  --url https://api.githubcopilot.com/mcp/
claude mcp list
```

```text
/mcp                              # 세션 내 연결 상태 점검
#123 이슈를 확인하고 구현해줘.
```

---

## 7-8. Slash Commands

Slash Command는 `/command-name` 형태로 **자주 반복하는 프롬프트를 단축키처럼 등록**해두는 기능이다. Skills가 절차 전체를 패키징하는 것이라면, Command는 훨씬 가볍게 지시문 자체를 재사용한다.

**핵심 원칙**

1. 매번 비슷한 문장으로 타이핑하는 프롬프트가 있다면 Command로 등록한다.
2. 프롬프트 템플릿 수준으로 가볍게 유지한다 — 복잡한 절차는 Skill로 승격한다.
3. `$ARGUMENTS`로 실행 시점에 값을 받는다.
4. 팀 공유는 `.claude/commands/`, 개인용은 `~/.claude/commands/`.

**예시**

`.claude/commands/fix-issue.md`

```markdown
---
description: GitHub 이슈 번호를 받아서 원인을 분석하고 수정한다
---

이슈 #$ARGUMENTS 의 내용을 확인하고, 관련 코드를 찾아 원인을 분석한 뒤 수정하세요.
수정 후에는 반드시 관련 테스트를 실행하고 결과를 보고하세요.
```

```text
/fix-issue 123
```

**쓸 수 있는 재료** — `$ARGUMENTS`(인자 전체) · `$1 $2`(위치 인자) · `` !`명령` ``(실행 결과 삽입) · `@경로`(파일 내용 삽입)

**Skill과의 차이**

| | Slash Command | Skill |
|---|---|---|
| 실체 | 파일 하나 | 폴더 (참조 문서·스크립트 포함) |
| 호출 | 사람이 `/x`라고 쳐야 실행 | Claude가 description 보고 스스로 꺼냄 |

프롬프트가 길어지고 참조 파일이 붙기 시작하면 Skill로 옮긴다.

---

## 7-9. Observability (세션 로그)

**Claude Code가 무엇을 실행했고 왜 그렇게 판단했는지를 나중에 추적할 수 있게 만드는 체계**다. 어떤 도구를 호출했는지, Hook이 언제 막았는지를 기록해야 재현 가능한 버그 리포트가 된다.

**핵심 원칙**

1. 도구 호출·Hook 실행·차단 이력을 남긴다.
2. 반복 실패 패턴이 보이면 CLAUDE.md·Rules·Hooks로 피드백한다.
3. 민감정보가 로그에 남지 않도록 마스킹하고, 로그 디렉터리는 `.gitignore`에 넣는다.
4. "에이전트가 이상하다"는 보고를 받으면 가장 먼저 로그부터 확인한다.

**세션 로그는 이미 남고 있다**

Claude Code는 모든 세션을 JSONL로 기록해 둔다.

```bash
ls ~/.claude/projects/<프로젝트-경로>/*.jsonl
```

**세션 안에서 확인**

| 명령 | 보여주는 것 |
|---|---|
| `/status` | 로드된 설정 파일, 모델, MCP 연결 |
| `/context` | Context를 무엇이 얼마나 차지하는지 |
| `/cost` | 이 세션의 토큰·비용 |

**직접 남기기**

`.claude/settings.json`

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          { "type": "command", "command": ".claude/hooks/log-tool-use.sh" }
        ]
      }
    ]
  }
}
```

`.claude/hooks/log-tool-use.sh`

```bash
#!/bin/bash
# 모든 도구 호출을 타임스탬프와 함께 기록한다.
mkdir -p .claude/logs
jq -c --arg ts "$(date -Iseconds)" '{ts: $ts, tool: .tool_name, input: .tool_input}' \
  >> .claude/logs/session.jsonl
```

도구 이름과 입력값은 환경변수가 아니라 **stdin의 JSON**으로 들어온다.

팀 단위로 모으려면 `CLAUDE_CODE_ENABLE_TELEMETRY`를 켜서 OpenTelemetry로 내보낸다.

---

## 7-10. Worktree (파일 시스템 격리)

Sub-agent는 대화를 격리하지만 파일 시스템은 그대로 공유한다. `git worktree`는 하나의 저장소에서 여러 작업 디렉터리를 만들어 각각 다른 브랜치를 독립 체크아웃한다.

**핵심 원칙**

1. 각 Worktree는 하나의 책임만 진다 — `auth-validate`(입력 검증), `auth-errors`(에러 메시지)처럼 쪼갠다.
2. 같은 브랜치를 여러 세션이 동시에 편집하지 않는다.
3. 각 Worktree는 자기 diff와 자기 테스트를 스스로 책임진다.
4. 메인 Worktree는 비교 기준으로 남기고 작업하지 않는다.
5. 머지는 메인에서 순차적으로 한다.

**명령어**

```bash
claude --worktree                              # 생성 + 독립 브랜치에서 세션 시작
git remote set-head origin -a                  # 기준점 재동기화
git worktree list                              # 목록
git worktree remove ../myproject-auth-validate # 제거
```

Worktree 경로는 `.gitignore`에 추가해 둔다.

**에이전트 단위로 격리**하려면 frontmatter에 한 줄이면 된다.

```markdown
---
name: refactor-network
description: 네트워크 레이어를 리팩터링한다. 다른 작업과 파일이 겹치면 안 될 때 사용.
tools: Read, Edit, Write, Bash
isolation: worktree
---
```

**충돌 났을 때**는 바로 "고쳐줘"라고 시키지 않고 세 단계로 나눠 지시한다.

```text
1. 원인 분석 — 양쪽이 같은 로직을 어떻게 다르게 고쳤는지 설명시킨다
2. 머지 계획 — 어떻게 합칠지 계획을 먼저 받는다
3. 실행      — 계획대로 수정하고 테스트한다
```

---

## 7-11. Verification (검증 강제)

"완료했습니다"라는 응답이 실제로 검증된 것인지를 시스템이 확인하게 만드는 구성이다. 세 곳에 나눠 붙인다.

| 무엇을 검증 | 어디에 |
|---|---|
| 테스트·빌드·린트 통과 | `Stop` 훅 |
| 변경 범위가 요청과 일치하는지 | Slash Command (7-8) |
| 반례·독립 리뷰 | Sub-agent (7-6) |

**핵심 원칙**

1. 통과 여부는 모델이 아니라 훅이 판정한다.
2. 무엇을 볼 것인지는 사람이 미리 파일에 적어둔다.
3. 코드를 작성한 세션이 아닌 곳에서 리뷰한다.
4. 검증은 파일 단위가 아니라 작업 단위로 돌린다.

**Stop 훅** — 응답을 마치려는 시점에 돌고, exit 2를 내면 끝내지 못한다.

`.claude/settings.json`

```json
{
  "hooks": {
    "Stop": [
      { "hooks": [
        { "type": "command", "command": ".claude/hooks/verify.sh" }
      ]}
    ]
  }
}
```

`.claude/hooks/verify.sh`

```bash
#!/bin/bash
# 코드가 변경됐으면 테스트를 통과해야만 종료할 수 있다.

git diff --quiet HEAD -- '*.swift' && exit 0

if ! OUT=$(swift test 2>&1); then
  echo "테스트 실패. 완료로 처리할 수 없습니다." >&2
  echo "$OUT" | tail -30 >&2
  exit 2
fi
exit 0
```

실패 원인을 stderr에 구체적으로 넘겨야 다음 시도가 개선된다.

**독립 리뷰**는 7-6에서 만든 리뷰 에이전트를 구현이 끝난 뒤 호출한다.

```text
code-reviewer 에이전트로 방금 변경분을 검증해줘.
```

---

## 7-12. 세션 관리 (State)

세션이 길어지면 초반에 준 제약이 뒤에 쌓인 정보에 묻힌다. 설정 파일이 아니라 명령어와 습관으로 관리하는 영역이다.

**핵심 원칙**

1. 작업이 바뀌면 `/clear`, 길어지면 `/compact`.
2. `/compact`는 남길 항목을 직접 지시한다.
3. 계속 유지할 규칙은 대화에 두지 말고 파일로 옮긴다.
4. 이전 세션의 임시 권한은 자동으로 승계되지 않는다.

**명령어**

| 명령 | 하는 일 | 언제 |
|---|---|---|
| `/clear` | 대화 히스토리를 통째로 비움 | 작업이 바뀔 때 |
| `/compact` | 요약본만 남기고 압축 | 작업이 길어질 때 |
| `/context` | Context 점유율 확인 | 압축 시점 판단 |
| `claude --resume` | 이전 세션 이어가기 | 어제 작업을 이을 때 |

`/compact`는 남길 항목을 함께 적는다.

```text
/compact 변경된 파일 목록, 실행한 커맨드와 결과, 확정된 결정사항, 남은 To-do만 남겨줘.
```

매번 쓰기 귀찮으면 `PreCompact` 훅으로 고정한다.

```json
{ "hooks": { "PreCompact": [{ "hooks": [
  { "type": "command",
    "command": "echo '보존: 변경 파일 / 테스트 결과 / 확정된 결정 / 남은 To-do'" }
]}]}}
```

**규칙을 파일로 승격** — 대화 중 나온 결론은 `#`으로 시작하는 메시지를 보내면 메모리에 추가할지 물어본다.

```text
# ViewModel에서 URLSession 직접 호출 금지
```

---

## 7-13. Plan Mode & Thinking

파일을 고치기 전에 Read-Only로 계획부터 세우게 하는 모드다.

**핵심 원칙**

1. 영향 범위를 확신할 수 없거나, 여러 모듈이 얽혀 있거나, 레거시라 연쇄 영향이 우려되면 Plan부터 받는다.
2. 계획에는 문제 정의 · 원인 후보 · 수정 범위 · 검증 순서를 포함시킨다.
3. Thinking은 판단 품질이 중요한 작업에만 쓴다.

**쓰는 법**

```text
Shift+Tab     권한 모드 순환 (기본 → 자동 수락 → Plan)
```

에이전트 단위로 고정하려면 frontmatter에 `permissionMode: plan`을 넣는다 (7-6).

**무엇을 언제 쓰나**

| 상황 | 선택 |
|---|---|
| 문제 단순, 범위 명확 | 바로 실행 |
| 문제 복잡, 범위 넓음 | Plan-First |
| 작업 유형이 서로 다름 | Sub-Agent (7-6) |
| 파일이 겹칠 위험 | Worktree (7-10) |
| 품질 중요한데 범위 애매 | Thinking |

---

## 7-14. Plugins (하네스 패키징)

지금까지 만든 구성요소들을 하나로 묶어 여러 저장소에 배포하는 단위다.

```text
my-plugin/
├── .claude-plugin/
│   └── plugin.json        # 이름·버전·설명
├── commands/              # 7-8
├── skills/                # 7-5
├── agents/                # 7-6
├── hooks/                 # 7-4
└── .mcp.json              # 7-7
```

**핵심 원칙**

1. 개별 설정이 안정된 뒤에 묶는다.
2. 한 플러그인은 하나의 목적만 갖는다.
3. 버전을 붙여서 롤백이 가능하게 한다.

**언제 필요한가**

| 상황 | 필요한 것 |
|---|---|
| 한 레포에서만 쓴다 | `.claude/`를 git 커밋 — 여기서 끝 |
| 여러 레포에서 같은 규칙을 쓴다 | Plugin |
| 조직 전체에 정책을 강제한다 | Enterprise 관리형 설정 |
