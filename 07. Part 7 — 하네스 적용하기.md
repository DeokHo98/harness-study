# Part 7. 하네스 적용하기 — 처음 쓰는 사람을 위한 순서

---

# 15장. 7개 하네스, 하나씩 붙이기

Claude Code를 막 쓰기 시작했다면 **7개를 한꺼번에 만들면 안 된다.**
불편을 겪은 순서대로 하나씩 붙인다. 겪지 않은 불편은 만들지 않는다.

**7개를 언제 만드는가**

| 단계 | 하네스 | 이런 일이 생기면 | 절 |
|---|---|---|---|
| **1단계**<br>오늘 | **Context** | 매번 프로젝트 설명을 다시 해야 한다 | 15.2 |
| | **Verification** | 고쳤다는데 뭘 고쳤는지 모르겠다 | 15.3 |
| **2단계**<br>이번 주 | **Permission** | 위험한 걸 건드릴까 봐 불안하다 | 15.4 |
| | **Debug** | 버그를 물어보면 엉뚱한 답이 온다 | 15.5 |
| **3단계**<br>불편해지면 | **Tool** | 같은 말을 반복한다 / 외부 정보가 필요하다 / 말을 안 듣는다 | 15.6 |
| | **작업 분리** | 세션이 지저분해진다 / 여러 작업이 섞인다 | 15.7 |
| | **State** | 며칠짜리 작업이 재시작마다 처음으로 돌아간다 | 15.8 |

> **1단계 두 개만 오늘 하면 된다.** 30분이면 끝난다. 나머지는 표의 "이런 일"을 실제로 겪은 뒤에 돌아온다.

**각 절은 똑같은 4칸으로 되어 있다.**

```text
① 이런 일이 생긴다   ← 실제 대화 예시
② 왜 생기나          ← 한 줄
③ 이렇게 한다         ← 그대로 따라 하면 되는 것
④ 됐는지 확인         ← 이걸 해봐야 만든 게 아니다
```

---

## 15.1 시작 전에 — 세 가지만 확인

### ① 지금 어떤 모드인지 확인한다

Claude Code는 **파일을 고치기 전에 물어보는 모드**와 **안 물어보고 고치는 모드**가 있다.
플랜에 따라 시작 모드가 다르다. **안 물어보는 모드로 시작될 수 있다.**

터미널에서 `Shift + Tab`을 누르면 모드가 바뀐다. 화면에 현재 모드가 표시된다.

> **처음 일주일은 물어보는 모드로 쓴다.** 무엇을 하려는지 눈으로 보는 게 먼저다. 익숙해지면 그때 푼다.

### ② git으로 관리되는 프로젝트에서 시작한다

```bash
git status
```

**변경사항이 없는 깨끗한 상태**에서 시작한다. 이게 안전망이다.
Claude가 뭘 하든 `git checkout .` 한 줄로 전부 되돌릴 수 있다.

### ③ 우리 프로젝트 명령 3개를 확인해둔다

빌드·테스트·린트 명령이 뭔지 확인한다. 뒤에서 계속 쓴다.

```bash
cat README.md | head -40
cat .github/workflows/*.yml 2>/dev/null    # CI가 돌리는 명령이 가장 정확하다
```

| | 웹 · Node | 서버 · Python | Android | iOS |
|---|---|---|---|---|
| 빌드 | `npm run build` | (없음) | `./gradlew assembleDebug` | `xcodebuild -scheme App` |
| 테스트 | `npm test` | `pytest` | `./gradlew test` | `xcodebuild test -scheme AppTests` |
| 린트 | `npm run lint` | `ruff check .` | `./gradlew ktlintCheck` | `swiftlint` |

---

# 1단계 · 오늘 (30분)

## 15.2 Context — 매번 설명하지 않게

### ① 이런 일이 생긴다

```text
나: 로그인 화면에 로딩 표시 추가해줘

Claude: 어떤 상태 관리 라이브러리를 쓰시나요?
        파일 구조를 알려주시면...

나: (또 설명한다)
```

다음 날, 새 세션에서 **똑같은 설명을 또 한다.**

### ② 왜 생기나

Claude는 세션이 끝나면 **아무것도 기억하지 못한다.** 프로젝트 규칙이 사람 머릿속에만 있으면 매번 다시 말해야 한다.

### ③ 이렇게 한다

**프로젝트 루트에 `CLAUDE.md` 파일을 만든다.** 세션이 시작될 때 자동으로 읽힌다.

빈 화면부터 쓰지 말고 **Claude에게 초안을 시킨다.**

```text
이 저장소를 훑어보고 CLAUDE.md 초안을 만들어줘.

- 빌드/테스트/린트 명령은 실제 파일(README, package.json, CI 설정 등)에서
  찾아서 넣어. 못 찾은 건 "확인 필요"라고 표시해.
- 코드에서 반복적으로 보이는 규칙을 5개만 뽑아줘. 추측한 건 표시해.
- 전체 40줄 이내로.
```

초안이 나오면 **내가 두 가지를 고친다.**

```markdown
# 프로젝트 규칙

## 개요
- 스택: (Claude가 채워줌 — 맞는지만 확인)
- 빌드: (15.1 ③에서 확인한 명령)
- 테스트: (15.1 ③에서 확인한 명령)

## 지켜야 할 것            ← ★ 여기는 내가 직접 쓴다
- 테스트를 돌려보지 않고 "완료"라고 말하지 않는다
- src/network/ 는 읽기만 하고 수정하지 않는다

## 컨벤션
- (Claude가 뽑아준 것 중 맞는 것만 남긴다)
```

**"지켜야 할 것"에 뭘 쓸지 모르겠다면** 이 질문에 답해본다.

| 질문 | 답이 곧 규칙 |
|---|---|
| 신입이 오면 반드시 알려주는 것은? | → 컨벤션 |
| 코드 리뷰에서 세 번 이상 지적한 것은? | → 컨벤션 |
| 건드리면 다른 팀에 영향 가는 폴더는? | → 수정 금지 |
| 우리 팀이 "다 됐다"고 부르는 조건은? | → 완료 기준 |

⚠️ **길게 쓰지 않는다.** 40~60줄이면 충분하다. 길면 뒷부분이 읽히지 않는다 (5.2).

### ④ 됐는지 확인

**새 세션을 열고** 이렇게 묻는다.

```text
이 프로젝트 규칙을 요약해봐.
```

| 결과 | 판정 |
|---|---|
| 내가 쓴 내용이 나온다 | ✅ 됐다 |
| 엉뚱한 소리를 한다 | ❌ 파일 위치 확인 (프로젝트 **루트**여야 한다) |
| 앞부분만 안다 | ❌ 너무 길다. 줄인다 |

---

## 15.3 Verification — 뭐가 바뀌었는지 보기

### ① 이런 일이 생긴다

```text
Claude: 수정을 완료했습니다. 테스트도 통과했습니다. ✅
```

그래서 그냥 커밋했다. **다음 날 다른 화면이 깨졌다.**

### ② 왜 생기나

**"완료했습니다"는 검증이 아니다.** 시킨 것 말고 다른 파일도 고쳤을 수 있는데, 물어보지 않으면 알 수 없다.

### ③ 이렇게 한다

Claude가 "완료"라고 하면 **터미널에서 이 명령부터 친다.**

```bash
git diff --stat
```

이렇게 나온다.

```text
 src/features/login/LoginForm       |  12 ++++--
 src/features/login/LoginViewModel  |  34 ++++++++++---
 src/network/ApiClient              |  47 ++++++++++++-------   ← ⚠️ 어?
 3 files changed, 93 insertions(+), 22 deletions(-)
```

**`--stat`은 파일 목록만 보여준다. 10초면 본다.**
여기서 **시키지 않은 파일이 보이면 본문을 읽을 필요도 없다.** 되돌리고 다시 시킨다.

```bash
git checkout .        # 전부 되돌리기
```

목록이 예상과 같으면 그때 본문을 본다.

```bash
git diff
```

**볼 때 확인하는 것 4가지**

| 확인 | 찾는 것 |
|---|---|
| 시키지 않은 변경 | "하는 김에 정리한" 코드 |
| 지워진 코드 (`-` 줄) | 조용히 사라진 예외 처리·검사 |
| 공용 함수 변경 | 다른 화면도 쓰는 함수의 시그니처가 바뀌었나 |
| 키·토큰 | 코드에 하드코딩되거나 로그로 찍히나 |

⚠️ **`-` 줄을 특히 본다.** 새로 추가된 코드는 눈에 띄지만, **없어진 코드는 안 보인다.**

**그리고 마지막에 이걸 묻는다.**

```text
지금 수정한 코드가 깨질 수 있는 입력을 3개 알려줘.
각각 현재 코드가 그 경우를 처리하는지도 같이 알려줘.
```

⚠️ **"이 diff 괜찮아?"라고 묻지 않는다.** 자기가 쓴 코드를 자기가 검토하면 통과시킨다 (7.4).

### ④ 됐는지 확인

커밋하기 전에 이 다섯 개에 답할 수 있으면 된다.

- [ ] 뭘 고쳤는가?
- [ ] `git diff --stat`이 내가 시킨 범위와 같은가?
- [ ] 테스트를 **내가** 돌려봤는가?
- [ ] 어떤 예외 상황을 확인했는가?
- [ ] 문제 생기면 어떻게 되돌리는가?

> 하나라도 막히면 아직 **"완료"가 아니다.**

---

# 2단계 · 이번 주

## 15.4 Permission — 위험한 걸 막아두기

### ① 이런 일이 생긴다

```text
나: 이 브랜치 정리 좀 해줘

Claude: (git push --force 를 실행하려고 한다)
```

또는 안 물어보는 모드였다면 — **이미 실행됐다.**

### ② 왜 생기나

Claude는 **무엇이 되돌릴 수 없는 일인지 모른다.** `CLAUDE.md`에 "하지 마세요"라고 써도 **부탁일 뿐 강제가 아니다.**

### ③ 이렇게 한다

**먼저 우리 프로젝트에서 위험한 게 뭔지 찾는다.**

```bash
# 시크릿·인증서
find . -type f \( -name "*.env*" -o -name "*.pem" -o -name "*.key" \
  -o -name "*.keystore" -o -name "*.p12" \) -not -path "./.git/*"

# 배포·CI 설정
ls -d .github/workflows fastlane .circleci Dockerfile 2>/dev/null
```

**판단 기준 한 줄**: *잘못 건드리면 되돌리는 데 **하루 이상** 걸리나?*

**그다음 `.claude/settings.json` 파일을 만든다.**

```json
{
  "permissions": {
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Bash(git push *)",
      "Bash(rm -rf *)"
    ]
  }
}
```

⚠️ **처음엔 `deny` 네 줄이면 충분하다.** 위에서 찾은 것 중 **가장 무서운 것**만 넣는다.

**스택별로 여기에 더 넣을 것**

| 스택 | 추가 |
|---|---|
| 웹 · Node | `"Bash(npm publish *)"` |
| 서버 | `"Edit(k8s/**)"`, `"Bash(* migrate *)"` |
| Android | `"Read(**/*.keystore)"`, `"Edit(**/AndroidManifest.xml)"` |
| iOS | `"Read(**/*.p12)"`, `"Edit(**/Info.plist)"`, `"Edit(fastlane/**)"` |

**쓰다 보면 두 가지를 추가하게 된다.**

* 승인 창이 **자꾸 뜨는** 안전한 명령 → `"allow"`에 추가 (테스트·린트·읽기)
* **"어? 이걸 하려고 했다고?"** 싶은 순간 → `"deny"`에 추가

```json
{
  "permissions": {
    "allow": ["Bash(npm test *)", "Bash(git diff *)", "Bash(git status)"],
    "ask":   ["Bash(git commit *)"],
    "deny":  ["Read(./.env)", "Bash(git push *)", "Bash(rm -rf *)"]
  }
}
```

**세 칸의 뜻**

| 칸 | 뜻 |
|---|---|
| `allow` | 안 물어보고 그냥 실행 |
| `ask` | 실행 전에 물어봄 |
| `deny` | **아예 실행 안 됨** |

⚠️ 파일 경로 규칙은 **`Read`와 `Edit`만** 통한다. `Write(...)`라고 쓰면 조용히 무시된다.

### ④ 됐는지 확인

**만들었다고 되는 게 아니다.** 새 세션에서 **일부러 시켜본다.**

| 시켜볼 것 | 기대 |
|---|---|
| `.env 파일 읽어줘` | **막힌다** |
| `git push 해줘` | **막힌다** |
| `테스트 돌려줘` | 물어보지 않고 실행 (allow를 넣었다면) |

**막히지 않으면 설정이 안 먹는 것이다.** 이 셋을 확인한다.

1. 파일 위치가 `.claude/settings.json`이 맞나 (프로젝트 루트 아래)
2. JSON 문법이 맞나 (마지막 항목 뒤 **쉼표**가 흔한 실수)
3. 세션 안에서 `/status` → 파일이 로드됐는지 표시된다

---

## 15.5 Debug — 버그를 제대로 물어보기

### ① 이런 일이 생긴다

```text
나: (에러 로그 전체를 붙여넣음) 이거 고쳐줘

Claude: (로그 마지막 줄만 보고) null 체크를 추가했습니다.

나: (고쳐졌는데 원인이 아니었다. 다음 날 또 터진다)
```

### ② 왜 생기나

**로그를 통째로 주면 마지막 줄만 본다.** 정보가 부족한 게 아니라 **정리가 안 된 것**이다.

### ③ 이렇게 한다

**로그를 붙여넣기 전에 다섯 칸으로 나눠서 쓴다.**

```text
[문제] 로그인에서 잘못된 비밀번호를 넣으면 아무 반응이 없다.
       에러 메시지도, 로딩 종료도 안 된다.

[환경] 스테이징에서만 발생. 로컬에서는 정상.

[증거] 로그에 401이 한 줄 찍히고 그 뒤로 아무것도 없음
       src/features/login/ 에 관련 테스트 없음

[제약] src/network/ 는 수정 금지
       ★ 먼저 고치지 말고 원인 후보부터 좁혀줘

[요청] 1. 원인 후보를 가능성 순으로 3개
       2. 각 후보를 확인할 방법
```

⚠️ **`[제약]`의 ★ 줄이 핵심이다.** 이 한 줄이 없으면 **첫 번째 그럴듯한 원인을 잡고 바로 코드를 고치기 시작한다.**

**환경 차이 문제라면** — 값을 보여주지 말고 **표로만** 준다.

```text
| 설정값        | 로컬 | 스테이징 |
|---------------|------|----------|
| API_BASE_URL  | 있음 | 있음     |
| AUTH_CLIENT_ID| 있음 | ❌ 없음  |
```

실제 값을 안 줘도 **문제 위치가 표에서 드러난다.** 시크릿을 대화에 올리지 않아도 된다.

### ④ 됐는지 확인

원인을 찾았다고 하면 **반드시 되묻는다.**

```text
이 가설이 틀릴 수 있는 조건은 뭐야?
```

여기서 말이 막히면 **아직 원인을 못 찾은 것이다.**

---

# 3단계 · 불편해지면

> 아래 셋은 **지금 만들지 않는다.** 해당하는 불편을 **세 번 이상 겪은 뒤**에 만든다.

## 15.6 Tool — 세 가지 중 하나만 고른다

| 불편 | 만들 것 |
|---|---|
| 같은 지시를 계속 반복한다 | **Skill** |
| "그 정보는 볼 수 없습니다"라고 한다 | **MCP** |
| `CLAUDE.md`에 썼는데 자꾸 어긴다 | **Hook** |

### Skill — 반복하는 지시를 파일로

`.claude/skills/이름/SKILL.md` 파일을 만든다. **폴더 이름이 곧 `/이름` 명령**이 된다.

```markdown
---
description: 버그를 증상·증거·원인후보로 정리한다. 에러나 테스트 실패를 들고 올 때 사용.
---

먼저 고치지 말고 아래 순서로만 답한다.

1. 증상: 무엇이 언제 실패하는가 (한 문장)
2. 증거: 로그에서 확인 가능한 사실만
3. 원인 후보: 가능성 순 3개
4. 확인 방법: 각 후보를 확인할 명령어
```

**`description`이 제일 중요하다.** Claude가 이걸 보고 **언제 꺼낼지** 정한다.

**확인**: `/이름`을 쳐서 실행되는지, 그리고 해당 상황을 말했을 때 알아서 꺼내는지.

### MCP — 외부 시스템 연결

GitHub 이슈나 DB를 **직접 읽게** 하고 싶을 때만. 프로젝트 루트에 `.mcp.json`.

```json
{
  "mcpServers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "headers": { "Authorization": "Bearer ${GITHUB_PAT}" }
    }
  }
}
```

⚠️ **붙였으면 권한을 좁힌다.** 도구 이름은 `mcp__서버__도구` 형식이다.

```json
{ "permissions": { "deny": ["mcp__github__delete_*"] } }
```

**확인**: 세션에서 `/mcp` → 연결 상태가 보인다.

### Hook — 진짜로 막기

`CLAUDE.md`는 부탁이고 Hook은 **강제**다. `.claude/settings.json`에 추가한다.

```json
{
  "hooks": {
    "PreToolUse": [
      { "matcher": "Edit|Write",
        "hooks": [{ "type": "command",
                    "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/protect.sh" }] }
    ]
  }
}
```

`.claude/hooks/protect.sh` (만든 뒤 `chmod +x` 필요)

```bash
#!/bin/bash
FILE=$(jq -r '.tool_input.file_path // empty')
case "$FILE" in
  *.env|*/.github/workflows/*)
    echo "보호된 파일입니다: $FILE" >&2
    exit 2 ;;          # exit 2 = 차단
  *) exit 0 ;;         # 그 외는 통과
esac
```

⚠️ 막는 건 **`PreToolUse`**만 된다. `PostToolUse`는 **이미 실행된 뒤**라 되돌릴 수 없다 (포매터 같은 뒷정리용).

**확인**: 보호 파일을 일부러 고쳐보라고 시킨다. 차단되면 정상. **테스트 안 한 Hook은 없는 것과 같다.**

---

## 15.7 작업 분리 — 섞이지 않게

### Subagent — 조사를 따로 시키기

**이럴 때**: 파일을 여기저기 뒤지느라 대화가 지저분해지고, 정작 중요한 지시가 묻힌다.

`.claude/agents/explorer.md`

```markdown
---
name: explorer
description: 관련 파일을 찾아 구조만 요약한다. 수정 범위를 정하기 전에 사용.
tools: Read, Grep, Glob
---

너는 탐색 전용이다. 코드를 절대 수정하지 않는다.
관련 파일과 각 파일의 역할을 찾아서 15줄 이내로 요약한다.
수정 후보와 건드리면 안 될 파일을 구분해서 알려준다.
```

**부르는 법** — 이름을 말하거나, 상황이 맞으면 알아서 불린다.

```text
explorer 로 로그인 관련 파일 먼저 찾아줘
```

**받는 것** — 뒤진 과정 전부가 아니라 **요약만** 돌아온다.

```text
관련 파일 3개
- src/features/login/LoginForm       : 화면. 에러 표시 없음
- src/features/login/LoginViewModel  : 에러 처리가 비어 있음  ← 원인 후보
- src/network/ApiClient              : 401을 그대로 던짐. 수정 금지 범위
```

> **검토용 에이전트를 하나 더 만들면 더 좋다.** 코드를 쓴 세션이 아니라 **다른 세션이 검토**해야 제대로 걸러진다 (7.4).

### Worktree — 여러 작업을 동시에

**이럴 때**: 두 가지를 동시에 하는데 `git diff`가 뒤섞인다.

```bash
claude --worktree        # 별도 폴더 + 별도 브랜치로 시작
```

* worktree 하나에 **작업 하나**
* 합칠 때는 **하나씩 순서대로**
* 충돌하면 **바로 "고쳐줘" 하지 않는다** → 원인 설명 → 계획 → 수정 순서로

**확인**: `git worktree list` — 브랜치가 서로 다른지, 각 폴더의 `git diff --stat`에 **같은 파일이 겹치지 않는지.**

---

## 15.8 State — 며칠짜리 작업 이어가기

### ① 이런 일이 생긴다

이틀째 작업. 새 세션을 열었더니 **어제 정한 걸 하나도 모른다.** 이미 끝낸 걸 또 하려고 한다.

### ② 왜 생기나

**대화는 저장되지 않는다.** 진행 상황이 대화 안에만 있으면 세션이 끝날 때 같이 사라진다.

### ③ 이렇게 한다

**작업이 이틀 이상 걸릴 때만** `state.md`를 만든다.

```markdown
# 진행 상황

## 완료
- 로그인 화면 에러 처리 (PR #412)

## 진행 중
- 회원가입 화면에 같은 패턴 적용

## 다음
- 나머지 화면 5개

## 결정한 것
- 에러 문구는 화면 쪽에서 만든다. 네트워크 레이어는 그대로 던지기만.

## 반복하면 안 되는 실패
- 네트워크 레이어에서 문구를 만들려다 되돌림
```

새 세션은 **이 파일부터 읽고** 시작한다.

**그리고 계속 지킬 규칙이 나오면 `CLAUDE.md`로 옮긴다.**

```text
이번에 정한 것 중에 앞으로도 계속 지켜야 할 규칙이 있으면
CLAUDE.md에 추가할 문장으로 정리해줘.
```

내가 확인하고 커밋한다. **대화에만 있는 규칙은 사라지고, 파일에 있는 규칙은 남는다.**

### ④ 됐는지 확인

**새 세션을 열고 아무 설명 없이** 이렇게 묻는다.

```text
state.md 읽고 지금 이어서 할 작업이 뭔지 알려줘.
```

다음 작업을 정확히 짚으면 성공. **되묻거나 끝낸 일을 다시 하려 하면** 파일이 부실한 것이다.

---

## 15.9 다 만들면 이렇게 된다

```text
내프로젝트/
├── CLAUDE.md                    ← 15.2  1단계
├── .mcp.json                    ← 15.6  필요할 때만
├── state.md                     ← 15.8  긴 작업일 때만
└── .claude/
    ├── settings.json            ← 15.4  2단계 (권한 + Hook)
    ├── skills/이름/SKILL.md      ← 15.6  필요할 때만
    ├── agents/explorer.md       ← 15.7  필요할 때만
    └── hooks/protect.sh         ← 15.6  필요할 때만
```

`.gitignore`에 한 줄 추가한다. (개인 설정은 팀과 공유하지 않는다)

```gitignore
.claude/settings.local.json
```

> **굵게 표시한 1·2단계 파일 두 개가 전부인 상태가 정상이다.** 나머지가 없다고 잘못된 게 아니다.

---

## 15.10 안 될 때

| 증상 | 확인할 것 |
|---|---|
| `CLAUDE.md`를 모르는 것 같다 | 프로젝트 **루트**에 있나 · 너무 길지 않나(40~60줄) |
| `deny`가 안 막는다 | `.claude/settings.json` 위치 · JSON 쉼표 · `/status`로 로드 확인 |
| 자꾸 엉뚱한 파일을 본다 | 설명을 **더** 하지 말고, 볼 폴더를 **좁혀서** 다시 요청 |
| 같은 실수를 반복한다 | 대화가 오염됐다 → `/clear` 후 짧게 다시 |
| 대화가 길어져 느려진다 | `/compact` + 남길 것 지시 (변경 파일·명령·결과·할 일) |
| Skill을 안 꺼낸다 | `description`이 모호하다. "언제 쓰는지"를 더 구체적으로 |
| 뭘 했는지 모르겠다 | `git diff --stat` (15.3) |
| 다 꼬였다 | `git checkout .` 로 되돌리고 처음부터 |

---

## 15.11 지금까지 만든 게 뭐였나

7개를 따로 외울 필요는 없다. **위 순서대로 했다면 이미 만든 것이다.**

| 만든 것 | 하네스 | 하는 일 |
|---|---|---|
| `CLAUDE.md` | **Context** | 무엇을 보게 할 것인가 |
| `git diff` 습관 | **Verification** | 결과를 어떻게 확인할 것인가 |
| `settings.json` | **Permission** | 무엇을 하게 할 것인가 |
| 5칸 프롬프트 | **Debug** | 실패 원인을 어떻게 좁힐 것인가 |
| Skill · MCP · Hook | **Tool** | 어떤 도구를 어떻게 쓸 것인가 |
| Subagent · Worktree | **작업 분리** | 어떻게 안 섞이게 할 것인가 |
| `state.md` | **State** | 무엇을 기록으로 남길 것인가 |

> 좋은 하네스는 **처음부터 완성된 설정**이 아니라, **사고가 날 뻔할 때마다 한 줄씩 늘어난 설정**이다.
