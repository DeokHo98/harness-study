# CodeCraft 미니 케이스 — Harness Engineering 적용하기 (1/3)

## 1. 가장 중요한 원칙

> **Claude Code의 코어 루프와 Harness 구조를 그대로 CodeCraft 문제 해결에 적용한다.**

이번 미니 케이스에서는 아직 실제 코드 작업은 하지 않는다. 먼저 Claude Code의 핵심 관점 — 코어 루프, Harness들, Deny First Rule, Context Window 관리, 확장 메커니즘 — 을 CodeCraft에 어떻게 그대로 적용하는지 다룬다.

---

## 2. Claude Code 코어 루프 복습

Claude Code의 코어 루프는 단순하다. **모델이 호출되고 → 도구 사용을 요청하고 → 도구를 실행하고 → 결과를 다시 모델에게 넘긴다.** 이 반복 주위를 여러 Harness가 감싸고 있다.

| Harness | 역할 |
|---|---|
| Context Harness | Claude가 어떤 정보를 봐야 하는지 정함 |
| Permission Harness | 어떤 행동을 해도 되는지 제안 |
| Tool Harness | 어떤 도구를 어떻게 쓸지 정함 |
| Verification Harness | 결과를 테스트·실행 결과로 확인 |
| Subagent Harness | 작업을 분리 |
| Persistence Harness | Session과 기록을 남김 |

모델은 파일 시스템, Shell, 네트워크에 직접 접근하지 않는다. 모델이 Tool Use를 만들면 Harness가 Parse하고, Permission을 확인하고, Tool Implementation에 Dispatch한다. 이 분리 덕분에 모델이 잘못 판단하거나 조작되더라도 Sandboxing·Permission Check·Deny First 같은 규칙으로 실행을 통제할 수 있다.

| 주체 | 하는 일 |
|---|---|
| 모델 | 판단, 다음 액션 제안, Tool Use 요청 |
| Harness | 요청 검증, 권한 확인, 도구 실행, 결과를 다시 루프에 넣기 |

---

## 3. CodeCraft에 적용하기

Claude에게 시켜야 할 일:

* 설계 후보(설계 옵션) 생각하기
* 패턴 후보(디자인 패턴) 제안하기
* 구현 방향 만들기

요구사항 Harness가 하는 일:

* Input/Output 정의
* 제약 조건 통과시키기
* 엣지 케이스를 테스트로 바꾸기
* 구현이 요구사항을 벗어나지 않게 하기

> Claude Code에서 모델은 판단만 하고 바로 실행되지 않는다. 실행 전에 Harness가 권한을 확인하고, 위험한 행동을 막고, 결과를 다시 모델에게 돌려준다. CodeCraft도 똑같다. "이렇게 구현하면 되겠다"라고 바로 가지 말고, 그 설계가 입력 조건을 만족하는지, 출력을 지키는지, 제약 조건을 위반하지 않는지, 엣지 케이스를 통과하는지 먼저 확인해야 한다.

---

## 4. Deny First Rule을 CodeCraft에도

Claude Code의 Deny First Rule: 모르는 액션은 그냥 허용하지 않는다. 위험한 액션은 차단하거나 사용자에게 묻는다.

CodeCraft를 위한 Deny First Rule:

* 모르는 요구사항은 그냥 가정하지 않는다
* 위험한 입력은 허용하지 않는다
* 애매한 정책은 코드 안에 숨기지 않는다

> 좋은 시스템은 정상 상황에서만 잘 되는 시스템이 아니다. 이상한 입력이 들어왔을 때도 어떻게 행동할지 정해진 시스템을 만들어야 한다.

---

## 5. 미니 케이스 — Delivery Fee Calculator

문제: **"Build a service that calculates delivery fees for customer orders."**

겉으로는 단순한 배달 수수료 계산기처럼 보이지만, 이 문장만 보고 바로 코드를 짜면 중요한 맥락을 놓친다. Deny First 질문들:

* 거리(distance)가 음수면 계산하지 않는가, 아니면 클램프(clamp)로 정책을 명확히 하는가
* 도시(city)가 비어 있을 때 그냥 "서울"로 가정해도 되는가 — 안 된다
* 거리가 음수인데 그냥 계산해도 되는가 — 안 된다
* 고객 타입(customer type)이 이상한 값일 때 그냥 "Regular"로 처리해도 되는가 — 명확히 정해야 한다

### Delivery Fee Calculator의 Context Harness

| 항목 | 내용 |
|---|---|
| 모호한 문제 | "배달 수수료 계산기를 만들어라" |
| 입력 | 주문 금액, 배달 거리, 도시, 고객 타입, 날씨, 주문 상태 |
| 출력 | 기본 배송비, 추가 요금, 할인 금액, 최종 배송비(음수 불가) |
| 제약 | 음수 거리 금지, 취소 주문 제외, 최종 배송비 음수 금지 |
| 변경 가능성 | 도시별 요금, 날씨별 추가 요금, 멤버십 할인, 프로모션 정책 |

배송비는 거리·주문 금액이 높으면 무료 배송인지, 비나 눈이 오면 추가 요금이 붙는지, 도시마다 기본 요금이 다른지, 프리미엄 고객은 할인을 받는지 — 이런 질문들이 정리되지 않으면 Claude도 사람도 코드도 모두 추측으로 움직이게 된다.

---

## 6. Context Window와 Context Harness

Claude Code에서 가장 중요한 제약사항은 Context Window다. 모델을 호출하기 전에 불필요하거나 너무 큰 정보를 줄이는 과정을 거친다. Tool Output이 너무 길면 줄이고, 오래된 대화는 잘라내거나 요약하고, 필요하면 전체 맥락을 더 압축된 형태로 바꾼다. 이를 **Context Reduction Pipeline**이라 부르며 다섯 단계로 이뤄진다.

> **Budget Reduction → Harness Snip → Micro Compact → Context Collapse → Auto Compact**

그 밖에도 `.md` 문서를 필요한 시점에만 불러오거나, Tool Schema를 처음부터 전부 넣지 않고 필요할 때만 로딩하거나, Subagent의 전체 대화가 아니라 요약만 부모 Context로 돌려주는 방식으로 Context 소비를 줄인다.

CodeCraft로 가져오면, 병목은 모델의 Context Window가 아니라 **문제 맥락을 얼마나 정확하게 잡았는가**다. 문제에 대한 프롬프트는 보통 짧다. 이 한 문장만 보고 코드를 만들면 추측으로 만들거나 맥락이 부족한 채로 만들어진다. 그래서 요구사항 분석 과정이 곧 CodeCraft의 Context Harness다. 비개발자와 전문 개발자의 결과물 갭이 큰 것도 이 때문이다.

---

## 7. Claude Code의 확장 메커니즘 → CodeCraft의 디자인 패턴

Claude Code는 MCP, Skills, Hooks, Plugin 같은 확장 메커니즘을 쓴다.

| Claude Code 확장 | 역할 |
|---|---|
| MCP | 외부 시스템을 연결 |
| Skills | 반복 작업을 능력화 |
| Hooks | 라이프사이클 중간에 개입 |
| Plugin | 기능을 확장 |

CodeCraft도 디자인 패턴을 통해 같은 방식으로 확장한다.

| 상황 | 디자인 패턴 | 역할 |
|---|---|---|
| 계산 정책이 교체될 때 | **Strategy** | 정책을 유연하게 데이터처럼 교체 |
| 상황별로 객체를 생성해야 할 때 | **Factory** | 시나리오별로 다른 객체를 생성 |
| 데이터 출처를 분리해야 할 때 | **Repository** | 데이터 출처를 분리 |
| 외부 API가 붙을 때 | **Adapter** | 외부 시스템과의 연결을 감쌈 |

모든 문제를 하나의 디자인 패턴으로 풀 수는 없다. 계산 정책이 바뀌면 Strategy, 객체 생성 규칙이 복잡해지면 Factory, 외부 API가 붙으면 Adapter — 이렇게 상황별로 쓸 수 있는 패턴이 다르다. 문제 유형별로 어떻게 확장할지는 다음 강의에서 이어서 다룬다.

---

## 3줄 요약

1. Claude Code의 코어 루프(모델 호출 → 도구 요청 → 실행 → 결과 반환)를 둘러싼 Context·Permission·Tool·Verification·Subagent·Persistence Harness는 CodeCraft에도 그대로 적용된다 — 설계도 바로 실행하지 않고 입력·출력·제약·엣지 케이스를 먼저 확인해야 한다.
2. Claude Code의 Deny First Rule(모르는 액션은 허용하지 않는다)처럼, CodeCraft에서도 모르는 요구사항을 가정하지 않고 위험한 입력을 허용하지 않으며 애매한 정책을 코드 안에 숨기지 않아야 한다.
3. Claude Code가 Context Window를 관리하듯 CodeCraft도 문제 맥락(입력·출력·제약·변경 가능성)을 정확히 잡는 게 핵심이고, MCP·Skills·Hooks·Plugin 같은 확장 메커니즘은 CodeCraft에서 Strategy·Factory·Repository·Adapter 같은 디자인 패턴으로 대응된다.
