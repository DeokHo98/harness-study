# 기능 요구사항 분석과 Harness Engineering — 핵심 요약

## 1. 가장 중요한 원칙

> **좋은 코드는 문제를 잘 푼 코드가 아니라, 요구사항이 바뀌어도 무너지지 않도록 입력·출력·제약사항·변경 가능성을 먼저 검토한 코드다.**

CodeCraft 문제를 받았을 때 바로 클래스를 만들거나 코드를 치면 안 된다. 먼저 아래 4가지를 정리해야 한다.

1. 이 기능이 실제로 해야 하는 일은 무엇인가 (기능 요구사항)
2. 입력 데이터는 어떤 형태로 들어오는가
3. 출력은 어떤 형태로 반환되는가
4. 어떤 제약사항과 변경 가능성이 설계를 바꾸는가

---

## 2. 왜 바로 코딩하면 안 되는가 — DoorDash 예시

문제: **"드라이버의 Payout Calculator를 만들어라."**

이 한 문장을 그대로 Claude Code에 주면 Claude Code는 다음을 알 수 없다.

* Payout 규칙이 무엇인지
* 금액이 라운딩되는지
* 잘못된 배달 기록을 걸러내는 기준
* 보너스·프로모션이 적용될 때의 처리 방식

그래도 Claude Code는 그럴듯한 코드를 만들어준다. 문제는 **어떤 가정을 했는지, 어떤 제약을 놓쳤는지, 어디가 바뀔 수 있는지를 보장할 수 없다**는 것이다.

> 설계 문제는 문장을 코드로 바꾸는 문제가 아니라, 모호한 제품 요구사항을 실행 가능한 형태로 바꿔주는 작업이다. 그래서 Claude Code에게 시키기 전에, 사람이 먼저 Harness Engineering을 해줘야 한다.

---

## 3. 기능 요구사항(Functional Requirements) — 구체적으로 쪼개기

기능 요구사항은 **시스템이 반드시 해야 하는 행동**이다. "Payout 계산하기"처럼 추상적으로 말하면 부족하다. 구체적으로 분해해야 한다.

Driver Payout Calculator를 예로 들면:

1. Delivery Record(배달 기록)를 입력으로 받는다
2. 각 Delivery Record의 Payout을 계산한다
3. 같은 드라이버에 속한 기록인지 구별한다
4. 드라이버별로 Payout을 총 합산한다
5. 잘못된 레코드(다른 드라이버 기록, 잘못된 거리 등)가 들어왔을 때를 처리한다

> 이렇게 구체적으로 나누면 코드 구조가 대략 예상되기 시작한다. 개발자가 Claude Code에게 주는 프롬프트와, 비개발자가 주는 한 문장짜리 프롬프트는 결과물 차이가 크다. 입력·계산·합산·검증의 책임이 분리되어 구현되는지가 갈리기 때문이다.

기능 요구사항을 찾을 때 던져야 할 질문들:

* 사용자는 이 시스템으로 무엇을 하려고 하는가
* 시스템(기능)이 해야 하는 액션은 무엇인가
* 반드시 지원해야 하는 흐름은 무엇인가
* 실패하거나 예외가 되는 상황은 무엇인가
* 지금 구현해야 하는 범위와, 나중에 확장되어야 하는 범위는 무엇인가

---

## 4. 모든 시스템의 기본 구조 — Input → Logic → Output

모든 시스템은 수많은 입력과 출력이 이어져서 만들어진다. **Input, Logic, Output**이 그 근본 구조다.

### Input 분석

* 입력 Type
* 필수 데이터 vs 선택 데이터
* 데이터 출처 (어디서 가져오는가)
* 유효성 조건 (잘못된 데이터의 기준)
* 중복 가능성
* 정렬 여부
* Null/Empty 가능성

> 예: Delivery Record에는 기본 요금, 거리, 팁, 지역 같은 데이터가 들어 있어야 계산할 수 있다. 거리가 음수일 수 있는지, 팁이 없을 수 있는지, 이미 정렬된 채로 들어오는지까지 따져야 한다. 발견하지 못한 가정은 나중에 버그가 된다.

### Output 분석

* 데이터 타입
* 정렬 기준
* 금액 단위, 라운딩 규칙
* 에러 반환 방식
* 빈 입력(Empty Input)일 때의 처리

> 예: 총 금액 하나만 반환할 수도 있고, 드라이버별 총 금액 리스트를 반환할 수도 있다. 입출력을 제대로 정의해야 테스트도 제대로 검증할 수 있다.

---

## 5. 제약 분석 — 무엇이 설계를 바꾸는가

제약의 종류: 정확성, 성능, 일관성, 보안·권한, 확장성, 테스트 가능성.

| 문제 유형 | 중요한 제약 | 예시 |
|---|---|---|
| 금액 계산 | 정확성 | float 대신 decimal/정수(cent)를 써서 소수점 오차 방지 |
| 실시간 랭킹 | 성능·최신성 | 매번 전체를 정렬하면 느리므로 캐시·사전계산 필요 |
| 결제 재시도 | 일관성·Idempotency | 같은 결제가 두 번 실행되지 않도록 보장 |

문제마다 설계를 결정하는 제약이 다르므로, 시스템별로 따져봐야 한다.

---

## 6. 변경 가능성 분석 — 분리 후보 찾기

변경 가능성이 높은 부분을 미리 찾아두는 것도 분석의 일부다.

* 정책이 자주 바뀌는가
* 계산 공식이 여러 종류인가
* 데이터 출처가 바뀔 수 있는가
* 출력 포맷이 여러 개인가
* 외부 호출이나 데이터베이스가 바뀔 수 있는가

> 변경 가능성이 높은 부분은 **"분리 후보"**로 표시해 둔다. Claude Code에게 "변경 가능성이 높은 부분은 분리 후보로 두고 구현해줘"라고 요청하면 프롬프트 자체가 달라진다.

---

## 7. 디자인 패턴은 변경 가능성에서 나온다

디자인 패턴은 변경 가능성이 높은 부분을 발견했을 때 필요해지는 경우가 많다.

> 예: Payout 계산 규칙이 하나뿐이면 단순 함수 하나로 충분하다. 하지만 지역별로 규칙이 달라지고, 시간대별 보너스가 생기고, 프로모션별로 계산 방식이 달라진다면 이야기가 다르다.

처음엔 단순 구현도 괜찮아 보이지만, 정책이 늘어날수록 코드가 점점 복잡해진다. 그래서 구현 전에 "단순하게 갈 것인가, 패턴으로 분리할 것인가"를 판단해야 한다. 디자인 패턴 자체를 몰라도 괜찮다 — Claude Code가 적합한 패턴을 추천·적용해줄 수 있다.

---

## 8. 요구사항 분석 템플릿

CodeCraft 문제를 풀 때마다 아래 템플릿을 먼저 채운다. 문제를 직접 풀 때도, Claude Code에게 작업을 맡길 때도 그대로 쓸 수 있다.

| 항목 | 내용 |
|---|---|
| 기능 요구사항 | 시스템이 반드시 해야 하는 일 (Happy Path + 예외 상황) |
| 입력 | 타입, 필수 필드, 선택 필드 |
| 출력 | 타입, 형식, 라운딩·에러 처리 |
| 제약사항 | 정확성, 성능, 일관성, 보안, 확장성 등 |
| 변경 가능성 | 분리 후보로 표시할 부분 |
| (선택) 디자인 패턴 | 필요하면 Claude Code에게 추천받기 |

---

## 3줄 요약

1. 좋은 코드는 문제를 잘 푼 코드가 아니라 요구사항이 바뀌어도 무너지지 않는 코드이므로, CodeCraft 문제를 받으면 코딩 전에 기능 요구사항·입력·출력·제약사항·변경 가능성을 먼저 분석해야 한다.
2. 모든 시스템은 Input → Logic → Output 구조이며, 입력은 타입·필수 여부·출처·유효성·중복·정렬·Null 가능성을, 출력은 타입·정렬·단위·라운딩·에러 처리를 구체적으로 정의해야 테스트도 제대로 검증할 수 있다.
3. 제약사항(정확성·성능·일관성 등)과 변경 가능성이 높은 부분(분리 후보)을 미리 찾아두면 디자인 패턴이 필요한 지점과 Claude Code에게 줄 프롬프트가 훨씬 구체적이고 정확해진다.
