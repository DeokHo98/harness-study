# Claude Code의 Subagent와 Worktree Harness, 제대로 이해하기

Claude Code를 쓰다 보면 "Subagent 여러 개 돌리면 더 빠르지 않을까?" 하는 생각이 드는 순간이 있다. 결론부터 말하면 **반은 맞고 반은 틀리다.** 이번 글에서는 왜 그런지, 그리고 iOS 개발자 입장에서 어떻게 이해하면 되는지 정리해본다.

## 핵심 주제: "병렬 처리보다 중요한 건 격리다"

여러 Claude 세션이나 Subagent를 동시에 돌리면 작업 처리량(throughput)은 분명 늘어난다. 하지만 **격리(isolation) 없이** 병렬로만 돌리면 이런 문제가 생긴다.

- 같은 파일을 여러 세션이 동시에 수정
- 같은 테스트 DB를 공유해서 결과가 꼬임
- 같은 브랜치에서 충돌 발생
- 각 세션의 컨텍스트(대화 맥락)가 서로 섞여버림
- 마지막엔 머지가 불가능한 변경 사항들이 쌓임

즉, "여러 개를 동시에 돌린다"가 목표가 아니라 **"충돌 없이 정리된 상태로 병렬 작업이 가능하게 만든다"**가 목표라는 얘기다.

iOS 개발로 비유하면, 여러 명이 같은 `main` 브랜치에서 동시에 작업하다가 서로의 변경사항이 충돌 나고, 심지어 각자 어떤 기능 브랜치 작업 중이었는지도 헷갈리는 상황과 같다. Git-flow를 왜 쓰는지 생각해보면 바로 이해될 것이다.

## Subagent: 컨텍스트를 격리하는 장치

Claude 공식 문서에 따르면 Subagent는 특정 작업을 처리하는 전문화된 AI 보조이며, 다음 특징을 갖는다.

- Main 대화와 **분리된 별도의 컨텍스트**를 사용
- 특정 **도구(tool)에 대한 접근 권한**만 가질 수 있음
- Main 대화의 컨텍스트 오염을 줄이고, 집중된 책임 범위(scope)를 갖도록 설계

iOS 개발자 입장에서 쉽게 말하면, Subagent는 **의존성 주입(Dependency Injection)으로 필요한 기능만 넘겨받은 별도 모듈** 같은 존재다. TCA로 치면 부모 Reducer가 전체 State/Action을 다 알 필요 없이, 자식 Reducer에게 필요한 슬라이스(scope)와 제한된 Dependency만 넘겨주는 것과 비슷하다. Subagent는 "병렬 처리 기능"이 아니라 **"컨텍스트 격리 메커니즘"**으로 봐야 한다는 게 핵심이다.

### 왜 이게 중요한가

Parent(Main) 세션이 Subagent에게 작업을 위임하면, Subagent는 자신만의 제한된 컨텍스트 창에서 작업하고, Main 세션에는 전체 대화 기록이 아니라 **요약 또는 최종 보고만** 돌려준다. 이 구조 덕분에 Main 세션이 조사 과정의 노이즈로 가득 차는 걸 막을 수 있다.

**예시**: 인증(Auth) 모듈을 조사한다고 해보자. Main 세션에서 직접 여러 파일을 읽고, `grep`으로 코드 검색하고, 가설을 세우는 작업을 계속하면 컨텍스트가 금방 지저분해진다. 반대로 탐색 전용 Subagent에게 "인증 흐름 구조만 조사하고 관련 파일과 위험 지점을 요약해서 달라"고 맡기면, Main 세션은 깔끔한 요약만 받고 바로 다음 판단으로 넘어갈 수 있다.

### 역할 분리: Explorer / Implement / Verify

Subagent Harness는 역할 분리에서 출발한다.

| Subagent 역할 | 하는 일 | 가지는 권한 |
|---|---|---|
| **Explorer (탐색)** | 코드 탐색, 구조 파악 | **읽기 전용(read-only)** |
| **Implement (구현)** | 실제 코드 수정 | 편집/쓰기(`edit`, `write`) 권한 |
| **Verify (검증)** | 테스트 실행, 변경사항 리뷰 | 테스트 실행 권한, 쓰기 권한은 제한 |

여기서 중요한 포인트는 **모든 Subagent에게 모든 도구를 주지 않는다**는 것이다. 탐색 에이전트는 읽기만 해도 충분하니 read-only 도구만 주고, 검증 에이전트는 테스트는 돌릴 수 있어야 하지만 코드를 함부로 고치지 못하게 쓰기 권한을 제한한다.

이건 사람으로 구성된 팀과도 똑같다. 좋은 팀은 한 사람이 리서치, 구현, 리뷰, 검증을 전부 동시에 하지 않는다. 역할이 분리되어야 각자의 책임 범위 안에서 사고 없이 작업할 수 있다.

## Worktree: 파일 시스템을 격리하는 장치

Subagent가 "대화(컨텍스트)"를 격리한다면, `git worktree`는 **파일 시스템**을 격리한다.

`git worktree`를 쓰면 하나의 저장소(repository)에서 **여러 개의 작업 디렉터리(worktree)**를 만들 수 있고, 각 worktree는 서로 다른 브랜치를 동시에 체크아웃해서 독립적으로 작업할 수 있다.

iOS 개발자라면 이렇게 이해하면 쉽다: 지금까지는 `git checkout`으로 브랜치를 왔다 갔다 하면서 작업했다면, worktree는 **브랜치별로 아예 별도의 로컬 폴더(체크아웃)를 만드는 것**이다. Xcode 프로젝트를 폴더별로 따로 클론해서 동시에 열어두고 작업하는 셈인데, 실제로는 `.git` 히스토리는 공유하면서 디스크 공간은 훨씬 효율적으로 쓴다.

### 왜 유용한가

병렬로 Claude 세션을 운영할 때 이 구조가 특히 유용하다.

- 세션 A: `bugfix/auth` 브랜치의 worktree에서 테스트 실패 수정
- 세션 B: `refactor/network` 브랜치의 worktree에서 리팩터링
- 세션 C: 또 다른 브랜치에서 실험적 작업

Main worktree(안정된 baseline)는 오염되지 않고, 각 작업의 변경 사항과 테스트 결과를 서로 비교할 수 있다.

**주의할 점**: 그냥 터미널을 여러 개 열어서 같은 브랜치에서 여러 세션이 동시에 편집하면, 이건 병렬 처리가 아니라 그냥 혼란이다. 어떤 세션이 어떤 변경을 만들었는지 추적이 안 되고, 테스트 결과가 어느 변경에 대한 것인지도 불명확해진다. Worktree를 쓰면 브랜치, 디렉터리, 테스트 결과, 변경 사항이 전부 분리되기 때문에 이런 문제가 사라진다.

## 정리: 두 개를 같이 써야 진짜 "병렬 격리"

| | 격리하는 대상 | 혼자 쓸 때 문제 |
|---|---|---|
| **Subagent** | 컨텍스트(대화) | 대화는 깨끗해지지만 파일 변경 충돌은 여전히 발생 |
| **Worktree** | 파일 시스템 | 파일은 분리되지만 역할(누가 뭘 하는지)이 섞일 수 있음 |

그래서 둘을 함께 써야 한다.

1. **탐색(Explorer) Subagent**: 읽기 전용으로 관련 파일을 찾아서 요약
2. **구현(Implement) 세션**: 별도 worktree에서 작은 단위의 패치를 생성
3. **검증(Verify) Subagent**: 변경분과 테스트 결과를 리뷰

이게 바로 "병렬 실행"이 아니라 **"병렬 격리(parallel isolation)"**다. 무작정 세션을 여러 개 띄우는 게 아니라, 컨텍스트와 파일 시스템을 각각 격리한 상태에서 역할별로 나눠 작업하는 것이 harness engineering 관점에서의 안전한 병렬 처리다.

---

## 3줄 요약

1. Subagent와 Worktree를 여러 개 돌리는 건 "빠르게"가 아니라 "충돌 없이"가 목적이다.
2. Subagent는 **컨텍스트(대화)**를 격리하고, Worktree는 **파일 시스템**을 격리한다 — 하나만 쓰면 반드시 구멍이 생긴다.
3. Explorer(읽기 전용) → Implement(별도 worktree, 쓰기 권한) → Verify(테스트만, 쓰기 제한)로 역할을 나누는 게 안전한 병렬 harness의 핵심이다.
