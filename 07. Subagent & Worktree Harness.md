# Claude Code Subagent & Worktree Harness — 핵심 요약

## 1. 가장 중요한 원칙

> **"병렬 처리(속도)"가 목표가 아니라 "격리(충돌 방지)"가 목표다.**

여러 세션/Subagent를 격리 없이 동시에 돌리면 다음이 발생한다.

* 같은 파일 동시 수정
* 같은 테스트 DB 결과 꼬임
* 같은 브랜치 충돌
* 세션 간 컨텍스트 혼입
* 머지 불가능한 변경사항 누적

---

## 2. Subagent — 컨텍스트(대화)를 격리

* Main 대화와 **분리된 별도 컨텍스트** 사용
* 필요한 **도구(tool)만** 접근 권한 부여
* Main 세션에는 조사 과정 전체가 아니라 **요약/최종 보고만** 전달 → Main 컨텍스트 오염 방지

### 역할 분리

| Subagent 역할 | 하는 일 | 권한 |
|---|---|---|
| **Explorer (탐색)** | 코드 탐색, 구조 파악 | 읽기 전용(read-only) |
| **Implement (구현)** | 실제 코드 수정 | 편집/쓰기 권한 |
| **Verify (검증)** | 테스트 실행, 변경 리뷰 | 테스트 실행만, 쓰기 제한 |

> 핵심: **모든 Subagent에게 모든 도구를 주지 않는다.**

---

## 3. Worktree — 파일 시스템을 격리

* `git worktree`로 **하나의 저장소에서 여러 작업 디렉터리** 생성
* 각 worktree가 **서로 다른 브랜치를 독립적으로 체크아웃**
* `.git` 히스토리는 공유, 디스크는 효율적으로 사용

### 왜 유용한가

* 세션 A: `bugfix/auth` worktree
* 세션 B: `refactor/network` worktree
* 세션 C: 실험 브랜치

→ Main worktree(baseline)는 오염되지 않고, 각 작업의 변경/테스트 결과를 서로 비교 가능

> **주의**: 같은 브랜치에서 여러 세션이 동시 편집하면 이는 병렬 처리가 아니라 혼란이다. 어떤 세션이 무엇을 바꿨는지 추적 불가능해진다.

---

## 4. 두 개를 함께 써야 진짜 "병렬 격리"

| | 격리 대상 | 혼자 쓸 때 문제 |
|---|---|---|
| **Subagent** | 컨텍스트(대화) | 대화는 깨끗해지지만 파일 충돌은 여전 |
| **Worktree** | 파일 시스템 | 파일은 분리되지만 역할이 섞일 수 있음 |

### 안전한 워크플로우

1. **탐색(Explorer)** — 읽기 전용, 관련 파일 요약
2. **구현(Implement)** — 별도 worktree에서 작은 단위 패치 생성
3. **검증(Verify)** — 변경분과 테스트 결과 리뷰

---

## 3줄 요약

1. Subagent와 Worktree를 여러 개 돌리는 목적은 "빠르게"가 아니라 "충돌 없이"다.
2. Subagent는 **컨텍스트(대화)**를, Worktree는 **파일 시스템**을 격리한다 — 하나만 쓰면 반드시 구멍이 생긴다.
3. Explorer(읽기 전용) → Implement(별도 worktree, 쓰기 권한) → Verify(테스트만, 쓰기 제한)로 역할을 나누는 것이 안전한 병렬 harness의 핵심이다.
