# Claude Code 제대로 쓰기: Plan-First, Thinking Mode, Sub-Agent

Claude Code 쓰다 보면 그냥 바로 "이거 고쳐줘" 던지고 싶은 유혹이 크다. 근데 강의 내용을 iOS 개발 관점으로 정리해보니, 이게 그냥 "천천히 신중하게 하자"는 뻔한 얘기가 아니라 실패 비용을 줄이는 구체적인 전략이더라. 정리해본다.

## 1. Plan-First: 바로 수정 들어가지 말고 Read-Only로 먼저 훑기

Claude에게 문제 주자마자 바로 코드 고치게 하면 당장은 빨라 보이지만, 복잡한 작업일수록 자주 실패한다. Anthropic 공식 문서에서도 Plan 모드를 **Read-Only 기반으로 코드베이스 분석해서 계획 세우는 모드**라고 설명하고, 복잡한 변경·코드베이스 탐색·안전한 리뷰에 적합하다고 안내한다.

iOS 개발자 입장에서 비유하면, TCA 기반 프로젝트에서 Reducer 하나 건드리기 전에 "이 State가 다른 Feature composition에 어떻게 물려있나" 파악 안 하고 바로 코드 치는 것과 비슷하다. 레거시 서비스 하나 깨지고 실패 로그도 여러 개, 관련 파일도 많은 상황에서 "다 고쳐줘"라고 바로 던지면 벌어지는 일은 뻔하다:

- 수정 범위가 계속 넓어짐
- 원인이랑 증상을 동시에 고치려고 함
- 필요 없는 파일까지 건드림

그래서 **작업 경계를 먼저 고정**하는 게 Plan-First다. 다음 세 가지 신호가 보이면 계획부터 세우는 걸 추천한다:

1. 영향 범위를 확신할 수 없을 때 (예: 이 State 하나 바꾸면 어디까지 리렌더링 영향 가는지 모를 때)
2. 여러 디렉토리·서비스가 얽혀 있을 때 (SPM 모듈 여러 개 걸쳐있는 변경)
3. 테스트가 약하거나 레거시라 수정 한 번이 연쇄 영향 만들 수 있을 때

주의할 점: Plan 모드 켜놓고도 대충 훑고 바로 수정으로 넘어가면 그건 계획이 아니라 그냥 워밍업이다. 진짜 Plan-First는 최소한 이 네 가지가 있어야 한다:

- 지금 해결할 문제 정의
- 가능한 원인 후보
- 수정 범위
- 테스트 검증 순서 + 반영 전에 멈춰서 확인할 지점

이게 없으면 Plan 모드 켰다고 해도 실제로는 여전히 즉흥적으로 움직이는 거다.

## 2. Sub-Agent: 역할 겹치면 만들지 말 것

Claude Code에 빌트인으로 들어있는 서브에이전트는 세 가지다: `explore`, `plan`, `general-purpose`. 필요하면 커스텀 서브에이전트도 만들 수 있다.

핵심은 **각 서브에이전트의 description이 곧 역할 경계**라는 점이다. Claude가 그 description을 보고 언제 위임할지 판단하기 때문에, 서브에이전트는 작업이 서로 섞이지 않게 만드는 구조적 장치라고 보면 된다.

iOS 쪽으로 치환하면 이건 딱 모듈 분리(SPM/Tuist) 원칙이랑 같다. Feature 모듈 경계가 애매한데 모듈부터 잘게 쪼개면 의존성만 꼬이는 것처럼, 서브에이전트도 **역할이 겹치면 만들지 말고, 역할이 분명할 때만 만든다.** 조사용/수정용/검토용처럼 역할이 뚜렷하면 분리 가치가 크지만, 작업 경계도 안 정해졌는데 서브에이전트부터 여러 개 만들면 혼란만 커진다. GitHub의 에이전틱 워크플로우 자료도 핵심은 "많이 돌리는 것"이 아니라 **작게 시작해서 측정하고 반복적으로 키우는 것**이라고 말한다. 처음부터 멀티 에이전트 구조를 크게 깔지 말고, 실패 지점 보면서 늘리라는 뜻이다.

## 3. Thinking Mode: 항상 켜두는 옵션이 아니라 불확실성 클 때만

Thinking Mode는 "더 똑똑한 답 받기" 용도가 아니다. **즉시 실행보다 판단 품질이 중요한 작업에 계산 자원을 더 쓰는 전략**이다. Anthropic 문서에서도 Thinking 계열은 복잡한 추론이랑 에이전틱 시스템 설계 맥락에서 다룬다.

유용한 상황:
- 영향 범위 넓은 수정
- 설계 대안 비교
- 원인 후보가 여러 개인 디버깅

반대로 로그 한 줄 파싱하거나 파일 경로 찾는 것 같은 단순 작업엔 과하다. iOS로 치면, print문 하나로 끝나는 버그에 Thinking Mode 켜는 건 간단한 크래시 로그 하나 보려고 Instruments 풀세팅 돌리는 거랑 비슷한 느낌이다. 반대로 SwiftUI 뷰 업데이트 사이클이 꼬여서 원인 후보가 여러 개인 상황(상태 관리 문제인지, 뷰 재구성 문제인지, TCA 리듀서 로직 문제인지)이면 Thinking Mode 켤 가치가 있다.

## 언제 뭘 쓸지 정리

- 문제 단순하고 수정 범위 명확 → 바로 실행
- 문제 복잡하고 범위 넓음 → Plan-First
- 작업 유형이 서로 다름 → Sub-Agent로 분리
- 결과 품질 중요한데 범위는 아직 애매함 → Thinking Mode

정리하면:
- **Plan-First**는 느린 게 아니라 실패 비용을 줄이는 방식
- **Thinking Mode**는 불확실성 큰 문제에 계산을 더 쓰는 전략
- **Sub-Agent**는 자동화용 장식이 아니라 작업 경계를 분리해서 결과 품질을 높이는 구조

---

### 3줄 요약
1. Plan-First는 복잡한 작업에서 바로 코드 고치지 말고 Read-Only로 먼저 원인·범위·검증 순서를 정하는 단계다.
2. Sub-Agent는 역할이 겹치면 만들지 말고, 역할이 분명할 때만 작게 시작해서 늘려가는 구조다.
3. Thinking Mode는 항상 켜두는 게 아니라 원인 후보가 여러 개거나 설계 판단이 필요한 불확실한 문제에만 쓴다.
