# Claude Code 제대로 쓰기: Plan-First / Thinking Mode / Sub-Agent — 핵심 요약

## 1. Plan-First — 바로 수정하지 말고 Read-Only로 먼저 훑기

Plan 모드는 **Read-Only 기반으로 코드베이스를 분석해서 계획을 세우는 모드**다. 복잡한 변경일수록 바로 수정에 들어가면 실패하기 쉽다.

### 계획부터 세워야 하는 신호 3가지

1. 영향 범위를 확신할 수 없을 때
2. 여러 디렉토리·서비스가 얽혀 있을 때
3. 테스트가 약하거나 레거시라 연쇄 영향이 우려될 때

### 진짜 Plan-First의 4가지 필수 요소

* 지금 해결할 문제 정의
* 가능한 원인 후보
* 수정 범위
* 테스트 검증 순서 + **반영 전에 멈춰서 확인할 지점**

> **주의**: Plan 모드를 켜놓고 대충 훑고 바로 수정으로 넘어가면 그건 계획이 아니라 워밍업일 뿐이다.

---

## 2. Sub-Agent — 역할이 겹치면 만들지 말 것

빌트인 서브에이전트 3종: `explore`, `plan`, `general-purpose` (커스텀도 가능)

> **핵심**: 서브에이전트의 **description이 곧 역할 경계**다. Claude는 이 description을 보고 언제 위임할지 판단한다.

* 역할이 겹치면 **만들지 않는다**
* 역할이 뚜렷할 때만(조사용/수정용/검토용) 분리 가치가 있다
* 작업 경계도 안 정해졌는데 서브에이전트부터 여러 개 만들면 혼란만 커진다

> **원칙**: 많이 돌리는 게 아니라 **작게 시작해서 측정하고 반복적으로 키운다.**

---

## 3. Thinking Mode — 항상 켜두는 옵션이 아니라 불확실성 클 때만

> **핵심**: "더 똑똑한 답"용이 아니라, **즉시 실행보다 판단 품질이 중요한 작업에 계산 자원을 더 쓰는 전략**이다.

### 유용한 상황

* 영향 범위 넓은 수정
* 설계 대안 비교
* 원인 후보가 여러 개인 디버깅

### 과한 상황

* 로그 한 줄 파싱, 파일 경로 찾기 같은 단순 작업

---

## 4. 언제 뭘 쓸지 정리

| 상황 | 선택 |
|---|---|
| 문제 단순, 수정 범위 명확 | 바로 실행 |
| 문제 복잡, 범위 넓음 | Plan-First |
| 작업 유형이 서로 다름 | Sub-Agent로 분리 |
| 결과 품질 중요한데 범위 애매 | Thinking Mode |

* **Plan-First**는 느린 게 아니라 실패 비용을 줄이는 방식
* **Thinking Mode**는 불확실성 큰 문제에 계산을 더 쓰는 전략
* **Sub-Agent**는 자동화용 장식이 아니라 작업 경계를 분리해 결과 품질을 높이는 구조

---

## 3줄 요약

1. Plan-First는 복잡한 작업에서 바로 코드 고치지 말고 Read-Only로 먼저 원인·범위·검증 순서를 정하는 단계다.
2. Sub-Agent는 역할이 겹치면 만들지 말고, 역할이 분명할 때만 작게 시작해서 늘려가는 구조다.
3. Thinking Mode는 항상 켜두는 게 아니라 원인 후보가 여러 개거나 설계 판단이 필요한 불확실한 문제에만 쓴다.
