# Claude Code를 제대로 쓰는 방법: Plan-First, Thinking Mode, Sub-Agent

AI 코딩 도구를 사용할 때 가장 흔한 실수 중 하나는 **문제를 받자마자 바로 코드를 수정하게 하는 것**이야.

간단한 작업이라면 이 방법도 괜찮아. 하지만 코드베이스가 크거나, 여러 파일과 모듈이 얽혀 있거나, 원인이 명확하지 않은 버그를 수정하는 상황에서는 오히려 문제가 커질 수 있어.

Claude Code를 사용할 때도 마찬가지야.

복잡한 작업일수록 바로 코드를 수정하기보다는 **먼저 분석하고, 작업 범위를 정하고, 필요하다면 역할을 나눠서 접근하는 것**이 훨씬 안정적이야.

이번 글에서는 이걸 위한 세 가지 개념인 **Plan-First, Thinking Mode, Sub-Agent**를 iOS 개발자의 관점에서 쉽게 정리해볼게.

---

## 1. Plan-First란?

Plan-First는 말 그대로 **코드를 수정하기 전에 먼저 계획을 세우는 방식**이야.

Claude에게 문제를 주자마자

> "이거 고쳐줘."

라고 하는 대신,

> "먼저 코드베이스를 분석하고 어떤 파일을 수정해야 하는지, 원인이 무엇인지, 어떤 순서로 수정하고 테스트할지 정리해줘."

라고 요청하는 거야.

iOS 개발자로 비유하면 익숙한 상황이야.

예를 들어 앱에서 특정 화면이 크래시 난다고 해보자.

단순한 문제라면 바로 해당 ViewController나 ViewModel을 수정하면 돼.

그런데 실제 프로젝트에서는 이런 상황일 수 있어.

```text
LoginViewController
    ↓
LoginViewModel
    ↓
AuthService
    ↓
TokenManager
    ↓
APIClient
    ↓
Network Layer
```

로그를 보니 인증 과정에서 에러가 발생하고 있는데 정확한 원인은 모르겠어.

이 상태에서 Claude에게

> "로그인 크래시 고쳐줘."

라고 하면 관련되어 보이는 코드를 여기저기 수정할 가능성이 있어.

그러면 원래 버그를 해결하는 게 아니라 **증상과 원인을 동시에 건드리면서 수정 범위가 커질 수 있어.**

그래서 먼저 분석하는 거야.

```text
문제 파악
   ↓
관련 코드 탐색
   ↓
원인 후보 정리
   ↓
수정 범위 결정
   ↓
수정 순서 결정
   ↓
테스트 방법 결정
   ↓
실제 코드 수정
```

이게 Plan-First의 핵심이야.

### Plan-First는 "생각을 많이 하자"가 아니다

여기서 중요한 포인트가 하나 있어.

Plan-First를 단순히 "Claude한테 생각 좀 해보라고 하는 것"으로 이해하면 안 돼.

핵심은 **코드를 수정하기 전에 작업 범위를 고정하는 것**이야.

예를 들어 좋은 계획이라면 최소한 다음 정도는 나와야 해.

* 지금 해결하려는 문제가 무엇인지
* 현재 코드에서 원인이 될 만한 부분이 무엇인지
* 어떤 파일과 모듈이 영향을 받는지
* 어떤 방식으로 수정할지
* 수정 후 어떤 테스트를 할지
* 실제 수정하기 전에 사람이 확인해야 할 부분은 무엇인지

이 정도가 있어야 "계획을 세웠다"고 볼 수 있어.

반대로 Claude가 프로젝트를 잠깐 훑어보고 바로 코드를 수정한다면 그건 Plan-First라기보다는 그냥 **워밍업 후 바로 실행한 것**에 가까워.

---

## 2. 언제 Plan-First를 사용해야 할까?

모든 작업에 Plan-First가 필요한 건 아니야.

오히려 간단한 작업까지 매번 계획을 세우면 불필요하게 느려질 수 있어.

다음과 같은 상황에서 특히 유용해.

### ① 영향 범위를 확신하기 어려울 때

예를 들어

> "이 API 응답 구조가 변경됐는데 어디까지 영향을 받는지 모르겠어."

이런 상황이야.

API 모델 하나를 변경하는 것처럼 보여도 실제로는

```text
DTO
 ↓
Repository
 ↓
UseCase
 ↓
ViewModel
 ↓
UI
 ↓
Unit Test
```

여러 계층에 영향을 줄 수 있어.

이럴 때는 먼저 전체적인 영향 범위를 확인하는 게 좋아.

### ② 여러 디렉토리나 모듈이 얽혀 있을 때

예를 들어 프로젝트가

```text
App
├── Feature
├── Domain
├── Data
├── Network
├── DesignSystem
└── Core
```

처럼 나뉘어 있다고 해보자.

하나의 기능을 수정했는데 여러 모듈에 영향을 줄 수 있다면 바로 코드를 수정하는 것보다 먼저 의존 관계를 파악하는 게 안전해.

### ③ 테스트가 부족한 레거시 코드일 때

개인적으로 이게 가장 중요하다고 봐.

테스트가 충분하지 않은 레거시 코드에서는 작은 코드 변경 하나가 예상하지 못한 곳에서 터질 수 있어.

예를 들어 공통 네트워크 코드 하나를 수정했는데 로그인, 결제, 프로필 등 전혀 다른 기능이 같이 깨질 수도 있지.

이런 코드베이스에서는

> "일단 수정하고 테스트 돌려보자."

보다

> "먼저 이 코드가 어디에서 사용되는지 확인하고 영향 범위를 정리하자."

가 훨씬 안전해.

---

# 3. Thinking Mode란?

Thinking Mode는 조금 다른 개념이야.

쉽게 말하면 **답을 빨리 내는 것보다 더 깊게 판단하는 데 계산 자원을 쓰는 방식**이라고 보면 돼.

여기서 중요한 건 Thinking Mode를 항상 켜놓는 게 아니라는 거야.

문제가 단순하다면 굳이 필요 없어.

예를 들어

> "이 파일의 경로 찾아줘."

> "이 로그에서 HTTP status code가 뭐야?"

> "이 배열을 정렬하는 코드 작성해줘."

이런 작업은 복잡한 추론이 필요하지 않아.

반면 이런 문제는 달라.

> "이 크래시의 원인이 여러 개 있을 수 있는데 가장 가능성 높은 원인을 찾아줘."

> "현재 아키텍처에서 이 기능을 추가할 때 어떤 설계가 가장 적합한지 비교해줘."

> "이 변경이 다른 모듈에 어떤 영향을 줄 수 있는지 분석해줘."

이런 작업은 단순히 코드를 찾는 것보다 **여러 가능성을 비교하고 판단하는 과정**이 중요해.

이럴 때 Thinking Mode가 유용해.

---

## 4. Plan-First와 Thinking Mode의 차이

둘이 비슷해 보여서 헷갈릴 수 있는데 목적이 조금 달라.

### Plan-First

**"무엇을 어떻게 수정할 것인가?"**

에 초점이 있어.

```text
문제
 ↓
범위 파악
 ↓
작업 계획
 ↓
수정
 ↓
검증
```

즉, **작업을 구조화하는 것**이 핵심이야.

### Thinking Mode

**"이 문제를 어떻게 판단하는 게 가장 정확할까?"**

에 초점이 있어.

예를 들어 버그 원인이

```text
A일 수도 있음
B일 수도 있음
C일 수도 있음
```

이라면 각각의 가능성을 비교하면서 가장 합리적인 원인을 찾아야 해.

즉, **판단의 품질을 높이는 것**이 핵심이야.

---

# 5. Sub-Agent란?

Sub-Agent는 말 그대로 **하나의 큰 작업을 여러 역할로 나눠서 처리하는 방식**이야.

Claude Code에는 기본적으로 여러 용도의 Sub-Agent가 있고, 필요하다면 커스텀 Sub-Agent도 만들 수 있어.

대표적으로 생각해볼 수 있는 역할은 이런 식이야.

```text
Explore
→ 코드베이스 조사

Plan
→ 작업 계획 수립

General-purpose
→ 일반적인 작업 수행
```

여기서 중요한 건 Sub-Agent를 많이 만드는 게 아니라 **역할을 명확하게 나누는 것**이야.

---

## 6. Sub-Agent는 iOS 개발에서 이렇게 생각하면 쉽다

iOS 프로젝트에서 하나의 거대한 작업을 진행한다고 해보자.

예를 들어

> "기존 결제 시스템을 새로운 결제 SDK로 교체해줘."

이건 단순한 코드 수정이 아니야.

확인해야 할 게 많아.

```text
기존 결제 코드 조사
        ↓
SDK 사용 위치 조사
        ↓
의존 관계 분석
        ↓
마이그레이션 계획
        ↓
코드 수정
        ↓
테스트
        ↓
회귀 검증
```

이걸 하나의 Agent에게 전부 맡기는 것보다 역할을 분리할 수 있어.

```text
[Explore Agent]
기존 결제 코드와 의존 관계 조사

        ↓

[Plan Agent]
조사 결과를 기반으로 마이그레이션 계획 작성

        ↓

[Implementation Agent]
실제 코드 수정

        ↓

[Review Agent]
변경된 코드 검토
```

이렇게 하면 각 Agent가 자기 역할에 집중할 수 있어.

---

# 7. Sub-Agent를 많이 만들면 좋은 걸까?

그건 아니야.

오히려 **역할이 겹치면 복잡해질 수 있어.**

예를 들어

```text
BugFinder
CodeExplorer
CodeAnalyzer
BugAnalyzer
DebugExplorer
DebugAnalyzer
```

처럼 비슷한 역할의 Agent를 잔뜩 만들어 놓으면 오히려 어떤 Agent에게 어떤 작업을 맡겨야 할지 애매해져.

그래서 중요한 원칙은 간단해.

> **역할이 명확하지 않다면 Sub-Agent를 만들지 않는다.**

예를 들어

* 조사
* 구현
* 리뷰

처럼 역할이 명확하게 분리된다면 Sub-Agent를 사용하는 가치가 커.

반대로 아직 문제 자체가 명확하지 않은 상태에서 Sub-Agent부터 여러 개 만드는 건 별로 도움이 안 돼.

---

# 8. Sub-Agent의 설명이 중요한 이유

커스텀 Sub-Agent를 만들 때 중요한 부분이 있어.

바로 **Agent의 설명 자체가 역할의 경계가 된다**는 거야.

Claude는 Sub-Agent의 설명을 보고 어떤 상황에서 해당 Agent에게 작업을 맡길지 판단할 수 있어.

그래서 단순히

```text
Code Agent
```

라고 만드는 것보다

```text
iOS 코드 변경 사항을 리뷰하고,
잠재적인 회귀 문제와 테스트 누락을 찾는 역할
```

처럼 역할을 명확하게 정의하는 게 좋아.

결국 Sub-Agent는 단순히 "AI를 여러 개 띄우는 기능"이 아니라 **작업의 책임 범위를 분리하는 장치**라고 이해하면 돼.

---

# 9. 세 가지를 어떻게 조합해서 사용할까?

이제 Plan-First, Thinking Mode, Sub-Agent를 각각 언제 사용하는지 정리해보자.

### 단순한 문제

문제가 명확하고 수정 범위도 작다면 바로 실행하면 돼.

```text
간단한 문제
→ 바로 실행
```

예를 들어 특정 파일의 오타 수정이나 간단한 리팩터링 같은 작업이야.

---

### 복잡하고 영향 범위가 넓은 문제

이럴 때는 Plan-First가 적합해.

```text
복잡한 문제
→ 코드베이스 탐색
→ 영향 범위 확인
→ 계획 수립
→ 구현
→ 테스트
```

---

### 서로 다른 종류의 작업이 섞여 있는 경우

Sub-Agent로 역할을 나누는 게 좋아.

```text
조사 → Explore Agent

계획 → Plan Agent

구현 → Implementation Agent

검토 → Review Agent
```

---

### 결과의 품질은 중요한데 판단이 어려운 경우

Thinking Mode를 활용할 수 있어.

```text
원인 후보가 여러 개
→ 깊게 분석
→ 가능성 비교
→ 가장 적절한 해결책 선택
```

---

# 10. 세 가지를 한 번에 이해하기

세 가지를 하나의 개발 작업으로 묶어서 생각하면 더 이해하기 쉬워.

예를 들어 레거시 iOS 프로젝트에서 크래시를 해결한다고 해보자.

### Step 1. Plan-First

먼저 코드를 수정하지 않고 분석해.

```text
"현재 크래시의 원인을 분석하고
관련 코드와 영향 범위를 확인한 뒤
수정 계획과 테스트 계획을 작성해줘."
```

여기서 작업 범위를 정해.

---

### Step 2. Thinking Mode

원인이 여러 개라면 더 깊게 분석해.

```text
"현재 발견된 원인 후보들을 비교하고
각각의 가능성과 근거를 분석해줘."
```

단순히 "이 코드가 문제입니다"라고 결론내리는 게 아니라 근거를 가지고 판단하게 하는 거야.

---

### Step 3. Sub-Agent

작업 자체가 여러 종류로 나뉜다면 역할을 분리해.

```text
Explore
→ 관련 코드 조사

Plan
→ 수정 계획 수립

Implementation
→ 코드 수정

Review
→ 변경 사항 검토
```

이렇게 하면 하나의 Agent가 조사부터 구현, 검증까지 전부 뒤섞어서 처리하는 걸 방지할 수 있어.

---

# 11. 결국 중요한 건 "AI에게 얼마나 많이 시키느냐"가 아니다

Claude Code 같은 Agent 기반 코딩 도구를 처음 사용하면 이런 생각을 하기 쉬워.

> "AI가 코드를 잘 작성하니까 최대한 많은 작업을 한 번에 시키는 게 효율적이지 않을까?"

그런데 복잡한 작업에서는 오히려 반대일 수 있어.

**큰 문제를 한 번에 던지는 것보다 작업을 적절하게 분리하는 게 더 빠를 수 있어.**

왜냐하면 잘못된 방향으로 수정했을 때 발생하는 비용이 크기 때문이야.

```text
잘못된 수정
    ↓
불필요한 코드 변경
    ↓
Diff 증가
    ↓
테스트 실패
    ↓
원상복구
    ↓
다시 분석
```

이 과정 자체가 시간을 많이 잡아먹어.

그래서 처음에 몇 분 정도 분석하고 계획을 세우는 것이 결과적으로는 더 빠를 수 있어.

---

# 12. 실무에서 사용할 때의 기준

개인적으로는 다음처럼 기억하면 가장 간단해.

| 상황                 | 사용할 것             |
| ------------------ | ----------------- |
| 단순하고 범위가 명확함       | 바로 실행             |
| 영향 범위를 모르겠음        | Plan-First        |
| 여러 모듈과 코드가 얽혀 있음   | Plan-First        |
| 레거시 코드 + 테스트 부족    | Plan-First        |
| 원인 후보가 여러 개임       | Thinking Mode     |
| 설계 대안을 비교해야 함      | Thinking Mode     |
| 조사/구현/리뷰처럼 역할이 분명함 | Sub-Agent         |
| 역할이 애매함            | Sub-Agent를 만들지 않음 |

이 표 하나만 기억해도 충분해.

---

# 마무리

결국 세 가지 개념은 각각 목적이 달라.

**Plan-First**는 **작업 범위를 정하고 실행 전에 계획을 세우는 방식**이야.

**Thinking Mode**는 **불확실성이 높은 문제에서 더 깊게 판단하도록 계산 자원을 사용하는 방식**이고.

**Sub-Agent**는 **조사, 구현, 리뷰처럼 서로 다른 작업의 책임 범위를 분리하는 방식**이야.

중요한 건 세 가지를 무조건 사용하는 게 아니야.

오히려 상황에 따라 적절하게 선택하는 게 중요해.

```text
단순한 문제
→ 바로 실행

복잡하고 영향 범위가 큼
→ Plan-First

판단이 어렵고 불확실성이 큼
→ Thinking Mode

작업 역할이 명확하게 나뉨
→ Sub-Agent
```

결국 Claude Code를 잘 사용한다는 건 AI에게 일을 많이 시키는 게 아니라 **문제를 적절한 크기로 나누고, 각 단계에서 필요한 수준의 사고와 실행을 선택하는 것**에 가깝다고 볼 수 있어.

### 3줄 요약

* **Plan-First**: 복잡한 작업은 바로 수정하지 말고 먼저 영향 범위와 수정 계획을 정해.
* **Thinking Mode**: 원인이 여러 개거나 설계 판단이 어려운 문제처럼 불확실성이 높을 때 사용해.
* **Sub-Agent**: 조사·구현·리뷰처럼 역할이 명확하게 나뉘는 작업을 분리해서 결과 품질을 높여.
