# Part 5. Tool Harness

---

# 9장. 개입 지점과 5가지 확장 메커니즘

> **"무엇을 더 붙일까?"가 아니라 "에이전트 루프의 어디에 개입할까?"**

Tool을 기능 목록으로 외우지 말고 **개입 지점**으로 이해한다.

```text
assemble()  모델이 무엇을 볼 것인가          → Skills
model()     모델이 무엇을 호출할 수 있는가    → MCP
execute()   실행 전후에 무엇을 강제할 것인가  → Hooks / Permissions
```

## 9.1 Skills — 반복되는 작업 절차

특정 작업을 일관된 방식으로 수행하게 만드는 **재사용 가능한 절차/지침.**

적합한 것: 디버깅 절차 · 화면 생성 규칙 · Crash 분석 절차 · PR 작성 형식 · 반복적인 개발 workflow

> **외부 시스템이 필요 없는 반복 절차 → Skill**

`CLAUDE.md`에 모든 규칙을 몰아넣는 것보다, 특정 작업에만 필요한 절차는 Skill로 분리한다.

> 💡 **실물 — `.claude/skills/debug-report/SKILL.md`** (원본 강의 밖 보충 · 공식 문서 기준)
> 8장의 디버깅 5단계를 Skill로 만든 것. 디렉터리 이름이 곧 `/debug-report` 명령이 된다.
>
> ```markdown
> ---
> description: 버그를 Symptom/Evidence/검증경로로 구조화한다. 사용자가 에러·크래시·테스트 실패를 들고 올 때 사용.
> allowed-tools: Read Grep Glob
> ---
>
> ## 현재 변경사항
>
> !`git diff HEAD --stat`
>
> ## 지시
>
> 먼저 고치지 말고 아래 순서로만 답한다.
> 1. **증상**: 무엇이 언제 실패하는가 (한 문장)
> 2. **증거**: 제공된 로그에서 확인 가능한 사실만
> 3. **원인 후보**: 가능성 순 최대 3개, 각각 근거와 함께
> 4. **확인 방법**: 각 후보를 확인할 최소 명령어
> 5. **반례**: 1순위 가설이 틀릴 수 있는 조건
> ```
>
> | 필드 | 역할 |
> |---|---|
> | `description` | Claude가 **언제 이 Skill을 꺼낼지** 판단하는 근거 — 가장 중요 |
> | `allowed-tools` | 이 Skill이 도는 턴 동안 승인 없이 쓸 도구 (10장의 "절차·범위를 함께 제한") |
> | `disable-model-invocation: true` | Claude가 스스로 꺼내지 못하게 하고 `/이름`으로만 실행 (배포 스킬 등) |
>
> `` !`git diff HEAD` ``는 **동적 컨텍스트 주입** — Claude가 읽기 전에 명령이 실행되어 결과가 그 자리에 박힌다. Skill 하나로 3장의 Context Pack을 자동으로 채우는 방법.
> 위치: 개인 `~/.claude/skills/<이름>/SKILL.md` · 프로젝트 `.claude/skills/<이름>/SKILL.md`

## 9.2 MCP — 외부 시스템 연결

Claude가 외부 시스템을 실제로 **조회·조작**할 수 있게 Tool을 제공한다. (GitHub, Jira, DB, 모니터링, 사내 API)

> **실제 외부 데이터·시스템 접근이 필요하다 → MCP**

⚠️ 붙이는 순간 **Trust Boundary가 넓어진다.**
Prompt Injection · Tool Poisoning · Over-sharing · 과도한 Write 권한 · Secret 노출 · 불필요하게 큰 Output

→ 기능만 연결하는 게 아니라 **Scope + Permission + 신뢰도 + 데이터 범위**까지 설계한다.

> 💡 **실물 — 프로젝트 루트 `.mcp.json`** (원본 강의 밖 보충)
>
> ```json
> {
>   "mcpServers": {
>     "github": {
>       "type": "http",
>       "url": "https://api.githubcopilot.com/mcp/",
>       "headers": { "Authorization": "Bearer ${GITHUB_PAT}" }
>     },
>     "database": {
>       "command": "npx",
>       "args": ["-y", "@bytebase/dbhub"],
>       "env": { "DB_URL": "${DB_URL}" }
>     }
>   }
> }
> ```
>
> 원격은 `type` + `url`(+`headers`), 로컬 프로세스는 `command` + `args`(+`env`). **`${VAR}` 확장이 되므로 토큰을 파일에 직접 쓰지 않는다.**
> CLI로도 된다: `claude mcp add --scope project --transport http github https://api.githubcopilot.com/mcp/`
>
> **붙였으면 반드시 범위를 좁힌다** — 도구 이름은 `mcp__<서버>__<도구>` 형식이다.
>
> ```json
> { "permissions": {
>     "allow": ["mcp__github__get_*"],
>     "ask":   ["mcp__github__create_pull_request"],
>     "deny":  ["mcp__github__delete_*"] } }
> ```

## 9.3 Hooks — 시스템이 강제하는 규칙

> **Skill = 지켜달라고 알려주는 것 / Hook = 실행 시점에 시스템이 강제하는 것**

| 이벤트 | 시점 |
|---|---|
| `PreToolUse` | 실행 **전** 검사 / 차단 |
| `PostToolUse` | 실행 **후** 포매터·린터 등 후처리 |
| `Stop` | 종료 전 검증 |
| `SessionStart` | 세션 시작 시 필요한 정보 주입 |
| `PreCompact` | Context 압축 전 중요 정보 보존 |

⚠️ **막아야 하는 것은 `PreToolUse`.** `PostToolUse`는 이미 실행된 뒤라 **실행 자체를 되돌릴 수 없다.**

> 💡 **실물 — `.env` 수정을 실제로 막는 훅** (원본 강의 밖 보충)
>
> `.claude/settings.json`
>
> ```json
> {
>   "hooks": {
>     "PreToolUse": [
>       { "matcher": "Edit|Write",
>         "hooks": [{ "type": "command",
>                     "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/protect.sh" }] }
>     ]
>   }
> }
> ```
>
> `.claude/hooks/protect.sh` (`chmod +x` 필요) — 훅은 stdin으로 JSON을 받는다.
>
> ```bash
> #!/bin/bash
> FILE=$(jq -r '.tool_input.file_path // empty')
> case "$FILE" in
>   *.env|*/.env.*|*/secrets/*)
>     jq -n --arg f "$FILE" '{hookSpecificOutput: {
>        hookEventName: "PreToolUse",
>        permissionDecision: "deny",
>        permissionDecisionReason: ("보호된 파일: " + $f)}}' ;;
>   *) exit 0 ;;   # 판단 없음 → 평소 권한 흐름대로
> esac
> ```
>
> | 차단 방법 | 동작 |
> |---|---|
> | stdout에 `permissionDecision: "deny"` | 차단 + 이유를 Claude에게 전달 |
> | **`exit 2`** (stderr가 이유가 됨) | 차단. **allow 규칙보다도 먼저 걸린다** |
>
> → `Bash`를 전부 allow해두고 위험한 것만 훅으로 막는 구성도 가능하다. 이게 "정말 강제해야 하면 Skill이 아니라 Hook"의 실물이다.
>
> **`PostToolUse` 후처리 예** — 저장 후 포매팅 (되돌리기 용도로는 못 쓴다)
>
> ```json
> { "hooks": { "PostToolUse": [
>     { "matcher": "Edit|Write",
>       "hooks": [{ "type": "command",
>                   "command": "npx prettier --write \"$CLAUDE_FILE_PATHS\"" }] } ] } }
> ```
>
> `matcher`는 `Bash`, `Edit|Write` 같은 도구 이름이거나 정규식(`mcp__github__.*`)이다.

## 9.4 Permissions — 행동 범위 자체를 제한

위험한 파일 접근 차단 · 특정 명령 실행 제한 · 민감 경로 제한 · 자동 실행 가능 범위 설정

> **Permissions = 할 수 있는 행동의 경계**
> **Hooks = 행동이 실행되는 시점의 검사/강제**

둘은 대체 관계가 아니라 **서로 다른 방어 레이어**다. (→ 6장 Layered Control)

## 9.5 Plugins — 하네스 구성 요소의 패키징

```text
Plugin ├── Settings ├── Skills ├── Hooks └── Agents
```

팀 전체에 동일한 AI 개발 환경과 정책을 배포할 때 쓴다.

## 9.6 선택 기준

| 목적 | 사용 | iOS 개발자식 |
|---|---|---|
| 반복 절차 재사용 | **Skills** | Code Snippet / 팀 절차 / 체크리스트 |
| 외부 시스템·데이터 연결 | **MCP** | 외부 SDK / API 통합 |
| 실행 전후 규칙 강제 | **Hooks** | Build Phase Run Script / Git Hook |
| 행동 범위 제한 | **Permissions** | App Sandbox / Entitlement |
| 묶어서 배포 | **Plugins** | Swift Package |

## 9.7 안티패턴 3

❌ **모든 것을 MCP로** — 외부 접근이 필요 없는 반복 절차면 Skill로 충분하다. 네트워크가 필요 없는 작업에 네트워크 레이어를 추가하지 않는다.

❌ **외부 데이터를 Skill로** — Skill은 절차를 알려줄 뿐 **데이터를 가져오지 못한다.** 외부 데이터가 필요하면 MCP.

❌ **강제 규칙을 Skill에만** — "반드시 테스트 후 종료하세요"를 Skill에만 적으면 잊거나 무시할 수 있다. 정말 강제해야 하면 **Hook / Permission으로.**

> 가장 중요한 질문: **"이 기능이 무엇을 할 수 있지?"가 아니라 "에이전트 루프의 어느 지점에 개입하지?"**

---
---

# 10장. Skills vs Tool use

> **Skills = "어떻게 일할지" 재사용 / Tool use = "무엇으로 실행할지" 확장**

| 구분 | Skills | Tool use |
|---|---|---|
| 정의 | 반복 가능한 워크플로우/전문성 추가 | 도구 호출로 실제 작업 수행 |
| 비유 | 컨벤션 문서 + PR 템플릿 + 배포 체크리스트 | 네트워크 레이어 / 외부 API 클라이언트 |
| 해결 문제 | 절차가 지정 안 된 문제 | 모델 혼자 할 수 없는 실행 문제 |

> 컨벤션 문서를 잘 써놔도 서버 통신은 안 되고, 네트워크 레이어가 좋아도 코드 스타일은 제각각이다 — **애초에 다른 문제를 푼다.**

## 10.1 Skills — 절차는 반복되는데 실행 능력은 불필요할 때

**사용자 요청을 특정 작업 절차로 번역하는 전문가 계층.** 설명 + 절차 + 허용 도구 제한(`allowed_tools`)까지 포함해 **절차·범위·컨텍스트를 함께 제한**한다.

저장하기 좋은 것: 버그 리포트 정리 · 로그 요약 · PR 설명 초안 · 배포 전 체크리스트 · 레거시 코드 읽는 절차 · 프레임워크용 디버깅 루틴

> 새 실행 능력이 필요한 게 아니라, **좋은 절차를 매번 다시 설명하지 않게 하는 문제**다.

## 10.2 Tool use — 모델 혼자 못 하는 실행이 필요할 때

외부 시스템 조회, 파일 시스템 밖 실행 주체 연결, 병렬 도구 호출. **Single / Parallel / Sequential** 패턴으로 오케스트레이션하며, **코드가 도구를 호출해 필요한 정보만 컨텍스트에 올린다.**

대표 케이스: 이슈 트래커 조회 · 외부 문서 검색 · 사내 시스템 상태 확인 · 다중 툴 병렬 실행 · 코드 외부 데이터 접근

> Skills에 설명을 아무리 길게 적어도 Claude는 실행을 못 한다 — **절차 설명과 실행 능력은 완전히 별개.**

## 10.3 선택 기준

| 상황 | 판단 |
|---|---|
| 같은 설명을 3번 이상 반복 중 | **Skills** 후보 |
| 절차는 아는데 외부 실행 능력이 없어 멈춤 | **Tool use** 후보 |
| 둘 다 필요 | Skills가 절차, Tool use가 실행 — 조합이 안정적 |

⚠️ 절차 문제를 실행 문제로 착각하면 **과설계**, 실행 문제를 절차 문제로 착각하면 **Claude는 그냥 멈춘다.**

> 절차를 먼저 Skills로 표준화 → 필요한 만큼만 Tool use를 붙이는 순서가 안정적이다. **작게 시작해 실제 사용을 보며 확대**하고, 절차가 안정되지 않은 채 도구만 늘리면 유지보수 비용만 커진다.
