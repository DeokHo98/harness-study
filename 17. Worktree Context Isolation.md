# Claude Code — Worktree로 병렬 세션 격리하기 핵심 요약

## 1. 가장 중요한 원칙

> **Worktree는 Git 기능이 아니라 "컨텍스트 + 파일 시스템을 함께 격리하는 도구"다.**

같은 디렉토리에서 세션을 여러 개 띄우면 대화창은 분리되어도 **파일 시스템은 공유**되어 다음 문제가 생긴다.

* 세션 A가 고친 파일을 세션 B가 모르는 채로 또 건드림 (파일 충돌)
* diff가 뒤섞여 어떤 작업 때문인지 추적 불가
* 테스트 결과가 어떤 세션의 변경 때문인지 불명확
* 리뷰·머지가 지옥이 됨

> **주의**: Worktree는 충돌을 없애는 마법이 아니라, 충돌이 **"언제, 얼마나 비싸게" 일어날지 통제하는 도구**다. 실시간 충돌 대신 머지 시점에 정리된 형태로 충돌을 마주하게 해준다.

---

## 2. 격리는 3계층 — 메모리 계층이 함정

| 계층 | 제공 주체 | 격리 상태 |
|---|---|---|
| **대화(컨텍스트) 격리** | 별도 세션/서브 에이전트 | 세션만 나눠도 자연스럽게 됨 |
| **파일 시스템 격리** | Worktree | Worktree가 직접 제공 (diff/충돌 분리) |
| **메모리 격리** | `CLAUDE.local.md` | ⚠️ **불완전** |

> **핵심 함정**: `CLAUDE.local.md`는 Worktree별로 분리 가능하지만, **오토메모리(auto memory)는 같은 Git 레포지토리의 모든 Worktree/서브디렉토리가 하나의 디렉토리를 공유**한다. 한쪽 Worktree에서 학습한 게 다른 Worktree 작업에 은근슬쩍 영향을 줄 수 있다.

---

## 3. 실제 명령어 워크플로우

1. `claude --worktree` → Worktree 자동 생성, 독립 브랜치에서 세션 시작
2. `git status`, `git remote set-head origin -a` → 기준점(origin 헤드) 재동기화
3. Worktree 관련 경로를 `.gitignore`에 추가
4. 작업 완료 후 `worktree remove`로 개별 제거

### 작업 쪼개기 예시

* `auth-validate` Worktree → 로그인 입력 검증만 담당
* `auth-errors` Worktree → 로그인 실패 에러 메시지 구조만 담당

→ 각 Worktree는 **딱 하나의 책임(concern)만** 지도록 스코프를 명확히 나눈다.

---

## 4. 진짜 중요한 건 생성이 아니라 머지

### 원칙

* 각 Worktree는 자기 diff와 자기 테스트를 **스스로 책임**진다
* 메인 Worktree에서 **순차적으로** 머지 (동시에 우르르 합치지 말 것)
* 충돌 발생 시 **Claude에게 바로 "고쳐줘"라고 시키지 말 것** — 한쪽을 기계적으로 덮어쓸 위험

### 충돌 해결 프롬프트 순서 (필수)

1. **원인 분석**: 왜 충돌이 났는지, 양쪽이 같은 로직 영역을 어떻게 다르게 고쳤는지 설명시키기
2. **머지 계획 제시**: 어떻게 합칠지 계획을 먼저 받기
3. **계획대로 수정 + 테스트**: 계획에 따라 수정 후 관련 테스트 실행

> 이 순서를 지켜야 "아무거나 하나 골라서 덮어쓰기" 같은 사고를 방지할 수 있다.

---

## 3줄 요약

1. Worktree는 단순 Git 기능이 아니라 **파일 시스템 + 컨텍스트를 격리해서 병렬 세션 충돌을 막는 장치**로 써야 한다.
2. 격리는 대화(세션) / 파일 시스템(Worktree) / 메모리 3계층인데, **오토메모리는 같은 레포 안에서 공유되므로 완전히 격리되지 않는다**는 점을 꼭 기억해야 한다.
3. Worktree 생성은 쉽지만 진짜 관건은 **머지**이고, 충돌 시엔 바로 수정시키지 말고 **원인 분석 → 머지 계획 → 수정/테스트** 순서로 진행해야 한다.
