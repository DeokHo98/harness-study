# Claude Code에서 Worktree로 병렬 세션 격리하기

Claude Code 강의 중에 "컨텍스트 하네스의 격리 설계"랑 "Worktree, 병렬 세션" 파트를 듣고 정리해봤다. 처음엔 그냥 서브 에이전트나 Worktree 기능 소개인 줄 알았는데, 실제로는 **Claude Code로 여러 작업을 동시에 돌릴 때 컨텍스트가 어떻게 섞이고, 그걸 어떻게 막을 것인가**에 대한 이야기라서 생각보다 중요한 내용이었다. iOS 개발자 입장에서 이해한 대로 풀어봄.

## 문제 상황: 같은 디렉토리에서 세션 여러 개 띄우면 생기는 일

Claude Code 세션을 하나의 프로젝트 디렉토리에서 여러 개 띄운다고 해보자. 대화창(세션)은 서로 분리되어 있지만, **파일 시스템은 공유**된다. Xcode 프로젝트 하나 열어놓고 터미널 탭 여러 개에서 Claude를 동시에 돌리는 상황을 생각하면 됨.

이러면 이런 문제가 생긴다:

- 세션 A가 고친 파일을 세션 B가 모르는 상태에서 또 건드림 → 파일 충돌
- diff가 뒤섞여서 "이 변경이 어떤 작업 때문에 생긴 건지" 추적이 안 됨
- 테스트 결과가 어떤 세션의 변경사항 때문에 통과/실패했는지 불명확해짐
- 나중에 리뷰하고 머지하기가 지옥이 됨

즉 세션은 나눴는데 작업 공간(파일)은 안 나눈 셈이라 의미가 없다는 거다.

## 해결책: Worktree = Git 기능이 아니라 "격리 장치"

여기서 강조하는 포인트가 핵심인데, **Git Worktree를 단순히 "브랜치별 폴더 따로 쓰는 기능"이 아니라 컨텍스트와 파일 시스템을 함께 격리하는 도구**로 쓰라는 거다.

Worktree를 쓰면:

- 세션 A, B가 각자 자기만의 파일 시스템(작업 디렉토리)을 가짐
- 각자 다른 Git 브랜치에서 작업
- diff도 자연스럽게 분리됨
- 테스트 소유권도 명확해짐 (이 테스트 결과는 이 worktree, 이 브랜치 거다라고 확실히 말할 수 있음)

중요한 포인트: **Worktree는 충돌을 아예 없애주는 마법 도구가 아니다.** 충돌이 "언제, 얼마나 비싸게" 일어날지를 통제하는 도구라고 보면 된다. 즉 실시간으로 파일 뒤섞이면서 나는 충돌 대신, 나중에 머지 시점에 한 번에 정리된 형태로 충돌을 마주하게 해준다는 뜻.

## 격리는 3계층으로 나뉜다

강의에서 격리를 세 가지 레이어로 정리해줌:

### 1. 대화(컨텍스트) 격리
각 세션이 독립된 Claude 세션 = 독립된 컨텍스트 윈도우 = 독립된 대화 히스토리를 가짐. 이건 그냥 세션을 따로 여는 것만으로도 자연스럽게 됨.

### 2. 파일 시스템 격리
각 세션이 Worktree 안에서 독립적으로 파일을 수정하니까 충돌이 안 나고, diff도 독립적으로 관리됨. 각자 독립된 작업 디렉토리를 갖는 셈. 이게 Worktree가 직접 제공해주는 부분.

### 3. 메모리 격리 (⚠️ 여기가 함정)
`CLAUDE.local.md` 같은 Worktree별 메모리 파일은 Worktree마다 분리해서 쓸 수 있음. 실험이나 개인적인 테스트를 여기 기록해두면 격리된 상태로 유지됨.

**근데 "오토메모리(auto memory)"는 다르다.** Worktree를 여러 개 만들었다고 해서 Claude의 오토메모리까지 완전히 분리되는 게 아님. 같은 Git 레포지토리 안에 있는 모든 Worktree와 서브디렉토리는 **하나의 오토메모리 디렉토리를 공유**한다. 그래서 한쪽 Worktree에서 Claude가 뭔가를 학습하면, 그게 다른 Worktree 작업에도 은근슬쩍 영향을 줄 수 있음.

정리하면:
- 파일 시스템 격리 → Worktree가 제공
- 대화 컨텍스트 격리 → 별도 세션/서브 에이전트가 제공
- 메모리 격리 → **완전하지 않음.** `CLAUDE.md`나 오토메모리 공유 여부를 명시적으로 신경 써서 관리해야 함

## 실제 명령어 워크플로우

`claude --worktree` 명령으로 터미널 여러 개에서 각각 독립된 세션 + Worktree를 동시에 병렬로 띄울 수 있음. 흐름은 대략 이렇다:

1. `claude --worktree` 로 Worktree 생성 → Claude Code가 자동으로 Worktree 이름 짓고 독립 브랜치에서 세션 시작
2. `git status`, `git remote set-head origin -a` 로 기준점 확인 (로컬에서 보고 있는 origin 헤드를 원격 기준으로 재동기화하는 작업)
3. Worktree 관련 경로를 `.gitignore`에 추가
4. 작업 끝나면 `worktree remove` 로 각각 독립적으로 제거

### 예시: 작업 쪼개기

로그인 기능 작업을 한다고 치면:

- `auth-validate` Worktree → 로그인 입력 검증(validation)만 담당
- `auth-errors` Worktree → 로그인 실패 시 에러 메시지 구조 개선만 담당

이렇게 서로 다른 관심사(concern)로 명확히 쪼개서 각 Worktree가 딱 하나의 책임만 지도록 하는 게 포인트. iOS로 치면 "이 브랜치는 네트워크 레이어만", "이 브랜치는 UI 바인딩만" 이런 식으로 스코프를 딱 정해주는 거랑 같음.

## 진짜 중요한 건 생성이 아니라 머지

Worktree 만드는 거 자체는 쉬움. 진짜 어려운 건 **마지막에 머지하는 순간**이다.

원칙:
- 각 Worktree는 자기 diff와 자기 테스트를 스스로 책임진다
- 메인 Worktree에서 **순차적으로** 머지를 진행해야 한다 (동시에 우르르 합치면 안 됨)
- 충돌이 발생하면 **Claude한테 바로 고치라고 시키면 안 됨.** 먼저 원인 분석이랑 머지 계획부터 요구해야 함

### 충돌 해결 프롬프트 순서 (중요)

바로 "이거 고쳐줘"라고 하면 Claude가 기계적으로 한쪽 변경사항을 덮어써버릴 위험이 있음. 그래서 순서를 이렇게 가져가야 함:

1. **원인 분석**: 왜 충돌이 났는지, 양쪽 변경이 같은 로직 영역을 각각 어떻게 다르게 고쳤는지 설명하게 시킨다
2. **머지 계획 제시**: 어떻게 합칠지 계획을 먼저 받는다
3. **계획대로 수정 + 테스트**: 계획에 따라 수정하고 관련 테스트까지 돌리게 한다

이 순서를 지켜야 "일단 아무거나 하나 골라서 덮어쓰기" 같은 사고를 방지할 수 있음.

## 병렬 작업 체크리스트

작업 시작 전 / 진행 중 / 완료 후, 이렇게 세 타이밍마다 한 번씩 체크리스트를 확인하는 습관을 들이는 게 좋다고 함. (구체적인 체크리스트 항목은 강의에서 별도로 다룰 걸로 보임)

---

## 3줄 요약

1. Worktree는 단순 Git 기능이 아니라 **파일 시스템 + 컨텍스트를 격리해서 병렬 세션 충돌을 막는 장치**로 써야 한다.
2. 격리는 대화(세션) / 파일 시스템(Worktree) / 메모리 3계층인데, **오토메모리는 같은 레포 안에서 공유되므로 완전히 격리되지 않는다**는 점을 꼭 기억해야 한다.
3. Worktree 생성은 쉽지만 진짜 관건은 **머지**이고, 충돌 시엔 바로 수정시키지 말고 **원인 분석 → 머지 계획 → 수정/테스트** 순서로 진행해야 한다.
