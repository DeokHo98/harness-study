# Claude Code 디버깅 하네스(Debugging Harness) — 핵심 요약

## 1. 가장 중요한 원칙

> **디버깅 하네스는 로그를 더 많이 주는 게 아니라, "풀 수 있는 문제 형태"로 바꿔주는 시스템이다.**

로그만 통째로 던지면 Claude가 Stack Trace 마지막 줄만 보고 잘못된 원인으로 성급하게 수정을 시작할 수 있다. **정보가 많아야 정확한 진단이 나오는 게 아니라, 정보가 구조화되어야** 정확한 진단이 나온다.

---

## 2. 좋은 디버깅 하네스는 세 가지를 분리한다

| 구성 요소 | 내용 |
|---|---|
| **증상(Symptom)** | 무엇이, 언제 실패했는가 (예: 로그인 요청 시 500 에러) |
| **증거(Evidence)** | Stack Trace, 실패 테스트 출력, 로그, 환경 변수 차이, 변경 이력 |
| **검증 경로(Verification Path)** | 재현 명령, 실패하는 테스트, 가설 확인용 추가 로그 |

> 핵심: **먼저 고치지 말고, 원인 후보부터 좁힌다.**
> 순서: Symptom → Evidence → 원인 후보 → Verification Loop(재현/테스트/롤백 계획)

---

## 3. 실전 프롬프트 5단계

1. **문제(Problem)**: 무엇이 잘못됐는가
2. **환경(Environment)**: 어디서 실패하는가 (로컬/Docker/CI 차이)
3. **증거(Evidence)**: Stack Trace/로그 첨부
4. **제약(Constraints)**: 스키마 변경 금지, API 응답 변경 금지, secret 접근 금지, **먼저 수정하지 말고 원인 후보부터 좁힐 것**
5. **요청(Request)**: 원인 후보 순위, 후보 확인용 최소 명령/파일, 회귀 테스트 필요 여부

---

## 4. 환경 변수 문제 — 값이 아니라 "범위와 가시성"을 준다

"로컬은 되는데 Docker/CI는 안 됨" 같은 문제는 대부분 코드 버그가 아니라 **설정/스코프 문제**다.

Claude에게 **값 자체를 보여주지 말고** 다음만 정리해서 제공:

* 키가 존재하는가 (존재 여부)
* 어디서 왔는가 (출처)
* 어디까지 유효한가 (범위)
* 런타임에서 실제로 보이는가 (가시성)

예시 매트릭스:

| 환경 변수 | 로컬 | Docker | CI |
|---|---|---|---|
| `JWT_SECRET` | 존재 (.env) | ❌ 전달 안 됨 | 존재 (Secret Manager) |
| `API_BASE_URL` | 존재 | 존재 | 존재 |

→ 실제 값을 몰라도 이 표만으로 문제 위치를 찾을 수 있는 **프라이버시 보존형 디버깅**이 가능하다.

---

## 5. 마지막 단계: 반례(Counter-example) 루프

Claude가 원인을 확신하는 순간, 반드시 되물어야 한다.

* 이 가설이 틀릴 수 있는 조건은?
* 이 수정이 실패할 수 있는 입력은?
* 이 로그와 모순되는 다른 원인은 없는가?

> 전체 흐름: **가설 세우기 → 증거로 후보 좁히기 → 원인 확정 → 반례로 검증**

---

## 3줄 요약

1. 디버깅 하네스의 목적은 로그를 더 주는 게 아니라, 문제를 **Symptom(증상) → Evidence(증거) → Verification(검증 경로)**로 구조화해서 "풀 수 있는 형태"로 만드는 것이다.
2. 프롬프트는 문제-환경-증거-제약-요청 5단계로 구성하고, 환경 변수는 값이 아니라 **존재 여부·출처·범위·가시성**만 표로 정리해서 준다 (프라이버시 보존).
3. 원인을 확정하기 전에 반드시 **반례(이 가설이 틀릴 조건은?)**를 물어서 검증 루프를 거쳐야 한다.
