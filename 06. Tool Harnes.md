# Claude Code Tool Harness — 핵심 요약

## 1. Tool Harness의 핵심 관점

> **“무엇을 더 붙일까?”가 아니라 “에이전트 루프의 어디에 개입할까?”**

Tool을 기능 목록으로 외우지 말고 **개입 지점**으로 이해한다.

```text
assemble()
→ 모델이 무엇을 볼 것인가
→ Skills

model()
→ 모델이 무엇을 호출할 수 있는가
→ MCP

execute()
→ 실행 전후에 무엇을 강제할 것인가
→ Hooks / Permissions
```

---

## 2. Skills = 반복되는 작업 절차

**Skill은 Claude가 특정 작업을 일관된 방식으로 수행하도록 만드는 재사용 가능한 절차/지침이다.**

적합한 것:

* 디버깅 절차
* 화면 생성 규칙
* Crash 분석 절차
* PR 작성 형식
* 반복적인 개발 workflow

핵심:

> **외부 시스템이 필요 없는 반복 절차 → Skill**

`CLAUDE.md`에 모든 규칙을 몰아넣는 것보다, 특정 작업에만 필요한 절차는 Skill로 분리하는 것이 좋다.

---

## 3. MCP = 외부 시스템을 연결하는 Tool

**MCP는 Claude가 외부 시스템을 실제로 조회하거나 조작할 수 있도록 Tool을 제공한다.**

예:

* GitHub
* Jira
* DB
* 모니터링 시스템
* 사내 API

핵심:

> **실제 외부 데이터나 시스템 접근이 필요하다 → MCP**

단, MCP를 붙이는 순간 **Trust Boundary가 넓어진다.**

주의할 것:

* Prompt Injection
* Tool Poisoning
* Over-sharing
* 과도한 Write 권한
* Secret 노출
* 불필요하게 큰 Output

따라서 MCP는 기능만 연결하는 것이 아니라 **Scope + Permission + 신뢰도 + 데이터 범위**까지 설계해야 한다.

---

## 4. Hooks = 모델이 아니라 시스템이 강제하는 규칙

Skill과 Hook의 가장 중요한 차이:

> **Skill = 지켜달라고 알려주는 것**
> **Hook = 실행 시점에 시스템이 강제하는 것**

예:

* `PreToolUse` → 실행 전에 검사 / 차단
* `PostToolUse` → 실행 후 포매터 / 린터 등 후처리
* `Stop` → 종료 전에 검증
* `SessionStart` → 세션 시작 시 필요한 정보 주입
* `PreCompact` → Context 압축 전 중요 정보 보존

특히 기억할 것:

> **막아야 하는 것은 `PreToolUse`, 실행 후 처리할 것은 `PostToolUse`.**

`PostToolUse`는 이미 Tool이 실행된 후이므로 **실행 자체를 되돌리는 용도로 사용할 수 없다.**

---

## 5. Permissions = 애초에 행동 범위를 제한

Permission은 Claude가 **무엇을 할 수 있는지 자체를 제한하는 경계**다.

* 위험한 파일 접근 차단
* 특정 명령 실행 제한
* 민감한 경로 접근 제한
* 자동 실행 가능한 행동 범위 설정

핵심 차이:

> **Permissions = 할 수 있는 행동의 경계**

> **Hooks = 행동이 실행되는 시점의 검사 / 강제**

둘은 서로 대체 관계가 아니라 **서로 다른 방어 레이어**다.

---

## 6. Plugins = 하네스 구성 요소의 패키징

Plugin은 여러 하네스 구성 요소를 묶어 **팀이나 프로젝트에 배포하기 위한 패키징 구조**다.

예:

```text
Plugin
├── Settings
├── Skills
├── Hooks
└── Agents
```

팀 전체에 동일한 AI 개발 환경과 정책을 배포할 때 유용하다.

---

# 7. 선택 기준은 이것만 기억한다

| 목적                            | 사용              |
| ----------------------------- | --------------- |
| 반복 절차를 재사용                    | **Skills**      |
| 외부 시스템 / 데이터 연결               | **MCP**         |
| 실행 전후 규칙 강제                   | **Hooks**       |
| 행동 범위 제한                      | **Permissions** |
| 여러 설정 / Skill / Hook 등을 묶어 배포 | **Plugins**     |

---

# 8. 가장 중요한 안티패턴 3개

### ❌ 모든 것을 MCP로 만들기

외부 시스템 접근이 필요 없는 단순 반복 절차라면 **Skill**이면 충분하다.

> 네트워크가 필요 없는 작업에 네트워크 레이어를 추가하지 않는다.

### ❌ 외부 데이터를 Skill로 해결하기

Skill은 **절차를 알려줄 뿐 실제 외부 데이터를 가져오는 기능이 아니다.**

외부 시스템의 데이터가 필요하면 **MCP**를 사용한다.

### ❌ 강제 규칙을 Skill에만 적기

예:

> “반드시 테스트 후 종료하세요.”

이것만 Skill에 적으면 Claude가 잊거나 무시할 수 있다.

정말 강제해야 한다면:

> **Skill이 아니라 Hook / Permission**

으로 옮긴다.

---

# 9. iOS 개발자식으로 기억하기

```text
Skills
≈ Code Snippet / 팀 개발 절차 / 체크리스트

MCP
≈ 외부 SDK / API 통합

Hooks
≈ Build Phase Run Script / Git Hook / 컴파일 단계 강제

Permissions
≈ App Sandbox / Entitlement / 접근 권한

Plugins
≈ Swift Package처럼 여러 하네스 구성 요소를 패키징
```

---

# 절대 기억할 핵심 공식

```text
반복 절차
→ Skill

외부 시스템
→ MCP

반드시 강제
→ Hook

애초에 못 하게 제한
→ Permission

팀 단위로 묶어 배포
→ Plugin
```

가장 중요한 질문은:

> **“이 기능이 무엇을 할 수 있지?”가 아니라 “에이전트 루프의 어느 지점에 개입하지?”**

---

## 3줄 요약

1. **Tool Harness는 도구를 많이 붙이는 것이 아니라 `assemble → model → execute` 중 어디에 개입할지를 설계하는 것이다.**
2. **반복 절차 = Skills, 외부 시스템 = MCP, 실행 강제 = Hooks, 행동 제한 = Permissions, 패키징 = Plugins.**
3. **특히 “반드시 지켜야 하는 규칙”은 Skill에 적는 것으로 끝내지 말고 Hooks/Permissions로 시스템적으로 강제한다.**
