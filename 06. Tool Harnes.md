# Claude Code의 Tool Harness: "무엇을 더 붙일까"가 아니라 "어디에 끼워 넣을까"

Claude Code를 쓰다 보면 Built-in Tools, Skills, MCP, Hooks, Plugins 같은 단어가 우르르 쏟아진다. 대부분의 강의나 문서는 이걸 **기능 목록**으로 소개한다. "Skills는 이런 거고요, MCP는 이런 거고요, Hooks는 이런 거고요."

그런데 그렇게 배우면 막상 쓸 때 헷갈린다. *"이 작업은 Skill로 만들어야 하나, MCP로 붙여야 하나?"* 라는 질문에 답이 안 나온다.

관점을 하나만 바꾸면 정리가 된다.

> **무엇을 더 붙일 수 있는가**가 아니라, **에이전트 루프의 어느 지점에 개입할 것인가**.

이게 하네스(harness) 엔지니어링 관점이다. 하네스는 원래 마구(馬具)·안전벨트를 뜻하는 말인데, 여기서는 **모델이라는 엔진에 채워 넣는 통제 장치**로 보면 된다. 엔진 성능을 올리는 게 아니라, 엔진이 어디로 어떻게 힘을 쓸지 붙잡아주는 구조다.

---

## 에이전트 루프를 세 지점으로 쪼개기

iOS 개발자에게 "에이전트 루프"라는 말이 낯설 수 있는데, 그냥 **Claude가 한 턴 동안 도는 사이클**이라고 보면 된다. 뷰가 그려지기까지 `viewDidLoad` → `viewWillAppear` → `viewDidAppear`를 거치듯, Claude도 매 턴마다 정해진 단계를 거친다.

이 루프를 개입 지점 기준으로 세 개로 나눌 수 있다.

| 단계 | 질문 | iOS 비유 |
|---|---|---|
| `assemble()` | 모델이 **무엇을 볼 것인가** | ViewModel에 주입되는 state, DI 컨테이너가 조립한 의존성 |
| `model()` | 모델이 **무엇을 호출할 수 있는가** | 프로토콜로 노출된 인터페이스, 앱이 접근 가능한 API 표면 |
| `execute()` | 실행 전후에 **무엇을 강제할 것인가** | Build Phase의 Run Script, `viewWillAppear` 같은 라이프사이클 훅 |

Skills, MCP, Hooks는 서로 경쟁하는 기능이 아니라 **각각 다른 지점에 꽂히는 장치**다. 이걸 알고 나면 "뭘 써야 하지?"가 "지금 내가 개입하려는 지점이 어디지?"로 바뀐다.

---

## Skills — 반복 절차를 모델의 사고 방식에 심는다

**개입 지점: `assemble()` — 모델이 무엇을 볼 것인가**

Skill은 외부 시스템을 연결하는 게 아니다. **Claude가 현재 컨텍스트 안에서 더 일관된 절차로 생각하게 만드는 장치**다.

예를 들어 "백엔드 디버깅" Skill을 만들어두면 Claude는 매번 이런 절차를 밟는다.

1. 로그 정리
2. 원인 후보 좁히기
3. 관련 파일 탐색
4. 검증 명령 실행 (범위 제한)

iOS 쪽으로 옮겨보면 이런 게 Skill 후보다.

- "새 화면 추가할 땐 View / ViewModel / Coordinator 순서로 만들고, Preview는 반드시 붙인다"
- "크래시 리포트 분석은 스택트레이스 심볼리케이션 → 재현 조건 특정 → 관련 커밋 확인 순으로"
- "PR 본문은 변경 이유 / 스크린샷 / 테스트 방법 세 섹션 고정"

Xcode의 **Code Snippet**이나 팀 위키에 박아둔 체크리스트에 가깝다. 차이가 있다면, Skill은 **필요할 때만 로드되는 낮은 컨텍스트 비용** 구조라는 점이다. Swift의 `lazy var`처럼, 안 쓰면 메모리(=컨텍스트 윈도우)를 잡아먹지 않는다. 모든 규칙을 `CLAUDE.md`에 몰아넣는 것과 결정적으로 다른 부분이다.

> **요약**: 반복되는 작업 방식, 팀의 디버깅 절차, PR 작성 규칙 → Skills

---

## MCP — 외부 시스템을 도구 인터페이스로 열어준다

**개입 지점: `model()` — 모델이 무엇을 호출할 수 있는가**

MCP(Model Context Protocol)는 GitHub, 데이터베이스, 모니터링 시스템, 사내 API, Jira 같은 **외부 시스템을 Claude가 호출 가능한 도구로 노출**시킨다.

iOS로 치면 **서드파티 SDK를 앱에 통합하는 것**과 구조가 같다. Firebase를 붙이면 앱이 할 수 있는 일이 늘어난다. MCP를 붙이면 Claude가 할 수 있는 일이 늘어난다.

그런데 SDK를 붙일 때 우리가 뭘 신경 쓰는지 떠올려보자. 이 SDK가 내 앱 데이터를 어디까지 볼 수 있지? 어떤 권한을 요구하지? 네트워크로 뭘 내보내지?

MCP도 똑같다. **Claude가 외부 시스템과 신뢰 경계(trust boundary)를 공유하게 된다.** 이게 핵심이다.

### MCP를 붙일 때 커지는 위험

- **프롬프트 인젝션(Prompt Injection)** — 외부에서 가져온 데이터 안에 "이전 지시를 무시하고 ~해라" 같은 문장이 섞여 있고, Claude가 그걸 지시로 착각하는 경우. 서버에서 내려온 JSON을 검증 없이 그대로 실행 로직에 태우는 것과 같은 종류의 사고다.
- **툴 포이즈닝(Tool Poisoning)** — 도구 자체의 설명(description)에 악의적 지시가 심어져 있는 경우. 신뢰할 수 없는 서드파티 SDK를 그냥 임포트하는 것과 비슷하다.
- **오버셰어링(Over-sharing)** — 필요 이상의 데이터가 모델 컨텍스트로 딸려 들어가는 것. `select *`를 그대로 던지는 상황을 떠올리면 된다.
- **잘못된 액션** — 읽기만 시키려 했는데 쓰기까지 가능한 도구가 열려 있는 경우.

그래서 MCP를 다룰 때는 기능만 볼 게 아니라 **스코프(Scope), 퍼미션(Permission), 서버 신뢰도, 아웃풋 사이즈, 시크릿 핸들링**을 같이 설계해야 한다. Info.plist에 권한을 선언하고 App Sandbox entitlement를 최소로 잡는 것과 같은 감각이다.

> **요약**: 외부 시스템 연결이 필요할 때 → MCP. 단, 권한과 신뢰 경계를 같이 설계할 것

---

## Hooks — 실행 전후의 규칙 강제 장치

**개입 지점: `execute()` — 실행 전후에 무엇을 강제할 것인가**

Skills와 Hooks의 결정적 차이는 **강제력**이다.

Swift로 비유하면 이렇다.

```swift
// Skill: "이 메서드는 메인 스레드에서 호출해주세요" — 주석
// 지켜지길 기대할 뿐, 안 지켜도 막을 방법이 없다
func updateUI() { ... }

// Hook: @MainActor — 컴파일러가 강제
// 잊거나 무시할 방법이 없다
@MainActor
func updateUI() { ... }
```

Skill에 "테스트 없이 끝내지 마세요"라고 적어두면 Claude가 **잊거나 무시할 수 있다**. Hook은 그렇지 않다. 셸 커맨드가 결정론적으로 실행된다. 모델의 판단이 개입하지 않는다.

Xcode의 **Build Phases → Run Script**와 정확히 같은 위치다. SwiftLint를 빌드 페이즈에 걸어두면 개발자가 깜빡해도 무조건 돈다. Git의 `pre-commit` 훅도 마찬가지다.

### 주요 이벤트

공식 문서 기준으로 자주 쓰이는 이벤트는 다음과 같다.

| 이벤트 | 시점 | 활용 예 |
|---|---|---|
| `PreToolUse` | 도구 실행 **직전** | 특정 경로(`.env`, 프로덕션 설정 등) 쓰기 차단 |
| `PostToolUse` | 도구 실행 **직후** | 파일 수정 후 포매터/린터 자동 실행 |
| `UserPromptSubmit` | 프롬프트 제출 시 | 컨텍스트 자동 주입, 입력 검증 |
| `Stop` | 턴 종료 시도 시 | 테스트 없이 끝내려는 흐름 차단 |
| `SubagentStop` | 서브에이전트 종료 시 | 하위 작업 결과 검증 |
| `PreCompact` | 컨텍스트 압축 전 | 중요 정보 보존 |
| `SessionStart` | 세션 시작/재개 시 | 최근 커밋 로그, 브랜치 상태 주입 |

참고로 현재 공식 문서에는 이보다 훨씬 많은 이벤트가 정의돼 있다. 다만 실무에서 손이 가는 건 대체로 위 목록, 그중에서도 `PreToolUse` / `PostToolUse` / `Stop` 세 개다. 나머지는 필요해질 때 찾아보면 된다.

### 주의점

Hooks는 **셸 커맨드를 자동 실행한다.** 모델의 확인 없이 그냥 돈다. 그래서 충분히 테스트하고 안전한 환경에서 설계해야 한다. Build Phase 스크립트를 잘못 짜서 빌드가 통째로 망가진 경험이 있다면 감이 올 것이다.

한 가지 더: `PostToolUse`는 **이미 실행된 걸 되돌릴 수 없다.** 파일은 이미 쓰인 뒤다. 막아야 하는 건 `PreToolUse`, 뒤처리하는 건 `PostToolUse`로 역할을 나눠야 한다.

> **요약**: 실행 전후에 규칙을 강제하고 싶다 → Hooks

---

## Plugins & Permissions

**Plugins**는 여러 요소(설정, 에이전트, Skill, Hook)를 **패키징해서 배포하는 구조**다. 사내 공통 모듈을 Swift Package로 묶어서 여러 프로젝트가 가져다 쓰게 하는 것과 같다. 팀 전체가 같은 하네스 설정을 쓰게 만들고 싶을 때 이걸 쓴다.

**Permissions**는 `settings.json`에서 관리하는 **행동 범위 제한**이다. Claude가 애초에 할 수 있는 일의 경계를 긋는다. App Sandbox entitlement, Info.plist 권한 선언과 같은 층위다.

Permissions와 Hooks는 둘 다 통제 장치지만 층위가 다르다. Permissions는 **애초에 못 하게 하는 것**, Hooks는 **하려고 할 때 검사해서 막거나 뒤처리하는 것**이다.

---

## 설정 파일은 어디서 뭘 담당하나

| 위치 | 역할 |
|---|---|
| `CLAUDE.md` | 프로젝트 컨텍스트, 지시사항 제공 (프로젝트 README + 팀 스타일 가이드) |
| `settings.json` | Permissions, 환경 변수, 도구 동작 구성, Hooks 등록 |
| Skills | 커스텀 프롬프트로 호출되는 절차/지침 |
| MCP 서버 | 추가 도구 및 외부 시스템 통합 |

---

## 선택 가이드

한 문장으로 정리하면 이렇다.

| 하고 싶은 것 | 답 |
|---|---|
| 반복 절차를 재사용하고 싶다 | **Skills** |
| 외부 시스템(모니터링 API, DB, Jira 등)을 연결하고 싶다 | **MCP** |
| 실행 전/후에 규칙을 강제하고 싶다 | **Hooks** |
| 설정·에이전트·스킬·훅을 묶어 배포하고 싶다 | **Plugins** |
| Claude가 할 수 있는 행동 범위를 제한하고 싶다 | **Permissions** |

---

## 흔한 안티패턴 세 가지

### 1. 모든 반복 작업을 MCP로 만들기

"MCP가 더 고급이니까 반복 작업도 MCP로 만들자"는 생각을 하기 쉽다. 하지만 **모든 반복 작업에 MCP를 붙이면 시스템이 불필요하게 무거워진다.**

외부 도구 호출이 필요 없는 절차는 Skills로 충분하다. 로컬에서 문자열 하나 포맷하면 될 일에 네트워크 레이어를 태우는 것과 같다.

### 2. 외부 데이터가 필요한데 Skills로 해결하려 하기

반대 방향의 실수다. Skill에 아무리 상세한 절차를 적어놔도 **Claude가 실제 데이터를 가져올 수는 없다.** 데이터가 필요하면 MCP가 답이다.

### 3. 강제 규칙을 Skills에 적어두기

가장 자주 보는 실수다. "반드시 테스트를 돌린 후 종료할 것" 같은 문장을 Skill에 적어두면 Claude가 잊거나 무시할 수 있다. **강제가 필요한 건 Hooks나 Permissions로 옮겨야 한다.**

앞서 본 주석 vs `@MainActor` 비유가 그대로 적용된다. 지켜지길 바라는 것과, 안 지킬 수 없게 만드는 것은 다르다.

---

## 정리

Tool Harness는 **더 많은 도구를 붙이는 작업이 아니다.** 각 메커니즘을 **정확한 위치에 배치하는 작업**이다.

- `assemble()` — 모델이 무엇을 볼 것인가 → **Skills**
- `model()` — 모델이 무엇을 호출할 수 있는가 → **MCP**
- `execute()` — 실행 전후에 무엇을 강제할 것인가 → **Hooks**, **Permissions**
- 이 모든 걸 묶어서 배포 → **Plugins**

새로운 기능이 나올 때마다 "이것도 붙여야 하나?"라고 묻는 대신, "이건 루프의 어느 지점에 꽂히는 물건이지?"라고 물으면 된다. 그 질문에 답할 수 있으면 나머지는 자연스럽게 따라온다.

---

## 3줄 요약

1. Tool Harness는 도구를 많이 붙이는 게 아니라, 에이전트 루프의 세 지점(`assemble` / `model` / `execute`) 중 어디에 개입할지 정하는 설계다.
2. 반복 절차는 Skills, 외부 시스템 연결은 MCP, 실행 전후 강제는 Hooks — 각각 꽂히는 자리가 다르므로 서로 대체할 수 없다.
3. 특히 강제해야 하는 규칙을 Skills에 적어두면 모델이 무시할 수 있으니, 주석 대신 `@MainActor`를 쓰듯 Hooks나 Permissions로 옮겨야 한다.
