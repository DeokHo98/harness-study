# 그래프 엔지니어링 — 실전 패턴과 운영

## 1. 가장 중요한 원칙

> **Graph Engineering의 핵심은 에이전트 수를 늘리는 데 있지 않다. 각 작업이 왜 분리되어야 하고, 무엇을 기다려야 하고, 어떤 조건을 통과해야 다음 단계로 갈 수 있는지 명확하게 설계하는 것에 있다.**

---

## 2. 실무에서 자주 쓰는 그래프 패턴 7가지

### ① Router 패턴

입력을 분류해서 적절한 처리 경로로 보낸다.

> 예: Support Ticket → Router → 결제 문제 / 계정 문제 / 기술 문제 처리

분류 자체에는 모델을 쓸 수 있다. 다만 모델이 다음 행동까지 자유롭게 결정하게 두는 것보다, **분류 결과를 구조화된 아웃풋(카테고리 + confidence)으로 반환**하게 만드는 편이 안전하다. 분류는 모델이, 실제 경로 선택(예: Billing Handler 실행)은 그 구조화된 출력을 읽은 일반 코드가 담당한다.

### ② Orchestrator-Worker 패턴

큰 작업을 작은 단위로 분해해야 할 때 쓴다. Orchestrator가 작업을 나눠 Worker들(B, C…)에게 전달하고, 각 Worker는 제한된 범위만 처리하며, 완료된 결과는 Synthesis 노드에서 다시 합친다. 대규모 마이그레이션이나 레포지토리 분석처럼 처음부터 처리 단위가 많은 작업에 적합하다.

### ③ Fan-Out / Fan-In 패턴

여러 독립 작업을 동시에 실행하고 결과를 다시 모으는 기본적인 병렬 구조다. 다이아몬드 패턴과 유사하다. 여러 소스를 분석하거나, 파일별로 리뷰하거나, 후보를 생성하는 것처럼 같은 형태의 작업이 반복될 때 자주 쓴다.

### ④ Evaluator-Optimizer 패턴

Generator가 결과를 만들고 Evaluator가 검토해서, 기준을 충족하지 못하면 구체적인 피드백과 함께 다시 Generate로 돌려보낸다. Pass면 종료한다.

> 이 구조에는 반드시 **실행 한도**가 필요하다. 완벽한 결과는 없기 때문에 계속 Fail할 수 있다.

실행을 제한하는 조건 예시: 최대 수정 3회, 점수 90점 이상이면 종료, 모든 필수 항목 충족 시 종료, 비용 한도에 도달하면 사람에게 전달. 종료 기준 없이 "좋아질 때까지 반복"으로 두면 비용과 시간이 계속 늘어난다.

### ⑤ Generate & Filter 패턴

처음부터 하나의 완성된 답을 만들려 하지 않고, 여러 후보를 생성한 뒤 기준에 맞는 것만 남긴다.

> 예: 후보 20개 생성 → Rubric으로 평가 → 상위 3개 선택

제목 생성, 마케팅 문구, UI 설계 대안, 기술적 접근 방법 비교 같은 작업에 적합하다.

### ⑥ 토너먼트 패턴

여러 에이전트가 같은 문제에 대한 결과를 만들고, Judge가 후보를 두 개씩 비교해서 더 나은 쪽을 선택한다.

> A vs B → 승자1, C vs D → 승자2, 승자1 vs 승자2 → 최종 선택.

정확한 절대 점수를 매기기는 어렵지만, 상대 비교는 가능한 작업에서 유용하다.

### ⑦ Human-in-the-loop Gate

결과를 되돌리기 어렵거나 책임이 큰 작업은 사람의 승인을 거쳐야 한다.

> 예: 프로덕션 배포, 고객 메시지 발송, 결제 실행, 데이터 삭제, 권한 변경, 중요 PR 머지.

작업 준비 → 자동 검증 → Human Approval 순서로 구성한다.

---

## 3. Static Graph vs Dynamic Graph

| | Static Graph | Dynamic Graph |
|---|---|---|
| 구조 | 실행할 때마다 같은 구조 | 실행 결과에 따라 새 노드·경로가 추가됨 |
| 예측 가능성 | 어떤 노드가 실행될지 예측하기 쉽고 비용·시간 추정이 좋음 | 실행 노드 수·전체 비용 예측이 어려움 |
| 디버깅 | 실행 기록 검토가 쉽고 오류 위치를 찾기 쉬움 | 실행 경로가 매번 달라짐 |
| 예시 | 경쟁사 3곳을 매주 조사 → 체크 → 비교 → 보고서 작성 | 코드 리뷰 중 특정 모듈에서 예상보다 복잡한 문제 발견 → 추가 분석·보안 리뷰를 그래프에 동적으로 추가 |

> Dynamic Graph는 무한히 새로운 작업을 생성하지 않도록 제한이 필요하고, 실행 계획과 생성 이유를 모두 남겨야 한다. **처음에는 Static Graph로 시작**하고, 고정된 구조로 반복 처리하지 못하는 예외가 발생했을 때만 Dynamic Graph를 고려하는 편이 좋다.

---

## 4. State 영속화 — 대화가 아니라 파일에 남긴다

장시간 실행되는 에이전트가 이전 결정을 잊는 가장 큰 이유는 진행 상태가 대화 안에만 있기 때문이다. 컨텍스트가 요약되거나 세션이 종료되면, 완료된 작업·실패한 작업·이전에 내린 결정·다음에 처리할 항목·반복되면 안 되는 실패 같은 정보가 약해지거나 사라진다.

* **state.md**: 현재 진행 상황(In Progress, Failed, Next, Budget, Completed 등)을 기록한다. 다음 실행은 이 파일을 읽고 멈춘 위치부터 이어간다.
* **vision.md**: 최종 목적과 완료 조건을 기록한다. state.md가 "지금 어디까지 왔는지"를 보여준다면, vision.md는 "처음 정한 목표"를 계속 유지하게 해준다. 긴 작업에서는 둘을 함께 쓰는 편이 좋다.
* **Queue도 파일 시스템 기준**: 대기 중인 작업 목록을 채팅 메모리에만 두지 않는다. 예: 소스 파일은 있지만 아웃풋 파일이 없는 항목만 다시 처리하도록 만들면, 작업이 60% 진행된 상태에서 중단돼도 재시작 시 이미 완료된 항목은 건너뛴다 — 이전 대화를 기억할 필요 없이 파일 시스템을 확인해서 완료 여부를 다시 계산하기 때문이다.

---

## 5. Worker보다 Judge를 먼저 만든다

많은 사람이 Worker 프롬프트부터 작성한다. 하지만 안정성을 높이려면 **완료 조건(Judge)부터 만들어야 한다.** Judge는 결과가 통과했는지 실패했는지 판단하는 역할을 맡는다.

> 예: 문서 검사 Judge — Empty File, Missing Requirements, Missing Architecture 같은 항목을 검사한다. 코드 작업이면 실행 명령어와 연결할 수도 있다.

Judge가 항상 Pass만 반환한다면 의미가 없다. 정상 결과뿐 아니라 **일부러 잘못 만든 결과(broken.md)**와 **정상 결과(good.md)**를 둘 다 넣어봐서, 정상 결과는 통과시키고 잘못된 결과는 거절하는지 확인해야 실제 게이트로 쓸 수 있다.

---

## 6. Rulebook — 같은 실수는 규칙으로 고친다

리뷰어가 같은 문제를 반복해서 발견하면, 결과물을 하나씩 손으로 고치기보다 **규칙을 수정**해야 한다.

> 예: 3개 문서에서 모두 소스에 없는 숫자가 추가되었다면, 그 문서들만 고치는 게 아니라 Rulebook에 규칙을 추가한다. 새 파일만 수정하면 다음 배치에서도 같은 문제가 반복된다.

### Rulebook 구성 예시

| 항목 | 내용 예시 |
|---|---|
| **Output Format** | 입력 하나당 출력 하나를 생성한다, Markdown으로 작성한다, 전체 결과를 Code Fence로 감싸지 않는다 |
| **Convention** | 날짜는 YYYY-MM-DD 형식, 소스에 없는 숫자는 생성하지 않는다, 모든 핵심 주장에 근거를 연결한다, 제목이 없으면 파일명을 제목으로, 5000단어 이상이면 상위 헤딩 기준으로 분할한다 |
| **Cannot Proceed** | 확인되지 않은 값은 임의로 작성하지 않는다, 처리할 수 없는 항목은 `TODO(agent): <이유>`로 표시한다 |

> 이 방식의 핵심은 개별 결과만 고치는 게 아니라, **같은 결과를 반복해서 만드는 규칙과 과정까지 수정하는 것**이다. 영향을 받은 배치는 규칙 수정 후 다시 실행한다.

---

## 7. 실패 종류에 따라 경로를 나눈다

단순한 루프는 실패하면 무조건 다시 시도하는 경우가 많다. 하지만 실패 원인이 다르면 처리 방식도 달라져야 한다.

> 예: Research 노드가 실패하는 이유들 — 일시적 네트워크 오류, 소스 접근 불가, 소스 간 정보 충돌(판단 불가), Budget 초과. 모든 실패를 같은 프롬프트로 다시 실행하면 같은 문제가 반복될 수 있다.

노드마다 미리 정해둬야 하는 것: 어떤 실패는 재시도할지, 최대 몇 번까지 시도할지, 대체할 수 있는 툴·소스가 있는지, 어느 시점에 사람에게 전달할지, 언제 안전하게 종료할지. 이것이 Graph Engineering에서 필요한 **복구 설계**다.

---

## 8. 검사 비용에 따라 실행 시점을 나눈다

모든 검사를 노드마다 실행하면 안정성은 높아지지만 전체 시간이 지나치게 길어질 수 있다.

| 검사 종류 | 예시 | 실행 시점 |
|---|---|---|
| **빠른 검사** (몇 초 이내) | Type Check, Lint, 작은 유닛 테스트, JSON 구조 검사, 필수 파일 존재 여부 | 작업 직후 바로 실행 — 빠르게 실패를 발견해서 잘못된 결과가 다음 작업으로 넘어가는 것을 막는다 |
| **느린 검사** (몇 분 이상) | 전체 Workspace Build, 전체 Integration Test, Repository 단위 Security Scan | 여러 변경을 모아 배치 단위로 실행 |

> 여러 에이전트가 각자 전체 빌드를 실행하면 같은 비용을 반복 지불하게 된다. 워커는 빌드 요청만 큐에 남기고, 하나의 "빌드 게이트"가 요청을 모아서 한 번에 처리하는 방식이 낫다.

---

## 9. 병렬 에이전트에는 독립 워크스페이스가 필요하다

병렬 실행에서는 두 종류의 실패를 구분해야 한다.

1. **에이전트 하나의 작업 실패** — 하나의 실패가 전체 배치를 중단시키지 않도록 처리해야 한다 (error_code로 구분)
2. **여러 에이전트가 같은 파일을 동시에 수정하는 경우** — 각자 작업은 성공했어도 서로의 변경을 덮어쓸 수 있다

> 코드를 수정하는 병렬 에이전트에는 별도의 Git Worktree나 Sandbox를 제공하는 편이 좋다. 각 Agent(A, B, C)가 독립적으로 수정·테스트한 뒤, 검증을 통과한 결과만 Merge한다.

---

## 10. 모델도 역할에 따라 나눠 쓴다

모든 노드에 가장 크고 비싼 모델을 쓸 필요는 없다.

* **작고 반복적인 작업** (티켓 분류, 파일별 정형 작업, 형식 변환) → 빠르고 저렴한 모델로 충분
* **Rulebook 작성, 아키텍처 검토, 복잡한 실패 원인 분석** → 더 높은 판단력이 필요한 모델

> Rulebook을 작성하는 노드에는 특히 성능을 충분히 투자해야 한다. Worker 하나의 실수는 결과 하나에만 영향을 주지만, 잘못된 규칙 하나는 이후 수백 개의 결과에 같은 문제를 만들 수 있다.

---

## 11. 로깅과 트레이싱은 처음부터

Linear Prompt는 순서대로 실행되기 때문에 비교적 추적이 쉽다. Graph는 여러 노드가 동시에 실행되고 서로 다른 경로를 거치기 때문에, 처음부터 로깅과 트레이싱을 준비해야 한다.

최소한 기록해야 할 것: RunID, NodeID, 시작·종료 시간, 입력·출력 위치, 출력 파일, 사용 모델, 토큰 사용량, 툴 호출 기록, Retry 횟수, 검증 결과.

> 이런 기록이 있으면 에이전트가 이상하게 동작했을 때 정확한 실패 지점을 확인할 수 있다. 예: "Review 노드가 두 번째 실행에서 테스트 3개가 실패했고, 이후 Fix 경로로 이동했다"처럼. 그래프가 커질수록 이런 관찰 가능성(Observability)이 중요해진다.

---

## 12. 흔한 안티패턴들

* **Fake Edge** — 실제 데이터 의존성이 없는데 프롬프트에 적힌 순서만으로 두 작업을 연결한 것. 독립적인 작업이 불필요하게 기다리게 된다.
* **불필요한 Barrier** — 다음 단계가 전체 결과를 필요로 하지 않는데도 모든 작업이 끝날 때까지 기다리는 것. 빠른 작업도 가장 느린 작업의 속도에 맞춰진다.
* **단순 데이터 처리에 에이전트를 쓰는 것** — 배열 정렬, 중복 제거처럼 일반 코드로 처리할 수 있는 일에 에이전트를 쓰면 비용만 높아진다.
* **리뷰어가 같은 컨텍스트를 쓰는 것** — 자신이 만든 결과를 같은 컨텍스트에서 직접 평가하면, 독립적 검증이 아니라 기존 결정을 다시 확인하는 과정이 되기 쉽다.
* **종료 조건 없는 사이클** — 새로운 결과가 더 나오지 않는데도 계속 검색하거나, 이전에 Reject한 파인딩을 기억하지 못해 다시 발견하는 것. 파인딩 상태를 Confirmed/Rejected/Duplicated/Unresolved로 저장해야 한다.
* **검증되지 않은 병렬 결과를 바로 합치는 것** — 하나의 잘못된 결과가 정상 결과와 함께 Synthesis 노드에 들어가 최종 결과의 신뢰도를 낮춘다.
* **State를 채팅에만 저장하는 것** — 세션이 종료되면 이전 진행 상황을 이어가기 어려워진다.
* **실행 한도가 없는 것** — 종료 조건을 만족하지 못한 상태에서 계속 재시도하거나 새 노드를 계속 만들 수 있다. 최대 Turn, Retry 수, 실행당 Token Budget, 하루 비용 한도를 제한해야 한다.

---

## 13. 그래프 엔지니어링의 숨은 비용

그래프는 실행 속도와 자동화 범위를 높이지만, 새로운 비용도 만든다.

* **리뷰 병목**: 에이전트가 결과를 만드는 속도가 사람의 검토 속도보다 빠르면, 검토되지 않은 결과가 계속 쌓인다. PR 100개를 만들어도 사람이 20개밖에 검토하지 못하면 실제 처리량은 20개다. 자동 생성량만 늘어난 것이다.
* **Cognitive Surrender**: 자동화가 안정적으로 동작할수록 사람은 결과를 덜 확인하게 되고, 확인이 줄면 잘못된 결정을 식별하는 능력도 함께 약해진다. 실행은 에이전트에게 맡기더라도 중요한 기술적 판단과 책임까지 넘겨서는 안 된다. 정기적으로 Merge된 diff를 직접 읽고, 왜 이런 구조가 선택되었는지 확인해야 한다.
* **Token Blowout**: 여러 노드를 병렬 실행하고, 실패한 작업을 재시도하고, 소스·컨텍스트를 반복해서 읽는다. 전체 비용은 프롬프트 한 번의 가격이 아니라 (실행 횟수 × 노드별 평균 리트라이 횟수 × 노드별 토큰 비용) + 툴 비용 + CI 비용 + 사람 리뷰 비용까지 합산된다. 실행 전에 Budget과 Retry 제한을 정해야 한다.

### 진짜 의미 있는 지표

생성된 코드 줄 수, 만들어진 PR 수, 실행된 에이전트 수, 생성된 토큰 수, 자동화된 워크플로우 수는 **작업량**을 보여줄 뿐 실제 품질이나 효율을 보장하지 않는다.

> **더 중요한 지표는 "승인된 변경 한 건당 전체 비용"이다.** 에이전트가 100개의 변경을 만들었지만 20개만 승인되었다면, 나머지 80개를 생성하고 검토한 비용까지 포함해서 계산해야 한다. **그래프 엔지니어링의 목표는 가능한 많은 결과를 만드는 게 아니라, 검증을 통과하는 결과를 더 낮은 비용과 짧은 시간으로 만드는 것이다.**

---

## 14. 그래프를 쓰지 않는 편이 나은 경우

모든 문제에 그래프가 적합한 건 아니다.

* 한 번만 수행하는 짧은 작업 — Node/Edge/State를 설계하는 시간이 실제 작업 시간보다 길어질 수 있다
* 처음 보는 코드베이스를 탐색하거나 원인을 알 수 없는 문제를 디버깅하는 단계 — 고정된 그래프를 너무 빨리 만들지 말고, 먼저 자유롭게 탐색하면서 실제 작업 구조를 파악해야 한다
* 완료 여부를 기계적으로 판단할 수 없는 작업 — 결과마다 사람의 판단이 필요하다면, Gate를 추가해도 형식적인 승인 절차가 되기 쉽다
* Payment, 인증, 데이터 삭제, 보안, 권한 변경처럼 영향이 큰 영역 — Human Gate 없이 완전히 자동화해서는 안 된다

> 좋은 그래프 아키텍트는 복잡한 그래프를 만드는 사람만을 의미하지 않는다. "이 작업은 단순히 스크립트나 루프로 충분하다"고 판단할 수 있는 것도 실력이다.

---

## 15. 현실적인 도입 순서 — 9단계

| 단계 | 내용 |
|---|---|
| 1 | Claude와 함께 작업을 직접 수행하며 어떤 지식이 필요한지, 어디서 자주 실패하는지, 완료 여부를 어떻게 판단할지 확인한다 |
| 2 | 반복되는 작업 절차를 Rulebook·Skill로 정리하고, 자동으로 통과 여부를 판단하는 Judge를 만든다 |
| 3 | 단일 루프를 만든다 — 실패하면 피드백을 반영해서 다시 작업하게 하고, 최대 Retry와 Budget을 함께 설정한다 |
| 4 | State를 저장한다 — state.md, vision.md 또는 데이터베이스에 진행 상황을 기록해서 중간에 멈춰도 이어서 실행할 수 있게 한다 |
| 5 | 서로 독립적인 작업을 찾아 병렬로 실행하는 Fan-out을 도입한다 |
| 6 | Checker와 Synthesis를 추가한다 — 병렬 결과를 검증하고, 통과한 결과만 최종 단계에서 합친다 |
| 7 | Router와 Failure Path를 추가한다 — 입력 종류와 실패 원인에 따라 처리 경로를 나눈다 |
| 8 | 충분히 검증된 Workflow에 사람 승인(Human Gate)과 예약 실행을 추가한다 |
| 9 | Static Graph로 처리하기 어려운 예외가 반복적으로 확인되었을 때만, 실행 중 Graph 확장(Dynamic Graph)을 허용한다 |

---

## 16. 종합 예시 — 경쟁사 비교 보고서 워크플로우

목표: 경쟁사 세 곳의 가격, 기능, 최근 변화, 시장 포지셔닝을 조사해서 비교 보고서를 만든다.

기존 Linear 구조(A조사 → B조사 → C조사 → 비교 → 초안 작성 → 사실 확인 → 형식 정리)는 순서대로 실행되어 시간이 오래 걸리고, 잘못된 조사 결과가 검증 없이 비교 단계로 넘어갈 수 있다.

### 그래프 구조

> Input → Task Setup → (A조사 / B조사 / C조사를 Worker가 병렬·독립 실행 → 각각 검증 → 실패 시 수정 단계로 개별 이동) → Normalize(데이터 형식 통일, 회사 간 정보 모순 확인) → (비교문 작성 / Pro Format이 서로 독립적이면 동시 실행) → Final Judge(전체 결과 검증) → Human Approval(게시·고객 전달 전 사람 승인)

### Node 계약(Contract) 예시 — 회사 조사 노드

* **입력**: `Company`, `Fields`, `As of Date`
* **출력**: 다음 노드가 바로 사용할 수 있도록 구조화 — 다음 단계가 자유형식의 긴 문장을 다시 해석하지 않아도 된다

### Checker의 검증 항목

* 필수 필드를 모두 포함하고 있는가
* 핵심 주장마다 소스가 있는가
* 서로 충돌하는 정보가 있는가

### 실패 원인별 경로

* 소스 부족 → 추가 리서치
* 정보 충돌 → Independent Reviewer 투입
* 접근 불가 → 대체 소스 사용

### Final Judge

모든 회사를 같은 기준으로 비교했는지, 핵심 주장의 근거가 연결되어 있는지, 사실과 추정이 구분되어 있는지를 모두 충족해야 완료로 처리한다.

---

## 17. 전체 정리

AI 에이전트가 긴 작업에서 일관성을 잃는 이유는 모델의 성능만으로 설명할 수 없다. 하나의 에이전트와 하나의 컨텍스트에 계획·구현·검증·진행 관리·완료 판단을 모두 맡긴 구조에도 원인이 있다.

* **루프**는 행동과 검증을 반복하게 해준다.
* **그래프**는 루프보다 한 단계 더 나아가 여러 작업의 의존성, 병렬 실행, 검증, 실패 처리, 상태 저장을 명시적으로 설계한다.
* 그래프의 기본 요소는 **Node**(하나의 명확한 작업), **Edge**(실제 데이터 의존성), **State**(다음 실행까지 보존해야 하는 진행 상황과 결정)다.

그래프를 설계할 때 가장 먼저 해야 할 일은 에이전트를 늘리는 게 아니다.

1. 현재 워크플로우에서 실제 데이터가 이동하지 않는 Fake Edge를 찾아낸다
2. 독립적인 작업은 병렬로 실행하고, 전체 결과가 필요한 지점에서만 Barrier를 둔다
3. 병렬 결과는 바로 합치지 않고 Checker를 통과시킨다

---

## 3줄 요약

1. 실무에서 자주 쓰는 그래프 패턴은 Router(분류 후 라우팅), Orchestrator-Worker(작업 분해), Fan-out/Fan-in(병렬 후 합치기), Evaluator-Optimizer(생성-평가 반복), Generate & Filter(후보 생성 후 필터링), 토너먼트(상대 비교), Human-in-the-loop Gate(고위험 작업 승인)가 있으며, 처음에는 Static Graph로 시작해 반복되는 예외가 확인될 때만 Dynamic Graph를 고려하는 게 좋다.
2. 안정적인 그래프 운영에는 State를 파일(state.md/vision.md)로 영속화하고, Worker보다 Judge를 먼저 만들고, 반복되는 실수는 개별 수정 대신 Rulebook을 고치고, 실패 원인별로 경로를 나누고, 검사 비용에 따라 실행 시점을 나누고, 병렬 에이전트에는 독립 워크스페이스(Worktree)를 주고, 처음부터 로깅·트레이싱을 준비하는 실행 규율이 필요하다.
3. 그래프의 진짜 비용은 리뷰 병목·Cognitive Surrender·Token Blowout이며, 목표는 결과물의 양이 아니라 "승인된 변경 한 건당 전체 비용"을 낮추는 것이므로, 한 번뿐인 짧은 작업이나 결제·보안·삭제처럼 영향이 큰 영역에는 그래프보다 사람의 판단과 게이트를 우선해야 한다.
